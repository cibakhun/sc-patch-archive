---
phase: quick-260802-7eb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/items.ts
  - src/i18n/itemText.ts
  - src/components/ItemDetail.astro
  - src/components/ItemListing.astro
  - src/components/ItemFinderApp.astro
  - assets/item-finder-app.js
  - tests/e2e/items-avg-price.test.js
autonomous: false
requirements: [QUICK-260802-7eb, D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "Jede Item-Detailseite (DE und EN) zeigt in der Bezugsquellen-Tabelle eine vierte Spalte mit dem UEX-Durchschnittspreis des Items (D-02)."
    - "Jede Kategorie-Listenseite (DE und EN) zeigt eine vierte Spalte mit dem Durchschnittspreis je gelistetem Item (D-01)."
    - "Das Item-Finder-Modal (DE und EN) zeigt dieselbe vierte Spalte mit demselben Wert wie die Detailseite (D-01, D-04)."
    - "Ein Item ohne einen einzigen Preis-Fundort zeigt in JEDER dieser Spalten den Gedankenstrich — niemals 0 (D-03)."
    - "Der angezeigte Mittelwert liegt fuer jedes Item zwischen seinem guenstigsten und seinem teuersten Preis."
    - "Die vollstaendige e2e-Suite bleibt gruen: Basislinie 98 bestanden / 0 Fehler, danach >= 98 bestanden / 0 Fehler."
  artifacts:
    - "src/lib/items.ts — exportierte Funktion avgPrice(i: Item): number | null"
    - "src/i18n/itemText.ts — Schluessel thAvg und avgNote in BEIDEN Sprachbloecken (de und en)"
    - "src/components/ItemDetail.astro — vierte Spalte in der dp-table plus Erlaeuterung in der dp-note"
    - "src/components/ItemListing.astro — vierte Spalte in der dp-table"
    - "assets/item-finder-app.js — gespiegelte avgPrice()-Funktion, vierte Spalte im Modal, Ø-Zusatz auf der Karte"
    - "src/components/ItemFinderApp.astro — CSS-Regeln .uif-td-avg und .uif-card-avg"
    - "tests/e2e/items-avg-price.test.js — 12 Pruefungen ueber Datenregel, dist/ (DE+EN) und Mock-DOM"
  key_links:
    - "avgPrice() in src/lib/items.ts <-> avgPrice() in assets/item-finder-app.js: IDENTISCHE Filterregel (price != null && price > 0) und identisches Runden. Weichen sie ab, widerspricht das Finder-Modal der Detailseite fuer dasselbe Item."
    - "ITEM_UI (src/i18n/itemText.ts) -> JS_T (ItemFinderApp.astro:58) -> window.__UIF.t (ItemFinderApp.astro:804): EINE Label-Quelle bedient statische Seiten UND Finder-Client. thAvg/avgNote dort eintragen genuegt fuer beide Sprachen auf allen drei Oberflaechen."
    - ".dp-best .dp-price::after (assets/data-page.css:253) haengt ein ★ an JEDES .dp-price in der guenstigsten Zeile. Die Ø-Zelle darf deshalb NICHT .dp-price tragen, sonst stehen zwei Sterne in der Bestzeile."
    - "ver() in ItemFinderApp.astro:25 hasht assets/item-finder-app.js per sha1 in den ?v=-Parameter. Der Cache-Bust greift durch die Dateiaenderung AUTOMATISCH — kein Hand-Hochzaehlen (bekannter wiederkehrender Fehler)."
    - "tests/e2e/helpers/dom-mock.js:369 setzt window.__UIF = { lang: 'de' } OHNE t. Jeder neue tr()-Aufruf in item-finder-app.js braucht deshalb zwingend einen deutschen Rueckfall-String, sonst zeigt das Mock-DOM den nackten Schluessel."
---

<objective>
Alle drei Item-Preis-Oberflaechen bekommen eine vierte Spalte mit dem UEX-Durchschnittspreis: Item-Datenblatt, Kategorie-Liste und Finder-Modal — vollstaendig auf Deutsch und Englisch.

Purpose: Der guenstigste Preis allein verschweigt, wie weit die Terminals auseinanderliegen. Bei 1445 Items (51,8 % der bepreisten) gibt es echte Streuung bis +14 %. Der Mittelwert macht sichtbar, ob der Weg zum guenstigsten Terminal sich lohnt.
Output: avgPrice()-Helfer (build-time und client-seitig gespiegelt), zweisprachige Labels, drei erweiterte Tabellen, ein Regressionstest, der die Nie-null-Regel festnagelt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Betroffene Quellen — vor der Aenderung lesen
@src/lib/items.ts
@src/i18n/itemText.ts
@src/components/ItemDetail.astro
@src/components/ItemListing.astro
@src/components/ItemFinderApp.astro
@assets/item-finder-app.js
@tests/e2e/behavior.test.js
</context>

<research_facts>
Bereits verifiziert — NICHT erneut untersuchen:

1. Die UEX-API-Felder `price_buy_avg`, `_avg_week`, `_avg_month` sind wertlos: ueber 4467 gepruefte kaufbare Zeilen ist `price_buy_avg` in 100 % der Faelle identisch mit `price_buy` (SC-NPC-Shops haben statische Preise je Terminal). **Keine Aenderung an scripts/fetch-uex-item-prices.mjs oder scripts/build-universal-db.mjs. Kein Datenlauf, kein Neuaufbau der DB.**

2. Der aussagekraeftige Mittelwert ist der Mittelwert ueber alle Verkaufsstellen EINES Items und laesst sich vollstaendig aus dem bereits vorhandenen `item.obtain[]` in `assets/universal-items.json` rechnen.

3. Datenlage in assets/universal-items.json (9788 Eintraege): 2974 Eintraege haben mindestens einen bepreisten obtain-Eintrag, 6814 (69,6 %) haben KEINEN. Der Gedankenstrich-Pfad ist also der Mehrheitspfad, nicht der Sonderfall.

4. Die bestehende Null-Behandlung ist ueberall korrekt und wird gespiegelt, nicht geaendert: `minPrice`/`maxPrice` liefern `null`; ItemListing.astro:71 und ItemDetail.astro:109 rendern `<span class="dp-muted">—</span>`; item-finder-app.js:798 rendert die `&mdash;`-Entitaet.

5. Verifizierte Test-Fixtures (gegen die echte JSON und das gebaute dist/ geprueft):
   - `10-series-greatsword-cannon` — 4 obtain-Eintraege, alle 4 bepreist, min 23905 / max 27287 / Mittelwert 25596. Detailseite und Kategorie-Seite `vehiclegear-weapons-guns.html` existieren in dist/ (DE und EN).
   - `adp-mk4-arms-justified` — 3 obtain-Eintraege, 0 davon bepreist -> Mittelwert null. Detailseite und Kategorie-Seite `armour-arms.html` existieren in dist/ (DE und EN).
   - `125a` — genau eine Preisquelle (1508220): Mittelwert == Minimum.

6. e2e-Basislinie am 02.08.2026 vor diesem Plan: `npm.cmd run test:e2e` -> **98 bestanden, 0 Fehler**. Die in `.planning/phases/01.1-.../deferred-items.md` beschriebenen 11 Altfehler sind nicht mehr reproduzierbar (layout.test.js akzeptiert den `?v=`-Hash inzwischen per Regex). Die Suite ist also ein harter Riegel, kein rotes Grundrauschen.
</research_facts>

<locked_decisions>
- **D-01 — Nur Items.** Alle Item-Preis-Oberflaechen. NICHT Schiffe, NICHT Handelswaren/Refinery.
- **D-02 — ItemDetail.astro bekommt eine echte vierte Tabellenspalte,** keine Kennzahl in `dp-keys`. Dem Nutzer wurde ausdruecklich gesagt, dass der Wert in jeder Zeile identisch wiederholt wird; er hat es trotzdem so gewaehlt. **Diese Spalte NICHT in eine Kennzahl "verbessern".**
- **D-03 — Harte Regel: ohne Preis der Gedankenstrich, NIEMALS 0.** Das ist das Hauptanliegen des Nutzers ("ADP-mk4 Arms Justified … da ist halt preis 0").
- **D-04 — DE und EN muessen beide vollstaendig sein.** Die Aufgabe ist nicht erledigt, wenn nur eine Sprache stimmt.
</locked_decisions>

<out_of_scope>
Bewusst NICHT anfassen — jede dieser Aenderungen waere eine Ausweitung ueber die Entscheidung hinaus:

- `src/lib/itemSeo.ts` (Titel/Meta-Texte) — der Mittelwert gehoert in die Tabelle, nicht in 9788 Meta-Beschreibungen.
- `ul.dp-keys` in ItemDetail.astro — dort stuende die verbotene Kennzahl-Form (D-02).
- `minPrice()` / `maxPrice()` — Verhalten bleibt Bit-fuer-Bit gleich; `avgPrice()` kommt daneben.
- `assets/data-page.css` — die vorhandenen Klassen `.dp-num`, `.dp-muted`, `.dp-price` reichen; keine neue Regel noetig.
- `scripts/fetch-uex-item-prices.mjs`, `scripts/build-universal-db.mjs`, `assets/universal-items.json` — kein Datenlauf (siehe research_facts 1+2).
- `npm run theme` NICHT ausfuehren. Die neuen CSS-Regeln nutzen ausschliesslich vorhandene Tokens (`var(--muted, …)`), es gibt nichts zu generieren; ein Lauf wuerde nur Handaenderungen in `:root[data-theme="light"]`-Bloecken verwerfen.
- Schiffe, Mining, Refinery, Crafting-Blueprints.
</out_of_scope>

<source_audit>
| # | Quelle | Element | Abgedeckt von |
|---|--------|---------|---------------|
| GOAL-01 | Quick-Aufgabe | UEX-Durchschnittspreis-Spalte fuer Items, DE+EN | Task 1 + Task 2 |
| D-01 | Nutzer | Nur Items, ALLE Item-Preis-Oberflaechen | Task 1 (Datenblatt), Task 2 (Liste + Finder-Modal + Finder-Karte) |
| D-02 | Nutzer | Echte 4. Tabellenspalte im Datenblatt, keine Kennzahl | Task 1 |
| D-03 | Nutzer | Ohne Preis der Gedankenstrich, nie 0 | Task 1 (Umsetzung + Test 2/5), Task 2 (Test 8/10) |
| D-04 | Nutzer | DE und EN beide vollstaendig | Task 1 (Tests 3/4/5), Task 2 (Tests 7/8/11), Task 3 (Sichtpruefung beider Sprachen) |
| RES-01 | Recherche | Keine API-/Skript-/Pipeline-Aenderung | out_of_scope, in allen Tasks eingehalten |
| RES-02 | Recherche | Mittelwert build-time aus item.obtain[] | Task 1 (avgPrice) |
| RES-03 | Recherche | Bestehende Null-Behandlung spiegeln, nicht aendern | Task 1 + Task 2 |
| RES-04 | Recherche | 69,6 % der Items ohne Preis -> Gedankenstrich ist der Mehrheitspfad | Task 1 Test 2 (Untergrenze 6000) |

Keine Luecke. Keine zurueckgestellten Punkte.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ø-Spalte end-to-end — Helfer, Labels, Datenblatt, Beweis in dist/</name>
  <files>src/lib/items.ts, src/i18n/itemText.ts, src/components/ItemDetail.astro, tests/e2e/items-avg-price.test.js</files>
  <precondition>`npm.cmd run test:e2e` laeuft vor jeder Aenderung mit 98 bestandenen und 0 fehlgeschlagenen Tests durch. Weicht die Zahl ab, den Ist-Stand notieren und als neue Basislinie verwenden, statt fremde Altfehler diesem Plan zuzurechnen.</precondition>

  <behavior>
    - avgPrice bei mehreren Preisen: `10-series-greatsword-cannon` (23905/24521/26671/27287) liefert 25596.
    - avgPrice bei genau einem Preis: `125a` liefert exakt dessen Preis (1508220).
    - avgPrice ohne bepreisten obtain-Eintrag: `adp-mk4-arms-justified` liefert `null` — nicht 0, nicht NaN (D-03).
    - Fuer jedes Item mit mindestens einem Preis gilt: minPrice <= avgPrice <= maxPrice.
    - Gerendertes Datenblatt EN zeigt den Spaltenkopf und den Mittelwert in JEDER Zeile der Bezugsquellen-Tabelle.
    - Gerendertes Datenblatt DE ebenso, mit deutschem Kopf und deutscher Zahlenformatierung.
    - Gerendertes Datenblatt eines Loot-Items traegt den Spaltenkopf, aber in der Tabelle keinen einzigen aUEC-Betrag.
  </behavior>

  <action>
    Vier Schichten, in dieser Reihenfolge — die duenne Bahn von der Rohzahl bis ins gebaute HTML.

    **(1) src/lib/items.ts** — direkt hinter `maxPrice` (aktuell Zeile 310-314) eine exportierte Funktion `avgPrice(i: Item): number | null` einsetzen. Sie summiert alle `o.price` aus `i.obtain` mit derselben Filterregel wie `minPrice`/`maxPrice` (`o.price != null && o.price > 0`), zaehlt die Treffer mit, und liefert `Math.round(summe / anzahl)` — bei null Treffern `null`. Die identische Filterregel ist Pflicht: sonst passen die drei Zahlen min/Ø/max in derselben Tabelle nicht zueinander. Kommentar auf Deutsch im Stil der Nachbarfunktionen und mit der Begruendung, warum `null` und nicht `0` (per D-03: 0 waere eine erfundene Aussage).

    **(2) src/i18n/itemText.ts** — in BEIDEN Sprachbloecken zwei Schluessel ergaenzen, direkt neben der bestehenden Zeile `thItem / thSources / thFrom` (de ~Zeile 106, en ~Zeile 235), damit die Tabellenkopf-Schluessel beisammen bleiben:
    - `thAvg`: de `Ø UEX`, en `UEX avg`. Kurz genug fuer einen Tabellenkopf; `.dp-table th` traegt bereits `white-space: nowrap`.
    - `avgNote`: de `Ø UEX = Mittelwert über alle gelisteten Kaufpreise dieses Items.`, en `UEX avg = mean of all listed buy prices for this item.` Praezise Formulierung: der Mittelwert laeuft ueber die Kaufpreise, nicht ueber Loot-Zeilen und nicht ueber die Zeit.
    Damit sind beide Sprachen fuer ALLE drei Oberflaechen versorgt — `ITEM_UI[lang]` wird in ItemFinderApp.astro:58 als `JS_T` uebernommen und in Zeile 804 komplett nach `window.__UIF.t` serialisiert.

    **(3) src/components/ItemDetail.astro** — `avgPrice` in den bestehenden Import aus `../lib/items` aufnehmen und neben `lo`/`hi` (Zeile 31-32) eine Konstante `avg` aus `avgPrice(item)` ableiten.
    - thead (Zeile 96-102): hinter der `thPrice`-Spalte eine vierte Kopfzelle mit Klasse `dp-num` und `{t('thAvg')}`.
    - tbody (Zeile 104-112): hinter der Preiszelle eine vierte Zelle mit Klasse `dp-num`, deren Inhalt ein `span` mit Klasse `dp-muted` ist — bei `avg != null` der Betrag aus `auec(avg, lang)`, sonst der Gedankenstrich, exakt wie in Zeile 109.
    - **Die Ø-Zelle darf NICHT die Klasse `dp-price` tragen.** `assets/data-page.css:253` haengt per `.dp-best .dp-price::after` ein Sternchen an jedes `dp-price` in der guenstigsten Zeile; mit `dp-price` in der Ø-Spalte stuenden dort zwei Sterne. `dp-muted` ist zugleich die richtige Gewichtung: der Wert wiederholt sich je Zeile und ist Zweitinformation neben dem echten Zeilenpreis.
    - dp-note (Zeile 116): den bestehenden Satz um einen bedingten Textausdruck erweitern, der `t('avgNote')` nur dann anhaengt, wenn `avg != null` ist — auf einem Katalog-Item waere der Hinweis nur Rauschen.
    - **`ul.dp-keys` (Zeile 73-87) bleibt unangetastet** (D-02).
    - Astro-Falle beachten: kein `{ausdruck}` als direktes Kind eines Elements auf oberster Template-Ebene. Die neuen Zellen liegen tief im Baum und folgen exakt dem Muster der Nachbarzelle, damit ist der Fall ausgeschlossen.

    **(4) tests/e2e/items-avg-price.test.js** — neue Datei im Stil von `tests/e2e/db.test.js` (`node:test` + `node:assert`, `fs.readFileSync` gegen `path.resolve(...)`, deutschsprachige Testnamen). Sie enthaelt eine lokale Referenzimplementierung des Mittelwerts (dieselbe Regel, unabhaengig geschrieben) und leitet ALLE Erwartungswerte zur Laufzeit aus `assets/universal-items.json` ab — **keine Zahl fest verdrahten**, sonst bricht der Test beim naechsten Preis-Snapshot. Fest verdrahtet werden nur die stabilen Item-IDs. Diese sechs Pruefungen:
    1. Fuer jedes Item mit mindestens einem bepreisten obtain-Eintrag liegt der Referenz-Mittelwert zwischen Minimum und Maximum.
    2. Fuer jedes Item ohne bepreisten obtain-Eintrag ist der Referenz-Mittelwert strikt `null` — nicht 0, nicht NaN. Zusaetzlich absichern, dass diese Menge mindestens 6000 Eintraege gross ist, damit die Regel nicht an einem leeren Set vorbeilaeuft.
    3. Fixture-Wachhund: `10-series-greatsword-cannon` hat weiterhin mindestens zwei bepreiste Quellen mit unterschiedlichen Preisen; sonst mit klarer Meldung fehlschlagen, dass ein neues Fixture gewaehlt werden muss.
    4. `dist/items/10-series-greatsword-cannon.html` enthaelt den englischen Spaltenkopf und den mit `toLocaleString('en-US')` formatierten Mittelwert mindestens so oft, wie das Item obtain-Zeilen hat.
    5. `dist/de/items/10-series-greatsword-cannon.html` dasselbe mit deutschem Kopf und `toLocaleString('de-DE')`.
    6. Fuer `adp-mk4-arms-justified` in BEIDEN Sprachen: den `<table class="dp-table">…</table>`-Block aus dem HTML herausschneiden und pruefen, dass der Spaltenkopf drinsteht, dass jede `<tr>` im tbody genau vier `<td>` hat, und dass der Block keinen Treffer fuer eine Zahl gefolgt von der Waehrungseinheit enthaelt (Regex ueber Ziffer + optionale Trenner + Leerraum + Einheit). Das ist die maschinelle Fassung von D-03.

    Danach `npm.cmd run build` — der Astro-Build ist die einzige verlaessliche Sichtquelle, der Dev-Server ist in diesem Repo vorbestehend kaputt (Inline-Skript in Layout.astro).
  </action>

  <verify>
    <automated>npm.cmd run build &amp;&amp; node --test tests/e2e/items-avg-price.test.js</automated>
  </verify>

  <done>
    `avgPrice` steht in src/lib/items.ts; `thAvg` und `avgNote` stehen in beiden Sprachbloecken von itemText.ts; die Bezugsquellen-Tabelle im Datenblatt hat vier Spalten; die sechs Pruefungen in tests/e2e/items-avg-price.test.js laufen gruen gegen ein frisches dist/. Commit: `feat(items): Ø-UEX-Spalte im Item-Datenblatt (DE+EN)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kategorie-Liste und Item-Finder auf dieselbe Ø-Spalte ziehen</name>
  <files>src/components/ItemListing.astro, assets/item-finder-app.js, src/components/ItemFinderApp.astro, tests/e2e/items-avg-price.test.js</files>

  <behavior>
    - Kategorie-Liste EN und DE: der Spaltenkopf steht im thead, die Zeile eines bepreisten Items traegt seinen Mittelwert, die Zeile eines Loot-Items traegt in beiden Zahlenspalten den Gedankenstrich.
    - Finder-Modal (Mock-DOM, DE-Rueckfaelle): Fixture `beta-helmet` (500 und 800) zeigt den Ø-Kopf und in JEDER seiner zwei Zeilen den Betrag 650.
    - Finder-Modal: Fixture `gamma-jacket` (eine reine Loot-Zeile) zeigt in der vierten Zelle den Gedankenstrich und keinen Betrag.
    - Finder-Karte: `beta-helmet` (Ø 650 != min 500) traegt den Ø-Zusatz; `epsilon-runner` (nur eine Preisquelle, Ø == min) traegt ihn NICHT.
    - Die gebauten Finder-Seiten transportieren das Label in beiden Sprachen in ihrer eingebetteten Konfiguration.
  </behavior>

  <action>
    **(1) src/components/ItemListing.astro** — `avgPrice` in den bestehenden Import aus `../lib/items` (Zeile 10-13) aufnehmen. Im thead (Zeile 51-57) hinter der `thFrom`-Spalte eine vierte Kopfzelle mit Klasse `dp-num` und `{t('thAvg')}`. Im tbody-Callback (Zeile 59-75) neben `const p = minPrice(i)` einen Mittelwert aus `avgPrice(i)` ableiten und als vierte Zelle mit Klasse `dp-num` rendern: ein `span` mit Klasse `dp-muted`, darin der Betrag aus `auec(...)` oder der Gedankenstrich. Auch hier kein `dp-price`. Hier ist der Wert pro Zeile ein ANDERES Item, die Spalte traegt also je Zeile echte neue Information; ein zusaetzlicher Erklaersatz ist nicht noetig.

    **(2) assets/item-finder-app.js** — direkt hinter `minPrice` (Zeile 45-52) eine Funktion `avgPrice(item)` mit exakt derselben Filterregel und demselben `Math.round` wie die TypeScript-Fassung. Darueber ein deutscher Kommentar im Stil von Zeile 53-55, der ausdruecklich auf `avgPrice()` in `src/lib/items.ts` als Zwilling verweist — laufen die beiden auseinander, widerspricht das Modal der Detailseite fuer dasselbe Item.
    - Modal-Tabelle (Zeile 790-807): den Mittelwert EINMAL vor dem `.map()` berechnen, nicht je Zeile. Im thead eine vierte Kopfzelle mit `esc(tr('thAvg', 'Ø UEX'))` — der deutsche Rueckfall ist Pflicht, weil `tests/e2e/helpers/dom-mock.js:369` `window.__UIF` ohne `t` setzt und alle Tests gegen die Rueckfaelle laufen. Je Zeile eine vierte Zelle mit Klasse `uif-td-avg`, Inhalt `fmtNum(avg) + ' aUEC'` oder — bei `null` — dieselbe Gedankenstrich-Entitaet, die Zeile 798 bereits verwendet.
    - Ergebnis-Karte (Zeile 440-454): nur im Zweig mit vorhandenem Minimum den Mittelwert holen und, **nur wenn er sich vom Minimum unterscheidet**, an `priceHtml` einen `span` mit Klasse `uif-card-avg` anhaengen, der das Ø-Zeichen und `fmtNum(...)` traegt (ohne Waehrungseinheit — die steht schon im Hauptbetrag davor). Die Unterdrueckung bei Gleichheit ist Absicht: bei rund der Haelfte der bepreisten Items gibt es keine Streuung, dort waere die Wiederholung derselben Zahl reines Rauschen. Die Zweige ohne Preis (`Nur Loot`, `Exklusiv`, `Keine Handelsdaten`) bleiben unveraendert — dort darf keinesfalls eine Zahl erscheinen (D-03).

    **(3) src/components/ItemFinderApp.astro** — im `<style is:global>`-Block direkt neben `.uif-td-kind` / `.uif-td-price` (Zeile 551-552) zwei Regeln ergaenzen: `.uif-td-avg` mit `white-space: nowrap` und gedaempfter Farbe, `.uif-card-avg` mit gedaempfter Farbe, normalem Gewicht, kleinerer Schriftgroesse und etwas Abstand nach links. Beide MUESSEN dem umgebenden Idiom `var(--token, #hexrueckfall)` folgen und duerfen keine neue Rohfarbe einfuehren — sonst braucht die Hell-Variante eine handgepflegte Entsprechung. Im vorhandenen Mobil-Media-Query am Ende des Style-Blocks `.uif-card-avg` ausblenden: `.uif-card-price` ist `white-space: nowrap` und sitzt in einem Flex-Fuss neben dem Fundortnamen — bei 375 px wuerde der Zusatz den Ortsnamen wegdruecken, wovor der Kommentar in Zeile 458-465 bereits warnt.
    - Am Cache-Bust NICHTS von Hand aendern: `ver()` in Zeile 25 hasht den Dateiinhalt per sha1 in den `?v=`-Parameter, die Skriptaenderung erneuert ihn von selbst. Ein handgesetztes Hochzaehlen ist der bekannte wiederkehrende Fehler in diesem Repo.

    **(4) tests/e2e/items-avg-price.test.js** erweitern (Erwartungswerte weiterhin zur Laufzeit ableiten, keine festen Betraege):
    7. `dist/items/category/vehiclegear-weapons-guns.html` und die DE-Entsprechung tragen den Spaltenkopf; die Zeile von `10-series-greatsword-cannon` enthaelt den je Sprache formatierten Mittelwert.
    8. In `dist/items/category/armour-arms.html` und der DE-Entsprechung enthaelt die Zeile von `adp-mk4-arms-justified` in den beiden Zahlenspalten den Gedankenstrich und keinen Betrag.
    9. Jede tbody-Zeile der Listen-Tabelle hat in beiden Sprachen genau vier `<td>`.
    10. Mock-DOM im Stil von `tests/e2e/behavior.test.js` (Block `Detail-Modal`, Zeile 377-398): `setupMockDOM` mit dem dortigen Fixture-Schema, `dom.runScript` auf `assets/item-finder-app.js`, Karte `beta-helmet` oeffnen, pruefen dass der Ø-Kopf im Modal steht und beide `.uif-obtain-row` den Mittelwert 650 tragen; Karte `gamma-jacket` oeffnen und pruefen, dass die vierte Zelle keinen Betrag enthaelt.
    11. Karten-Regel im Mock-DOM: die Karte von `beta-helmet` traegt ein `.uif-card-avg`, die von `epsilon-runner` nicht.
    12. `dist/item-finder.html` und `dist/de/item-finder.html` enthalten in der eingebetteten `window.__UIF`-Konfiguration den `thAvg`-Eintrag mit dem jeweils richtigen Sprachwert — das ist der Nachweis, dass D-04 auch fuer den Client-Pfad haelt.

    Danach `npm.cmd run build`, dann die volle Suite.
  </action>

  <verify>
    <automated>npm.cmd run build &amp;&amp; node --test tests/e2e/items-avg-price.test.js &amp;&amp; npm.cmd run test:e2e</automated>
  </verify>

  <done>
    Alle drei Oberflaechen tragen die Spalte; die zwoelf Pruefungen in tests/e2e/items-avg-price.test.js sind gruen; `npm.cmd run test:e2e` meldet mindestens 98 bestandene und 0 fehlgeschlagene Tests. Commit: `feat(items): Ø-UEX-Spalte auch in Kategorie-Liste und Item-Finder`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vierte Spalte mit dem UEX-Durchschnittspreis auf allen drei Item-Preis-Oberflaechen, in beiden Sprachen: Datenblatt (Wert je Zeile identisch — so ausdruecklich entschieden), Kategorie-Liste (Wert je Zeile unterschiedlich) und Finder-Modal, dazu ein dezenter Ø-Zusatz auf der Finder-Karte, wenn er sich vom "ab"-Preis unterscheidet. Items ohne Preis zeigen ueberall den Gedankenstrich.
  </what-built>
  <how-to-verify>
    Der Astro-Dev-Server ist in diesem Repo vorbestehend kaputt — gegen das gebaute `dist/` pruefen. Aus dem Repo-Wurzelverzeichnis `npx serve dist -l 4321` starten (oder ein beliebiger statischer Server auf `dist/`), dann diese sechs Seiten ansehen — DE und EN gelten erst zusammen als fertig:

    1. `http://localhost:4321/items/10-series-greatsword-cannon.html` — Bezugsquellen-Tabelle: vier Spalten, in der Ø-Spalte in allen vier Zeilen 25,596 aUEC in gedaempfter Farbe. In der guenstigsten Zeile steht **genau ein** Sternchen (am Zeilenpreis, nicht am Ø-Wert). Unter der Tabelle steht der Erklaersatz zur Ø-Spalte.
    2. `http://localhost:4321/de/items/10-series-greatsword-cannon.html` — dasselbe auf Deutsch, Kopf `Ø UEX`, Zahl `25.596 aUEC`.
    3. `http://localhost:4321/items/adp-mk4-arms-justified.html` und die `/de/`-Fassung — in der Ø-Spalte steht in jeder Zeile der Gedankenstrich. **Nirgends eine 0.** Das ist der Punkt, um den es dem Nutzer ging.
    4. `http://localhost:4321/items/category/vehiclegear-weapons-guns.html` und `/de/…` — vierte Spalte, je Zeile ein eigener Wert; Zeilen ohne Preis mit Gedankenstrich in beiden Zahlenspalten.
    5. `http://localhost:4321/item-finder.html` und `/de/item-finder.html` — ein Item mit mehreren Verkaufsstellen suchen (z. B. "Greatsword"), Karte anklicken: das Modal zeigt vier Spalten mit demselben Ø-Wert wie das Datenblatt. Auf der Karte selbst steht der Ø-Zusatz nur dann, wenn er vom "ab"-Preis abweicht.
    6. Fenster auf 375 px Breite ziehen: die Tabellen bleiben horizontal scrollbar (`.dp-tablewrap` / `.uif-table-wrapper`), der Ø-Zusatz auf den Karten ist ausgeblendet und der Fundortname wird nicht weggedrueckt.

    Beurteilen: Ist die gedaempfte Ø-Spalte im Datenblatt ertraeglich, oder wirkt die Wiederholung in jeder Zeile stoerend? Ist `Ø UEX` / `UEX avg` als Kopf verstaendlich? Sitzt der Ø-Zusatz auf der Karte gut oder ist er zu viel?
  </how-to-verify>
  <resume-signal>„freigegeben" tippen oder beschreiben, was nicht stimmt (Beschriftung, Gewichtung, Karten-Zusatz, Mobilverhalten).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assets/universal-items.json -> gerendertes HTML (build-time) | Datendatei aus eigener Extraktion; Astro escaped Textausdruecke automatisch |
| assets/universal-items.json -> innerHTML im Browser (item-finder-app.js) | Client baut HTML-Strings zusammen; Escaping liegt in eigener Hand (`esc()`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7eb-01 | Tampering | Neue Ø-Zelle in `item-finder-app.js`, per `innerHTML` gesetzt | low | mitigate | Der eingesetzte Wert ist ausschliesslich die Ausgabe von `fmtNum(Number)` bzw. eine feste HTML-Entitaet — nie ein Zeichenkettenfeld aus der Datendatei. Kein Pfad fuer eingeschleustes Markup. |
| T-7eb-02 | Information Disclosure | Kein neues Datenfeld, nur eine Ableitung aus bereits veroeffentlichten Preisen | low | accept | Der Mittelwert enthaelt keine Information, die nicht schon in der ausgelieferten `universal-items.json` steht. |
| T-7eb-03 | Tampering | Lieferkette | low | accept | Dieser Plan installiert kein einziges Paket (npm/pip/cargo). Kein Legitimitaets-Gate erforderlich. |
</threat_model>

<verification>
- `npm.cmd run build` laeuft ohne Fehler durch.
- `npm.cmd run test:e2e` meldet mindestens 98 bestandene und 0 fehlgeschlagene Tests (Basislinie 98/0).
- `node --test tests/e2e/items-avg-price.test.js` meldet 12 bestandene Tests.
- Sichtpruefung des Checkpoints in DE und EN freigegeben.
</verification>

<success_criteria>
- Datenblatt, Kategorie-Liste und Finder-Modal tragen in DE und EN eine vierte Spalte mit dem UEX-Durchschnittspreis.
- Kein Item ohne Preis zeigt irgendwo eine 0; ueberall steht der Gedankenstrich.
- Der angezeigte Mittelwert liegt fuer jedes Item nachweislich zwischen Minimum und Maximum.
- `avgPrice()` in TypeScript und in `item-finder-app.js` liefern fuer dasselbe Item denselben Wert.
- Keine Aenderung an Datenskripten, an der JSON, an data-page.css, an minPrice/maxPrice oder an `dp-keys`.
- Die e2e-Suite ist nicht schlechter als vorher.
</success_criteria>

<output>
Nach Abschluss `.planning/quick/260802-7eb-uex-durchschnittspreis-spalte-fuer-items/260802-7eb-SUMMARY.md` schreiben.
</output>
