---
phase: 14-schiffs-datenkarte-entstapeln
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-shipcard.mjs
  - scripts/lib/shipcard-exclusions.mjs
  - scripts/lib/gate-registry.mjs
  - package.json
  - scripts/probes/schiffskarte-messung.mjs
  - scripts/probes/README.md
autonomous: true
requirements: [D-02, D-03]

must_haves:
  truths:
    - "Es gibt ein Pruefskript `scripts/verify-shipcard.mjs`, das gegen das GEBAUTE `dist/` prueft und nicht gegen die Quelle — die Entdopplung wird am Auslieferungsartefakt belegt, nicht an der Vorlage (D-03, `docs/maschinelle-validierung.md` Grundsatz 7)."
    - "Das Tor war einmal VORGEFUEHRT ROT: gegen den heutigen Stand von `dist/` reisst es, und seine Meldung steht woertlich im SUMMARY (Grundsatz 1)."
    - "Das Tor druckt je Zusicherung eine Soll-/Ist-Zeile und nennt, WIE VIELE Schiffsseiten es gelesen hat; unter der Klinke von 440 Seiten reisst es (Grundsatz 2)."
    - "Das Tor laeuft zuerst als reiner BERICHT (`--report`, kein Urteil, Rueckgabecode 0) — die Ausnahmeliste entsteht aus dem, was der Bericht wirklich findet, nicht aus einer Annahme."
    - "Jede verbleibende Wiederholung steht als benannte Ausnahme mit ihrem Anlass in `scripts/lib/shipcard-exclusions.mjs`; eine Ausnahme ohne Begruendungstext ist eine Luecke, kein Ausschluss (Grundsatz 6)."
    - "Ein Zombie-Waechter reisst, sobald eine Ausnahme in einem Lauf nicht mehr gegriffen hat — eine Ausnahme, deren Anlass verschwunden ist, deckt sonst spaeter echte Drift zu."
    - "Das Tor steht in `scripts/lib/gate-registry.mjs` und in `package.json`; `verify:wiring` bleibt gruen (Bijektion Verzeichnis <-> Dateibestand <-> package.json)."
    - "Der Registry-Eintrag traegt VORERST `disabled` mit dem Anlass und der Nennung des Plans, der ihn scharf schaltet — `npm run gate` bleibt in dieser Welle gruen, und die Schuld wird bei JEDEM Lauf gedruckt (Praezedenz `verify:sync`: erst beheben, dann scharf)."
    - "Das Tor braucht weder git noch Netz noch `Data.p4k` und ist damit schienenfaehig fuer Schiene A (Grundsatz 4)."
    - "Es gibt eine Messsonde `scripts/probes/schiffskarte-messung.mjs`, die gegen die gerenderte Seite in einem echten Browser misst und an KEINER Strecke der Torkette haengt — Sonden liegen ausserhalb der Bijektion von `verify:wiring`."
    - "Die Sonde waehlt ihre drei Pruefschiffe AUS DEN DATEN (Carrack als Bezug der Ausgangsmessung, groesstes nach `lengthM`, kargstes nach gemessenem Fuellgrad) statt sie fest zu verdrahten — sonst veraltet sie beim naechsten Datenlauf still (Konkrete Vorgabe 2)."
    - "Die Ausgangsmessung ist mit DIESEM Werkzeug reproduziert: die Carrack liegt bei 1280x720 bei rund 5.554 px. Weicht die Zahl ab, wird die Abweichung erklaert, BEVOR die Phase weiterlaeuft — eine Sperrklinke, die mit einem anderen Massstab gemessen wurde als der Ausgangswert, misst nichts."
    - "Diese Welle aendert keine einzige ausgelieferte Zeile: `npm run build` und `npm run gate` bleiben gruen, `git status` nennt ausschliesslich die sechs Dateien dieses Plans."
  artifacts:
    - scripts/verify-shipcard.mjs
    - scripts/lib/shipcard-exclusions.mjs
    - scripts/probes/schiffskarte-messung.mjs
  key_links:
    - "`scripts/verify-shipcard.mjs` <-> `scripts/lib/gate-registry.mjs` <-> `package.json`: alle drei muessen denselben Namen `verify:shipcard` tragen. Fehlt einer, reisst `verify:wiring` — und das ist Absicht (der Waechter hat schon zweimal seinen eigenen Erbauer gefangen)."
    - "Regionenbegriff <-> Rot-Vorfuehrung: die Region ist als DIREKTES KIND von `div.sd`, zusaetzlich aufgeteilt an jeder Unterueberschrift mit der Klasse `sd__sub`, definiert. Dieser eine Satz gilt VOR und NACH dem Umbau — nur so ist die Rot-Vorfuehrung gegen den heutigen Stand aussagekraeftig und dieselbe Regel spaeter das Gruen-Urteil."
    - "Attributwerte <-> Entdopplung: der `aria-label` der Perzentilspur traegt den Rohwert absichtlich weiter (Screenreader). Der Zaehler liest deshalb NUR sichtbaren Text; Attributwerte, `script`- und `style`-Ruempfe fallen vor dem Zaehlen heraus. Ohne diese Regel meldet das Tor eine Doppelung, die eine Barrierefreiheitszusage ist."
    - "Aehnliche Schiffe <-> Entdopplung: der Block mit dem Raster fuer aehnliche Schiffe zeigt die Laengen FREMDER Schiffe. Er faellt vollstaendig aus dem Zaehlbereich; sonst meldet das Tor jedes Mal Alarm, wenn zufaellig ein Nachbarschiff dieselbe Laenge hat."
    - "Sonde <-> `scripts/lib/theme-color.mjs`: die Kontrastrechnung kommt aus derselben Bibliothek, die `verify-layers.mjs` benutzt. Eine zweite Kontrastformel waere ein zweites Fehlerbild fuer dieselbe Zahl (Praezedenz Phase 12)."
    - "Sonde <-> Vorschau-Server: der Vorschau-Server im Worktree serviert dessen EIGENES `dist/`. Wer gegen einen fremden Port misst, misst einen fremden Stand."
---

<objective>
Werkzeug vor Eingriff. Diese Welle baut die beiden Messinstrumente, mit denen
diese Phase spaeter beurteilt wird — und fuehrt das eine davon einmal
absichtlich rot vor, bevor die erste Zeile der Seite angefasst wird.

Purpose: `14-CONTEXT.md` Konkrete Vorgabe 5 sagt es woertlich: „Die Entdopplung
wird maschinell belegt, nicht behauptet ... und muss vor der Umsetzung einmal
vorgefuehrt rot sein." Ein Tor, das erst NACH dem Umbau entsteht, ist gegen
genau den Zustand geschrieben, den es beurteilen soll — es kann gar nicht mehr
rot werden. Und eine Sperrklinke auf 4.200 px ist wertlos, solange nicht
belegt ist, dass dasselbe Werkzeug den Ausgangswert 5.554 px reproduziert.

Output: `scripts/verify-shipcard.mjs` samt benannter Ausnahmeliste,
Registry-Eintrag (vorerst ausgesetzt) und npm-Skript; die Sonde
`scripts/probes/schiffskarte-messung.mjs` samt Zeile in der Sonden-Uebersicht;
und zwei protokollierte Messwerte: die rote Tormeldung und die reproduzierte
Ausgangsmessung.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-schiffs-datenkarte-entstapeln/14-CONTEXT.md
@.planning/phases/14-schiffs-datenkarte-entstapeln/14-UI-SPEC.md

@CLAUDE.md
@docs/maschinelle-validierung.md
</context>

<planning_notes>
**Warum der Tracer NICHT in dieser Welle steht.** Der Tracer dieser Phase —
ein Kapitel end-to-end mit Sprungleiste — ist Task 1 in `14-02-PLAN.md`. Diese
Welle geht ihm bewusst voraus, weil die Hausregel es erzwingt: das
Entdopplungs-Tor muss VOR der Umsetzung einmal vorgefuehrt rot gewesen sein
(`14-CONTEXT.md` Konkrete Vorgabe 5, `docs/maschinelle-validierung.md`
Grundsatz 1). Ein Tor, das nach dem Umbau entsteht, war nie rot und ist
Dekoration. Diese Welle aendert keine ausgelieferte Zeile — sie ist reine
Messapparatur und damit kein horizontaler Unterbau, sondern die Vorbedingung,
unter der der Tracer ueberhaupt beurteilbar ist.

**Kein `npm install`.** `playwright-core` und `sharp` sind bereits
devDependencies (belegt durch `scripts/probes/mining-locview-messung.mjs` und
`scripts/browser-smoke.mjs`). Die Paket-Legitimitaetspruefung ist damit
gegenstandslos, `package-lock.json` bleibt unberuehrt. Am `package.json` wird
AUSSCHLIESSLICH eine Zeile im `scripts`-Block ergaenzt.

**Reversibilitaet.** Diese Welle traegt keine `one-way`-Entscheidung; ein
Entscheidungs-Checkpoint entfaellt deshalb ausdruecklich. Als `costly`
gefuehrt: der Regionenbegriff des Tors (Task 1) — er entscheidet, was die
Phase spaeter als Doppelung sieht, und ein spaeterer Wechsel entwertet die
Ausnahmeliste mit.

**Kein e2e-Testfall fuer die Sprungleiste, benannt statt verschwiegen.**
`tests/e2e/` prueft Inline-Skripte gegen ein Mock-DOM; fuer die Schiffs-
Detailseite existiert heute KEIN Helfer (nur `tests/e2e/helpers/ships-dom.js`
fuer die Uebersichtsseite). Ein neuer Helfer nur fuer die Aktivmarkierung der
Sprungleiste kostete mehr, als er traegt: die Sprungleiste funktioniert
vollstaendig ohne JavaScript (echte Anker), die Struktur deckt
`verify:shipcard` ab, das Rendern die Sonde. Bewusst nicht gebaut, hier
benannt.
</planning_notes>

<tasks>

<task type="auto">
  <name>Task 1: Das Entdopplungs-Tor — erst Bericht, dann Urteil, einmal vorgefuehrt rot</name>
  <files>scripts/verify-shipcard.mjs, scripts/lib/shipcard-exclusions.mjs, scripts/lib/gate-registry.mjs, package.json</files>
  <precondition>`dist/` existiert und enthaelt `dist/schiffe/anvl-carrack.html` sowie `dist/de/schiffe/anvl-carrack.html`. Fehlt es, zuerst `npm run build` — dieses Skript prueft den GEBAUTEN Stand, nicht die Quelle.</precondition>
  <reversibility rating="costly">Der Regionenbegriff (direktes Kind von `div.sd`, aufgeteilt an jeder `sd__sub`-Unterueberschrift) legt fest, was diese Phase als Doppelung SIEHT. Ein spaeterer Wechsel entwertet jede bis dahin gesammelte Ausnahme und macht die Rot-Vorfuehrung ungueltig.</reversibility>
  <read_first>
    - `scripts/verify-fx.mjs` VOLLSTAENDIG — die Hausform eines dist-Pruefers: Abbruch mit klarer Meldung wenn `dist/` fehlt, je Zusicherung eine nummerierte Ueberschrift plus Soll-/Ist-Zeile, `fail()`-Sammelurteil, Rueckgabecode 1. Diese Form wird Zeile fuer Zeile uebernommen, nicht neu erfunden.
    - `scripts/verify-sync.mjs` — Kopfkommentar und die Funktion `tokenize()` (Kommentar-, `script`- und `style`-Ruempfe entfernen; oeffnende Tags per Regex einsammeln; Klassenliste ueber `class`-Attribut in Autorenreihenfolge). Genau dieses Tag-Abtasten wird hier wiederverwendet — es kommt KEINE HTML-Bibliothek dazu.
    - `scripts/lib/sync-exclusions.mjs` Z. 1-70 — die Form einer benannten Ausnahmeliste: `id`, Modus, Trefferbedingung, `reason` als vollstaendiger Begruendungstext. Der Warnsatz im Kopf gilt hier wortgleich.
    - `scripts/lib/page-pairs.mjs` — `findPagePairs()` und `assertMinimumPairs()`; dieselbe Paarung wie `verify:fx`/`verify:sync`/`verify:typo`, kein vierter eigener Code.
    - `scripts/lib/gate-registry.mjs` Kopfkommentar (Feldbeschreibung, besonders `rail`, `env`, `disabled`, `why`) und die Schiene-A-Liste bis `verify:mining`.
    - `scripts/verify-metrics.mjs` Kopfkommentar — die Sperrklinken-Sprache dieses Hauses (`min`/`exakt`, „Anlass" je Zeile).
    - `src/components/ShipDetail.astro` Z. 1180-1190 (`div.sd` und der erste Panel-Kopf), Z. 1265-1300 (Beschreibung, Datenblatt, Beginn Masse &amp; Fracht), Z. 1571-1614 (Patch-Spine, aehnliche Schiffe, Verweise, Quellenzeile) — das sind die direkten Kinder von `div.sd`, aus denen der Regionenbegriff abgeleitet wird.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-CONTEXT.md` § „Die Doppelungen, maschinell gezaehlt" und Konkrete Vorgabe 5.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-UI-SPEC.md` Detailvertrag Punkt 5, 6 und 7 (wo welcher Wert kuenftig steht, und dass es je Kapitel genau EINE Kopfzeilen-Codezeile gibt).
    - `docs/maschinelle-validierung.md` § 4 — die sieben Grundsaetze, besonders 1, 2, 3, 4, 6 und 7.
  </read_first>
  <action>
Lege `scripts/verify-shipcard.mjs` an. Es prueft die gebauten Schiffs-
Detailseiten unter `dist/schiffe/*.html` und `dist/de/schiffe/*.html` und
sonst nichts. Kein git, kein Netz, kein Kindprozess, keine `Data.p4k` — damit
ist es schienenfaehig fuer Schiene A. Kopfkommentar in der Hausform: warum es
existiert (die sieben gemessenen Doppelwerte aus `14-CONTEXT.md`, die
Ausgangsmessung als Beleg), gegen WELCHES Artefakt es prueft, die Aufzaehlung
der Zusicherungen, und der Aufrufhinweis.

ACHT ZUSICHERUNGEN, jede mit eigener nummerierter Ueberschrift und einer
Soll-/Ist-Zeile:

1 Bestand und Selbstauskunft. Zaehle die gelesenen Schiffsseiten je Sprache
und in Summe und drucke die Zahl. Untergrenze als Sperrklinke: unter 440
Seiten ist der Lauf selbst kaputt, nicht die Seite — reissen mit einer
Meldung, die den Ist-Stand nennt. Heute sind es 454 (227 je Sprache, belegt
durch `scripts/lib/metrics-baseline.mjs` Eintrag `seitenSchiffe` = 227).

2 Kapitelgeruest. Je Seite: genau EIN Element mit der Klasse `sd__jump`;
mindestens eines und hoechstens vier Elemente mit der Klasse `sd__chapter`;
jedes davon traegt eine `id` aus der festen Menge `ch-buy`, `ch-profile`,
`ch-gear`, `ch-context`, und keine `id` doppelt. Drucke die Verteilung (wie
viele Seiten mit vier, drei, zwei, einem Kapitel). Klassen werden IMMER ueber
die zerlegte Klassenliste geprueft, nie ueber die literale Zeichenkette des
`class`-Attributs — die Kapitel tragen Modifikatorklassen neben der
Grundklasse.

3 Bijektion Pille zu Kapitel. Jeder Anker innerhalb der Sprungleiste zeigt auf
genau eine vorhandene Kapitel-`id` derselben Seite, und jedes Kapitel mit
`id` hat genau eine Pille. Drucke die Zahl der geprueften Paare. Die eine
Richtung deckt `audit:site` bereits als toten Anker ab; die andere — ein
Kapitel ohne Pille — deckt heute nichts.

4 Der Einheitsrahmen ist weg. Ueber alle Schiffsseiten null Elemente mit der
Klasse `sd__panel`, und je Seite ist die Zahl der Elemente mit der Klasse
`sd__code` GLEICH der Zahl der Kapitel (UI-SPEC Punkt 7: genau eine
Kopfzeilen-Codezeile je Kapitelkopf).

5 Balken nur, wo sie vergleichen (D-02). Ueber alle Schiffsseiten null
Elemente mit der Klasse `sd__gtrack`. Elemente mit der Klasse `sd__proftrack`
gibt es ausschliesslich auf Seiten, die ein Kapitel mit der `id` fuer das
Leistungsprofil tragen, und dort mindestens eines. Drucke beide Summen.

6 Entdopplung (D-03) — der Kern. Verfahren, in dieser Reihenfolge:
(a) Schneide aus der Seite den Inhalt von `div.sd` heraus; das schliessende
Tag wird ueber eine Tiefenzaehlung ueber oeffnende und schliessende `div`
gefunden, gleiches Verfahren wie `cutRegion()` in `verify-sync.mjs`.
(b) Entferne Kommentare sowie die RUEMPFE von `script` und `style`.
(c) Entferne ALLE Attributwerte. Begruendung als Kommentar an der Fundstelle:
der `aria-label` der Perzentilspur traegt den Rohwert absichtlich weiter, das
ist eine Barrierefreiheitszusage und keine Doppelung.
(d) Zerlege in Regionen. Eine Region beginnt bei jedem DIREKTEN Kind von
`div.sd` und zusaetzlich bei jeder Ueberschrift `h3` oder `h4`, die die Klasse
`sd__sub` traegt. Der Regionenschluessel ist die `id` des umschliessenden
Blocks, sonst dessen erste Klasse, jeweils mit fortlaufendem
Unterabschnittsindex. Dieser Satz gilt vor UND nach dem Umbau — er ist die
Bedingung dafuer, dass die Rot-Vorfuehrung gegen den heutigen Stand
aussagekraeftig ist.
(e) Wirf die eine Region weg, die das Raster fuer aehnliche Schiffe enthaelt
(Klasse `sd__simgrid`): dort stehen die Laengen FREMDER Schiffe. Ohne diesen
Ausschluss meldet das Tor Alarm, sobald ein Nachbarschiff zufaellig dieselbe
Laenge hat — ein Fehlalarm, und Fehlalarme sind teurer als Luecken
(Grundsatz 3). Der Ausschluss steht als benannte Ausnahme in der
Ausnahmeliste, nicht im Skriptkoerper.
(f) Sammle aus dem verbliebenen sichtbaren Text Zeichenketten der Form Zahl
plus Einheit. Der Einheitenvorrat ist fest und klein: Meter, SCU, Meter je
Sekunde, Kilometer je Sekunde, HP, DPS. Laengster Treffer gewinnt, damit
Meter je Sekunde nicht als Meter gelesen wird. Nicht im Vorrat und je als
benannte Ausnahme begruendet: aUEC (Preiszeilen wiederholen sich
betriebsbedingt ueber mehrere Verkaufsstellen), Grad je Sekunde, Gigameter,
Sekunden und Minuten (kommen je genau einmal vor und tragen keine
Doppelungsgeschichte).
(g) Urteil: ein Token, das in mehr als EINER Region vorkommt, ist ein Befund.
Jeder Befund muss durch eine benannte Ausnahme erklaert sein, sonst FEHLER.
Drucke: gelesene Seiten, gefundene Token, Befunde, davon durch Ausnahmen
erklaert.

7 Sprachparitaet. Ueber `findPagePairs()` je EN-Seite mit DE-Gegenstueck: die
Zaehlungen von Kapiteln, Sprungleisten-Pillen, Balkenspuren, Perzentilspuren
und Kopfzeilen-Codezeilen muessen paarweise uebereinstimmen. Ein Koerper fuer
beide Sprachen heisst, dass jede Abweichung hier eine sprachbedingte
Verzweigung ist, die es nicht geben darf. Eigene Untergrenze mit
`assertMinimumPairs()`: unter 200 Paaren ist die Paarung selbst kaputt.

8 Zombie-Waechter. Jede Ausnahme aus `scripts/lib/shipcard-exclusions.mjs`
muss in DIESEM Durchgang mindestens einmal gegriffen haben. Eine Ausnahme,
deren Anlass verschwunden ist, deckt sonst spaeter echte Drift zu
(Grundsatz 6). Drucke je Ausnahme, wie oft sie gegriffen hat.

BERICHTSMODUS. Mit dem Schalter `--report` laeuft dasselbe Verfahren, druckt
alle Befunde aus Zusicherung 6 gruppiert nach Token und Region, faellt aber
KEIN Urteil und gibt 0 zurueck. Begruendung im Kopfkommentar: am 16.08.2026
steckten in diesem Projekt zwei Fehler im Pruefer selbst, und ein drittes Tor
waere zu 89 % Fehlalarm gewesen — ein neues Tor faehrt zuerst als reiner
Bericht.

`scripts/lib/shipcard-exclusions.mjs` legt die benannten Ausnahmen an, in der
Form von `scripts/lib/sync-exclusions.mjs`: `id`, Trefferbedingung, und ein
`reason`, der den ANLASS nennt, nicht nur den Sachverhalt. Beim Anlegen
enthaelt sie genau zwei Eintraege — den Ausschluss des Rasters fuer aehnliche
Schiffe und den Einheitenvorrat mit seiner Begruendung. Weitere Eintraege
entstehen ERST aus dem Berichtslauf in Welle 4 und nicht auf Verdacht: eine
Ausnahme auf Vorrat reisst sofort den Zombie-Waechter.

Trage das Tor in `scripts/lib/gate-registry.mjs` in die Schiene-A-Liste ein,
hinter `verify:sync`: `id` und `npm` je `verify:shipcard`, `script` der
Dateipfad, `rail` A, `checks` in einem Satz. `env` bleibt leer — das Skript
beruehrt weder git noch Netz noch Kindprozess. Setze `disabled` mit dem
Anlass: das Tor beurteilt den Zielzustand dieser Phase und ist bis zu deren
Abschluss zwangslaeufig rot; scharf geschaltet wird es in `14-04-PLAN.md`.
Die Praezedenz steht im Kopf von `scripts/verify-sync.mjs` („erst beheben,
dann scharf") und ist im Anlass zu nennen. Ergaenze in `package.json` im
`scripts`-Block die Zeile `verify:shipcard`, im Stil der Nachbarn.

FUEHRE DAS TOR ROT VOR. Baue nichts um; rufe das Skript gegen den heutigen
Stand von `dist/` auf, einmal mit `--report` und einmal ohne. Ohne Schalter
MUSS es reissen. Protokolliere im SUMMARY woertlich: die erste reissende
Zusicherung mit ihrer Soll-/Ist-Zeile, die gelesene Seitenzahl und die Zahl
der Befunde aus Zusicherung 6. Erwartet werden mindestens: null Sprungleisten
statt einer je Seite, zehn Einheitsrahmen auf der Carrack statt null, 14
Balkenspuren auf der Carrack statt null. Wird das Tor gegen den heutigen
Stand GRUEN, ist es kaputt und nicht die Seite — dann ist der Fehler im
Pruefer zu suchen, bevor irgendetwas anderes passiert.
  </action>
  <verify>
    <automated>node scripts/verify-shipcard.mjs --report</automated>
    <automated>node scripts/verify-shipcard.mjs; test $? -ne 0</automated>
    <automated>npm run verify:wiring</automated>
    <automated>npm run build</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-shipcard.mjs --report` laeuft durch und gibt 0 zurueck; die Ausgabe nennt eine Seitenzahl von mindestens 440 und listet Befunde gruppiert nach Token und Region.
    - `node scripts/verify-shipcard.mjs` gibt gegen den heutigen Stand von `dist/` einen Rueckgabecode ungleich 0 zurueck — die vorgefuehrte Rot-Meldung. Ihre erste reissende Zusicherung steht woertlich im SUMMARY.
    - Die Ausgabe enthaelt fuer JEDE der acht Zusicherungen eine Zeile mit den Woertern `Soll` und `Ist` — Selbstauskunft, Grundsatz 2.
    - `npm run verify:wiring` bleibt gruen — Beleg fuer die Bijektion Verzeichnis, Dateibestand, `package.json`.
    - `node -e "import('./scripts/lib/gate-registry.mjs').then(m=>{const e=m.CHECKS.find(c=>c.id==='verify:shipcard');console.log(JSON.stringify({rail:e.rail,hasDisabled:!!e.disabled,hasChecks:!!e.checks}))})"` liefert `rail` A, `hasDisabled` true und `hasChecks` true.
    - `node scripts/run-gate.mjs --list` nennt `verify:shipcard` als ausgesetzten Schuldenposten und fuehrt es NICHT im Laufplan.
    - `npm run gate` bleibt gruen — das ausgesetzte Tor darf die Kette in dieser Welle nicht reissen.
    - `grep -c "reason" scripts/lib/shipcard-exclusions.mjs` liefert mindestens 2, und `node -e "import('./scripts/lib/shipcard-exclusions.mjs').then(m=>console.log(m.EXCLUSIONS.every(e=>typeof e.reason==='string'&&e.reason.length>80)))"` liefert `true` — keine Ausnahme ohne ausformulierten Anlass.
    - `grep -n "child_process\|execFileSync\|fetch(" scripts/verify-shipcard.mjs` liefert keinen Treffer — Schienenfaehigkeit fuer Schiene A, Grundsatz 4.
    - `git status --porcelain` nennt ausschliesslich die vier Dateien dieses Tasks; `src/` bleibt unberuehrt.
  </acceptance_criteria>
  <done>
Das Entdopplungs-Tor existiert, ist verkabelt, war einmal vorgefuehrt rot und
seine Meldung ist protokolliert. `npm run gate` bleibt gruen, weil der Eintrag
seinen Anlass als ausgesetzte Strecke traegt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Die Messsonde und die reproduzierte Ausgangsmessung</name>
  <files>scripts/probes/schiffskarte-messung.mjs, scripts/probes/README.md</files>
  <precondition>Ein Vorschau-Server laeuft gegen das `dist/` DIESES Worktrees (`npm run preview`, Standardport 4321), und unter `C:/Program Files/Google/Chrome/Application/chrome.exe` liegt ein installierter Chrome. `playwright-core` bringt keinen Browser mit.</precondition>
  <read_first>
    - `scripts/probes/mining-locview-messung.mjs` VOLLSTAENDIG — Kopfkommentar („EINE SONDE, KEIN TOR"), die Browser-Kandidatenliste, das Ableiten der Pruefziele aus den Daten statt fester Verdrahtung, das Messen am Bildpunkt ueber `sharp`, die Kontrastrechnung aus `scripts/lib/theme-color.mjs`, und die Aufrufschalter `--base`, `--only`, `--headed`.
    - `scripts/browser-smoke.mjs` Z. 30-60 — Aufloesung von `--base`/`SMOKE_BASE` und dieselbe Browser-Kandidatenliste; die Sonde uebernimmt beide Bauformen wortgleich.
    - `scripts/probes/README.md` VOLLSTAENDIG — Tabellenform und Aufrufzeilen; die neue Sonde bekommt dort ihre eigene Zeile.
    - `scripts/lib/theme-color.mjs` — `contrast()` und `luminance()`; es entsteht KEINE zweite Kontrastformel.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-CONTEXT.md` § „Ausgangsmessung" (Seitenhoehe 5.554 px, Blockhoehen in Scrollreihenfolge) und Konkrete Vorgaben 1 bis 4.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-UI-SPEC.md` Detailvertrag Punkt 1 (Hoehenbudget der Sprungleiste, `scroll-margin-top`), Punkt 8 (die versteckte Bildlaufleiste, der lokale Wert von `--maxw`) und Punkt 9 (die Rechnung auf 4.200 px).
    - `src/data/vehicles.json` — nur die Feldnamen, die den Fuellgrad bestimmen (`lengthM`, `cargoSCU`, `scmSpeed`, `pitch`, `pilotDps`, `hullHp`, `shieldHp`, `qtSpeedMs`, `h2Fuel`, `insClaimMin`, `components`). Nicht die ganze Datei lesen.
    - `src/components/SiteNav.astro` Z. 936-949 — `--nav-h` ist 68 px, mit sichtbarer Zurueck-Marke 104 bis 116 px. Die Sonde MISST diesen Wert, sie nimmt ihn nicht an.
  </read_first>
  <action>
Lege `scripts/probes/schiffskarte-messung.mjs` an, in der Bauform von
`scripts/probes/mining-locview-messung.mjs`. Kopfkommentar mit demselben
Warnsatz: eine Sonde, kein Tor — sie liegt in `scripts/probes/`, wird von
keinem npm-Ziel gerufen und haengt an keiner Strecke der Torkette. Nenne den
Grund (ein echter, installierter Browser; der Baucontainer hat keinen) und den
Zweck (die Hoehen-Sperrklinke aus Erfolgskriterium 6, die Backstop-Punkte aus
`## UI Considerations` und der Kontrast des Kapitel-Zahl-Chips am Bildpunkt).

PRUEFZIELE AUS DEN DATEN, nicht fest verdrahtet. Die Sonde liest
`src/data/vehicles.json` und bestimmt drei Schiffe:
— den Bezug: die Carrack (`anvl-carrack`), weil die Ausgangsmessung an ihr
haengt; ihre Kennung ist der einzige fest genannte Wert, und sie ist als
solcher zu kommentieren.
— das groesste nach `lengthM` (heute `aegs-idris-m`, 243 m).
— das kargste nach einem berechneten Fuellgrad: die Zahl der belegten Felder
aus der oben gelesenen Feldliste (heute `argo-atls` mit 2 von 11).
Erwartete Werte im Kommentar festhalten, damit ein spaeterer Datenlauf, der
sie verschiebt, beim Lesen auffaellt — aber die Auswahl selbst bleibt
berechnet.

MESSMATRIX. Drei Schiffe mal zwei Sprachen (Wurzelpfad und `/de/`-Praefix) mal
zwei Breiten (1280x720 und 360x740) mal zwei Farbmodi (Attribut `data-theme`
auf dem Wurzelelement gesetzt und ein Neuzeichnen abgewartet). Je Lauf
gemessen und mit Soll und Ist gedruckt:

a Seitenhoehe: das Scrollmass des Wurzelelements. Sperrklinke ausschliesslich
fuer die Carrack bei 1280x720 im dunklen Modus: hoechstens 4.200 px. Bis die
Seite umgebaut ist, ist diese Marke zwangslaeufig gerissen — die Sonde meldet
sie als Ist-Wert und gibt in diesem Lauf `--baseline` als Schalter mit, der
das Urteil aussetzt und nur berichtet.

b Sprungleiste: vorhanden oder nicht. Ist sie da, ihre gemessene Hoehe, der
gemessene Wert von `--nav-h`, und ob ihre Unterkante ohne Scrollen oberhalb
von 720 px liegt (Erfolgskriterium 3). Ist sie nicht da, wird das als
`nicht vorhanden` gemeldet — die Sonde darf in dieser Welle nicht daran
sterben, dass es noch keine gibt.

c Der Wert, den `scroll-margin-top` braucht: gemessener `--nav-h` plus
gemessene Sprungleistenhoehe. UI-SPEC Punkt 1 nennt 64 px als aufgerundete
Schaetzung und verlangt ausdruecklich, sie am gerenderten Bildpunkt
nachzumessen und bei Abweichung auf den TATSAECHLICHEN Wert zu korrigieren.

d Sprungleiste bei 360 px: Zahl der Pillen, Scrollbreite gegen sichtbare
Breite des inneren Kastens, und ob jede Pille durch Scrollen erreichbar ist.
Zusaetzlich, ob die Bildlaufleiste des inneren Kastens am Bildpunkt sichtbar
ist — nicht ob sie im CSS steht. Die Bildlaufleisten sind site-weit per
`!important` versteckt; ein Kasten ohne Eintrag in der Ausnahmeliste ist
unsichtbar scrollbar.

e Umbruch: ob eine Pille umbricht oder ihre Hoehe die der uebrigen
uebersteigt (die laengste ist deutsch „Ausstattung", 11 Zeichen).

f Kontrast des Kapitel-Zahl-Chips am Bildpunkt, in BEIDEN Farbmodi, gegen die
Marke 4,5:1. Verfahren wie in der Phase-12-Sonde: eng zugeschnittener
Bildausschnitt, dunkelster und hellster Bildpunkt darin, `contrast()` aus
`scripts/lib/theme-color.mjs`. Kein zweites Kontrastmass.

g Waagerechter Ueberlauf bei 360 px: Scrollbreite des Wurzelelements gegen
seine sichtbare Breite. Soll: kein Ueberlauf.

h Struktur: Zahl der Kapitel und Zahl der Pillen je Seite — als Gegenprobe
zum Tor, gemessen am lebenden Dokument statt am HTML-Text.

Rueckgabecode 1, sobald eine scharfe Marke gerissen ist; mit `--baseline` nur
Bericht. Schalter `--base`, `--only`, `--headed`, `--baseline`. Die Sonde
schreibt NICHTS in den Arbeitsbaum.

Ergaenze `scripts/probes/README.md` um eine Zeile in der bestehenden Tabelle
(Frage, die die Sonde beantwortet; wo ihr Ergebnis liegt: das SUMMARY dieses
Plans und spaeter das von `14-04-PLAN.md`) und um die Aufrufzeile im
bestehenden Block.

FUEHRE DIE AUSGANGSMESSUNG. Baue, starte den Vorschau-Server gegen DIESES
`dist/`, und lasse die Sonde mit `--baseline` laufen. Protokolliere im SUMMARY
die volle Tabelle. Der entscheidende Wert: die Carrack bei 1280x720 muss bei
rund 5.554 px landen. Weicht sie um mehr als 2 % ab, ist die Ursache zu
klaeren und zu benennen, BEVOR die Phase weiterlaeuft — eine Sperrklinke, die
mit einem anderen Massstab gemessen wurde als der Ausgangswert, misst nichts.
Halte zusaetzlich die Ausgangswerte des kargen und des groessten Schiffs fest;
sie sind der Bezug fuer die Schlussmessung in Welle 4.
  </action>
  <verify>
    <automated>node --check scripts/probes/schiffskarte-messung.mjs</automated>
    <automated>node scripts/probes/schiffskarte-messung.mjs --base http://localhost:4321 --baseline</automated>
    <automated>npm run verify:wiring</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/probes/schiffskarte-messung.mjs --base http://localhost:4321 --baseline` laeuft durch, gibt 0 zurueck und druckt mindestens 24 Messzeilen (3 Schiffe x 2 Sprachen x 2 Breiten x 2 Farbmodi).
    - Die gedruckte Seitenhoehe der Carrack bei 1280x720 im dunklen Modus liegt zwischen 5.443 und 5.665 px (5.554 px plus/minus 2 %). Der Ist-Wert steht im SUMMARY. Liegt er ausserhalb, steht die erklaerte Ursache dort statt einer angepassten Marke.
    - Die Sonde nennt fuer das kargste und das groesste Schiff Kennung UND den Grund der Auswahl (Fuellgrad bzw. `lengthM`); die Kennungen sind nicht im Quelltext fest verdrahtet — `grep -c "aegs-idris-m\|argo-atls" scripts/probes/schiffskarte-messung.mjs` liefert 0 ausserhalb von Kommentaren, nachgeprueft mit `grep -v '^\s*\(//\|\*\|/\*\)' scripts/probes/schiffskarte-messung.mjs | grep -c "aegs-idris-m\|argo-atls"` = 0.
    - Die Messzeilen zur Sprungleiste melden in diesem Lauf durchgaengig `nicht vorhanden` — es gibt sie noch nicht, und die Sonde stirbt nicht daran.
    - `grep -c "theme-color.mjs" scripts/probes/schiffskarte-messung.mjs` liefert 1 — die Kontrastrechnung kommt aus der bestehenden Bibliothek.
    - `npm run verify:wiring` bleibt gruen — eine Datei unter `scripts/probes/` beruehrt die Bijektion nicht, und das ist zu belegen statt anzunehmen.
    - `grep -c "schiffskarte-messung" scripts/probes/README.md` liefert mindestens 2 (Tabellenzeile und Aufrufzeile).
    - `git status --porcelain` nennt ausschliesslich die zwei Dateien dieses Tasks.
  </acceptance_criteria>
  <done>
Die Sonde existiert, waehlt ihre drei Pruefschiffe aus den Daten, misst in
beiden Sprachen, beiden Breiten und beiden Farbmodi — und hat die
Ausgangsmessung mit demselben Massstab reproduziert, mit dem spaeter die
Sperrklinke gemessen wird.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Vollstaendige Aufzaehlung der Bezeichner, die diese PHASE neu anlegt — ueber
alle vier Plaene. Der Drift-Abgleich liest diesen Abschnitt, um neu
Entstandenes nicht als Abweichung zu melden. Was hier nicht steht, ist nicht
neu.

**Neue Dateien:** `scripts/verify-shipcard.mjs`,
`scripts/lib/shipcard-exclusions.mjs`,
`scripts/probes/schiffskarte-messung.mjs`

**Neues npm-Skript:** `verify:shipcard`

**Neue Gate-Registry-Kennung:** `verify:shipcard` (Schiene A, in Welle 1 mit
`disabled`, in Welle 4 scharf)

**Neue CSS-Klassen und -Selektoren (Welle 2 bis 4):** `.sd__jump`,
`.sd__jump__in`, `.sd__jump a`, `.sd__jump a.is-active`, `.sd__chapter`,
`.sd__chapter--buy`, `.sd__chapter--profile`, `.sd__chapter--gear`,
`.sd__chapter--context`, `.sd__chtitle`, `.sd__chnum`, `.sd__ch2col`,
`.sd__chsec`

**Neue Element-Ids:** `ch-buy`, `ch-profile`, `ch-gear`, `ch-context`

**Neue i18n-Schluessel in `src/i18n/ui.ts`, je DE und EN:** `ship.ch.gear`,
`ship.ch.context`, `ship.jump.buy`, `ship.jump.profile`, `ship.jump.aria`,
`ship.code.spec`, `ship.code.ctx`, `arm.stat.totalDps`

**Neuer Eintrag in der Bildlaufleisten-Ausnahmeliste
(`assets/mobile-ux.css`, Abschnitt 5c):** `.sd__jump__in`

**Erweiterte, nicht neue Typen:** `ProfileBar` in `src/lib/shipExtras.ts`
bekommt das Feld `showValue`; `MetricDef` dort bekommt `valueInProfile`

**Getilgt, nicht neu:** die Klasse `sd__panel` samt Regel; die fuenf
Gauge-Regeln `sd__g`, `sd__grow`, `sd__gtrack`, `sd__gfill`, `sd__gval` samt
zugehoeriger Medienabfrage; der Aufruf von `buildFacts` und die Konstante
`facts` in `src/components/ShipDetail.astro`; der Eintrag fuer den
Quantum-Tank in der Liste `fuel` in `src/lib/shipVisuals.ts`

**Ausdruecklich NICHT getilgt:** `.sd__glabel` — die Klasse traegt neben den
Balkenzeilen auch die beiden Ueberschriften im Abschnitt Masse &amp; Fracht und
bleibt deshalb stehen. `buildFacts` und der Typ `Fact` in
`src/lib/shipFacts.ts` bleiben als allgemeine Bibliotheksausfuhr erhalten;
entfernt wird nur ihre Verwendung.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dist/`-Dateien -> Pruefskript | Das Tor liest gebautes HTML und wertet es mit regulaeren Ausdruecken aus. Eingabe ist eigenes Bauprodukt, kein Fremdinhalt — aber Katalogdaten (Schiffs-, Shop- und Ortsnamen) stecken darin. |
| Vorschau-Server -> Sonde | Die Sonde laedt Seiten ueber HTTP von einem lokal laufenden Server und steuert einen echten Browser. |
| Prozessargumente -> Skripte | `--base`, `--only` sind vom Aufrufer kontrolliert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Denial of Service | Regulaere Ausdruecke in `scripts/verify-shipcard.mjs` ueber 454 HTML-Dateien | medium | mitigate | Keine verschachtelten Quantoren, keine Rueckverfolgung ueber ganze Dokumente: Tag-Abtastung wie in `verify-sync.mjs` `tokenize()` mit einem einzigen linearen Durchlauf je Datei. Laufzeit wird als Ist-Wert gedruckt; ueber 30 s ist ein Befund, kein Normalzustand. |
| T-14-02 | Information Disclosure | Ausgabe des Tors und der Sonde | low | mitigate | Beide drucken nur Klassennamen, Zahlen und Regionenschluessel. Sie schreiben NICHTS in den Arbeitsbaum und geben keine Dateiinhalte aus. |
| T-14-03 | Tampering | `--base` der Sonde zeigt auf einen fremden Host | low | accept | Handwerkzeug auf der Entwicklungsmaschine, kein CI-Lauf; dieselbe Lage wie bei `scripts/browser-smoke.mjs`, das fremde Hosts abbricht statt zu werten. Die Sonde meldet den benutzten Basispfad in ihrer Kopfzeile, damit im Protokoll steht, welches Artefakt gemessen wurde (Grundsatz 7). |
| T-14-04 | Repudiation | Ein spaeterer Lauf kann nicht mehr sagen, gegen welchen Stand gemessen wurde | medium | mitigate | Tor und Sonde drucken je eine Kopfzeile mit Artefakt (Pfad bzw. Basis-URL) und gelesener Einheitenzahl. Die Rot-Meldung und die Ausgangsmessung wandern woertlich ins SUMMARY. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | accept | Diese Phase installiert kein Paket. `playwright-core` und `sharp` sind bereits devDependencies; `package-lock.json` bleibt unberuehrt und steht NICHT in `files_modified`. Am `package.json` wird ausschliesslich eine Zeile im `scripts`-Block ergaenzt. Gegenprobe: `git status --porcelain` nennt `package-lock.json` nicht. |

ASVS-Stufe 1, blockierend ab `high`. Der einzige `high`-Befund ist
gegenstandslos (keine Installation). V5 (Eingabevalidierung) ist die einzige
beruehrte Kategorie und betrifft hier ausschliesslich Werkzeuge auf der
Entwicklungsmaschine, keinen ausgelieferten Pfad.
</threat_model>

<verification>
Vor jedem Push, ohne Ausnahme (`CLAUDE.md` § Liefern):

```
npm run build
npm run gate
```

Beides gruen. Dieser Plan aendert nichts am Layout und nichts an der
ausgelieferten Seite — der Vorschau-Bau mit `STAGING=1` ist hier NICHT
faellig; er wird ab Welle 2 gefahren, wo das Layout beruehrt wird.

Auf Windows in PowerShell `npm.cmd` statt `npm`. Der Vorschau-Server im
Worktree serviert dessen EIGENES `dist/`.

Diese Strecken kann dieser Dateisatz reissen und muessen einzeln gruen sein:
`verify:wiring` (der Meta-Waechter — er faengt genau diesen Fehler), `verify`,
`verify:metrics`.
</verification>

<success_criteria>
- `scripts/verify-shipcard.mjs` prueft gegen `dist/`, druckt acht Zusicherungen
  mit Soll und Ist und nennt die gelesene Seitenzahl.
- Das Tor war einmal vorgefuehrt rot; die Meldung steht woertlich im SUMMARY.
- Der Berichtsmodus `--report` faellt kein Urteil und gibt 0 zurueck.
- `scripts/lib/shipcard-exclusions.mjs` traegt genau zwei Ausnahmen, jede mit
  ausformuliertem Anlass; der Zombie-Waechter ist verkabelt.
- Der Registry-Eintrag steht auf Schiene A mit `disabled` und benanntem
  Anlass; `npm run gate` bleibt gruen und druckt die Schuld.
- `verify:wiring` gruen — Verzeichnis, Datei und `package.json` deckungsgleich.
- Die Sonde misst drei aus den Daten gewaehlte Schiffe in zwei Sprachen, zwei
  Breiten und zwei Farbmodi und stirbt nicht an der fehlenden Sprungleiste.
- Die Ausgangsmessung ist reproduziert: Carrack bei 1280x720 rund 5.554 px.
- `npm run build` und `npm run gate` gruen; `src/` unberuehrt.
</success_criteria>

<output>
Create `.planning/phases/14-schiffs-datenkarte-entstapeln/14-01-SUMMARY.md` when done
</output>
