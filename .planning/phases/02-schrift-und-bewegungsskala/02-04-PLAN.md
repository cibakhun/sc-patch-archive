---
phase: 02-schrift-und-bewegungsskala
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/components/ShipDetail.astro
  - src/components/CraftingApp.astro
  - src/components/PrecisionJumpApp.astro
  - src/components/MiningApp.astro
  - src/components/ItemFinderApp.astro
  - src/components/MissionDetail.astro
  - src/components/SupportBody.astro
  - src/components/SignatureIdentifier.astro
  - src/components/RefineryFinder.astro
  - src/components/MissionsApp.astro
  - src/components/FracturingCalc.astro
  - src/components/SearchOverlay.astro
  - src/components/FeedbackForm.astro
  - src/components/ItemFinderPage.astro
  - src/components/ArmorSets.astro
  - src/components/TopicFacts.astro
  - src/components/WikeloBridge.astro
  - src/components/Attribution.astro
  - src/components/OnepagerLink.astro
  - src/components/RelatedTopics.astro
  - src/components/ToolHelp.astro
  - src/components/HomeLaunchpad.astro
  - src/components/FxToggle.astro
  - src/components/LangSwitcher.astro
  - src/components/ThemeToggle.astro
  - src/components/dossier/PatchDossier.astro
  - src/components/dossier/QuickFacts.astro
  - src/layouts/Layout.astro
  - assets/tool-help.js
  - assets/crafting-app.js
  - assets/hero-video.js
autonomous: true
requirements: [TYPO-01, TYPO-02]

must_haves:
  truths:
    - "Die elf Werkzeuge und die geteilten Bauteile tragen ihre Schriftgrade, Laufweiten und Bedienübergänge aus der Skala (D-01, D-02, TYPO-01, TYPO-02)."
    - "Die Hilfe-Mechanik aus Phase 1.2 und der Effekt-Umschalter aus Phase 1.1 funktionieren unverändert."
    - "Auch die vier zur Laufzeit per JavaScript gesetzten Werte folgen der Skala — kein bewusst übergangener Rest."
  artifacts: []
  key_links:
    - "ToolHelp.astro trägt die Hilfe-Mechanik aller elf Werkzeuge — verify:help ist das Tor dafür"
    - "FxToggle.astro trägt den Partikel-Umschalter aus Phase 1.1 — verify:fx ist das Tor dafür"
    - "Layout.astro setzt html { font-size: 112.5% } — dieser eine Wert ist die Wurzel der ganzen Skala und wird NICHT auf ein Token gelegt"
---

<objective>
Die Werkzeug- und Bauteil-Körper: 27 Komponenten unter `src/components/` (plus zwei unter
`dossier/`), das Layout und die vier zur Laufzeit gesetzten Werte in `assets/*.js`.
573 `font-size`, 246 `letter-spacing`, 123 `transition` — der zweitgrößte Block der Phase und
der mit den meisten laufenden Mechaniken darin.

Purpose: Hier sitzen die elf Werkzeuge, die Phase 1.2 gerade erst erklärt hat, und der
Umschalter, den Phase 1.1 gerade erst eingebaut hat. Beide haben ein Prüftor; nach diesem Plan
müssen beide unverändert grün sein.
Output: 30 umgestellte Dateien, `verify:help` und `verify:fx` unverändert grün.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schrift-und-bewegungsskala/02-01-PLAN.md
@.planning/phases/02-schrift-und-bewegungsskala/02-03-SUMMARY.md
@scripts/migrate-typo-motion.mjs
@scripts/audit-typo-motion.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die sechs großen Werkzeug-Körper</name>
  <files>src/components/ShipDetail.astro, src/components/CraftingApp.astro, src/components/PrecisionJumpApp.astro, src/components/MiningApp.astro, src/components/ItemFinderApp.astro, src/components/MissionDetail.astro</files>
  <action>
    Diese sechs tragen zusammen 329 `font-size`, 124 `letter-spacing` und 58 `transition` —
    mehr als die Hälfte des Plans. Ausgangszahlen zum Gegenprüfen (fs/ls/tr): `ShipDetail`
    76/43/22, `CraftingApp` 71/14/13, `PrecisionJumpApp` 54/27/7, `MiningApp` 50/18/5,
    `ItemFinderApp` 46/10/10, `MissionDetail` 32/12/1.

    Vorgehen je Datei: erst `--dry --only <Datei>`, die Verweigerungsliste lesen, dann
    schreiben, dann `audit-typo-motion.mjs --only <Datei> --expect-remaining 0`. Die
    Verweigerungen von Hand nachziehen, soweit sie Entscheidungen sind — die
    `clamp()`-Formeln, die `em`-Schriftgrade und die dynamischen `style={\`…\`}`-Zuweisungen
    bleiben nach der Regel aus `02-01-PLAN.md` § „Ausreißer" stehen und brauchen keine
    Einzelentscheidung.

    Zwei Stellen brauchen Aufmerksamkeit:

    - `ItemFinderApp.astro` hat mit `1.75rem` (−8,6 %) und `1.3rem` (+7,7 %) zwei der sieben
      site-weiten Ausreißer über der 6-%-Schwelle. Der Codemod verweigert sie; einrasten ist
      die Entscheidung (D-05), die Verschiebung gehört ins Summary beziffert. `1.3rem` trifft
      dabei `--fs-13` exakt — die 7,7 % im Bericht kommen von einem NACHBARWERT in derselben
      Datei; im Zweifel die Ausgabe des Erhebungsskripts lesen statt zu raten.
    - `ItemFinderApp.astro` und `CraftingApp.astro` bauen ihre Oberflächen zum großen Teil
      client-seitig auf (`assets/item-finder-app.js`, `assets/crafting-app.js`). Die
      gescopten `<style>`-Blöcke der Komponenten gelten trotzdem für dieses Markup — die
      Tokens vererben sich über `:root`. Es ist keine Sonderbehandlung nötig, aber nach dem
      Bau ist zu prüfen, dass die Chips und Karten dort weiterhin auf Skalenwerte rendern
      (`dist/_astro/*.css` durchsuchen, nicht das HTML: das Markup entsteht erst im Browser).

    ⚠ `MiningApp.astro` und `PrecisionJumpApp.astro` tragen die Hilfe-Einstiege aus Phase 1.2
    (Annahmen A2, noch nicht vom Betreiber bestätigt). An Platzierung und Markup wird nichts
    geändert — nur an Schriftgrad, Laufweite und Übergangsdauer.
  </action>
  <verify>
    <automated>node scripts/audit-typo-motion.mjs --only src/components/ShipDetail.astro --only src/components/CraftingApp.astro --only src/components/PrecisionJumpApp.astro --only src/components/MiningApp.astro --only src/components/ItemFinderApp.astro --only src/components/MissionDetail.astro --expect-remaining 0</automated>
    <automated>npm.cmd run build; npm.cmd run verify:typo; npm.cmd run verify:help</automated>
  </verify>
  <done>
    Die sechs Dateien melden 0 verbliebene skalenpflichtige Rohwerte. `npm run verify:help`
    meldet unverändert 11 von 11 Werkzeugen mit allen fünf Zusicherungen. Die beiden Ausreißer
    in `ItemFinderApp.astro` sind im Summary mit ihrer Verschiebung beziffert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Die 24 kleineren Bauteile, das Layout und die vier Laufzeitwerte</name>
  <files>src/components/SupportBody.astro, src/components/SignatureIdentifier.astro, src/components/RefineryFinder.astro, src/components/MissionsApp.astro, src/components/FracturingCalc.astro, src/components/SearchOverlay.astro, src/components/FeedbackForm.astro, src/components/ItemFinderPage.astro, src/components/ArmorSets.astro, src/components/TopicFacts.astro, src/components/WikeloBridge.astro, src/components/Attribution.astro, src/components/OnepagerLink.astro, src/components/RelatedTopics.astro, src/components/ToolHelp.astro, src/components/HomeLaunchpad.astro, src/components/FxToggle.astro, src/components/LangSwitcher.astro, src/components/ThemeToggle.astro, src/components/dossier/PatchDossier.astro, src/components/dossier/QuickFacts.astro, src/layouts/Layout.astro, assets/tool-help.js, assets/crafting-app.js, assets/hero-video.js</files>
  <action>
    Der Rest der Komponentenwurzel — 244 `font-size`, 122 `letter-spacing`, 65 `transition` —
    plus drei Sonderfälle.

    **Sonderfall 1: `src/layouts/Layout.astro`.** Die Datei trägt genau eine `font-size` und
    eine `letter-spacing`. Die `font-size` ist `html { font-size: 112.5% }` in Zeile 240 —
    **sie bleibt wörtlich stehen.** Das ist die Wurzel, aus der `1rem = 18px` folgt; sie durch
    ein Token zu ersetzen, das selbst in `rem` rechnet, wäre ein Zirkelbezug. Der Codemod
    verweigert Prozentwerte ohnehin; die Regel gehört trotzdem in einen Kommentar an der
    Stelle, damit sie nicht bei einem späteren Durchlauf doch fällt. Die `letter-spacing` sitzt
    in einem rohen `style="…"`-Attribut eines Entwickler-Hinweisbanners (Zeile 282) — auf ein
    Token legen, das Attribut liest Custom Properties genauso.

    **Sonderfall 2: `ToolHelp.astro` und `FxToggle.astro`.** Beide tragen frisch gebaute
    Mechanik: die Hilfe aus Phase 1.2 (`verify:help`, elf Werkzeuge, fünf Zusicherungen) und
    den Partikel-Umschalter aus Phase 1.1 (`verify:fx`, sieben Zusicherungen, darunter eine
    Sprachparitätsprüfung über 8.677 Seitenpaare). Klassennamen, Attributnamen, Ereignisnamen
    und die Reihenfolge im Kopf bleiben unangetastet — nur Werte ändern sich. `FxToggle.astro`
    hat 8 `transition` bei nur 2 `font-size`; das ist der Umschalter selbst und damit reine
    Bedienbewegung, also vollständig skalenpflichtig.

    **Sonderfall 3: die vier Laufzeitwerte in `assets/*.js`.** `assets/tool-help.js` setzt zwei
    `font-size` und `assets/crafting-app.js` eine, jeweils in einer per JavaScript erzeugten
    Stilzeichenkette; `assets/hero-video.js` setzt einen `transition`. Sie sind bisher in
    keiner Erhebung dieser Phase enthalten, weil das Erhebungsskript `src/**/*.astro` und
    `assets/*.css` durchsucht. D-01 verlangt site-weit — es wären genau vier bewusst
    übergangene Werte, und das ist der Anfang der nächsten Drift. Auf `var(--fs-N)` bzw.
    `var(--dur-*)`/`var(--ease-ui)` umstellen: Custom Properties funktionieren in einer per
    JavaScript gesetzten Stilzeichenkette unverändert, weil sie über `:root` vererbt werden.
    Diese vier Stellen im Summary namentlich nennen — sie sind eine Erweiterung gegenüber der
    Erhebung, keine stille Zugabe.

    ⚠ `ItemFinderPage.astro` enthält Scroll-Reveal. Dessen Übergang bleibt unangetastet
    (Ambiente, FX-07) — der Codemod verweigert ihn, das ist die richtige Antwort.
  </action>
  <verify>
    <automated>node scripts/audit-typo-motion.mjs --only src/components/ --only src/layouts/ --expect-remaining 0</automated>
    <automated>node -e "const fs=require('fs');const L=fs.readFileSync('src/layouts/Layout.astro','utf8');const wurzel=L.includes('font-size: 112.5%')||L.includes('font-size:112.5%');let rest=0;for(const f of ['assets/tool-help.js','assets/crafting-app.js','assets/hero-video.js']){const s=fs.readFileSync(f,'utf8');const m=s.match(/font-size\s*:\s*(?!var\()[^;}\`'\x22]+/g)||[];const t=s.match(/transition\s*:\s*(?!var\()[^;}\`'\x22]+/g)||[];rest+=m.length+t.length;if(m.length+t.length)console.log('  Rest in '+f+': '+[...m,...t].join(' | '));}console.log('Wurzelwert 112.5% steht:',wurzel,'| Rohwerte in assets/*.js:',rest);if(!wurzel||rest)process.exit(1)"</automated>
    <automated>npm.cmd run build; npm.cmd run verify:typo; npm.cmd run verify:fx; npm.cmd run verify:help; npm.cmd run verify; npm.cmd run audit:site; npm.cmd run audit:csp</automated>
  </verify>
  <done>
    `src/components/` und `src/layouts/` melden 0 verbliebene skalenpflichtige Rohwerte.
    `html { font-size: 112.5% }` steht unverändert in `Layout.astro`. Die vier Laufzeitwerte in
    `assets/*.js` lesen aus der Skala. `verify:fx` (7 Zusicherungen) und `verify:help`
    (11 von 11) sind unverändert grün; die übrigen Hausgates ebenso.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codemod → 30 Komponenten mit laufender Mechanik | Die Grenze ist „nur Werte, nie Namen": Klassen-, Attribut- und Ereignisnamen der Phasen 1.1 und 1.2 dürfen sich nicht ändern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Denial of Service | `ToolHelp.astro`, `FxToggle.astro` | medium | mitigate | Ein Treffer auf einen Klassen- oder Ereignisnamen würde die Hilfe oder den Partikel-Umschalter lautlos abhängen. Gegenmaßnahme: `npm run verify:help` und `npm run verify:fx` laufen im Verify-Schritt beider Tasks; der Codemod fasst nur Werte hinter `font-size:`/`letter-spacing:`/`transition:` an, nie Selektoren. |
| T-02-09 | Tampering | `src/layouts/Layout.astro` Zeile 240 | high | mitigate | `html { font-size: 112.5% }` ist die Wurzel der gesamten Skala; ein Token an dieser Stelle wäre ein Zirkelbezug und würde die Seitenschrift auf den Browser-Default zurückfallen lassen. Gegenmaßnahme: eigene Zusicherung im Verify-Schritt von Task 2 plus ein Kommentar an der Stelle. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Keine Paketinstallation in diesem Plan (`02-RESEARCH.md` § Package Legitimacy Audit: nicht anwendbar). |
</threat_model>

<verification>
- `node scripts/audit-typo-motion.mjs --only src/components/ --only src/layouts/ --expect-remaining 0`
- `npm run verify:fx` sieben Zusicherungen grün, `npm run verify:help` 11 von 11
- `npm run verify:typo`, `npm run verify`, `audit:site`, `audit:csp` grün
- `git diff --stat`: genau die im Frontmatter gelisteten Dateien
</verification>

<success_criteria>
Alle Werkzeug- und Bauteil-Körper lesen Schrift und Bedienbewegung aus der Skala, die
Mechaniken der Phasen 1.1 und 1.2 sind nachweislich unberührt, und der Wurzelwert der
Schriftgröße steht unverändert.
</success_criteria>

<output>
Create `.planning/phases/02-schrift-und-bewegungsskala/02-04-SUMMARY.md` when done
</output>
