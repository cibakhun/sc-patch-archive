---
phase: 02-schrift-und-bewegungsskala
plan: 06
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/pages/evolution.astro
  - src/pages/de/evolution.astro
  - src/pages/downloads.astro
  - src/pages/de/downloads.astro
  - src/pages/datenschutz.astro
  - src/pages/de/datenschutz.astro
  - src/pages/impressum.astro
  - src/pages/de/impressum.astro
  - src/pages/precision-jump.astro
  - src/pages/de/precision-jump.astro
  - src/pages/404.astro
  - src/components/topics/
  - src/components/account/RefineryDashboard.astro
  - src/components/account/ProfileCard.astro
  - src/components/account/FriendsManager.astro
  - src/components/account/AccountShell.astro
  - src/components/account/AccountDashboard.astro
  - src/components/pilot/PilotPage.astro
  - src/components/ships/ShipsOverview.astro
autonomous: true
requirements: [TYPO-01, TYPO-02]

must_haves:
  truths:
    - "Die handgepflegten Seitenpaare, die 22 Themen-Körper, die Konto-Ansichten und die Schiffsübersicht tragen Schrift und Bedienbewegung aus der Skala (D-01, D-02)."
    - "Jedes DE/EN-Paar ist im selben Arbeitsschritt gefallen — kein Paar läuft nach diesem Plan auseinander (SYNC-01)."
  artifacts: []
  key_links:
    - "src/pages/ enthält die von Hand doppelt gepflegten Seitenpaare — hier ist die Sprachparität am dünnsten und ein einseitiger Durchlauf am wahrscheinlichsten"
    - "ShipsOverview.astro ist seit Phase 6 EIN gemeinsamer Körper für /schiffe.html und /de/schiffe.html — dort gibt es kein Paar mehr, nur eine Datei"
---

<objective>
Der letzte Block der Breite: 11 Seitendateien, 22 Themen-Körper, sechs Konto-/Piloten-Ansichten
und die Schiffsübersicht — zusammen 40 Dateien mit 364 `font-size`, 149 `letter-spacing`,
57 `transition`.

Purpose: Hier liegen die von Hand doppelt gepflegten Seitenpaare. `CONCERNS.md` führt sie als
Class-A-Befund; `verify-fx.mjs` und `verify-help.mjs` vergleichen bereits 8.677 Paare, und ein
einseitiger Durchlauf würde dort auffallen. Der Plan behandelt jedes Paar deshalb als EINE
Einheit, nicht als zwei Dateien.
Output: 40 umgestellte Dateien, alle Seitenpaare deckungsgleich.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schrift-und-bewegungsskala/02-01-PLAN.md
@.planning/phases/02-schrift-und-bewegungsskala/02-03-SUMMARY.md
@scripts/migrate-typo-motion.mjs
@scripts/verify-typo-motion.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die fünf Seitenpaare und die 404-Seite — jedes Paar in einem Schritt</name>
  <files>src/pages/evolution.astro, src/pages/de/evolution.astro, src/pages/downloads.astro, src/pages/de/downloads.astro, src/pages/datenschutz.astro, src/pages/de/datenschutz.astro, src/pages/impressum.astro, src/pages/de/impressum.astro, src/pages/precision-jump.astro, src/pages/de/precision-jump.astro, src/pages/404.astro</files>
  <action>
    123 `font-size`, 49 `letter-spacing`, 24 `transition` in elf Dateien. Ausgangszahlen je
    Paar (fs/ls/tr, für EN und DE gleich): `evolution` 28/13/5, `downloads` 20/8/6,
    `datenschutz` 5/1/0, `impressum` 3/1/0, `precision-jump` 3/1/0; `404.astro` 5/2/1 steht
    allein.

    SYNC-01 verlangt, dass eine Änderung an einer Seitendatei die EN- und die DE-Fassung im
    selben Arbeitsschritt trifft. Praktisch heißt das: `--only` bekommt beide Pfade eines
    Paares gleichzeitig, und die Sollzahl wird für beide zusammen geprüft. Ein Paar, bei dem
    die Zahlen auseinandergehen, ist ein Fund — dann sind die beiden Fassungen bereits vor
    diesem Plan auseinandergelaufen, und das gehört ins Summary, nicht stillschweigend
    angeglichen. `downloads` trägt bereits sechs `transition` je Fassung und ist damit das
    Paar mit dem meisten Bewegungsanteil.

    `precision-jump` ist zusätzlich in `CONCERNS.md` als driftendes Paar verzeichnet (die
    Seiten-Chrome ist dort von Hand gedoppelt, siehe SYNC-04). Dieses Paar ausdrücklich Zeile
    für Zeile vergleichen, nicht nur die Trefferzahlen.

    ⚠ `src/pages/404.astro` hat kein DE-Gegenstück; die Paritätsprüfung von `verify:typo`
    überspringt sie automatisch. Das ist richtig und kein Fund.
  </action>
  <verify>
    <automated>node scripts/audit-typo-motion.mjs --only src/pages/ --expect-remaining 0</automated>
    <automated>node -e "const fs=require('fs');const paare=[['evolution.astro'],['downloads.astro'],['datenschutz.astro'],['impressum.astro'],['precision-jump.astro']];const c=(s,n)=>s.split(n).length-1;const k=['var(--fs-','var(--ls-','var(--dur-','var(--ease-ui)'];let bad=0;for(const [f] of paare){const a=fs.readFileSync('src/pages/'+f,'utf8'),b=fs.readFileSync('src/pages/de/'+f,'utf8');for(const n of k){if(c(a,n)!==c(b,n)){console.error('  Paar '+f+' Abweichung bei '+n+': EN='+c(a,n)+' DE='+c(b,n));bad++;}}console.log('  '+f+': '+k.map(n=>n+'='+c(a,n)).join(' '));}console.log('Paar-Abweichungen:',bad);if(bad)process.exit(1)"</automated>
    <automated>npm.cmd run build; npm.cmd run verify:typo</automated>
  </verify>
  <done>
    `src/pages/` meldet 0 verbliebene skalenpflichtige Rohwerte. Für alle fünf Paare stimmen
    die vier Zählmuster zwischen EN und DE überein. `verify:typo` Zusicherung 5
    (Sprachparität) ist grün und nennt ≥ 60 verglichene Paare.
  </done>
</task>

<task type="auto">
  <name>Task 2: Themen-Körper, Konto-Ansichten und Schiffsübersicht</name>
  <files>src/components/topics/, src/components/account/RefineryDashboard.astro, src/components/account/ProfileCard.astro, src/components/account/FriendsManager.astro, src/components/account/AccountShell.astro, src/components/account/AccountDashboard.astro, src/components/pilot/PilotPage.astro, src/components/ships/ShipsOverview.astro</files>
  <action>
    29 Dateien, 241 `font-size`, 100 `letter-spacing`, 33 `transition`. Drei Gruppen mit
    unterschiedlichem Charakter:

    **Die 22 Themen-Körper** (`src/components/topics/`, 116/39/6). Jeweils EIN Körper für DE
    und EN — es gibt hier kein Paritätsrisiko, sondern nur eine Datei je Thema. Die drei
    größten sind `wikelo-emporium` 26/12/3, `mining` 18/6/2 und
    `4-0-1-fight-for-pyro` 7/2/1; die übrigen 19 liegen bei 2–7 `font-size`. Auffällig: 53 der
    116 Schriftgrade sind `clamp()`-Formeln — mehr als in jeder anderen Gruppe. Der Codemod
    verweigert sie, die Ausbeute ist entsprechend klein, und das ist richtig so.
    `mining.astro` trägt zwei unabhängige Hilfe-Instanzen aus Phase 1.2 (Mining und
    Refinery-Finder auf derselben gebauten Seite) — an deren Markup und Kennungen ändert sich
    nichts.

    **Die sechs Konto-/Piloten-Ansichten** (98/47/23). `PilotPage.astro` trägt mit `2.8rem`
    (−10,7 %) und `2.2rem` (−9,1 %) zwei der sieben site-weiten Ausreißer über der 6-%-Schwelle;
    `RefineryDashboard.astro` und `FriendsManager.astro` je eine `1.5rem`-Stelle (−6,7 %). Der
    Codemod verweigert sie, die Entscheidung lautet einrasten (D-05), und die Verschiebung
    gehört im Summary beziffert. `PilotPage.astro` und die Konto-Ansichten liegen hinter einer
    Anmeldung bzw. einer öffentlichen Profil-URL; sie sind maschinell prüfbar, aber die
    Sichtprüfung dort braucht ein Konto — dieser Punkt geht in Plan 07 an den Betreiber, nicht
    an den Executor.

    **`ShipsOverview.astro`** (27/14/4). Seit Phase 6 EIN gemeinsamer Körper für
    `/schiffe.html` und `/de/schiffe.html`; die Filterleiste aus den Phasen 6 und 7 sitzt
    darin. Nur Werte ändern, keine Feldnamen — `sf-rolefam`, `sf-compsize`, `sf-size` und die
    übrigen Kennungen sind Vertrag für die Tests aus Phase 7
    (`tests/e2e/ship-component-filter.test.js`, 26 Fälle).
  </action>
  <verify>
    <automated>node scripts/audit-typo-motion.mjs --only src/components/topics/ --only src/components/account/ --only src/components/pilot/ --only src/components/ships/ --expect-remaining 0</automated>
    <automated>npm.cmd run build; npm.cmd run verify:typo; npm.cmd run verify:help; npm.cmd run test:e2e</automated>
    <automated>npm.cmd run verify; npm.cmd run audit:site; npm.cmd run audit:csp; npm.cmd run verify:fx</automated>
  </verify>
  <done>
    Die 29 Dateien melden 0 verbliebene skalenpflichtige Rohwerte. `npm run test:e2e` läuft
    nicht schlechter als vorher (insbesondere die 26 Filter-Testfälle aus Phase 7).
    `verify:help` meldet unverändert 11 von 11. Die vier Ausreißer in `PilotPage.astro`,
    `RefineryDashboard.astro` und `FriendsManager.astro` sind im Summary mit ihrer
    prozentualen Verschiebung beziffert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codemod → handgepflegte DE/EN-Seitenpaare | Ein einseitiger Lauf bricht die Deckungsgleichheit. Die Grenze wird pro Paar gezogen, nicht pro Datei. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | `src/pages/` Seitenpaare | high | mitigate | Ein einseitiger Durchlauf lässt EN und DE auseinanderlaufen — sichtbar erst für den Besucher der anderen Sprachfassung. Gegenmaßnahmen: `--only` bekommt beide Pfade eines Paares gleichzeitig, eine eigene Paar-Zusicherung im Verify-Schritt von Task 1, und `verify:typo` Zusicherung 5 über alle Seitenpaare. |
| T-02-13 | Denial of Service | `ShipsOverview.astro` Filterkennungen | medium | mitigate | Ein Treffer auf `sf-rolefam`/`sf-compsize`/`sf-size` würde die Filter aus Phase 6/7 abhängen. Gegenmaßnahme: `npm run test:e2e` mit den 26 Filter-Testfällen im Verify-Schritt; der Codemod fasst nur Werte hinter den drei Eigenschaftsnamen an. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Keine Paketinstallation in diesem Plan (`02-RESEARCH.md` § Package Legitimacy Audit: nicht anwendbar). |
</threat_model>

<verification>
- `node scripts/audit-typo-motion.mjs --only src/pages/ --only src/components/topics/ --only src/components/account/ --only src/components/pilot/ --only src/components/ships/ --expect-remaining 0`
- Alle fünf Seitenpaare mit übereinstimmenden Zählmustern für EN und DE
- `npm run test:e2e`, `verify:help`, `verify:fx`, `verify:typo`, `verify`, `audit:site`, `audit:csp`: nicht schlechter als vorher
</verification>

<success_criteria>
Die handgepflegten Seitenpaare, die Themen-Körper, die Konto-Ansichten und die
Schiffsübersicht lesen Schrift und Bedienbewegung aus der Skala, und kein Seitenpaar ist durch
diesen Plan auseinandergelaufen.
</success_criteria>

<output>
Create `.planning/phases/02-schrift-und-bewegungsskala/02-06-SUMMARY.md` when done
</output>
