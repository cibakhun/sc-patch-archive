---
phase: 02-schrift-und-bewegungsskala
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/e2e/typo-motion-morph.test.js
  - .planning/WINDOWS.md
autonomous: true
requirements: [TYPO-03]

must_haves:
  truths:
    - "Kopfleiste und Startseite lesen sich nach der Umstellung weiterhin als EIN Bewegungsbild — die Wandlung aus Phase 1 hat dieselben Eingangswerte wie vorher (TYPO-03)."
    - "Ein automatisierter Test schlägt fehl, sobald jemand später einen der beiden Messeingänge der Wandlung auf einen anderen Wert legt."
    - "Die Sichtprüfung, die kein Skript entscheiden kann, ist als benannte Liste beim Betreiber — nicht stillschweigend abgehakt."
  artifacts:
    - tests/e2e/typo-motion-morph.test.js
    - ".planning/WINDOWS.md (Eintrag zur Sichtrunde Phase 2)"
  key_links:
    - "measureMorph() in SiteNav.astro liest fa aus .hero__mark h1 und fb aus .snav__brand span — --s ist ihr Quotient"
    - "--dx kommt aus der gerenderten Breite der Wortmarke und hängt damit an ihrer Laufweite (0.18em)"
---

<objective>
Das Tor vor der Breite. Plan 01 hat die Kette an Kopfleiste und Startseite bewiesen; die
Pläne 03–06 fassen danach 91 weitere Dateien an. Bevor das passiert, wird die eine Stelle
nachgemessen, die dieser Meilenstein schon einmal fein austariert hat: die scroll-verknüpfte
Wandlung aus Phase 1.

`02-RESEARCH.md` Pitfall 6 nennt den Fehlermodus genau: die Wandlung bleibt FUNKTIONAL
korrekt, weil `measureMorph()` zur Laufzeit neu rechnet — aber die subjektive Choreografie
(wann beginnt die Überlappung, wie schnell schrumpft der Text) verschiebt sich, wenn sich das
Größenverhältnis ändert. Ein Plan, der Erfolgskriterium 3 als „sieht noch gut aus" abhakt,
ohne die Zahlen aus `01-SUMMARY.md` erneut zu ziehen, hat es nicht geprüft.

Purpose: Ein Bruch in der Wandlung nach 4 geänderten Dateien ist ein Fix. Nach 95 ist es eine
Suche.
Output: ein Regressionstest, der die beiden Messeingänge festnagelt, und eine benannte
Sichtrunde beim Betreiber.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wortmarken-wandlung/01-SUMMARY.md
@.planning/phases/02-schrift-und-bewegungsskala/02-01-SUMMARY.md
@.planning/WINDOWS.md
@src/components/SiteNav.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Die beiden Messeingänge der Wandlung festnageln</name>
  <files>tests/e2e/typo-motion-morph.test.js</files>
  <behavior>
    - Der Schriftgrad-Token, aus dem `.snav__brand span` liest, hat in `dist/assets/theme.css`
      exakt den Wert `1rem`. Ändert jemand ihn auf `0.95rem`, schlägt der Test fehl.
    - Der Laufweiten-Token, aus dem `.snav__brand` liest, hat exakt den Wert `0.18em`.
    - `.hero__mark h1` trägt im gebauten HTML beider Startseiten weiterhin
      `clamp(2.9rem,12vw,8.5rem)` und `letter-spacing:-.02em` — nicht `var(...)`.
    - Die 360-px-Regel für `.hero__mark h1` steht weiterhin auf `2.5rem` (MARK-07).
    - `.snav__brand` liest seinen Schriftgrad und seine Laufweite über `var(--fs-…)` /
      `var(--ls-…)` — die Umstellung hat wirklich stattgefunden, der Test prüft nicht bloß
      einen unveränderten Altzustand.
    - Randfall: fehlt `dist/`, bricht der Test mit klarer Meldung ab statt grün durchzulaufen.
  </behavior>
  <action>
    Testdatei nach dem Muster der bestehenden `tests/e2e/**/*.test.js` anlegen (`node:test`,
    `node:assert`) — sie wird durch den vorhandenen Glob in `npm run test:e2e` automatisch
    mitgenommen, es ist kein Eintrag in `package.json` nötig.

    Die Begründungskette gehört als Kommentarkopf in die Datei, weil sie sonst beim nächsten
    Lesen wie eine willkürliche Zusicherung wirkt: `measureMorph()` in `SiteNav.astro`
    (Zeile ~1810) berechnet den Maßstab der Wandlung als Quotient der beiden gerenderten
    Schriftgrade und die horizontale Wegstrecke aus der gerenderten Breite der Wortmarke.
    Solange der Hero-Titel seine `clamp()`-Formel behält und die Wortmarke auf einem Token
    sitzt, dessen Wert exakt der alte ist, sind Maßstab und Wegstrecke rechnerisch dieselben
    wie vor der Umstellung. Der Test prüft genau diese Voraussetzung — er ersetzt nicht die
    Sichtprüfung aus Task 2, er macht sie nur einmalig statt bei jeder künftigen Änderung nötig.

    Gelesen wird der GEBAUTE Stand (`dist/assets/theme.css`, `dist/index.html`,
    `dist/de.html`), nicht die Quelle. Grund ist derselbe wie bei `verify-fx.mjs`: geprüft
    gehört, was ausgeliefert wird.
  </action>
  <verify>
    <automated>npm.cmd run build; node --test tests/e2e/typo-motion-morph.test.js</automated>
    <automated>npm.cmd run test:e2e</automated>
  </verify>
  <done>
    Der Test läuft grün und ist Teil von `npm run test:e2e`. Eine testweise Änderung des
    Schriftgrad-Tokens der Wortmarke auf einen Nachbarwert lässt ihn fehlschlagen (einmal
    ausprobieren, dann zurücknehmen — ein Test, der nie rot war, beweist nichts).
    `npm run test:e2e` insgesamt nicht schlechter als vor diesem Plan; die 11 vorbestehenden,
    unabhängigen Fehlschläge aus `deferred-items.md` bleiben unverändert und werden hier
    NICHT behoben.
  </done>
</task>

<task type="auto">
  <name>Task 2: Die Sichtrunde benennen und an den Betreiber übergeben</name>
  <files>.planning/WINDOWS.md</files>
  <action>
    Erfolgskriterium 3 der ROADMAP — „Ein Scrollen über die ganze Startseite zeigt keinen Bruch
    im Bewegungsverhalten zwischen Kopfleiste und Seiteninhalt" — ist eine Sichtprüfung. Kein
    Automat entscheidet „liest sich als ein Bild"; `02-RESEARCH.md` weist die Beurteilung in der
    Architectural Responsibility Map ausdrücklich dem Menschen zu. Diese Konfiguration führt
    menschliche Prüfpunkte am Phasenende (`human_verify_mode: end-of-phase`), sie werden also
    gesammelt und nicht als blockierender Halt gefahren.

    Einen Eintrag in `.planning/WINDOWS.md` anlegen, im Format der bestehenden Einträge (Vorbild:
    Eintrag id 2, die unerledigte Sichtrunde aus Phase 1.2). Er nennt Prüfweg, erwartetes
    Ergebnis und Gegenprobe — nicht „bitte mal draufschauen". Die Punkte:

    1. `/` und `/de/` im gebauten Stand öffnen (nicht im Dev-Server: der bricht hier
       vorbestehend an einem Inline-Skript in `Layout.astro` ab). Landung bei 1280 px Breite,
       ungescrollt. Erwartet: „VerseBase" oben mittig, Kopfleiste an dieser Stelle ohne
       Schriftzug — wie in `01-SUMMARY.md` gemessen (Mittelpunkt 657, 30 in beiden Farbmodi).
    2. Langsam bis zum Seitenende scrollen und wieder zurück. Erwartet: die Überschrift wandert
       stetig in die Kopfleiste, ohne Sprung und ohne Umschaltpunkt; der Maßstab ist bei etwa
       55 % des Weges fertig, die Überlappung beginnt bei etwa 75 %, der Schriftzug blendet im
       letzten Fünftel auf. Das sind die Marken aus `01-SUMMARY.md`.
    3. Gegenprobe zur Choreografie: fühlt sich der Weg jetzt schneller oder langsamer an als
       vorher? Er sollte identisch sein — Maßstab und Wegstrecke rechnen aus unveränderten
       Eingangswerten (Task 1 belegt das maschinell). Ein spürbarer Unterschied wäre ein Fund
       und gehört zurückgemeldet.
    4. Erfolgskriterium 3 im engeren Sinn: beim Scrollen über die Kachelreihen, die
       Werkzeugkarten und den Patch-Block — wirken Kopfleiste und Seiteninhalt wie EINE
       Bewegungssprache? Die Bedienübergänge liegen jetzt alle auf 150/200/300 ms und einer
       Kurve. Erwartet: ruhiger, gleichmäßiger als vorher. Gegenprobe: das Aufziehen der
       Kacheln beim Scrollen (Scroll-Reveal, 0,6 s) muss sich UNVERÄNDERT anfühlen — es ist
       Ambiente und wurde bewusst nicht angefasst (D-03, FX-07). Fühlt es sich schneller an,
       hat der Durchlauf zu viel erwischt.
    5. Beide Farbmodi und 360 px Breite. Bei 360 px zeigt die Kopfleiste nur das Monogramm
       (MARK-07) — die Wandlung hat dort kein Ziel und die Überschrift bleibt im Hero. Das ist
       richtig so, kein Fund.

    Den Eintrag ausdrücklich als offen führen. Er wird in Plan 07 mit der Schluss-Sichtrunde
    zusammengeführt; bis dahin bleibt Phase 2 nicht abnehmbar.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/WINDOWS.md','utf8');const hit=/Phase 2|02-schrift/i.test(s)&&/1280|360/.test(s);console.log('WINDOWS.md Eintrag Phase 2 vorhanden:',hit);if(!hit)process.exit(1)"</automated>
    <human-check>Die fünf Punkte oben in DE und EN durchgehen. Erwartet: kein Bruch im Bewegungsverhalten, Scroll-Reveal unverändert. Ergebnis in `.planning/WINDOWS.md` eintragen.</human-check>
  </verify>
  <done>
    `.planning/WINDOWS.md` trägt einen offenen Eintrag „Sichtrunde Phase 2" mit fünf Punkten,
    je mit Prüfweg, erwartetem Ergebnis und Gegenprobe. Er nennt die Marken aus
    `01-SUMMARY.md` als Vergleichsmaßstab, nicht als Erinnerung.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(keine neue)* | Reine Prüf- und Dokumentationsarbeit; kein ausgelieferter Code ändert sich. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Repudiation | Sichtprüfung Erfolgskriterium 3 | medium | mitigate | Eine unbelegt abgehakte Sichtprüfung ist der Ausfallmodus, den Phase 1.2 bereits einmal hatte. Gegenmaßnahme: Task 2 führt sie als benannten offenen Eintrag in `.planning/WINDOWS.md`, mit Prüfweg und Gegenprobe statt als Zusage. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Keine Paketinstallation in diesem Plan (`02-RESEARCH.md` § Package Legitimacy Audit: nicht anwendbar). |
</threat_model>

<verification>
- `node --test tests/e2e/typo-motion-morph.test.js` grün
- `npm run test:e2e` nicht schlechter als vor diesem Plan
- `.planning/WINDOWS.md` trägt den offenen Sichtrunden-Eintrag
</verification>

<success_criteria>
Die Wandlung aus Phase 1 ist gegen künftige Verschiebung ihrer beiden Messeingänge gesichert,
und die Beurteilung, die nur ein Mensch treffen kann, liegt als benannte Liste beim Betreiber
statt als Behauptung im Summary.
</success_criteria>

<output>
Create `.planning/phases/02-schrift-und-bewegungsskala/02-02-SUMMARY.md` when done
</output>
