---
phase: 14-testpilot-zugang-staging-hinter-der-discord-rolle
plan: 12
type: execute
wave: 6
depends_on: [14-04, 14-06, 14-08, 14-09, 14-10, 14-11]
files_modified:
  - .planning/WINDOWS.md
  - docs/maschinelle-validierung.md
autonomous: true
requirements: [D-07, D-11, D-12, D-16, D-19, D-20]

must_haves:
  truths:
    - "Die volle Torkette ist grün — normal UND als Vorschau-Bau mit STAGING=1 (CLAUDE.md verlangt beides, weil diese Phase Tore, Auslieferung und Layout berührt)."
    - "Die ausgelieferte Vorschau zeigt den neuen Stand — belegt mit npm run check:staging, nicht mit „committet" oder „CI grün" (D-07)."
    - "Die ausgelieferte Vorschau sperrt wirklich — belegt mit npm run check:gate gegen die echte Adresse."
    - "Die Live-Seite ist unverändert erreichbar (D-12)."
    - "Jedes Sichturteil dieser Phase liegt als benannter Eintrag in .planning/WINDOWS.md beim Betreiber."
    - "Die beiden neuen Prüfstrecken stehen im Konzept, nicht nur in der Registry."
    - statement: "Nach dem Ausrollen kommt ein Testpilot mit der Rolle hinein und ein Besucher ohne sie nicht — an der echten Adresse gemessen, nicht am Container."
      verification: backstop
  prohibitions:
    - "MUSS NICHT „fertig\" melden, bevor die ausgelieferte Seite den neuen Stand zeigt — staging hat schon vier Stunden den Vortagsstand ausgeliefert, während alle Commits an Ort und Stelle lagen."
    - "MUSS NICHT nach main pushen; das bleibt ohne ausdrückliche Ansage tabu."
    - "MUSS NICHT ein Sichturteil selbst fällen; solche Punkte gehen benannt an den Betreiber."
    - "MUSS NICHT eine Untergrenze eines bestehenden Tores senken, um diese Phase grün zu bekommen."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "die Sichturteile dieser Phase als benannte, offene Einträge"
    - path: "docs/maschinelle-validierung.md"
      provides: "die beiden neuen Prüfstrecken im Konzept beschrieben"
  key_links:
    - from: "npm run check:staging"
      to: "https://staging.verse-base.com/build.json"
      via: "der Stempel, den D-07 bewusst offen lässt"
      pattern: "build\\.json"
    - from: "npm run check:gate"
      to: "https://staging.verse-base.com"
      via: "die ausgelieferte Zugriffskontrolle, mit und ohne Ausweis"
      pattern: "check-gate"
---

<objective>
Scharfschalten und abnehmen: die volle Torkette in beiden Bauarten, das
Ausrollen auf staging, der Beleg an der ausgelieferten Seite — und die
Übergabe aller Sichturteile an den Betreiber.

Purpose: CLAUDE.md macht `npm run check:staging` zur Bedingung jeder
Fertig-Meldung, und diese Phase ist genau der Fall, für den die Regel
geschrieben wurde: sie berührt Tore, Auslieferung und Layout, hat vier
Subsysteme angefasst und stellt eine Zugriffskontrolle scharf. „Committet",
„gemergt" und „CI grün" sind hier keine Antworten.

Output: eine ausgelieferte, gesperrte Vorschau, eine unveränderte Live-Seite,
und ein Ledger-Eintrag je Sichturteil.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-testpilot-zugang-staging-hinter-der-discord-rolle/14-CONTEXT.md
@.planning/phases/14-testpilot-zugang-staging-hinter-der-discord-rolle/14-09-SUMMARY.md
@CLAUDE.md
@docs/maschinelle-validierung.md
</context>

<tasks>

<task type="auto">
  <name>Aufgabe 1: Die volle Torkette in beiden Bauarten, plus das Konzept nachziehen</name>
  <files>docs/maschinelle-validierung.md</files>
  <read_first>
    - `CLAUDE.md` VOLLSTÄNDIG — der Abschnitt „Liefern" (was vor jedem Push grün sein muss, wann zusätzlich der Vorschau-Bau, wann eine Fertig-Meldung erlaubt ist) und die sieben Grundsätze
    - `docs/maschinelle-validierung.md` (die Datei, die geändert wird) — § 4 und § 5, wo die Schienen und die bestehenden Strecken beschrieben sind
    - `scripts/lib/gate-registry.mjs` (die beiden neuen Einträge aus Plan 09)
    - `scripts/run-gate.mjs` (in welcher Reihenfolge die Kette läuft)
  </read_first>
  <action>
Erst messen, dann liefern. Beide Bauarten, in dieser Reihenfolge, und jede
Ausgabe protokolliert:

1. `npm run build` gefolgt von `npm run gate` — der gewöhnliche Lauf. Unter
   Windows `npm.cmd`.
2. `$env:STAGING = '1'; npm run build; npm run gate` — der Vorschau-Bau. CI
   baut immer so, und der Unterschied ist nicht kosmetisch: er hat schon einen
   Build gerissen, der lokal grün war (leere Sitemaps, site-weites `noindex`,
   Zähl-Host aus der CSP genommen). Jeder Lauf nennt in seiner Kopfzeile,
   welches Artefakt er geprüft hat — diese Kopfzeilen gehören in die
   Zusammenfassung, nicht nur das grüne Ergebnis.
   ⚠ Danach `$env:STAGING` zurücksetzen, sonst trägt jeder weitere Bau dieser
   Sitzung den Vorschau-Zustand.
3. `npm run test:e2e` und `npm run verify:wiring` einzeln, damit ein Fehlschlag
   nicht in der Kette untergeht.

Reißt irgendetwas, ist es zu beheben — nicht seine Untergrenze zu senken.
Sperrklinken wandern nur nach oben; nach unten nur per Commit, dessen Botschaft
die Ursache nennt. Das ist Grundsatz 5 und in dieser Phase besonders
verlockend, weil zwei neue Strecken dazugekommen sind.

**Das Konzept nachziehen.** `docs/maschinelle-validierung.md` beschreibt die
Torkette; ab jetzt hat sie zwei Strecken mehr. Ergänze `verify:gate`
(Schiene A) und `check:gate` (Schiene C) an den Stellen, an denen die Datei
ihre Strecken aufzählt, jeweils mit einem Satz zu ihrem Gegenstand und — für
`check:gate` — mit der Begründung, warum sie nicht auf Schiene A kann. Die
Registry ist die Wahrheit; das Konzept ist das, was der nächste Mensch liest,
und eine Torkette, deren Beschreibung zwei Strecken verschweigt, erzieht dazu,
der Beschreibung nicht zu glauben.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run gate &amp;&amp; npm run test:e2e &amp;&amp; node -e "const d=require('fs').readFileSync('docs/maschinelle-validierung.md','utf8'); const fehlt=['verify:gate','check:gate'].filter(t=>!d.includes(t)); console.log('Neue Strecken im Konzept — Soll: 2   Ist: '+(2-fehlt.length)); if(fehlt.length){console.error('fehlt: '+fehlt.join(', '));process.exit(1);}"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npm run gate` läuft grün; die Kopfzeile jedes Prüfers steht in der Zusammenfassung.
    - Derselbe Lauf mit `STAGING=1` läuft ebenfalls grün; seine Kopfzeilen stehen ebenfalls in der Zusammenfassung, und die beiden Läufe sind als getrennte Blöcke erkennbar.
    - `npm run test:e2e` und `npm run verify:wiring` einzeln grün.
    - Keine Untergrenze eines bestehenden Tores wurde gesenkt; `git diff` über `scripts/` zeigt keine verkleinerte Klinke.
    - `docs/maschinelle-validierung.md` beschreibt `verify:gate` und `check:gate`, letzteres mit der Begründung für Schiene C.
    - `$env:STAGING` ist nach dem Lauf wieder leer.
  </acceptance_criteria>
  <done>Beide Bauarten sind grün, und die Beschreibung der Torkette stimmt wieder mit ihr überein.</done>
</task>

<task type="auto">
  <name>Aufgabe 2: Auf staging ausrollen und an der ausgelieferten Seite belegen</name>
  <precondition>Der Vorschau-Container in Coolify trägt die Umgebungsvariablen VB_GATE_SECRET, VB_SUPABASE_URL und VB_SUPABASE_ANON_KEY, und der Bot-Container trägt SUPABASE_URL und SUPABASE_SERVICE_ROLE_KEY.</precondition>
  <files>(kein Dateiwechsel — dieser Schritt liefert aus und misst gegen die ausgelieferte Seite)</files>
  <read_first>
    - `CLAUDE.md`, Abschnitt „Liefern" (`main` bleibt tabu ohne Ansage; fertige, geprüfte Arbeit geht ohne Nachfrage auf `staging`)
    - `scripts/check-deployed.mjs` (was `npm run check:staging` genau fragt und woran es scheitert)
    - `scripts/check-gate.mjs` (aus Plan 09 — der Prüfer, der die ausgelieferte Zugriffskontrolle misst)
    - `.github/workflows/deploy-staging.yml` (die Kette, die aus dem Push ein Image und ein Ausrollen macht)
  </read_first>
  <action>
⚠ ZUERST, VOR ALLEM ANDEREN — die vertagte Entscheidung aus Plan 06 (D-16).
Der Trockenlauf vom 18.08.2026 fand 0 Traeger der Rolle "Test Pilots" (bei 5
gelesenen Mitgliedern — die 5 belegt, dass die Liste wirklich gelesen wurde).
Der Betreiber hat den Entzug daraufhin NICHT abgesagt, sondern VERTAGT: die
Zahl gilt fuer den Tag, an dem sie gemessen wurde, und er kann zwischenzeitlich
jemandem die Rolle gegeben haben. Deshalb hier, unmittelbar vor dem Push, der
das Tor scharf macht:

  1. `cd discord && node tester-dry-run.mjs` erneut fahren (nur Lesen).
  2. Ergebnis dem Betreiber vorlegen — Namen und Anzahl, woertlich.
  3. Ihn entscheiden lassen: entziehen (`node tester-revoke.mjs
     --rolle-wirklich-entziehen`) oder stehen lassen. NICHT selbst entscheiden,
     und NICHT selbst ausfuehren — die Berechtigungssperre hat schon zweimal
     zu Recht gestoppt (Plan 05 build.mjs, Plan 06 revoke).

Erst danach weiter. Verfolgt in .planning/WINDOWS.md (Eintrag 20).

Vor dem Push auflisten, was alles mitgeht: `git log --oneline origin/staging..HEAD`.
Am 14.08.2026 hat ein „pushe auf live" als Fast-forward 28 Commits mitgenommen,
die niemand gemeint hatte. Hier geht es zwar auf `staging` und nicht auf
`main`, aber die Liste kostet fünf Sekunden und steht danach in der
Zusammenfassung.

Auf `staging` pushen. **Nicht auf `main`** — das bleibt ohne ausdrückliche
Ansage tabu, und diese Phase hat keine.

Den Workflow beobachten. Er ist die erste echte Probe für zwei Dinge aus
Plan 09: der Rauchtest muss mit seinem je Lauf gewürfelten Bypass durchkommen,
und `check-gate.mjs` muss danach gegen den laufenden Container grün melden.
Reißt der Rauchtest hier, ist das kein Zufall, sondern die in RESEARCH Pitfall 2
vorhergesagte Stelle — dann ist der Bypass falsch verdrahtet und nicht der
Rauchtest schuld.

Dann warten, bis die Vorschau den neuen Stand WIRKLICH ausliefert, und es
belegen — in dieser Reihenfolge:

1. `npm run check:staging` — der Stempel muss den gerade gepushten Commit
   nennen. Das ist der Schritt, den CLAUDE.md zur Bedingung jeder
   Fertig-Meldung macht und dessen Fehlen schon vier Stunden Vortagsstand
   gekostet hat. Er läuft nur, weil D-07 `/build.json` bewusst offen lässt —
   schlägt er mit einer Weiterleitung auf die Torseite fehl, ist die
   Ausnahmeliste kaputt und nicht der Deploy.
2. `npm run check:gate` gegen `https://staging.verse-base.com` — die
   ausgelieferte Zugriffskontrolle, gegen das richtige Artefakt: nicht `dist/`,
   nicht das Image, sondern die Seite. Weist Cloudflare den Rechner ab, sagt
   der Prüfer das; vom Entwicklungsrechner aus sollte er hinkommen.
3. `npm run check:live` und ein Aufruf einer gewöhnlichen Live-Seite ohne jedes
   Cookie — die Live-Seite muss unverändert erreichbar sein. Das ist D-12,
   gegen das Artefakt geprüft, und es ist die Zusicherung, die am teuersten
   wäre, wenn sie stillschweigend falsch wäre.
4. Ein Aufruf von `https://staging.verse-base.com/schiffe.html` in einem
   frischen Browserprofil ohne Anmeldung: er muss auf der Torseite landen.
5. Derselbe Aufruf mit einem Konto, das die Rolle trägt: er muss durchkommen,
   und danach muss in `discord_role_state.last_staging_seen` ein frischer
   Zeitstempel stehen (D-13, am Ende der Kette belegt).

Alle fünf Ergebnisse in die Zusammenfassung. „Fertig" gilt erst ab hier.

Bleibt Coolify hängen (an einem Tag waren es 20 Minuten), ist das kein
Fehlschlag dieser Phase — aber auch keine Fertig-Meldung. Warten und erneut
messen.
  </action>
  <verify>
    <automated>npm run check:staging &amp;&amp; npm run check:gate</automated>
  </verify>
  <acceptance_criteria>
    - Die Liste der mitgehenden Commits steht in der Zusammenfassung; es ist nach `staging` gepusht, nicht nach `main`.
    - Der Workflow ist grün, insbesondere der Rauchtest und der neue `check-gate`-Schritt.
    - `npm run check:staging` nennt den gerade gepushten Commit.
    - `npm run check:gate --base https://staging.verse-base.com` ist grün (oder meldet ehrlich, dass Cloudflare den Rechner abweist — dann vom Entwicklungsrechner wiederholen).
    - Die Live-Seite liefert eine gewöhnliche Seite ohne Cookie mit `200` aus.
    - Ein Aufruf der Vorschau ohne Anmeldung landet auf der Torseite; mit einem Konto, das die Rolle trägt, kommt er durch.
    - Nach dem erfolgreichen Durchgang steht in `discord_role_state.last_staging_seen` ein Zeitstempel aus den letzten Minuten.
  </acceptance_criteria>
  <done>Die ausgelieferte Vorschau sperrt, die ausgelieferte Live-Seite nicht, und beides ist an der Seite gemessen — nicht am Commit.</done>
</task>

<task type="auto">
  <name>Aufgabe 3: Die Sichturteile an den Betreiber übergeben</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md` VOLLSTÄNDIG (die Datei, die geändert wird) — das Tabellenformat, die Frontmatter-Zähler `open_count`/`total_count`, die Konvention der Art `unrun-verify`
    - die Zusammenfassungen der Pläne 03, 04, 10 und 11 (dort sind die Sichtpunkte bereits als `<human-check>` formuliert und warten auf ihre Übergabe)
    - `CLAUDE.md`, Abschnitt „Sonst" („Sichturteile entscheidet kein Skript. Sie gehen als benannter Punkt nach `.planning/WINDOWS.md` an den Betreiber.")
  </read_first>
  <action>
Diese Phase hat fünf Urteile hinterlassen, die kein Skript fällen kann. Sie
gehen als benannte Einträge in den Ledger — einzeln, nicht als ein Sammelposten,
damit der Betreiber sie einzeln abhaken kann und keiner im Rutsch verschwindet.

Verwende `gsd-tools windows` beziehungsweise das in der Datei etablierte
Verfahren; trage die Zähler in der Frontmatter korrekt fort (`open_count`,
`total_count`) — ein Ledger mit falschem Kopf ist beim nächsten Lauf ein
eigener Befund.

Die fünf Einträge, Art `unrun-verify`, Phase `14`:

1. **Riegel an der Anmeldung (aus Plan 03).** Vier echte Anmeldevorgänge im
   Browser auf der Live-Seite: Registrierung, Anmeldung, Ab- und Wiederanmeldung,
   Passwort zurücksetzen. ⚠ Klemmt einer, ist `trg_block_discord_signup` sofort
   zu entfernen — er sperrt dann jede Neuanmeldung aus.
2. **Discord-Anmeldeweg und Torseite (aus Plan 04).** Die vier Durchläufe:
   bekanntes Konto, unbekanntes Discord-Konto (Absage statt Konto), Rücksprung
   auf die gewünschte URL, und das Urteil über die Torseite selbst — trägt sie
   die Handschrift der Seite, ist der Weg zur Rolle erkennbar, steht der
   Discord-Knopf klar vor dem E-Mail-Formular? Bei 1920×1080 UND 360 px, in
   beiden Farbmodi.
3. **Das Abzeichen (aus Plan 10).** Steht das Testpilot-Abzeichen neben der
   RSI-Verifizierung, ohne die Rangfolge zu verdrehen? Ist es in beiden
   Farbmodi lesbar? ⚠ Braucht ein Konto, das die Eigenschaft trägt; als Gast
   ist es nicht zu sehen.
4. **Konto und Übersicht (aus Plan 10).** Der Zustimmungsschalter und die
   Testpiloten-Übersicht. ⚠ Braucht ein angemeldetes Konto, die Übersicht ein
   Admin-Konto. Genau dieser Fall — als Gast geprüft, kontogebundene UI nie
   gesehen — hat in diesem Projekt schon drei Fehler durchgelassen, obwohl alle
   Tore grün waren.
5. **Deploy-Ping und XP (aus Plan 11).** Liest sich der Ping wie eine
   Änderungsliste oder wie ein Protokoll? Ist der Testpiloten-Kanal für ein
   Zweitkonto ohne Rolle wirklich unsichtbar? Bringt ein zweiter Beitrag im
   selben Thread wirklich kein XP?

Jeder Eintrag nennt in seiner Beschreibung: was gebaut wurde, was maschinell
belegt ist, und was genau der Mensch entscheiden soll. Ein Eintrag „Sichtrunde
Phase 14" ohne diese drei Angaben ist in drei Wochen wertlos.

Zum Schluss den Stand der Phase ehrlich benennen: technisch fertig (12 von 12
Plänen), NICHT als „Complete" markiert, solange die fünf Einträge offen sind —
dieselbe Konvention, die die Phasen 1.2, 2, 3, 9, 10 und 12 bereits verwenden.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/WINDOWS.md','utf8'); const zeilen=s.split('\n').filter(l=>/^\|\s*\d+\s*\|\s*14\s*\|/.test(l)); console.log('Ledger-Eintraege fuer Phase 14 — Soll: 5   Ist: '+zeilen.length); if(zeilen.length!==5){process.exit(1);} const offen=zeilen.filter(l=>/\|\s*open\s*\|/.test(l)).length; console.log('davon offen — Soll: 5   Ist: '+offen); if(offen!==5){process.exit(1);} const fm=s.slice(0,s.indexOf('---',4)); const oc=/open_count:\s*(\d+)/.exec(fm), tc=/total_count:\s*(\d+)/.exec(fm); console.log('Kopfzaehler: open='+oc[1]+' total='+tc[1]); const alleOffen=(s.match(/\|\s*open\s*\|/g)||[]).length; if(+oc[1]!==alleOffen){console.error('open_count stimmt nicht mit den Zeilen ueberein: '+oc[1]+' vs '+alleOffen);process.exit(1);}"</automated>
    <human-check>
Die fünf Einträge im Ledger sind die Übergabe. Der Betreiber arbeitet sie einzeln ab und hakt sie mit `gsd-tools windows fixed <id>` ab. Vor dem letzten Haken gilt Phase 14 als technisch fertig, nicht als abgenommen.

Zusätzlich, und außerhalb des Ledgers, eine Frage, die nur der Betreiber beantworten kann: **Die Rolle trägt derzeit niemand** — der Trockenlauf aus Plan 06 fand am 18.08.2026 null Träger, und der Entzug wurde deshalb vertagt statt ausgeführt (siehe Aufgabe 2 oben und WINDOWS.md 20). Wem soll sie gegeben werden? Ohne diesen Schritt steht eine gesperrte Vorschau da, die außer dem Betreiber selbst niemand betreten kann — technisch richtig und praktisch nutzlos. Die Vergabe ist bewusst Handarbeit (D-16, D-17): Rechtsklick auf das Mitglied in Discord, Rolle „Test Pilots" setzen. Der Bot schreibt den Rest binnen Sekunden.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` enthält genau fünf neue Zeilen mit Phase `14`, alle mit Status `open` und Art `unrun-verify`.
    - Jede Zeile nennt, was gebaut wurde, was maschinell belegt ist und was der Mensch entscheiden soll.
    - Die Einträge 3 und 4 nennen ausdrücklich, dass sie ein angemeldetes Konto brauchen (Eintrag 4 zusätzlich ein Admin-Konto).
    - Die Frontmatter-Zähler `open_count` und `total_count` sind fortgeschrieben und stimmen mit der Zahl der Zeilen überein.
    - Die Zusammenfassung nennt den Phasenstand als „technisch fertig, nicht Complete" mit Verweis auf die fünf offenen Einträge.
    - Die Zusammenfassung nennt den Hinweis an den Betreiber, dass die Rolle nach dem Entzug neu zu vergeben ist.
  </acceptance_criteria>
  <done>Alles, was ein Mensch entscheiden muss, liegt benannt beim Menschen — und nichts davon ist als „grün" verbucht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokaler Bau → ausgeliefertes Image → ausgelieferte Seite | drei verschiedene Artefakte; ein Befund über eines sagt nichts über das nächste |
| Push nach `staging` | die erste Stelle, an der die Änderung echte Besucher erreicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-75 | Elevation of Privilege | die Änderung landet versehentlich auf der Live-Seite | critical | mitigate | Es wird ausschließlich nach `staging` gepusht; die mitgehenden Commits werden vor dem Push aufgelistet; `check:live` und ein cookie-freier Aufruf belegen danach, dass die Live-Seite unverändert ist |
| T-14-76 | Denial of Service | fehlende Umgebungsvariablen im Vorschau-Container sperren alle aus | high | mitigate | Als `precondition` der Aufgabe geführt; der Türsteher fällt bei fehlendem Geheimnis geschlossen aus, was hier der sichere, aber sichtbare Zustand ist — Aufgabe 2 misst ihn und meldet ihn statt ihn zu übergehen |
| T-14-77 | Tampering | „fertig" gemeldet, während die Vorschau den Vortagsstand ausliefert | high | mitigate | `npm run check:staging` als Bedingung der Fertig-Meldung, plus `check:gate` gegen dieselbe Adresse; beide Ergebnisse in der Zusammenfassung |
| T-14-78 | Repudiation | ein Sichturteil verschwindet im Sammelposten | medium | mitigate | Fünf einzelne Ledger-Einträge statt eines; ein automatisiertes Kriterium zählt sie und gleicht den Kopfzähler ab |
| T-14-79 | Information Disclosure | die Ausnahmeliste leckt erst an der echten Adresse (andere Cache-Schicht, Cloudflare davor) | medium | mitigate | `check:gate` läuft gegen `https://staging.verse-base.com`, nicht nur gegen den Container — Grundsatz 7: gegen das Artefakt prüfen, und wissen, welches |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 12)

**Geänderte Dateien**
- `.planning/WINDOWS.md` — fünf neue Einträge, Phase `14`, Art `unrun-verify`, Status `open`; Kopfzähler fortgeschrieben
- `docs/maschinelle-validierung.md` — `verify:gate` (Schiene A) und `check:gate` (Schiene C) im Konzept beschrieben

**Belege, die in der Zusammenfassung stehen müssen**
- Kopfzeilen beider Torketten-Läufe (normal und `STAGING=1`)
- Liste der mit dem Push mitgehenden Commits
- Ausgabe von `npm run check:staging` mit der Commit-Kennung
- Ausgabe von `npm run check:gate` gegen `https://staging.verse-base.com`
- Beleg, dass die Live-Seite ohne Cookie mit `200` antwortet
- Zeitstempel in `discord_role_state.last_staging_seen` nach dem ersten echten Durchgang

**Offener Punkt außerhalb des Ledgers**
- Die Rolle trägt derzeit niemand (Trockenlauf Plan 06, 18.08.2026: 0 Träger bei 5 gelesenen Mitgliedern); der Entzug ist vertagt, nicht ausgeführt — vor dem Scharfschalten neu messen. Die Neuvergabe ist Handarbeit des Betreibers (D-16, D-17)
</artifacts_produced>

<verification>
- `npm run build && npm run gate` grün, und derselbe Lauf mit `STAGING=1` grün — beide Kopfzeilenblöcke protokolliert.
- `npm run test:e2e`, `npm run verify:wiring` einzeln grün.
- Workflow grün, einschließlich Rauchtest mit Bypass und `check-gate`.
- `npm run check:staging` nennt den gepushten Commit; `npm run check:gate` grün gegen die echte Adresse.
- `npm run check:live` grün und eine Live-Seite ohne Cookie mit `200`.
- Fünf offene Ledger-Einträge, Kopfzähler stimmig.
</verification>

<success_criteria>
- Beide Bauarten grün, keine gesenkte Untergrenze.
- Die ausgelieferte Vorschau sperrt; die ausgelieferte Live-Seite nicht.
- Die Fertig-Meldung steht auf `npm run check:staging`, nicht auf „gemergt".
- Jedes Sichturteil liegt benannt beim Betreiber, und die Phase ist ehrlich als „technisch fertig, nicht Complete" verbucht.
</success_criteria>

<output>
Create `.planning/phases/14-testpilot-zugang-staging-hinter-der-discord-rolle/14-12-SUMMARY.md` when done
</output>
