---
phase: 01.3-datenschicht-aufraeumen
plan: 03
type: execute
wave: 3
depends_on: ["01.3-02"]
files_modified:
  - src/data/vehicle-prices.json
  - MINING-DATENQUELLE.md
  - FAKTEN-AUDIT.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [DATA-08, DATA-09]
user_setup: []

must_haves:
  truths:
    - "Die Fahrzeugpreise sind nicht aelter als der Datamine-Lauf aus Plan 02; `fetchedAt` nennt den Tag des Zugs (D-18, DATA-08, Erfolgskriterium 8)."
    - "`MINING-DATENQUELLE.md` nennt die Kennung, die die Mining-Daten tatsaechlich tragen — der Exaktheits-Beweis der Mining-Seite stimmt wieder mit dem Bestand ueberein (D-19, DATA-09)."
    - "`FAKTEN-AUDIT.md` fuehrt den erledigten offenen Punkt nicht mehr als offen; die 305 Haekchen der Checkliste sind unangetastet (D-20)."
    - "`.planning/codebase/STRUCTURE.md` beschreibt das Verzeichnis so, wie es nach dieser Phase aussieht — keine entfernte Datei, kein entferntes Skript steht mehr darin (DATA-09, Erfolgskriterium 9)."
    - "Alle sechs Tore laufen nicht schlechter als vor der Phase, gemessen gegen die in Plan 01 aufgenommene Messlatte (Erfolgskriterium 10)."
    - "Der Nachweis fuer Erfolgskriterium 1 ist am FINALEN Build wiederholt — nach dem Datamine-Lauf, nicht nur davor."
  artifacts:
    - src/data/vehicle-prices.json (frischer UEX-Stand)
    - scratch/01.3-abschluss.txt (Tor-Kennzahlen gegen die Messlatte)
  key_links:
    - "UEX antwortet Rechenzentrums-IPs mit HTTP 403 hinter Cloudflare. `sync:prices` steht zwar in `.github/workflows/build.yml`, laeuft dort aber mit `continue-on-error` ins Leere — der Zug MUSS lokal passieren, sonst bleibt der Stand vom 03.07.2026 stehen (D-18)."
    - "`sync-prices.mjs` hat einen eigenen Riegel: weniger als 20 gematchte Fahrzeuge ueberschreiben den Snapshot nie. Der Riegel bleibt, er wird nicht umgangen."
    - "`MINING-DATENQUELLE.md` nennt eine Laserzahl. Nach Plan 02 stehen zwei namenlose S0-Geraete nicht mehr in der Auswahl — die Zahl im Dokument muss die neue Wirklichkeit nennen, sonst entwertet sie sich selbst."
    - "Erfolgskriterium 1 wurde in Plan 01 am damaligen Build bewiesen. Der Datamine-Lauf hat `assets/crafting-db.json` neu geschrieben; der Nachweis gehoert deshalb an den finalen Build wiederholt (Hinweis zur Reihenfolge in ROADMAP.md)."
---

<objective>
Den Rest nachziehen, der erst nach dem Datenlauf richtig sein kann: die Preise, die
Dokumente und die Tore.

Ziel der Phase (woertlich aus ROADMAP.md): „Die Datenschicht sagt ueberall die Wahrheit
ueber sich selbst und schleppt keine Reste alter Datenlaeufe mehr mit."

Purpose: Doku ist Teil des Bestands (D-19, D-20). Ein Exaktheits-Beweis mit falscher
Build-Kennung beweist nichts, und ein Audit, das einen erledigten Punkt als offen fuehrt,
schickt den naechsten Bearbeiter auf eine Arbeit, die schon getan ist. Dazu kommt der
letzte fehlende Datenstand: die Fahrzeugpreise stehen auf dem 03.07.2026, weil der Zug
aus CI grundsaetzlich nicht funktioniert. Und zuletzt wird gemessen, ob die Phase
irgendwo etwas verschlechtert hat — gegen die Messlatte, die Plan 01 aufgenommen hat.

Output: Frische UEX-Fahrzeugpreise, drei begradigte Dokumente und ein Abschlussprotokoll,
das jedes der zehn Erfolgskriterien mit einer Zahl oder einem Zitat belegt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-datenschicht-aufraeumen/01.3-CONTEXT.md
@.planning/phases/01.3-datenschicht-aufraeumen/01.3-01-SUMMARY.md
@.planning/phases/01.3-datenschicht-aufraeumen/01.3-02-SUMMARY.md
</context>

<tasks>

<!-- planner-discipline-allow: sc-craft.tools -->
<!-- planner-discipline-allow: crafting-blueprints -->
<!-- planner-discipline-allow: calibrate-holo-markers -->
<!-- planner-discipline-allow: fetch-craft -->

<task type="auto">
  <name>Task 1: Fahrzeugpreise von UEX nachziehen — lokal, mit Plausibilitaetsprobe</name>

  <read_first>
    - scripts/sync-prices.mjs (Zeile 1-20 Kopf, Ziel-URLs und der Fail-Safe-Hinweis; die Stelle, an der `fetchedAt` und der Snapshot geschrieben werden; die Stelle, an der die 20er-Schwelle greift)
    - src/data/vehicle-prices.json (nur den Kopf: `fetchedAt`, `source`, und die Zahl der Eintraege)
    - .github/workflows/build.yml (Zeile 41-43 — der Aufruf mit `continue-on-error`)
  </read_first>

  <precondition>
    Der Zug laeuft nur von einem Anschluss, den UEX nicht als Rechenzentrum einstuft. Vor
    Beginn einen Probeabruf gegen `https://api.uexcorp.space/2.0/star_systems` machen und
    den HTTP-Status protokollieren. Antwortet er mit 403 oder mit der Cloudflare-
    Zwischenseite, hier anhalten und melden — dann bleibt der Snapshot stehen, und
    DATA-08 wird als nicht erfuellbar mit Begruendung ins SUMMARY geschrieben, statt
    einen Fehlschlag als Erfolg zu buchen (Projekt-Erinnerung `uex-price-averages`).
  </precondition>

  <files>src/data/vehicle-prices.json</files>

  <action>
Die Fahrzeugpreise stehen auf dem 03.07.2026 — nicht aus Nachlaessigkeit, sondern weil
`npm run sync:prices` zwar in `.github/workflows/build.yml` steht, dort aber mit
`continue-on-error` ins Leere laeuft: UEX antwortet Rechenzentrums-IPs mit HTTP 403
hinter Cloudflare. Der Zug muss lokal passieren (D-18).

1. Ausgangsstand notieren: `fetchedAt` und die Zahl der Fahrzeuge mit Kaufpreis bzw.
   Mietpreis aus `src/data/vehicle-prices.json`.

2. `npm run sync:prices` fahren. Der eingebaute Fail-Safe (weniger als 20 gematchte
   Fahrzeuge ueberschreiben den Snapshot nie) bleibt unangetastet und wird nicht
   umgangen.

3. Nachher-Stand pruefen: `fetchedAt` nennt den heutigen Tag; die Zahl der Fahrzeuge mit
   Preis liegt nicht unter 90 % des Ausgangswerts. Faellt sie darunter, ist das ein
   Befund — Zahlen melden, den Snapshot per `git checkout` zuruecknehmen und anhalten,
   statt einen ausgeduennten Preisstand auszuliefern.

4. Der Item-Preis-Zug (`sync:item-prices`) wird ausdruecklich NICHT gefahren: er ruft
   `fetch-uex-item-prices.mjs` UND anschliessend `build-universal-db.mjs`, und der
   Katalog wurde in Plan 02 bereits gegen die frische Localization gebaut. Ein zweiter
   Lauf brauchte hier keinen Gewinn und traegt nur Risiko. Diese Auslassung mit
   Begruendung ins SUMMARY.

5. `npm run build` und nachsehen, dass der Datumsstempel auf den Fahrzeugseiten den
   neuen Stand zeigt — die Seite zeigt ihre Datenstaende offen an, ein neuer Zug
   veraendert also sichtbaren Seitentext.
  </action>

  <verify>
    <automated>node -e "const r=await fetch('https://api.uexcorp.space/2.0/star_systems',{headers:{'User-Agent':'sc-patch-archiv fan site (non-commercial German patch archive)',Accept:'application/json'}});console.log('UEX HTTP',r.status);if(!r.ok)throw new Error('UEX nicht erreichbar (Status '+r.status+') — Zug hier anhalten und DATA-08 begruendet offen lassen')" --input-type=module</automated>
    <automated>npm run sync:prices</automated>
    <automated>node -e "const j=require('./src/data/vehicle-prices.json');const heute=new Date().toISOString().slice(0,10);if(j.fetchedAt!==heute)throw new Error('fetchedAt ist '+j.fetchedAt+', erwartet '+heute);const n=Array.isArray(j.vehicles)?j.vehicles.length:Object.keys(j.vehicles||j.prices||{}).length;if(n<1)throw new Error('kein Fahrzeug mit Preis im Snapshot');console.log('Fahrzeugpreise:',n,'Eintraege, Stand',j.fetchedAt)"</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
    - Der Probeabruf gegen UEX ist mit HTTP-Status protokolliert (Erfolgs- wie Fehlerfall).
    - `src/data/vehicle-prices.json` traegt als `fetchedAt` den Tag des Zugs und ist damit nicht aelter als der Datamine-Lauf aus Plan 02 (DATA-08, Erfolgskriterium 8).
    - Die Zahl der Fahrzeuge mit Kauf- bzw. Mietpreis ist vorher und nachher protokolliert und nicht unter 90 % gefallen.
    - Der Fail-Safe des Skripts wurde nicht umgangen.
    - `npm run build` fehlerfrei; der sichtbare Datumsstempel auf einer Fahrzeugseite nennt den neuen Stand (eine Seite namentlich mit altem und neuem Text im SUMMARY).
    - Fall UEX unerreichbar: DATA-08 steht mit Status, Uhrzeit und Begruendung im SUMMARY und der Snapshot ist unveraendert — kein stiller Fehlschlag.
  </acceptance_criteria>

  <done>
    Die Fahrzeugpreise sind frisch gezogen und nicht aelter als der Datenlauf — oder der
    Grund, warum das heute nicht ging, steht mit HTTP-Status im Protokoll.
  </done>

  <reversibility rating="reversible">
    Ein Snapshot mit eigenem Fail-Safe; `git checkout src/data/vehicle-prices.json` stellt
    den Vorstand her.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Drei Dokumente auf den Bestand bringen</name>

  <read_first>
    - MINING-DATENQUELLE.md (Zeile 1-40 — Kopf, die Patch-Kennung und die Tabelle mit der Laserzahl; danach den Abschnitt „Der Beweis")
    - FAKTEN-AUDIT.md (Zeile 1-16 — Kopf und der Block „Bekannte offene Punkte")
    - .planning/codebase/STRUCTURE.md (die Abschnitte zu `src/data/`, `assets/` und `scripts/`)
    - src/data/patches/4-8-3.json (nur, dass die Datei existiert und welches Datum sie traegt)
    - .planning/phases/01.3-datenschicht-aufraeumen/01.3-02-SUMMARY.md (die dort protokollierte Kennung und die neue Laserzahl)
  </read_first>

  <files>MINING-DATENQUELLE.md, FAKTEN-AUDIT.md, .planning/codebase/STRUCTURE.md</files>

  <action>
Drei Dokumente behaupten etwas, das nicht mehr stimmt. Keines davon wird umgeschrieben,
alle drei werden an genau den Stellen berichtigt, an denen sie falsch sind.

A) `MINING-DATENQUELLE.md` (D-19, DATA-09). Das Dokument ist der Exaktheits-Beweis der
   Mining-Seite; eine falsche Build-ID darin entwertet den Beweis. Zu berichtigen:
   - Die Patch-Kennung im Abschnitt „Woher die Daten kommen" nennt heute `12232306`,
     waehrend die Daten vor dieser Phase `12248363` trugen und nach ihr `12326004`
     tragen. Sie wird auf den Wert gesetzt, den `assets/mining-model.json` nach Plan 02
     tatsaechlich fuehrt — abgelesen, nicht abgeschrieben.
   - Die Zeile in der Extraktions-Tabelle nennt eine Laserzahl. Nach Plan 02 stehen zwei
     namenlose S0-Geraete nicht mehr in der ausgelieferten Auswahl; die Zahl wird auf
     den neuen Stand gesetzt, und ein Halbsatz sagt, dass zwei Geraete ohne
     Localization-Namen bewusst ausgelassen sind und mit ihrer Klassenbezeichnung in der
     Ausgabedatei gefuehrt werden. Das ist die ehrliche Form: die Auslassung steht im
     Beweisdokument, nicht nur im Code.
   - Falls das Dokument ein Datum des Datenstands nennt, wird es auf den Tag des Laufs
     gesetzt (dasselbe Datum, das `snapshot_date` nach Plan 02 traegt).

B) `FAKTEN-AUDIT.md` (D-20). Unter „Bekannte offene Punkte" steht als Nummer 2, dem
   Archiv fehle der Patch-Eintrag zu 4.8.3 komplett. `src/data/patches/4-8-3.json`
   existiert seit dem 16.07.2026. Dieser eine Punkt wird als erledigt gekennzeichnet —
   mit Datum und einem Verweis auf die Datei, damit nachvollziehbar bleibt, warum er
   weg ist. Ob er gestrichen oder durchgestrichen als erledigt stehen bleibt, ist frei;
   erledigt und sichtbar erledigt ist besser als spurlos weg. Der Kopf des Dokuments
   traegt „Stand 2026-07-03" — dort einen Hinweis ergaenzen, wann und in welchem Umfang
   nachgepflegt wurde.
   AUSDRUECKLICH NICHT anfassen: die 305 offenen Haekchen der Checkliste und Punkt 1
   („XenoThreat @ 4.8.0"). Sie sind in CONTEXT.md als zurueckgestellt vermerkt; wer sie
   hier mitnimmt, sprengt die Phase.

C) `.planning/codebase/STRUCTURE.md` (DATA-09, Erfolgskriterium 9). Nach Plan 01 und
   Plan 02 stimmen mehrere Aufzaehlungen nicht mehr:
   - Der Abschnitt zu `src/data/` fuehrt die verwaiste Blueprint-Datei (in Plan 01
     bereits gestrichen — pruefen, dass es dabei geblieben ist) und die Sammel-Form fuer
     die Holo-Dateien; letztere umfasst jetzt eine Datei weniger.
   - Falls Skripte aufgezaehlt sind, fehlen dort kuenftig der geloeschte
     Fremdquellen-Schreiber und der geloeschte Marker-Erzeuger.
   - Der Abschnitt zu `assets/` beschreibt `*-gamefiles.json` als gitignorierte
     Build-Eingaben; das stimmt weiterhin und bleibt.
   Vorgehen: den Bestand lesen und die Aufzaehlungen dagegen abgleichen, nicht aus dem
   Gedaechtnis korrigieren. Jede Aenderung an diesem Dokument ist eine Zustandsbeschreibung
   und muss durch einen Blick ins Verzeichnis gedeckt sein.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const m=JSON.parse(fs.readFileSync('assets/mining-model.json','utf8'));const doc=fs.readFileSync('MINING-DATENQUELLE.md','utf8');const kennung=String(m.game_version).replace(/^4[.]9[.]0-live[.]/,'');if(!doc.includes(kennung))throw new Error('MINING-DATENQUELLE.md nennt die Kennung '+kennung+' nicht');if(/12232306|12248363/.test(doc))throw new Error('MINING-DATENQUELLE.md traegt noch eine alte Kennung');console.log('Doku-Kennung ok:',kennung)"</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('FAKTEN-AUDIT.md','utf8');const block=d.slice(d.indexOf('Bekannte offene Punkte'),d.indexOf('Bekannte offene Punkte')+900);if(/dem Archiv fehlt der Patch-Eintrag komplett/.test(block))throw new Error('der erledigte Punkt steht unveraendert als offen');if(!fs.existsSync('src/data/patches/4-8-3.json'))throw new Error('4-8-3.json fehlt — dann war der Punkt doch offen');const offen=(d.match(/^- \[ \]/gm)||[]).length;console.log('offene Haekchen der Checkliste:',offen,'(muessen 305 bleiben)')"</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/codebase/STRUCTURE.md','utf8');for(const t of ['crafting-blueprints','calibrate-holo-markers','fetch-craft','holo-markers'])if(s.includes(t))throw new Error('STRUCTURE.md nennt noch: '+t);console.log('STRUCTURE.md ohne entfernte Bestandteile')"</automated>
    <automated>node -e "const{execSync}=require('child_process');const out=execSync('git grep -n -e holo-markers -e crafting-blueprints -e calibrate-holo -e fetch-craft -- src scripts assets tests package.json MINING-DATENQUELLE.md FAKTEN-AUDIT.md .planning/codebase || exit 0',{encoding:'utf8'});if(out.trim())throw new Error('Restverweise ausserhalb der Phasenakten:\n'+out);console.log('keine Restverweise im Bestand')"</automated>
  </verify>

  <acceptance_criteria>
    - `MINING-DATENQUELLE.md` nennt die Kennung, die `assets/mining-model.json` tatsaechlich traegt, und keine der beiden alten Kennungen mehr (D-19).
    - Die Laserzahl im Dokument entspricht der ausgelieferten Auswahl; die zwei bewusst ausgelassenen Geraete sind dort in einem Halbsatz erklaert.
    - Der erledigte offene Punkt in `FAKTEN-AUDIT.md` ist als erledigt gekennzeichnet, mit Datum und Verweis auf `src/data/patches/4-8-3.json` (D-20).
    - Die Zahl der offenen Haekchen in `FAKTEN-AUDIT.md` ist unveraendert bei 305; Punkt 1 ist unangetastet.
    - `.planning/codebase/STRUCTURE.md` nennt keine der in dieser Phase entfernten Dateien oder Skripte mehr.
    - `git grep` ueber `src`, `scripts`, `assets`, `tests`, `package.json` und die drei Dokumente findet 0 Verweise auf die entfernten Namen (Erfolgskriterium 9).
  </acceptance_criteria>

  <done>
    Die drei Dokumente beschreiben den Bestand, wie er nach dieser Phase ist — inklusive
    dessen, was bewusst ausgelassen wurde.
  </done>

  <reversibility rating="reversible">
    Reine Textkorrekturen an drei Dokumenten.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Zehn Kriterien, zehn Belege — Abschlussmessung gegen die Messlatte</name>

  <read_first>
    - scratch/01.3-baseline.txt (die in Plan 01 aufgenommene Messlatte)
    - .planning/ROADMAP.md (Phase 01.3, die zehn Erfolgskriterien — woertlich)
    - .planning/phases/01.3-datenschicht-aufraeumen/01.3-01-SUMMARY.md und 01.3-02-SUMMARY.md
  </read_first>

  <files>scratch/01.3-abschluss.txt</files>

  <action>
Diese Aufgabe baut nichts. Sie misst — und sie misst gegen den finalen Zustand, nicht
gegen den Zwischenstand, in dem die einzelnen Nachweise entstanden sind.

1. Frisch bauen (`npm run build`) und alle sechs Tore fahren: `node scripts/_verify.mjs`,
   `npm run audit:site`, `npm run audit:csp`, `npm run test:e2e`, `npm run verify:mining`.
   Kennzahlen nach `scratch/01.3-abschluss.txt` schreiben, Zeile fuer Zeile neben die
   Werte aus `scratch/01.3-baseline.txt`. Jede Verschlechterung wird benannt und erklaert
   oder behoben — „nicht schlechter als vorher" ist Erfolgskriterium 10 und keine
   Formsache.

2. Erfolgskriterium 1 am FINALEN Build wiederholen (Hinweis zur Reihenfolge in
   ROADMAP.md): der Datamine-Lauf hat `assets/crafting-db.json` neu geschrieben, der in
   Plan 01 gefuehrte Nachweis stammt aus einem aelteren Build. Also erneut ueber
   `dist/**/*.html` und `dist/**/*.js` suchen und die Gegenprobe auf die vier
   Crafting-Flaechen fahren. Beide Zahlen ins Protokoll.

3. Erfolgskriterium 5 am finalen Bestand wiederholen: die Kennungen aller sechs
   kennungstragenden Ausgaben erneut einsammeln und auf Gleichheit pruefen. Wenn Task 1
   dieses Plans den Preis-Snapshot erneuert hat, gehoert `src/data/vehicle-prices.json`
   NICHT dazu — er traegt kein Build-Feld, sondern ein Abrufdatum, und seine Quelle ist
   UEX, nicht der Client. Diesen Unterschied im Protokoll benennen, damit er nicht beim
   naechsten Mal als Abweichung gilt.

4. Die Abnahme-Tabelle schreiben: die zehn Erfolgskriterien aus ROADMAP.md untereinander,
   je Kriterium der Beleg — eine Zahl, ein Exit-Code oder ein Zitat aus einer der drei
   SUMMARY-Dateien. Kein Kriterium bleibt ohne Beleg; ein „durch Codelektuere bestaetigt"
   ist kein Beleg.

5. Die Sichtpruefung vorbereiten und durchfuehren (siehe `<human-check>` unten). Das
   Ergebnis dieser Phase ist an drei Stellen sichtbar, und die Projekt-Erinnerung
   `verify-by-looking` ist eindeutig: bei sichtbarem Ergebnis erst hinsehen, dann melden
   — und „fertig" gilt erst, wenn DE und EN stimmen.
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/_verify.mjs</automated>
    <automated>npm run audit:site</automated>
    <automated>npm run audit:csp</automated>
    <automated>npm run test:e2e</automated>
    <automated>npm run verify:mining</automated>
    <automated>node -e "const fs=require('fs'),p=require('path');const bad='sc-craft'+'.tools';const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(p.join(d,e.name)):[p.join(d,e.name)]);const hits=walk('dist').filter(f=>/[.](html|js)$/i.test(f)).filter(f=>fs.readFileSync(f,'utf8').includes(bad));if(hits.length)throw new Error('am finalen Build noch '+hits.length+' Treffer: '+hits.slice(0,5).join(', '));const norm=s=>s.replace(/<[^>]*>/g,' ').replace(/\s+/g,' ');for(const f of ['dist/crafting/index.html','dist/de/crafting/index.html','dist/topics/crafting.html','dist/de/topics/crafting.html'])if(!/(Quellen|Sources):\s*RSI/.test(norm(fs.readFileSync(f,'utf8'))))throw new Error('Quellenzeile fehlt am finalen Build: '+f);console.log('Erfolgskriterium 1 am finalen Build: 0 Treffer, 4 Flaechen mit RSI')"</automated>
    <automated>node -e "const fs=require('fs');const ziel='12326004';const files=['assets/mining-model.json','assets/mining-db.json','assets/crafting-db.json','src/data/missions.json','src/data/ship-hardpoints.json'];for(const f of files){const j=JSON.parse(fs.readFileSync(f,'utf8'));const kopf=JSON.stringify(Object.fromEntries(Object.entries(j).filter(([k,v])=>typeof v!=='object')));if(!kopf.includes(ziel))throw new Error(f+' traegt nicht '+ziel)}const vp=JSON.parse(fs.readFileSync('src/data/vehicle-prices.json','utf8'));console.log('Erfolgskriterium 5 am finalen Bestand ok |',files.length,'Ausgaben auf',ziel,'| Preise (UEX, ohne Build-Feld) Stand',vp.fetchedAt)"</automated>
    <human-check>
      Sichtpruefung im gebauten Zustand — `npm run preview` (Port 4321), NICHT
      `npm run dev`: der Astro-Dev-Server bricht laut STATE.md auf
      `src/layouts/Layout.astro` mit `Unexpected ")"` ab (vorbestehend, der
      Produktionsbuild ist nicht betroffen).

      1. Crafting-Fuss, DE und EN: http://localhost:4321/crafting/ und
         http://localhost:4321/de/crafting/ ganz nach unten scrollen. Erwartung: die
         Quellen-Zeile nennt `RSI` und keine Fremdquelle; der Datumsstempel darunter
         zeigt den Stand aus dem neuen Lauf. Dasselbe auf einer beliebigen
         Blueprint-Detailseite und auf http://localhost:4321/topics/crafting.html.
      2. Fracturing-Rechner, DE und EN: http://localhost:4321/topics/mining.html, zum
         Rechner scrollen, die Laser-Auswahl aufklappen. Erwartung: jeder Eintrag traegt
         einen lesbaren Geraetenamen; kein Eintrag sieht aus wie ein Dateiname.
      3. Hologramm, drei Stichproben: die Schiffsseiten von Hammerhead, einer
         Lackvariante und Gladius aufrufen, in den Holo-Modus schalten. Erwartung: das
         Hologramm laedt, die Marker sitzen am Modell, die Beschriftungen zeigen
         Item-Namen.
      Bei allen drei Punkten gilt: erst hinsehen, dann melden. Ein Screenshot oder eine
      woertliche Beschreibung des Gesehenen gehoert ins SUMMARY, nicht bloss „geprueft".
    </human-check>
  </verify>

  <acceptance_criteria>
    - `scratch/01.3-abschluss.txt` stellt die Kennzahlen aller sechs Tore denen aus `scratch/01.3-baseline.txt` gegenueber; keine Verschlechterung bleibt unerklaert (Erfolgskriterium 10).
    - `npm run verify:mining`, `npm run audit:csp` und `npm run test:e2e` laufen gruen.
    - Erfolgskriterium 1 ist am FINALEN Build erneut belegt: 0 Treffer ueber `dist/**/*.html` und `dist/**/*.js`, vier Crafting-Flaechen mit `RSI`.
    - Erfolgskriterium 5 ist am finalen Bestand erneut belegt; die Sonderstellung des UEX-Preis-Snapshots ist im Protokoll erklaert.
    - Eine Abnahme-Tabelle im SUMMARY belegt jedes der zehn Erfolgskriterien mit Zahl, Exit-Code oder Zitat.
    - Die Sichtpruefung ist fuer alle drei Flaechen in DE und EN durchgefuehrt und mit dem Gesehenen beschrieben.
  </acceptance_criteria>

  <done>
    Jedes der zehn Erfolgskriterien der Phase traegt einen Beleg, kein Tor ist schlechter
    als vor der Phase, und die drei sichtbaren Stellen sind in beiden Sprachen angesehen
    worden.
  </done>

  <reversibility rating="reversible">
    Reine Messung; es entsteht nur eine nicht committete Protokolldatei.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UEX-API (Netz, Drittanbieter) → committeter Preis-Snapshot | oeffentliche GET-Aufrufe ohne Token; die Antwort wandert unveraendert in eine ausgelieferte Datendatei |
| Dokumentation → naechster Bearbeiter | `MINING-DATENQUELLE.md` ist der Exaktheits-Beweis der Mining-Seite; `FAKTEN-AUDIT.md` steuert, woran als naechstes gearbeitet wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.3-11 | Tampering | `scripts/sync-prices.mjs` gegen eine unvollstaendige UEX-Antwort | medium | mitigate | Der bestehende Fail-Safe (unter 20 gematchten Fahrzeugen kein Ueberschreiben) bleibt und wird nicht umgangen; zusaetzlich Vorher/Nachher-Zahlenvergleich mit 90-%-Schwelle und Ruecknahme per `git checkout` im Befundfall. |
| T-01.3-12 | Spoofing | UEX-Endpunkt hinter Cloudflare | low | accept | Der Abruf laeuft ueber HTTPS gegen den dokumentierten Host ohne Zugangsdaten; es werden keine Geheimnisse gesendet. Ein 403 fuehrt zum Anhalten, nicht zu einem Ausweichpfad auf eine andere Quelle. |
| T-01.3-13 | Repudiation | falsche Build-Kennung in `MINING-DATENQUELLE.md` | medium | mitigate | Die Kennung wird aus `assets/mining-model.json` ABGELESEN und automatisiert gegengeprueft; die alten Kennungen duerfen im Dokument nicht mehr vorkommen. |
| T-01.3-SC | Tampering | npm/pip/cargo installs | low | accept | In diesem Plan wird kein Paket installiert. Keine neue Lieferkette. |
</threat_model>

<verification>
1. UEX-Probeabruf mit HTTP-Status protokolliert; `sync:prices` gefahren oder begruendet ausgelassen.
2. `src/data/vehicle-prices.json` traegt den heutigen `fetchedAt`; Eintragszahl vorher/nachher notiert.
3. Drei Dokumente berichtigt; automatisierte Gegenproben gruen; 305 offene Haekchen unangetastet.
4. Sechs Tore gefahren und gegen `scratch/01.3-baseline.txt` gestellt.
5. Erfolgskriterien 1 und 5 am finalen Build/Bestand wiederholt.
6. Sichtpruefung fuer Crafting-Fuss, Laser-Auswahl und drei Hologramme, in DE und EN.
7. `git diff --stat` zeigt genau die in `files_modified` genannten Dateien.
</verification>

<success_criteria>
- Die Fahrzeugpreise sind frisch gezogen (Erfolgskriterium 8) — oder ihr Ausbleiben ist mit HTTP-Status belegt.
- `MINING-DATENQUELLE.md`, `FAKTEN-AUDIT.md` und `.planning/codebase/STRUCTURE.md` stimmen mit dem Bestand ueberein (Erfolgskriterium 9).
- Die sechs Tore laufen nicht schlechter als vor der Phase (Erfolgskriterium 10).
- Jedes der zehn Erfolgskriterien traegt im SUMMARY einen Beleg.
</success_criteria>

## Artifacts this plan produces

| Art | Name | Ort |
|-----|------|-----|
| Messdatei (nicht committet) | `scratch/01.3-abschluss.txt` (Tor-Kennzahlen gegen die Messlatte) | scratch/ |
| Abnahme-Tabelle | zehn Erfolgskriterien mit je einem Beleg | 01.3-03-SUMMARY.md |
| Aktualisiert | `src/data/vehicle-prices.json`, `MINING-DATENQUELLE.md`, `FAKTEN-AUDIT.md`, `.planning/codebase/STRUCTURE.md` | — |

<output>
Create `.planning/phases/01.3-datenschicht-aufraeumen/01.3-03-SUMMARY.md` when done.
Enthalten muessen sein: der HTTP-Status des UEX-Probeabrufs, Eintragszahlen vorher/nachher,
die abgelesene Kennung aus `assets/mining-model.json`, die Zahl der offenen Haekchen in
`FAKTEN-AUDIT.md` (muss 305 sein), die Gegenueberstellung aller sechs Tor-Kennzahlen, die
Abnahme-Tabelle ueber die zehn Erfolgskriterien und die Beschreibung des in der
Sichtpruefung Gesehenen — in DE und EN.
</output>
