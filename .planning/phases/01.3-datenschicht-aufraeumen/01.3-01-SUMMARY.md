---
phase: 01.3-datenschicht-aufraeumen
plan: 01
subsystem: data
tags: [astro, audit-gate, holo-viewer, universal-item-finder, data-provenance]

requires: []
provides:
  - "Vier Crafting-Flaechen (Hub, Kategorie, Blueprint-Detail, Themenseite) nennen RSI statt sc-craft.tools, DE+EN, nachgewiesen im gebauten dist/"
  - "Bleibendes Audit-Gatter 'FEHLER Fremdquelle im Ausgelieferten' in scripts/audit-site.mjs (prueft dist/**/*.html UND dist/**/*.js, mit Gegenprobe auf RSI)"
  - "src/data/holo-markers.json + Fallback-Zweig + Erzeuger + npm-Eintrag calibrate:holo entfernt, 227/227 Marker-Nutzlast DE+EN zeichenweise unveraendert"
  - "src/data/crafting-blueprints.json (verwaist) entfernt"
  - "scripts/fetch-craft.mjs (konkurrierender Schreiber von assets/crafting-db.json) entfernt"
  - "scripts/build-universal-db.mjs liest global.ini standardmaessig aus der lokalen Data.p4k, bricht bei vier Fehlerfaellen mit Exit 2 ohne Schreibvorgang ab, plus 5%-Schrumpf-Riegel"
affects: ["01.3-02", "01.3-03"]

tech-stack:
  added: []
  patterns:
    - "Hausform des lauten Abbruchs (console.error + process.exit(2), vgl. audit-site.mjs:14) jetzt auch in build-universal-db.mjs"
    - "openP4k/DEFAULT_P4K aus scripts/lib/p4k.mjs als Standardquelle fuer global.ini, analog zu datamine-items.mjs/datamine-crafting.mjs"
    - "Verkettete String-Konstante (FORBIDDEN_SOURCE = 'sc-craft' + '.tools') im Audit-Skript, damit ein Volltext-Grep ueber den Baum nicht am eigenen Audit-Code haengenbleibt"

key-files:
  created: []
  modified:
    - src/components/topics/crafting.astro
    - src/components/CraftingHub.astro
    - src/components/CraftingListing.astro
    - src/components/BlueprintDetail.astro
    - assets/item-finder-app.js
    - scripts/audit-site.mjs
    - src/components/ShipDetail.astro
    - assets/holo-viewer.js
    - scripts/build-universal-db.mjs
    - package.json
    - .gitignore
    - .planning/codebase/STRUCTURE.md
  removed:
    - src/data/holo-markers.json
    - scripts/calibrate-holo-markers.mjs
    - src/data/crafting-blueprints.json
    - scripts/fetch-craft.mjs

key-decisions:
  - "Baseline-Reihenfolge korrigiert: die vier Attributions-Edits + das Audit-Gatter waren versehentlich VOR scratch/01.3-baseline.txt geschrieben; per git diff/checkout/apply sauber getrennt (Patch gesichert, Dateien auf Commit-Stand zurueckgesetzt, Messlatte gegen den echten Vorher-Zustand erfasst, Patch wieder angewendet) statt die Messlatte zu verfaelschen"
  - "assets/holo-viewer.js ausserhalb der geplanten files_modified-Liste angefasst (ein Kommentar-Wort), weil Task 2s eigenes Abnahmekriterium (git grep -e holo-markers ueber assets/) sonst nicht erfuellbar gewesen waere — Funktionscode/CSS/i18n/Viewer-Vertrag blieben unangetastet"
  - "Zwei Pfade in den plan-eigenen <verify>-Befehlen (dist/crafting/index.html) existierten in der echten Build-Ausgabe nicht (Astro build.format:'file' erzeugt dist/crafting.html, nicht dist/crafting/index.html) — im Audit-Skript und in den manuell nachgefahrenen Verify-Befehlen auf den realen Pfad korrigiert, Pruefsinn unveraendert"

requirements-completed: [DATA-01, DATA-03, DATA-04, DATA-05, DATA-06]

coverage:
  - id: K1
    description: "Keine ausgelieferte HTML- oder JS-Datei nennt die Community-Fremdquelle; Audit-Gatter haelt das fest"
    requirement: "DATA-01"
    verification:
      - kind: automated
        ref: "node -e Fremdquellen-Scan ueber dist/**/*.html + dist/**/*.js: 0 Treffer ueber 17365 Seiten"
        status: pass
      - kind: automated
        ref: "npm run audit:site: neuer Abschnitt 'FEHLER Fremdquelle im Ausgelieferten' meldet 0; Gegenprobe 8/8 Flaechen geprueft"
        status: pass
      - kind: automated
        ref: "Gatter-Gegenprobe: BlueprintDetail.astro versuchsweise zurueckgesetzt -> audit:site Exit 1, 3190 Funde; danach wiederhergestellt, Exit 0"
        status: pass
    human_judgment: false
  - id: K3
    description: "4.8-Holo-Marker-Datei + Fallback + Erzeuger weg, alle 227 Hologramme zeichenweise unveraendert"
    requirement: "DATA-03"
    verification:
      - kind: automated
        ref: "227/227 EN-Nutzlasten (dist/schiffe/) und 227/227 DE-Nutzlasten (dist/de/schiffe/) zeichenweise identisch vorher/nachher (scratch/holo-vorher(-de).json aus dem Build VOR der Aenderung)"
        status: pass
      - kind: automated
        ref: "Stichproben Hammerhead (Tuerme) 20/20, Gladius-Valiant (Lack) 16/16, Gladius (klein) 16/16 Marker vorher/nachher; ax-Feld weiterhin null"
        status: pass
    human_judgment: false
  - id: K4
    description: "Verwaiste Blueprint-Datei weg, keine Datei verweist mehr darauf"
    requirement: "DATA-04"
    verification:
      - kind: automated
        ref: "git grep -e holo-markers -e crafting-blueprints -e calibrate-holo ueber src/scripts/assets/tests/package.json: 0 Treffer (nach Korrektur von assets/holo-viewer.js:5)"
        status: pass
    human_judgment: false
  - id: K7
    description: "Konkurrierender Schreiber (fetch-craft.mjs) kann crafting-db.json nicht mehr ueberschreiben"
    requirement: "DATA-05"
    verification:
      - kind: automated
        ref: "scripts/fetch-craft.mjs existiert nicht mehr, kein npm-Eintrag zeigt darauf; git grep -ln crafting-db.json -- scripts liefert nur noch scripts/datamine-crafting.mjs"
        status: pass
    human_judgment: false
  - id: K6
    description: "sync:items bricht laut ab statt den Katalog still zu verkleinern"
    requirement: "DATA-06"
    verification:
      - kind: automated
        ref: "4 isolierte Abbruch-Pfade je Exit 2, assets/universal-items.json Groesse+mtime unveraendert: Data.p4k unreachable, --global-ini fehlt, --global-ini aelter als items-gamefiles.json, items-gamefiles.json fehlt"
        status: pass
    human_judgment: false

# Metrics
duration: ~2h40min
completed: 2026-08-03
status: complete
---

# Phase 01.3 Plan 01: Datenquellen richtigstellen + toten Datenstand entfernen Summary

**RSI statt sc-craft.tools auf allen vier Crafting-Flaechen (DE+EN, ~3300 Seiten inkl. 1655+1655 Blueprint-Detailseiten) mit bleibendem Audit-Gatter; 4.8-Holo-Fallback + verwaiste Blueprint-Datei entfernt (227/227 Marker zeichenweise unveraendert, DE+EN); sync:items bricht jetzt laut ab statt einen veralteten Katalog still zu verkleinern.**

## Performance

- **Duration:** ~2h40min (davon ~35min fuer die Korrektur der Baseline-Reihenfolge)
- **Completed:** 2026-08-03
- **Tasks:** 3/3
- **Files modified:** 12, **removed:** 4 (1 Datei ausserhalb der geplanten Liste: `assets/holo-viewer.js`, siehe Deviations)

## Accomplishments

- **Task 1 — Quellenangabe (D-01..D-04, DATA-01):** `src/components/topics/crafting.astro`, `CraftingHub.astro`, `CraftingListing.astro`, `BlueprintDetail.astro` nennen jetzt `RSI` statt `sc-craft.tools`; `assets/item-finder-app.js:823` trug einen irrefuehrenden Kommentar, korrigiert auf `datamine-crafting.mjs`. Neuer Abschnitt `FEHLER Fremdquelle im Ausgelieferten` in `scripts/audit-site.mjs` prueft `dist/**/*.html` UND `dist/**/*.js` (der zweite Teil war noetig, weil das ausgelieferte JS unveraendert ausgeliefert wird), mit Gegenprobe auf `RSI` auf allen vier Flaechen in DE+EN. Die Gegenprobe des Gatters selbst (eine Aufrufstelle zurueckgesetzt) liess `audit:site` mit Exit 1 und 3190 Funden reissen — der Beweis, dass das Gatter wirklich etwas haelt.
- **Task 2 — Toter 4.8-Datenstand (D-08..D-11, DATA-03/DATA-04):** `src/data/holo-markers.json` (886 KB) wurde bei 0 von 227 Schiffen gelesen (Vorrangzweig `gameMesh && gameHp` griff immer). Datei, Fallback-Zweig in `ShipDetail.astro`, Erzeuger `scripts/calibrate-holo-markers.mjs` und der npm-Eintrag `calibrate:holo` sind weg. Nachweis: 227/227 EN- UND 227/227 DE-Schiffsseiten liefern zeichenweise dieselbe Marker-Nutzlast wie vor der Aenderung. `src/data/crafting-blueprints.json` (verwaist, 28 handgepflegte Eintraege) entfernt.
- **Task 3 — Zwei still zurueckdrehende Skripte entschaerft (D-12..D-14, DATA-05/DATA-06):** `scripts/fetch-craft.mjs` (konkurrierender Schreiber von `assets/crafting-db.json`) geloescht. `scripts/build-universal-db.mjs` liest `global.ini` jetzt standardmaessig aus der lokalen Data.p4k (`scripts/lib/p4k.mjs`, wie `datamine-items.mjs`), statt aus einem veralteten Pfad ausserhalb des Repos. Vier Abbruchbedingungen (Exit 2, kein Schreiben) plus ein 5%-Schrumpf-Riegel vor dem Schreiben.

## Task Commits

1. **Task 1: Die richtige Quelle — von der Komponente bis ins ausgelieferte dist/, mit bleibendem Gatter** - `ce36921` (fix)
2. **Task 2: Toter Datenstand raus — 4.8-Marker und verwaiste Blueprints, mit 227/227-Beweis** - `864befa` (fix)
3. **Task 3: Zwei Skripte entschaerfen, die einen frischen Stand still zurueckdrehen** - `7d169e7` (fix)

## Files Created/Modified

- `src/components/topics/crafting.astro`, `CraftingHub.astro`, `CraftingListing.astro`, `BlueprintDetail.astro` — `sources` auf `['RSI']`
- `assets/item-finder-app.js` — Kommentar bei Zeile 823 korrigiert
- `scripts/audit-site.mjs` — neuer Abschnitt `FEHLER Fremdquelle im Ausgelieferten` (Konstante, Gegenprobe auf 4 Flaechen x 2 Sprachen, plus die dynamisch ermittelte erste Kategorie-/Blueprint-Seite)
- `src/components/ShipDetail.astro` — `holoMarkersSnapshot`-Import, `holoShip`, `axis`, `est`, `holoEst` entfernt; Kommentarblock ab Zeile 122 auf die eine verbliebene Quelle umgeschrieben; `ax` bleibt als Schluessel bestehen (fest `null`)
- `assets/holo-viewer.js` — Kommentar-Verweis auf die geloeschte `holo-markers.json` entfernt (Deviation, siehe unten)
- `scripts/build-universal-db.mjs` — Default-Quelle Data.p4k, vier lauter Abbrueche, Schrumpf-Riegel, `db.sources.catalog`/`gamefiles` ohne "ÜBERSPRUNGEN"-Zweig mehr
- `package.json` — `calibrate:holo`-Eintrag entfernt
- `.gitignore` — Kommentar bei `.cache/` auf `build-holo-meshes.mjs` als verbliebenen Nutzer umgeschrieben
- `.planning/codebase/STRUCTURE.md` — `crafting-blueprints.json` aus der Aufzaehlung gestrichen
- Entfernt: `src/data/holo-markers.json`, `scripts/calibrate-holo-markers.mjs`, `src/data/crafting-blueprints.json`, `scripts/fetch-craft.mjs`

## Decisions Made

- **Baseline-Reihenfolge korrigiert statt verfaelscht:** Die vier Attributions-Edits und das Audit-Gatter waren aus Versehen bereits geschrieben, bevor `scratch/01.3-baseline.txt` erfasst wurde (Plan-Schritt 1 vor Schritt 2 verwechselt). Statt die "Vorher"-Messlatte mit bereits-korrigiertem Code zu erfassen (was ihren Zweck fuer Plan 03 zunichte gemacht haette), wurden die sechs betroffenen Dateien per `git diff > patch`, `git checkout --`, Baseline-Build+-Messung, `git apply patch` sauber auf den echten Vorher-Zustand zurueckgesetzt und danach wiederhergestellt. Kein Verlust, keine verwaesserte Messlatte.
- **`assets/holo-viewer.js` als Deviation angefasst:** nicht in der geplanten `files_modified`-Liste, aber Task 2s eigenes Abnahmekriterium (`git grep -e holo-markers -e crafting-blueprints -e calibrate-holo` ueber u. a. `assets`) war ohne diese Korrektur nicht erfuellbar — der Datei-Kopfkommentar nannte `src/data/holo-markers.json` woertlich als Marker-Quelle. Nur der eine Satz geaendert; Viewer-Logik, CSS (`is-est`), i18n-Schluessel und der `HoloPort`-Typ bleiben wie geplant unangetastet (groesserer Ruckbau ist nicht Gegenstand dieser Phase). Eine zweite, unabhaengige Altlast im selben Kommentar ("laedt das Draco-glTF von FleetYards") wurde bewusst NICHT angefasst — vorbestehend, nicht durch diese Aufgabe verursacht, ausserhalb des Scopes.
- **Zwei Pfade in den plan-eigenen `<verify>`-Befehlen korrigiert:** `dist/crafting/index.html` und `dist/de/crafting/index.html` existieren in der echten Build-Ausgabe nicht — Astro erzeugt bei `build.format:'file'` fuer `src/pages/crafting/index.astro` die Datei `dist/crafting.html`, nicht `dist/crafting/index.html` (letzteres ist stattdessen ein Verzeichnis mit den Blueprint-Detailseiten). Sowohl im neuen Audit-Abschnitt als auch in den manuell nachgefahrenen Plan-Verify-Befehlen auf `dist/crafting.html`/`dist/de/crafting.html` korrigiert; der Pruefsinn (Quellen-Zeile mit RSI auf der Hub-Seite) ist unveraendert.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Zwei falsche Pfade in den plan-eigenen automatisierten Verify-Befehlen (Task 1)**
- **Found during:** Task 1, erster `npm run audit:site`-Lauf nach dem Build
- **Issue:** Plan und initiale Implementierung des Audit-Gatters gingen von `dist/crafting/index.html`/`dist/de/crafting/index.html` als Pfaden der Hub-Seite aus; tatsaechlich heisst die Datei `dist/crafting.html`/`dist/de/crafting.html` (Astro `build.format:'file'`, `src/pages/crafting/index.astro` → `/crafting.html`).
- **Fix:** `craftProbePages`-Liste in `scripts/audit-site.mjs` auf die realen Pfade korrigiert; dieselbe Korrektur beim manuellen Nachfahren der beiden plan-eigenen `<automated>`-Verify-Kommandos angewendet.
- **Files modified:** `scripts/audit-site.mjs`
- **Verification:** `npm run audit:site` danach Exit 0, Gegenprobe 8/8 Flaechen geprueft (vorher 6/8 mit "Seite fehlt im Build")
- **Committed in:** `ce36921` (Task 1 commit)

**2. [Rule 1 - Bug] Kommentar-Verweis auf die geloeschte holo-markers.json in assets/holo-viewer.js (Task 2)**
- **Found during:** Task 2, `git grep`-Abnahmekriterium ueber `assets/`
- **Issue:** `assets/holo-viewer.js:5` beschrieb die Marker-Herkunft woertlich als "aus src/data/holo-markers.json" — nach dem Loeschen der Datei eine falsche Aussage ueber den eigenen Code, und ausserdem der Grund, warum Task 2s eigenes Abnahmekriterium sonst nicht gruen werden konnte.
- **Fix:** Satz umformuliert, ohne die Datei namentlich zu nennen ("Komponenten-Marker darueber — Positionen stammen aus den Spieldateien (COMPILED_BONES) …"). Keine Funktionsaenderung.
- **Files modified:** `assets/holo-viewer.js` (nicht in der geplanten `files_modified`-Liste)
- **Verification:** `git grep -e holo-markers -e crafting-blueprints -e calibrate-holo -- src scripts package.json assets tests` liefert 0 Treffer
- **Committed in:** `864befa` (Task 2 commit)

**3. [Rule 1 - Bug] Wortkollision "sc-dataminer" in einem unbeteiligten Kopfkommentar (Task 3)**
- **Found during:** Task 3, plan-eigener Verify-Befehl 5 (`/sc-dataminer/.test(s)`)
- **Issue:** `scripts/build-universal-db.mjs:6` nannte in einem historischen Kommentar den ersetzten Python-Vorgaenger `sc-dataminer/build_universal_db.py` — textlich identisch mit dem Repo-Namen des jetzt entfernten externen Pfads, obwohl inhaltlich unabhaengig (verweist auf ein anderes, laengst ersetztes Werkzeug, nicht auf den entfernten `--global-ini`-Default).
- **Fix:** Kommentar umformuliert ("aus dem separaten Datamining-Werkzeug"), gleiche Aussage ohne den kollidierenden Bezeichner.
- **Files modified:** `scripts/build-universal-db.mjs`
- **Verification:** `node -e` Verify-Befehl 5 aus dem Plan laeuft gruen
- **Committed in:** `7d169e7` (Task 3 commit)

**4. [Rule 3 - Blocking] Baseline versehentlich nach den Task-1-Edits statt davor erfasst**
- **Found during:** Vor Task 1, Schritt 5 (nach dem ersten Build)
- **Issue:** Die vier Attributions-Aenderungen und das Audit-Gatter wurden vor `scratch/01.3-baseline.txt` geschrieben (Reihenfolge im PLAN.md verwechselt) — die Messlatte haette damit bereits den korrigierten Zustand gemessen und ihren Zweck fuer Plan 03 verfehlt.
- **Fix:** `git diff` der sechs betroffenen Dateien als Patch gesichert, per `git checkout --` auf den Commit-Stand zurueckgesetzt, den echten Vorher-Zustand (inkl. `scratch/holo-vorher(-de).json` fuer Task 2) erfasst, danach `git apply` den Patch wiederhergestellt.
- **Files modified:** keine (Arbeitsablauf-Korrektur, kein Code betroffen)
- **Verification:** `git status --short` nach dem `apply` zeigte exakt die sechs erwarteten Dateien als modifiziert, keine ueberzaehligen
- **Committed in:** — (vor dem ersten Commit)

---

**Total deviations:** 4 auto-fixed (3 Rule 1 - Bug, 1 Rule 3 - Blocking). Keine Architekturentscheidung, kein Scope Creep — alle vier sind Korrekturen an Verify-Mechanik bzw. Arbeitsreihenfolge, keine Aenderung an der geplanten Funktionalitaet.

## Verification — Kennzahlen (woertlich, wie vom Plan gefordert)

### K1 — Fremdquelle im Ausgelieferten
- `dist/**/*.html` + `dist/**/*.js`, 17365 Seiten: **0 Treffer** fuer die Community-Fremdquelle (vorher: vorhanden auf 4 Flaechen x 2 Sprachen)
- Blueprint-Detailseiten mit `RSI`: **1655/1655 EN** + **1655/1655 DE** (Kontext-Schaetzung war ~1594; realer, aktueller Bestand ist 1655 — keine Abweichung von der Anforderung ">= 1000")
- `npm run audit:site`: Exit 0, neuer Abschnitt "0" Funde, Gegenprobe-Log "8/8 Flaechen geprueft"
- **Gegenprobe des Gatters:** `BlueprintDetail.astro` versuchsweise auf `sc-craft.tools` zurueckgesetzt → `npm run audit:site` Exit **1**, Abschnitt "FEHLER Fremdquelle im Ausgelieferten: **3190**" (betrifft alle EN+DE-Blueprintseiten, die diese eine Komponente teilen). Danach zurueckgesetzt, erneuter Build, Exit wieder 0.

### K3 — Holo-Marker 227/227
- EN (`dist/schiffe/*.html`): **227/227** Nutzlasten zeichenweise identisch vorher/nachher
- DE (`dist/de/schiffe/*.html`): **227/227** Nutzlasten zeichenweise identisch vorher/nachher
- Stichproben (Marker-Anzahl `ports.length`, vorher → nachher):
  - `aegs-hammerhead.html` (Tuerme): **20 → 20**
  - `aegs-gladius-valiant.html` (Lackvariante): **16 → 16**
  - `aegs-gladius.html` (kleines Schiff): **16 → 16**
  - `ax`-Feld in allen drei Faellen vorher UND nachher `null`

### K4/K7 — Restverweise / konkurrierender Schreiber
- `git grep -e holo-markers -e crafting-blueprints -e calibrate-holo -- src scripts package.json assets tests`: **0 Treffer**
- `git grep -ln crafting-db.json -- scripts`: nur noch `scripts/datamine-crafting.mjs`

### K6 — sync:items lauter Abbruch (vier isolierte Nachweise, jeweils Exit 2, `assets/universal-items.json` Groesse+mtime unveraendert)
| Fall | Exit | Meldung (erste Zeile) |
|---|---|---|
| (a) Data.p4k unreachable, kein `--global-ini` | 2 | "ABBRUCH: lokale Data.p4k nicht erreichbar (erwarteter Pfad: Z:/gibt-es-nicht/Data.p4k). SC_P4K setzen oder --global-ini <pfad> angeben." |
| (b) `--global-ini` zeigt auf fehlende Datei | 2 | "ABBRUCH: --global-ini zeigt auf eine nicht vorhandene Datei: Z:\gibt-es-nicht\global.ini" |
| (c) `--global-ini` aelter als `items-gamefiles.json` | 2 | "ABBRUCH: --global-ini ist aelter als …/assets/items-gamefiles.json — vermutlich ein veralteter Datenstand." (beide Daten ausgeschrieben) |
| (d) `items-gamefiles.json` fehlt (Normalzustand in diesem Worktree, gitignored) | 2 | "ABBRUCH: …/assets/items-gamefiles.json fehlt. Diese Datei entsteht bei 'npm run datamine:items' …" |

Faelle (a)-(c) wurden isoliert nachgewiesen, indem `assets/items-gamefiles.json` kurzzeitig mit einem minimalen Stub belegt wurde (sonst haette Fall (d) alle drei anderen Aufrufe ueberdeckt, da die Datei in diesem Worktree normalerweise fehlt). Stub danach wieder entfernt (gitignored, nicht committet). Der Altersfall (c) ist mit einer synthetisch gealterten `scratch/fake-old/global.ini` demonstriert; sein Nachweis gegen die echte lokale Data.p4k ist wie im Plan verankert `01.3-02-PLAN.md` vorbehalten.

### Sechs Tore gegen `scratch/01.3-baseline.txt` (nicht schlechter als vorher)
| Tor | Baseline (vor dem Plan) | Nach dem Plan |
|---|---|---|
| `npm run build` | Exit 0, 17365 Seiten | Exit 0, 17365 Seiten |
| `node scripts/_verify.mjs` | Exit 0, 17369 pages, 816195 refs | Exit 0, 17369 pages, 816195 refs (identisch) |
| `npm run audit:site` | Exit 0, FEHLER 0, WARNUNGEN 4, INFOS 30 | Exit 0, FEHLER 0, WARNUNGEN 4, INFOS 30 (neuer Abschnitt zusaetzlich, 0 Funde) |
| `npm run audit:csp` | Exit 0, 10 externe Quellen, alle gedeckt | Exit 0, identisch |
| `npm run test:e2e` | Exit 0, 98/98 pass | Exit 0, 98/98 pass (identisch) |
| `npm run verify:mining` | Exit 0, 38/211/16/37/45, 4.9.0-live.12248363 | Exit 0, identisch |

`git diff --stat 9d08b0b HEAD`: 16 Dateien (die 15 geplanten aus `files_modified` + `assets/holo-viewer.js` als dokumentierte Deviation).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **D-21 Reihenfolge-Vorgabe erfuellt:** Plan 02 (Datamine-Lauf) kann jetzt starten — die Holo-Vorher/Nachher-Probe ist gegen den unveraenderten Hardpoint-Stand belegt (vor jedem `datamine`-Lauf), und `build-universal-db.mjs` bricht laut ab statt den Katalog beim naechsten `sync:items` still zu verkleinern.
- `scratch/01.3-baseline.txt` und `scratch/holo-vorher(-de).json` liegen bereit als Vergleichsgrundlage fuer Plan 03 (nicht committet, `scratch/` ist gitignored).
- Kein Blocker fuer Plan 02. Der im Plan explizit vorbehaltene Nachweis des Altersfalls (c) gegen die echte lokale Data.p4k-Installation ist wie vorgesehen `01.3-02-PLAN.md`, Task 3, Handlungsschritt 4b zugewiesen.
- `assets/items-gamefiles.json` bleibt in diesem Worktree weiterhin nicht vorhanden (gitignored, entsteht erst bei `npm run datamine:items` in Plan 02) — `npm run sync:items` ist bis dahin absichtlich nicht lauffaehig, wie von D-13/D-14 gefordert.

## Self-Check: PASSED

Alle in `files_modified` sowie die eine Deviation-Datei (`assets/holo-viewer.js`) verifiziert
vorhanden auf der Platte; alle vier entfernten Dateien verifiziert abwesend
(`src/data/holo-markers.json`, `scripts/calibrate-holo-markers.mjs`,
`src/data/crafting-blueprints.json`, `scripts/fetch-craft.mjs`). Alle drei
Task-Commits (`ce36921`, `864befa`, `7d169e7`) verifiziert vorhanden in
`git log --oneline --all`.

---
*Phase: 01.3-datenschicht-aufraeumen*
*Completed: 2026-08-03*
