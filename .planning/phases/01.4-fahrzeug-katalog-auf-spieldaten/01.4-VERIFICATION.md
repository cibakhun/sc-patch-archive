---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
verified: 2026-08-04T01:30:00Z
status: passed
score: 8/8 Erfolgskriterien erfüllt (2 mit dokumentierten Anmerkungen, keine davon blockierend)
behavior_unverified: 0
overrides_applied: 0
---

# Phase 01.4: Fahrzeug-Katalog auf Spieldaten — Verifizierungsbericht

**Phasenziel:** Der Schiffskatalog kommt aus der eigenen `Data.p4k`-Extraktion statt aus der
Star-Citizen-Wiki-API. Extern bleiben nur Felder, die in keiner Spieldatei stehen.
**Verifiziert:** 2026-08-04
**Status:** passed (mit zwei dokumentierten Anmerkungen und drei nicht-blockierenden Empfehlungen)
**Re-Verifizierung:** Nein — Erstverifizierung

## Methodik

Diese Prüfung akzeptiert keine SUMMARY-Behauptung ohne eigenen Nachweis. Für jedes der acht
ROADMAP-Erfolgskriterien wurde der tatsächliche Repo-/Build-Zustand direkt gemessen: Skripte
per `git ls-files`, Datenfelder per `node -e`-Einzeiler gegen die committeten JSON-Dateien,
gerenderte Werte per `grep`/Python-Parsing gegen das **gebaute** `dist/`, alle sieben
Qualitäts-Tore live erneut ausgeführt (nicht aus dem SUMMARY übernommen). Zusätzlich wurde der
committete Nachher-Stand (`src/data/vehicles.json`, HEAD) gegen den committeten Vorher-Stand
(`git show b56804f^:src/data/vehicles.json`, der Wiki-Snapshot unmittelbar vor dem Tausch)
feldweise verglichen, um stille Kollateralschäden an der meistgelesenen Datendatei der Seite
aufzudecken.

## Erfolgskriterien (ROADMAP.md, Phase 01.4)

| # | Kriterium | Status | Beleg |
|---|---|---|---|
| 1 | Vier (fünf) Extraktor-Skripte liegen versioniert im Repo | ✓ ERFÜLLT | `git ls-files` zeigt `scripts/lib/cryxml.mjs`, `scripts/datamine-vehicles.mjs`, `scripts/verify-vehicles.mjs`, `scripts/verify-weapon-sizes.mjs`, `scripts/datamine-ship-loadouts.mjs` — alle fünf getrackt, `git log` zeigt die Commits `c4470e3`…`0846487` |
| 2 | `vehicle-external.json` trägt Pledge-Preis, RSI-Link, Abmessungen, Bild-URL; sonst nichts | ✓ ERFÜLLT (mit Anmerkung) | Datei trägt tatsächlich **10** Felder (`msrpUSD`, `pledgeUrl`, `lengthM`, `widthM`, `heightM`, `image`, `crewMax`, `statusEn`, `statusDe`, `fociDe`) — der ROADMAP-Wortlaut "sonst nichts" ist seit D-18 (CONTEXT.md) überholt: der Betreiber wählte ausdrücklich den aufwendigeren Weg (erst suchen, dann einfrieren) und erweiterte die Liste um fünf zusätzliche, einzeln belegte quellenlose Felder. Keines davon war laut Plan-02-Suchdurchgang als "aus dem Spiel" markiert und wurde trotzdem eingefroren — geprüft gegen `01.4-02-SUMMARY.md`s "Eingefroren bleiben"-Abschnitt, keine Diskrepanz gefunden. `REQUIREMENTS.md` VEH-02 nennt weiterhin wörtlich nur fünf Felder und ist als "Complete" markiert — dieselbe veraltete Formulierung, kein Code-Fehler |
| 3 | `node scripts/verify-vehicles.mjs`: 0 Abweichungen bei name/manufacturer/makerCode | ✓ ERFÜLLT | Live-Lauf: `name 227/0`, `manufacturer 227/0`, `makerCode 227/0`, Exit 0 |
| 4 | Für crew, cargoSCU, shieldHp ist je Fahrzeug belegt, welcher Wert gilt und warum | ✓ ERFÜLLT (mit Einschränkung) | Funktional bestätigt: `verify-vehicles.mjs` zeigt `crewMin 227/0`, `crewMax 218/0` (9 rückläufige Spannen bewusst ausgelassen, algorithmische Regel `crewMax >= crewSize` in `build-vehicle-external.mjs`, nicht Handpflege), `cargoSCU 227/0`, `shieldHp 208/0`. Die Regeln sind im Code kommentiert und nachvollziehbar (D-17/D-12/D-20). **Aber:** die im SUMMARY zitierten Beleg-Tabellen `scratch/01.4-urteile-daten.md`, `01.4-urteile-kosmetik.md`, `01.4-feldurteile.md` sind **nicht committet** — `git ls-files \| grep urteile` liefert nichts, `.gitignore` deckt `scratch/` vollständig ab. Die je-Fahrzeug-Begründung existiert nur als lokale Datei auf dieser Maschine und ginge bei einem Aufräumen des Arbeitsverzeichnisses verloren; nur die verdichtete Fassung in den committeten SUMMARY.md-Dateien bleibt dauerhaft |
| 5 | Spieldaten-Katalog führt 227 Fahrzeuge inkl. 4 ATLS-Varianten | ✓ ERFÜLLT | `vehicles.json`: `count: 227`, `vehicles.length: 227`, alle 227 IDs eindeutig, ATLS-IDs (`argo-atls`, `argo-atls-geo`, `argo-atls-geo-collector-grad01`, `argo-atls-geo-collector-grad03`) vorhanden |
| 6 | Datenblatt zeigt in DE/EN dieselben oder belegt bessere Werte; Beschreibungen aus Spieldateien | ✓ ERFÜLLT (mit Fund) | Stichproben in `dist/` bestätigt (s. Tabelle unten). `descriptionEn` 223/227, `descriptionDe` 227/227 aus dem Spiel. **Fund (Kollateralschaden, s. u.):** `pilotDps`/`turretDps` fehlen jetzt bei ca. 30 Schiffen komplett (z. B. Avenger Warlock: vorher 2359,6 DPS sichtbar, jetzt keine DPS-Anzeige), weil die Summenfunktion "alle-oder-nichts" verlangt (ein einzelnes Nicht-Waffen-Item mit `dps:null` im selben Loadout lässt die ganze Summe auf `null` fallen) — nicht in den acht Erfolgskriterien verlangt, aber eine sichtbare Verschlechterung gegenüber dem Vorher-Stand, die in keinem Plan/SUMMARY als akzeptierte Abweichung benannt ist |
| 7 | `sync-vehicles.mjs` gelöscht, `build.yml` ruft keinen Wiki-Sync mehr auf | ✓ ERFÜLLT | `scripts/sync-vehicles.mjs` und `scripts/enrich-weapon-sizes.mjs` existieren nicht mehr auf der Platte; `grep -i wiki` in `.github/workflows/build.yml` und `package.json` liefert 0 Treffer |
| 8 | Stichprobenvergleich Buccaneer/Carrack/Freelancer MAX/315p belegt die Werte gegen das Spiel | ✓ ERFÜLLT | Siehe Stichprobentabelle unten — alle vier Fälle eigenständig nachgemessen, nicht aus dem SUMMARY übernommen |

### Stichprobentabelle (eigene Messung gegen `dist/`, DE + EN)

| Schiff | Feld | EN | DE | Bewertung |
|---|---|---|---|---|
| Buccaneer | Hülle | 7.980 HP | 7.980 HP | korrekt, identisch |
| Buccaneer | Rolle-Chip | „Light Fighter" | „Leichter Jäger" | korrekt (vorher „Interdiction"/„Abriegelung", veraltete Wiki-Rolle) |
| Buccaneer | Nasenwaffe „Revenant Gatling" | S4 | S4 | korrekt — vorher fälschlich S6 (klassischer `display-name-not-a-key`-Fehler, jetzt über `cls` gejoint) |
| Carrack | Besatzung | 6–12 | 6–12 | korrekt, D-17 wie vorgesehen (Minimum aus dem Spiel, Maximum eingefroren) |
| Freelancer MAX | Fracht | 120 SCU | 120 SCU | korrekt, unverändert |
| 315p | Fracht | 12 SCU | — | unterscheidet sich korrekt von 300i |
| 300i | Fracht | 8 SCU | — | Referenzwert — 315p erbt NICHT die 300i-Werte, Varianten-Modifikation greift nachweislich |

## Kollateralschäden — Prüfung gegen den Vorzustand

Der Tausch betrifft die meistgelesene Datendatei der Seite (25+ Leser). Ein direkter
Feld-für-Feld-Vergleich `git show b56804f^:src/data/vehicles.json` (Wiki-Stand unmittelbar vor
dem Tausch) gegen den aktuellen `HEAD`-Stand ergab:

| Prüfpunkt | Ergebnis |
|---|---|
| `patches[]` (Archiv-Verweise) | **20/20 erhalten** — kein Fahrzeug hat seine Archiv-Verweise verloren (D-19-Falle vermieden) |
| `verify-weapon-sizes.mjs` | Echt prüfend: `geprüft: 204 bewaffnete Schiffe · 375 Waffeneinträge`, Exit 0 — keine leere, stumm-grüne Prüfung |
| `cargoSCU`-Abweichungen (beide Werte non-null, aber unterschiedlich) | Nur 3: A2/M2 Hercules Starlifter (kleine Rundungsreste, bereits vor der Phase vorhanden) + Hammerhead 2949 Best In Show (0→40, dokumentierter Fund) — keine unerklärten Fälle |
| `shieldHp`-Abweichungen | 0 — vollständig deckungsgleich mit der in Plan 04 belegten Redundanz-Regel |
| `hullHp`-Abweichungen | 37 (Plan 04 nannte 32 zum damaligen Zwischenstand) — Verhältnisse unregelmäßig (0,595 bis 2,625), deckt sich mit der Diagnose „Wiki veraltet, kein Zählmuster" (D-10); keine neue Auffälligkeit |
| **`fixedWeaponMounts`** (Hardpoint-Größen, unabhängig von Bestückung) | **168/227 → 132/227 (−36)**, u. a. bei Corsair, Caterpillar, Cutter, Apollo Medivac. Wird **nirgends im UI gerendert** (nur in `verify-weapon-sizes.mjs` für Prüfung 3 „Waffe zu groß für Hardpoint" verwendet) — für diese 36 Schiffe läuft Prüfung 3 seit dem Tausch still leer (`if (hp.length && …)`, kein Fehler, kein Hinweis), die Waffengrößen selbst bleiben durch Prüfung 1/2 weiterhin korrekt geprüft. Kein Blocker, aber eine unbenannte Lücke in der Prüftiefe |
| **`pilotDps`/`turretDps`** (aggregierte Waffen-DPS) | **192/227 → 162/227 (pilotDps, −30)**. Ursache: `sumDps()` in `datamine-vehicles.mjs` verlangt, dass JEDES Item im Loadout eine bekannte `dps` trägt — ein einzelnes Nicht-Waffen-Item (z. B. EMP-Gerät beim Avenger Warlock) mit `dps:null` lässt die GESAMTE Summe auf `null` fallen, obwohl die übrigen Waffen einzeln bekannt sind. Sichtbare Folge: der DPS-Messwert (Gauge) verschwindet komplett von der Seite für diese Schiffe, wo vorher ein (Wiki-basierter) Wert stand. Nicht Teil der acht Erfolgskriterien, aber ein echter, unbenannter Rückschritt in der Informationsdichte |
| `qtFuel`/`h2Fuel`/Flugwerte (`scmSpeed`/`maxSpeed`/`boostForward`/`pitch`/`yaw`/`roll`) bei Bodenfahrzeugen | Größtenteils unbedenklich: Wiki führte für Bodenfahrzeuge ohne Quantenantrieb `0`, das Spiel liefert ehrlich `null` (z. B. Cyclone, ROC) — keine inhaltliche Verschlechterung, nur `0` vs. `null` |
| Flugwerte bei Raumschiffen (`scmSpeed` u. a.) | 196/227 → 183/227 (−13) — deckt sich mit dem bereits in Plan 01 gemessenen strukturellen Deckungsstand der IFCS-Extraktion (183/223 von Anfang an bekannt), keine neue Regression durch spätere Pläne, aber gegenüber dem Wiki-Stand ein bestehender, nie einzeln beurteilter Rückgang für 13 Schiffe |
| Beschreibungstexte (DE/EN) | Keine neue Verschlechterung. **Vorbestehender Fund:** die vier ATLS-Einträge führen `descriptionDe` mit wörtlich englischem Text (`"Single-handedly load and unload…"`) und **kein** `descriptionEn` — bestätigt im gebauten `dist/de/schiffe/argo-atls.html`. Gegenprobe: derselbe Fehler steht bereits im Wiki-Snapshot vor dem Tausch (`git show b56804f^:src/data/vehicles.json`) UND in der alten `vDesc()`-Logik vor Plan 05 — die deutsche ATLS-Seite zeigte also schon vor dieser Phase englischen Text. Keine Regression dieser Phase, aber jetzt durch den saubereren Rest des Katalogs auffälliger |
| `descriptionEn`/`descriptionDe` sonst (223 übrige Fahrzeuge) | Keine leeren/Platzhalter-Texte gefunden (Stichproben 300i, Buccaneer, Carrack — vollständiger CIG-Originaltext in beiden Sprachen) |

**Einordnung:** Keiner der gefundenen Kollateralbefunde verletzt eines der acht
ROADMAP-Erfolgskriterien direkt — `fixedWeaponMounts`/`pilotDps`/`turretDps` sind nicht Teil der
Kriterien, und die ATLS-Beschreibungslücke ist nachweislich vorbestehend. Sie sind trotzdem real
und in keinem Plan/SUMMARY als akzeptierte Abweichung benannt — anders als `crewMax`, `cargoSCU`,
`shieldHp`, `hullHp`, wo jede Differenz einzeln beurteilt und dokumentiert wurde.

## Qualitäts-Tore (live erneut ausgeführt, nicht aus SUMMARY übernommen)

| Tor | Ergebnis | Messlatte vor der Phase | Bewertung |
|---|---|---|---|
| `npm run build` | 17351 Seiten, Exit 0 | 17351 Seiten | ✓ identisch |
| `node scripts/_verify.mjs` | 17355 Seiten / 816575 Referenzen aufgelöst | 17355/816573 | ✓ identisch (minimale Referenz-Differenz durch neue Fahrzeugfelder, keine Fehlreferenz) |
| `npm run audit:site` | FEHLER 0 / WARN 4 / INFO 30 | FEHLER 0 / WARN 4 | ✓ identisch |
| `npm run audit:csp` | 10/10 externe Quellen gedeckt | 10/10 | ✓ identisch |
| `npm run test:e2e` | 122/122 bestanden | 122/122 | ✓ identisch |
| `npm run verify:weapons` | Exit 0, 204 Schiffe / 375 Waffeneinträge geprüft | (vor dem Tausch strukturell rot, s. Plan 01) | ✓ jetzt grün UND echt prüfend |
| `node scripts/verify-vehicles.mjs` | Exit 0, keine Feld-Deckung unter der Untergrenze | — | ✓ |
| `npm run verify:mining` | OK, 38 Elemente/211 Komp./14 Laser/37 Minerale/45 Bodies, `4.9.0-live.12326004` | — | ✓ unberührt |

Kein Tor lief schlechter als vor der Phase. Alle acht Werte wurden in dieser Sitzung selbst
gemessen, keiner aus dem SUMMARY übernommen.

## Requirements-Abdeckung

| Requirement | Status | Beleg |
|---|---|---|
| VEH-01 | ✓ SATISFIED | 5 Skripte versioniert |
| VEH-02 | ✓ SATISFIED (Wortlaut in REQUIREMENTS.md veraltet, s. Kriterium 2) | 10 belegt quellenlose Felder |
| VEH-03 | ✓ SATISFIED | name/manufacturer/makerCode 0 Abweichungen |
| VEH-04 | ✓ SATISFIED (Beleg-Durability-Einschränkung, s. Kriterium 4) | crewMin/crewMax entschieden und umgesetzt |
| VEH-05 | ✓ SATISFIED | cargoSCU 224+/227, alle 31 vormals-Null-Fälle gefüllt |
| VEH-06 | ✓ SATISFIED | shieldHp 208/0, Redundanzregel belegt |
| VEH-07 | ✓ SATISFIED | 227/227 inkl. ATLS |
| VEH-08 | ✓ SATISFIED | vehicles.json game-sourced, Wiki-Kette abgeschaltet |

Keine verwaisten Requirements gefunden (alle acht VEH-IDs in den fünf Plan-Frontmatter-Blöcken
referenziert und hier geprüft).

## Anti-Pattern-Scan

Kein `TBD`/`FIXME`/`XXX` in den geänderten Kern-Skripten (`datamine-vehicles.mjs`,
`datamine-ship-loadouts.mjs`, `verify-vehicles.mjs`, `verify-weapon-sizes.mjs`,
`build-vehicle-external.mjs`) gefunden, die nicht durch eine benannte Begründung (D-Referenz,
Kommentar) gedeckt wäre. Kein `console.log`-Stub, keine hartcodierte leere Rückgabe in den
Leserpfaden. Der einzige Platzhalter-artige Fund (`fixedWeaponMounts: []`/`pilotDps: null`) ist
kein Stub, sondern eine ehrliche „nicht auflösbar"-Ausgabe der Extraktions-/Aggregationslogik —
oben unter Kollateralschäden bewertet.

## Empfehlungen (nicht blockierend)

Alle acht Wahrheiten dieser Phase sind automatisiert belegt — keine davon hängt von einer noch
ausstehenden menschlichen Prüfung ab. Die folgenden drei Punkte sind Redaktionsentscheidungen zu
Kollateralfunden außerhalb der acht Erfolgskriterien, keine offenen Verifizierungsfragen:

1. Sollen `scratch/01.4-urteile-daten.md`, `01.4-urteile-kosmetik.md` und `01.4-feldurteile.md`
   dauerhaft ins Repo übernommen werden, damit die je-Fahrzeug-Begründung ein Aufräumen des
   Arbeitsverzeichnisses übersteht (Kriterium 4)?
2. Soll die „alle-oder-nichts"-Regel bei `pilotDps`/`turretDps` in `datamine-vehicles.mjs`
   gelockert werden, damit Schiffe mit einem einzelnen Nicht-Waffen-Item (z. B. EMP-Gerät) ihre
   übrigen bekannten Waffen-DPS trotzdem summiert zeigen (Kriterium 6, Kollateralfund)?
3. Soll die deutsche ATLS-Beschreibung übersetzt werden (vorbestehender Fund, durch diese Phase
   erstmals sichtbar gemacht, nicht verursacht)?

## Gesamturteil

**ERFÜLLT (passed).** Alle acht ROADMAP-Erfolgskriterien der Phase 01.4 sind im tatsächlichen
Repo-/Build-Zustand nachgewiesen, nicht nur behauptet: der Fahrzeug-Katalog kommt nachweislich
aus der eigenen `Data.p4k`-Extraktion, die Wiki-Kette ist abgeschaltet, alle sieben
Qualitäts-Tore laufen mindestens auf Messlatten-Niveau, und die geforderte Stichprobe
(Buccaneer/Carrack/Freelancer MAX/315p) hält in DE und EN. Zwei Kriterien tragen dokumentierte,
nicht-blockierende Anmerkungen (Kriterium 2: mehr eingefrorene Felder als der ROADMAP-Wortlaut
suggeriert, aber durch D-18 gedeckt; Kriterium 4: die je-Fahrzeug-Beleg-Tabellen sind nicht
committet). Zusätzlich fand diese Prüfung drei echte, in keinem Plan/SUMMARY benannte
Kollateralbefunde außerhalb der acht Kriterien (`fixedWeaponMounts`-Deckungsverlust,
verschwindende `pilotDps`/`turretDps`-Anzeige bei ~30 Schiffen, vorbestehende ATLS-DE-Beschreibung)
— keiner davon verletzt ein Erfolgskriterium oder erfordert eine offene menschliche
Verifizierung, alle drei sind als Empfehlung für eine spätere Redaktionsentscheidung
festgehalten.

---
*Verifiziert: 2026-08-04*
*Verifier: Claude (gsd-verifier)*
