---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
plan: 05
subsystem: data
tags: [datacore, vehicle-catalog-swap, patch-spine, weapon-size-guard, wiki-decommission]

requires:
  - phase: 01.4-fahrzeug-katalog-auf-spieldaten
    provides: "Plan 01-04: Feldmatrix, 8 aus dem Spiel extrahierte Feldgruppen, vehicle-external.json (10 Fremdfelder + 4 ATLS-Uebernahmen), crewMin/crewMax/cargoSCU/shieldHp/hullHp je Fahrzeug beurteilt — 227/227-Katalog bereit zum Tausch"
provides:
  - "src/data/vehicles.json: game-sourced (Data.p4k, Client-Kennung sc-alpha-4.9.0@12326004), 227 Fahrzeuge, ersetzt die Wiki-API als Quelle (VEH-08)"
  - "scripts/datamine-vehicles.mjs: schreibt jetzt den ausgelieferten Katalog direkt; traegt das Patch-Rueckgrat (patches[], D-19), Waffen-cls fuer den Groessen-Waechter, Beschreibungs-Bereinigung (Kopfzeile + literale \\n)"
  - "scripts/verify-weapon-sizes.mjs: joint ueber cls statt Anzeigename, meldet die Zahl der geprueften Eintraege, Gegenprobe mit verfaelschten Groessen bestaetigt rot (D-19)"
  - "scripts/sync-vehicles.mjs und scripts/enrich-weapon-sizes.mjs geloescht (D-16); npm-Eintrag sync:vehicles entfernt; .github/workflows/build.yml ohne Wiki-Fahrzeug-Sync"
  - "src/data/vehicles-en.json (Rueckuebersetzungsdatei) geloescht (D-07); vDesc() liest descriptionEn/descriptionDe direkt"
affects: []

tech-stack:
  added: []
  patterns:
    - "Patch-Rueckgrat-Berechnung (Normalisierung + Alias-Tabelle) unveraendert aus dem sterbenden Sync-Skript in den Katalog-Generator umgezogen, BEVOR das Skript geloescht wurde — verhindert den stillen Datenverlust, den ein leeres patches:[] nicht anzeigen wuerde (D-19)"
    - "Waffengroessen-Wächter joint jetzt ueber die Item-Klasse (cls), nicht den Anzeigenamen — dieselbe Lehre wie display-name-not-a-key, hier auf den Pruefer selbst angewandt statt nur auf die Anzeige"
    - "Beschreibungstexte werden AN DER QUELLE bereinigt (Generator), nicht erst beim Rendern — weil die Abnahme den committeten Rohwert in vehicles.json prueft, nicht nur das gerenderte HTML"
    - "Schema-Felder aus einer teilweise befuellten Fremddatei (vier ATLS-Uebernahmen ohne roleEn/roleDe/image/msrpUSD/...) brauchen .nullable().default(null) statt nur .nullable() — ein fehlender Schluessel ist fuer zod etwas anderes als ein vorhandener null-Wert"

key-files:
  created:
    - scratch/01.4-datenblatt-stichprobe.md (nicht committet)
  modified:
    - src/data/vehicles.json
    - scripts/datamine-vehicles.mjs
    - scripts/verify-weapon-sizes.mjs
    - scripts/verify-vehicles.mjs
    - src/content.config.ts
    - src/i18n/vehicleText.ts
    - src/lib/weaponSizes.ts
    - src/components/ShipDetail.astro
    - src/components/ships/ShipsOverview.astro
    - src/i18n/ui.ts
    - src/data/ships.json
    - package.json
    - .github/workflows/build.yml
    - .planning/codebase/INTEGRATIONS.md

key-decisions:
  - "Checkpoint (Task 1): Vorgabe 'tauschen' bestaetigt vom Auftraggeber in dieser Sitzung, Vorbedingung (Plan 04 vollstaendig, Selbst-Check PASSED) geprueft VOR dem Zugriff — getauscht, nicht gewartet."
  - "vFoci()/vRole() (vehicleText.ts) lesen kuenftig roleEn/roleDe direkt statt der 65-Zeilen-FOCI_EN-Handtabelle auf fociDe — die Tabelle ist geloescht; fociDe bleibt im Schema (weiterhin aus vehicle-external.json eingefroren), wird aber nicht mehr angezeigt, ausser roleEn/roleDe fehlt (nur die 4 ATLS-Eintraege)."
  - "weaponSizes.ts: die Fixpunkt-Iteration (Gleichungssystem ueber alle Schiffe) entfaellt zugunsten einer direkten Name->Groesse-Tabelle aus den jetzt vollstaendigen Katalogdaten — der exportierte Vertrag (resolveGuns(), gunSize()) bleibt unveraendert, ShipDetail.astro/shipFacts.ts laufen unveraendert weiter."
  - "enrich-weapon-sizes.mjs geloescht statt geriegelt (T-01.4-19): das Skript schrieb Wiki-abgeleitete Waffengroessen direkt in vehicles.json — nach dem Tausch waere ein versehentlicher Lauf ein Rueckwaertsgang in den game-sourced Katalog. Kein Konsument (kein npm-Skript, kein Workflow) rief es auf."
  - "content.config.ts: image/msrpUSD/pledgeUrl/crewMax bekommen .default(null) — vier der zehn Fremdfelder fuehren bei einigen Fahrzeugen (216-218/227) den Schluessel gar nicht, weil vehicle-external.json nur nicht-leere Werte schreibt; ein fehlender Schluessel ist fuer zod ein Validierungsfehler, kein automatisches null (Rule-1-Fund waehrend des ersten Baus)."
  - "Rule-1-Fund: 9 fest verbaute Kapitalschiff-Bauteile (Idris/Javelin/Polaris-Radar, ROC-DS-Laser) tragen im Spiel eine Item-Klasse ohne Shop-Lokalisierung (Klassenname traegt \"_TEMP\") -> name war null, Schema verlangt einen String. Ruckfall auf die rohe Item-Klasse (cls) statt eines synthetischen Namens — ehrlicher als ein erfundenes Label, erfuellt aber das Schema."

requirements-completed: [VEH-08]

coverage:
  - id: D1
    description: "src/data/vehicles.json entsteht aus scripts/datamine-vehicles.mjs (Data.p4k, Client-Kennung 12326004), 227 Fahrzeuge, keine Wiki-Nennung im Kopf mehr"
    requirement: "VEH-08"
    verification:
      - kind: automated
        ref: "node -e Katalog-Kopf-Pruefung (227 Fahrzeuge, 12326004 vorhanden, keine Wiki-Nennung) — Exit 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Patch-Rueckgrat erhalten (20/20 verknuepfte Fahrzeuge, umgezogen samt SPINE_ALIAS) und Waffengroessen-Waechter auf cls-Join umgestellt (204 Schiffe/375 Eintraege gruen, Gegenprobe mit verfaelschten Groessen rot)"
    requirement: "VEH-08"
    verification:
      - kind: automated
        ref: "node scripts/verify-weapon-sizes.mjs (Exit 0, 204/375) + Gegenprobe-Skript (Exit 1 bei verfaelschten Groessen) + patches[]-Zaehlung (20/20)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Wiki-Kette abgeschaltet: sync-vehicles.mjs + enrich-weapon-sizes.mjs geloescht, npm-Eintrag und CI-Schritt entfernt, 0 Restverweise"
    requirement: "VEH-08"
    verification:
      - kind: automated
        ref: "git grep sync-vehicles (0 Treffer), package.json/build.yml-Pruefungen — alle Exit 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "Stichprobe am gerenderten Datenblatt (D-15): Buccaneer/Carrack/Freelancer MAX/315p je DE+EN, Rollenwechsel und Waffengroessen-Korrektur belegt, 315p unterscheidet sich nachweislich von 300i"
    verification:
      - kind: automated
        ref: "node-Einzeiler gegen dist/schiffe + dist/de/schiffe (Buccaneer 7980 sichtbar, Carrack 6-12 sichtbar, 315p!=300i) + scratch/01.4-datenblatt-stichprobe.md"
        status: pass
    human_judgment: true
    rationale: "Ob eine Wert-Aenderung 'belegt besser' oder ein unerklaerter Fund ist, ist eine Redaktionsentscheidung — ein Mensch sollte die Stichprobentabelle und die Diff-Kategorisierung (scratch/01.4-datenblatt-diff.json) gegenlesen, bevor der Katalog als final gilt."
  - id: D5
    description: "Alle sieben Tore laufen nicht schlechter als scratch/01.4-messlatte.txt (build 17351 Seiten, _verify 17355/816575 aufgeloest, audit:site 0/4/30, audit:csp 10/10, test:e2e 122/122, verify:weapons rot->gruen, verify:vehicles 0 Feld-Deckung unter der Untergrenze)"
    verification:
      - kind: automated
        ref: "npm run build / node scripts/_verify.mjs / npm run audit:site / npm run audit:csp / npm run test:e2e / npm run verify:weapons / npm run verify:vehicles — alle Exit 0"
        status: pass
    human_judgment: false

# Metrics
duration: ~3h
completed: 2026-08-04
status: complete
---

# Phase 01.4 Plan 05: Der Tausch — vehicles.json auf Spieldaten, Wiki-Kette abgeschaltet Summary

**`src/data/vehicles.json` kommt jetzt aus der eigenen Data.p4k-Extraktion (Client
`sc-alpha-4.9.0@12326004`, 227 Fahrzeuge) statt aus der Star-Citizen-Wiki-API; das
Patch-Rueckgrat (20 verknuepfte Fahrzeuge) und der Waffengroessen-Waechter (jetzt
ueber die Item-Klasse statt den Anzeigenamen joinend) sind mitgezogen, die
Wiki-Sync-Kette ist geloescht, und eine Stichprobe an vier Schiffen in DE und EN
belegt, dass die neuen Werte stimmen — u. a. der Buccaneer-Rollenwechsel
„Interdiction"→„Light Fighter" und die Korrektur der Nasenwaffen-Groesse S6→S4.**

## Performance

- **Duration:** ~3h
- **Completed:** 2026-08-04
- **Tasks:** 3/3 (Task 1 Checkpoint bestaetigt, Task 2 der Tausch, Task 3 Wiki-Kette abgeschaltet + Stichprobe)
- **Files modified:** 14 (s. `key-files`) + 2 geloescht (`scripts/sync-vehicles.mjs`, `scripts/enrich-weapon-sizes.mjs`) + 1 geloescht (`src/data/vehicles-en.json`)

## Task 1 — Checkpoint (Vorwarnung vor dem Tausch)

**Antwort: `tauschen`** (die Vorgabe). Der Auftraggeber hatte den Tausch in dieser
Sitzung explizit beauftragt und den vollen Umfang (meistgelesene Datendatei, ein
Dutzend Verbraucher, `scripts/sync-vehicles.mjs` wird geloescht, der Wiki-Schritt
faellt aus `build.yml`) ausdruecklich bestaetigt. Vorbedingung geprueft: die Urteile
aus Plan 04 lagen vollstaendig vor (`01.4-04-SUMMARY.md`, Selbst-Check PASSED,
crewMin/crewMax nach D-17 umgesetzt, cargoSCU/shieldHp/hullHp beurteilt) — die
Vorgabe war korrekt anwendbar (D-14 erfuellt, kein Warten noetig).

## Task 2 — Der Tausch (`b56804f`)

**Kopf des neuen Katalogs** (`src/data/vehicles.json`):
```
fetchedAt:    "2026-08-03"
gameVersion:  "sc-alpha-4.9.0@12326004"
source:       "Data.p4k — DataCore (Game2.dcb), Fahrzeug-Implementierungen (CryXmlB), InventoryContainer"
external:     "src/data/vehicle-external.json (msrpUSD, pledgeUrl, Abmessungen, image, crewMax,
               statusEn/De, fociDe — nicht in den Spieldateien; plus vier komplett
               übernommene ATLS-Datensätze, D-13)"
count:        227
```
Keine Nennung der Wiki-API an irgendeiner Stelle des Kopfes (automatisiert geprüft).

**Patch-Rückgrat (D-19):** die Berechnung (`strip()`-Normalisierung + `SPINE_ALIAS`)
ist unverändert aus `sync-vehicles.mjs` in `scripts/datamine-vehicles.mjs` umgezogen,
**vor** dem Löschen des alten Skripts (Task 3) — 20 von 20 heute verknüpften
Fahrzeugen behalten ihre Archiv-Verweise (vorher: 20, nachher: 20, kein Rückgang).

**Englische Beschreibungen (D-07):** `descriptionEn` liegt für 223/227 Fahrzeuge vor
(≥ 219 gefordert), `descriptionDe` für 227/227. Beide Felder werden **an der Quelle**
bereinigt (`cleanDesc()` in `datamine-vehicles.mjs`): die Metadaten-Kopfzeile
(„Manufacturer: …\nFocus: …\n\n") wird abgeschnitten, wörtliche `\n`-Zeichenfolgen
werden zu echten Zeilenumbrüchen — Anzeige über `.sd__desc { white-space: pre-line }`.
`src/data/vehicles-en.json` ist geloescht (Task 3), 0 Restverweise.

**Umgestellte Leser, abgeglichen gegen `scratch/01.4-feldmatrix.md`:**

| Datei | Änderung | Status |
|---|---|---|
| `src/content.config.ts` | Schema um `roleEn/roleDe/sizeClass/descriptionEn/turretWeapons` erweitert; `fixedWeapons`/`turretWeapons`-Items tragen jetzt optional `size`/`cls`; `image`/`msrpUSD`/`pledgeUrl`/`crewMax` mit `.default(null)` (Rule-1-Fund, s. u.) | geändert |
| `src/i18n/vehicleText.ts` | `vDesc()` liest `descriptionEn`/`descriptionDe` statt der Rückübersetzungsdatei; `vFoci()`/`vRole()` lesen `roleEn`/`roleDe` statt `fociDe` + `FOCI_EN`-Handtabelle (Tabelle entfernt, 65 Zeilen) | geändert |
| `src/lib/weaponSizes.ts` | Fixpunkt-Iteration entfällt, Name→Größe direkt aus den vollständigen Katalogdaten; Vertrag (`resolveGuns()`, `gunSize()`) unverändert | geändert |
| `src/components/ShipDetail.astro` | Quellenzeile nennt Spieldaten statt der Wiki-API (kein Link mehr auf `api.star-citizen.wiki`); `.sd__desc` rendert `\n` | geändert |
| `src/components/ships/ShipsOverview.astro` | Fußnote nennt Spieldaten statt „Community-Projekt"/Wiki-Link | geändert |
| `src/i18n/ui.ts` | ungenutzten Übersetzungsschlüssel `ship.source.wikiNote` (DE+EN) entfernt | geändert |
| `src/pages/schiffe.astro`, `src/pages/de/schiffe.astro` | dünne Hüllen, delegieren unverändert an `ShipsOverview.astro` — kein Code dort, keine Änderung nötig | unverändert (bestätigt) |
| `src/lib/miningShips.ts` | liest `crewMin/crewMax/oreSCU/manufacturer/image/name/id` direkt aus dem rohen JSON — Feldnamen unverändert seit Plan 03/04, kein Code-Änderung nötig | unverändert (bestätigt) |
| `src/components/pilot/PilotPage.astro` | liest `d.data.image`/`d.data.manufacturer`/`d.data.name` über die Content-Collection — unverändert kompatibel | unverändert (bestätigt) |
| `src/lib/shipExtras.ts` | keine direkten Fahrzeug-Feldzugriffe, die sich mit dem Tausch ändern | unverändert (bestätigt) |
| `discord/bot/src/data.mjs`, `data-commands.mjs` | liest `manufacturer/makerCode/typeEn/typeDe/statusEn/statusDe/fociDe/crewMin/crewMax` direkt — alle Feldnamen unverändert, `FOCUS_EN`-Tabelle bleibt gültig (fociDe ist weiterhin eingefroren) | unverändert (bestätigt) |
| `scripts/sync-ships.mjs` | `vehId`-Join über `v.name.toLowerCase()` — der game-sourced Katalog liefert denselben manufacturer-gestrippten Namen wie der Wiki-Katalog | unverändert (bestätigt), `ships.json` neu erzeugt |
| `src/lib/sitemap.ts` | `vehiclesSnapshot.fetchedAt` als `lastmod` — Feldname im Kopf beibehalten (`fetchedAt`, nicht `generatedAt`), damit dieser Leser unverändert bleibt | unverändert (bestätigt) |
| `src/lib/searchIndex.ts`, `src/lib/holoItems.ts` | lesen über die Content-Collection bzw. IDs, keine wiki-spezifischen Felder | unverändert (bestätigt) |

**Rule-1-Fixes (während Task 2, vor der Verifikation behoben):**

1. **Beschreibungen mit sichtbaren Steuerzeichen im Rohdatenfeld.** Der erste Lauf
   bereinigte `descriptionEn/De` nur bei der Anzeige (vehicleText.ts) — die Abnahme
   prüft aber den committeten Rohwert in `vehicles.json` direkt, der noch die
   Metadaten-Kopfzeile und wörtliche `\n`-Folgen trug. Fix: Bereinigung in
   `datamine-vehicles.mjs` verschoben (an der Quelle), `vehicleText.ts` behält eine
   idempotente zweite Bereinigung als Sicherheitsnetz für die vier ATLS-Einträge.
2. **Schema-Validierungsfehler beim ersten Bau** (`image`/`msrpUSD`/`pledgeUrl`/
   `crewMax` fehlten als Schlüssel bei Fahrzeugen ohne diesen Fremddatei-Wert).
   Fix: `.nullable().default(null)` statt nur `.nullable()`.
3. **`components.radars[0].name` war `null`** bei 9 Kapitalschiff-Bauteilen
   (Idris-M/P-Radar, Javelin-Waffen, Polaris-Waffe, ROC-DS-Laser — Item-Klasse trägt
   `_TEMP`, keine Shop-Lokalisierung im Spiel). Fix: Rückfall auf die rohe
   Item-Klasse (`it.cls`) statt `null`, direkt beim Einlesen der Loadout-Ports.

## Task 3 — Wiki-Kette abgeschaltet + Stichprobe (`0846487`)

**`scripts/sync-vehicles.mjs` — Abschnittsliste vor dem Löschen:**

| Abschnitt | Vermerk |
|---|---|
| `loc()`-Helfer (DE/EN aus Wiki-JSON) | aufgegeben — Spieldaten liefern beide Sprachen nativ |
| `strip()` + Patch-Rückgrat-Berechnung (`SPINE_ALIAS`) | **umgezogen** nach `datamine-vehicles.mjs` (Task 2) |
| `page()`-Fetch-Helfer + Pagination-Schleife | aufgegeben — kein API-Aufruf mehr |
| Waffen-/Turm-/Port-Aggregation aus der API-Antwort | ersetzt durch die Loadout-Auswertung in `datamine-vehicles.mjs` |
| Bild-URL-Auflösung (`thumbAt()`) | aufgegeben — der Bildwert selbst ist bereits einmalig in `vehicle-external.json` eingefroren (Plan 03); diese Berechnungslogik wird nicht mehr gebraucht |
| Editions-Dedup (`EDITION`-Regex) | aufgegeben — der game-sourced Katalog hat pro Entity eine eindeutige ID, keine Namenskollisionen |
| Unclassified-Filter + Basher-`RESCUE`-Map | aufgegeben — Spieldaten liefern Typ/Status sofort, keine Wiki-Nachlauf-Lücke mehr |
| Fail-Safe (`< 100 Fahrzeuge`) + Schreiblogik | aufgegeben — ersetzt durch die Schreiblogik in `datamine-vehicles.mjs` |

Kein Abschnitt ohne Vermerk — die Prüfung, die D-19 verlangt, ist damit abgeschlossen.

**`scripts/enrich-weapon-sizes.mjs` — Entscheidung: gelöscht** (nicht geriegelt).
Das Skript löste Waffengrößen über die Wiki-API auf und schrieb sie direkt in
`vehicles.json` — nach dem Tausch wäre ein versehentlicher Lauf ein Rückwärtsgang
in den game-sourced Katalog (T-01.4-19, derselbe Fehlertyp wie DATA-05 aus Phase
01.3). Kein `package.json`-Skript und kein Workflow rief es auf; „liegen lassen"
war laut Plan ohnehin ausgeschlossen.

**`.github/workflows/build.yml`:** der Schritt „Sync vehicle catalog (Star Citizen
Wiki API)" ist ersatzlos entfernt; `sync:ships`, `sync:prices`, `sync:extras`
bleiben unangetastet (automatisiert geprüft). Ein Kommentar erklärt, warum der
Katalog in CI nicht erneuerbar ist (lokale `Data.p4k` nötig).

**`.planning/codebase/INTEGRATIONS.md`:** der Wiki-API-Eintrag für den
Fahrzeugkatalog ist auf den tatsächlichen Stand gebracht — nur noch
`scripts/verify-item-prices.mjs` bleibt als (unveränderter, phasenfremder) Client.

### Stichprobe am gerenderten Datenblatt (D-15, Erfolgskriterium 8)

Volle Tabelle in `scratch/01.4-datenblatt-stichprobe.md` (nicht committet). Kurzfassung:

| Schiff | Feld | Vorher | Nachher | Urteil |
|---|---|---|---|---|
| Buccaneer (EN/DE) | Rolle-Chip | „Interdiction"/„Abriegelung" | „Light Fighter"/„Leichter Jäger" | belegt besser — der sichtbarste Beleg für die veraltete Wiki-Rolle |
| Buccaneer (EN/DE) | Hülle | (Wiki, D-10 bereits als veraltet erkannt) | **7.980 HP** | belegt besser, auf BEIDEN Sprachseiten geprüft |
| Buccaneer (EN/DE) | Nasenwaffe „Revenant Gatling" | **S6** (falscher Namens-Join, eines von vier gleichnamigen Items) | **S4** (richtig, Join über `cls`) | belegt besser — genau der `display-name-not-a-key`-Fehler |
| Carrack (EN/DE) | Besatzung | „6–12" | „6–12" | **gleich** — D-17 verlangt genau das (Minimum aus dem Spiel, Maximum eingefroren) |
| Freelancer MAX (EN/DE) | Fracht | „120 SCU" | „120 SCU" | **gleich** — Plan 04s Formel trifft den Wert unverändert |
| 315p vs. 300i (Katalog) | Hülle/Fracht/Schild | — | 315p: 9.100 HP/**12 SCU**/4.488 HP · 300i: 8.700 HP/**8 SCU**/4.320 HP | unterscheidet sich in allen drei Feldern — die Varianten-Modifikation greift nachweislich, exakt die im Plan genannten Referenzwerte (8/12 SCU) |

**Vorher/Nachher-Diff über alle 227×2 Seiten** (`scratch/capture-datenblatt.mjs` +
`scratch/diff-datenblatt.mjs`, Rohdaten in `scratch/01.4-datenblatt-diff.json`):
367 von 454 Seiten zeigen mindestens eine Änderung, **0 Kennzahlen-Zeilen sind von
einem Wert auf leer gefallen**. Jede Änderungsklasse ist auf ein bereits in Plan
01/03/04 beurteiltes Feld zurückgeführt:

| Änderungsklasse | Anzahl Zeilen | Zurückgeführt auf |
|---|---|---|
| Typ/Rolle-Anzeige (Type/Typ) | 137 EN + 205 DE | roleEn/roleDe ersetzt die Wiki-Foci-Handübersetzung (Task 2, geplante Änderung) |
| Größe (Size/Größe) | 41 | `sizeDe`, 40 Abweichungen bereits in Plan 01/03 (D-09) dokumentiert, dort bewusst unverändert gelassen |
| Besatzung (Crew/Besatzung) | 9 | exakt die 9 D-17-Fälle mit zurücklaufender Wiki-Spanne (Plan 03) |
| Range/Quantum fuel | 2 je | P-72 Archimedes Emerald/C8R Pisces Rescue, in Plan 04 „Next Phase Readiness" bereits als bekannter Rundungsrest benannt |
| Fracht (Hammerhead Showdown, A2/M2 Starlifter) | 6 (3 Schiffe × 2 Sprachen) | exakt die in Plan 04 dokumentierten Funde (Hammerhead 2949 Best In Show: 0→40 korrekt; A2/M2: bereits vor Task 2 abweichend, kein Teil der 31) |

Kein Fund außerhalb dieser Klassen.

## Task Commits

1. **Task 2: Der Tausch** — `b56804f` (feat)
2. **Task 3: Wiki-Kette abgeschaltet + Stichprobe** — `0846487` (feat)

(Task 1 ist ein Checkpoint ohne eigenen Commit — die Antwort ist oben protokolliert.)

## Files Created/Modified

- `src/data/vehicles.json` — game-sourced Katalog, 227 Fahrzeuge, Kennung `12326004`
- `scripts/datamine-vehicles.mjs` — schreibt jetzt `vehicles.json` direkt, Patch-Rückgrat, Waffen-`cls`, Beschreibungs-Bereinigung
- `scripts/verify-weapon-sizes.mjs` — cls-Join, Selbstprüfung (Zahl der geprüften Einträge), Gegenprobe-fähig
- `scripts/verify-vehicles.mjs` — vergleicht künftig frischen Lauf gegen committeten Katalog
- `src/content.config.ts` — neue Felder, `.default(null)` für teilweise befüllte Fremddatei-Felder
- `src/i18n/vehicleText.ts` — `vDesc()`/`vFoci()`/`vRole()` auf Spieldaten-Felder umgestellt
- `src/lib/weaponSizes.ts` — Fixpunkt-Iteration entfernt, direkte Name→Größe-Tabelle
- `src/components/ShipDetail.astro`, `src/components/ships/ShipsOverview.astro` — Quellenzeile ohne Wiki-Link
- `src/i18n/ui.ts` — ungenutzten Übersetzungsschlüssel entfernt
- `src/data/ships.json` — neu erzeugt (22 Einträge, unverändert)
- `package.json` — `sync:vehicles` entfernt
- `.github/workflows/build.yml` — Wiki-Fahrzeug-Sync-Schritt entfernt
- `.planning/codebase/INTEGRATIONS.md` — aktueller Stand
- Gelöscht: `scripts/sync-vehicles.mjs`, `scripts/enrich-weapon-sizes.mjs`, `src/data/vehicles-en.json`

## Decisions Made

- Checkpoint mit „tauschen" beantwortet (Vorgabe, vom Auftraggeber vorab bestätigt).
- `enrich-weapon-sizes.mjs` gelöscht statt geriegelt (T-01.4-19) — kein Aufrufer, Riegel wäre unnötige Komplexität für ein totes Skript.
- Beschreibungs-Bereinigung an der Quelle (Generator), nicht erst beim Anzeigen — die Abnahme prüft den Rohwert.
- Fremddatei-Felder mit teilweiser Abdeckung bekommen `.default(null)` statt nur `.nullable()`.
- `fociDe` bleibt im Schema (weiterhin eingefroren), wird aber von `vFoci()`/`vRole()` nicht mehr gelesen — nur noch Rückfall für die 4 ATLS-Einträge ohne `roleEn`/`roleDe`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Beschreibungstexte trugen sichtbare Steuerzeichen im Rohdatenfeld**
- **Found during:** Task 2, erster automatisierter Verify-Lauf gegen `vehicles.json`
- **Issue:** Bereinigung lag zunächst nur in `vehicleText.ts` (Anzeige) — die
  Plan-Abnahme prüft aber den committeten `descriptionEn`-Rohwert direkt und fand
  222 Fälle mit „Manufacturer: …\nFocus: …\n\n"-Kopfzeile bzw. wörtlichen `\n`.
- **Fix:** `cleanDesc()` in `scripts/datamine-vehicles.mjs` verschoben (an der
  Quelle); `vehicleText.ts` behält eine idempotente zweite Bereinigung als
  Sicherheitsnetz für die vier ATLS-Einträge (deren Text nicht durch den
  Generator läuft).
- **Files modified:** `scripts/datamine-vehicles.mjs`, `src/i18n/vehicleText.ts`
- **Verification:** `node`-Einzeiler gegen `vehicles.json` — 0 Beschreibungen mit
  sichtbaren Steuerzeichen/Kopfzeile.
- **Committed in:** `b56804f`

**2. [Rule 3 - Blocking] Schema-Validierungsfehler beim ersten Bau (fehlende Fremddatei-Schlüssel)**
- **Found during:** Task 2, erster `npm.cmd run build`
- **Issue:** `orig-600i-bis2951` (und 8 weitere) hatten keinen `image`-Schlüssel
  (Wiki-Snapshot führte dort kein Bild); `msrpUSD`/`pledgeUrl`/`crewMax` betrafen
  ähnlich 9-11 Fahrzeuge. `z.object(...).nullable()` ohne `.default()` lehnt einen
  fehlenden Schlüssel ab (anders als einen vorhandenen `null`-Wert).
- **Fix:** `.default(null)` an `image`/`msrpUSD`/`pledgeUrl`/`crewMax` ergänzt.
- **Files modified:** `src/content.config.ts`
- **Verification:** `npm.cmd run build` läuft danach fehlerfrei durch (17351 Seiten).
- **Committed in:** `b56804f`

**3. [Rule 1 - Bug] `components.radars[0].name` war `null` bei 9 Kapitalschiff-Bauteilen**
- **Found during:** Task 2, zweiter `npm.cmd run build`
- **Issue:** Idris-M/P-Radar, Javelin-Waffen (×3), Polaris-Waffe, ROC-DS-Laser
  referenzieren im Spiel eine Item-Klasse ohne Shop-Lokalisierung (Klassenname
  trägt sogar „_TEMP") — `locEn()`/`locDe()` lieferten `null`, das Schema verlangt
  aber einen `string` für `name`.
- **Fix:** Rückfall auf die rohe Item-Klasse (`it.cls`) statt `null`, direkt beim
  Einlesen der Loadout-Ports in `datamine-vehicles.mjs` (betrifft alle
  Komponenten-/Waffen-Kategorien gleichermaßen, nicht nur Radare).
- **Files modified:** `scripts/datamine-vehicles.mjs`
- **Verification:** `npm.cmd run build` läuft danach fehlerfrei durch.
- **Committed in:** `b56804f`

---

**Total deviations:** 3 (1× Rule 1 Beschreibungs-Bereinigung, 1× Rule 3 Schema-Default,
1× Rule 1 Bauteilname-Fallback). Alle vor der jeweiligen Task-Verifikation behoben,
im Task-2-Commit enthalten.
**Impact on plan:** Kein Scope Creep. Alle drei Funde sind Entdeckungen während der
Umsetzung des Plan-Auftrags (erster Bau nach dem Schemawechsel), keine neuen Features.

## Known Stubs

Keine. Alle Pflichtfelder des neuen Schemas sind entweder aus dem Spiel befüllt,
aus `vehicle-external.json` eingefroren, oder tragen einen ehrlichen Rückfall
(Item-Klasse statt erfundenem Namen, s. Deviation 3).

## Issues Encountered

- `ships.json` (`scripts/sync-ships.mjs`) führt seit jeher nur 22 kuratierte
  Einträge (die per Patch-Daten referenzierten Schiffe), nicht den vollen
  227er-Katalog. Der Plan-Verify-Code (`if(s.length<200)throw...`) geht von einer
  deutlich größeren Datei aus — geprüft gegen den committeten Vorzustand
  (`git show HEAD~2:src/data/ships.json`, ebenfalls 22 Einträge): **kein
  Rückgang**, die „200"-Schwelle im Plan-Text ist für diese Datei nicht zutreffend
  (Plan-Text-Ungenauigkeit, keine Abweichung meiner Arbeit). Der eigentliche Zweck
  der Prüfung — „ships.json nicht geschrumpft" — ist erfüllt (22 vorher, 22 nachher,
  0 Fahrzeuge ohne Besatzungsangabe).
- `sd__glabel`-Beschriftungen für Hülle/Schild („Hull"/„Shields") sind auf der
  DE-Seite ebenfalls englisch — ein vorbestehender, von diesem Plan nicht
  verursachter Lokalisierungs-Lücke (die Werte selbst, `7,980 HP` etc., sind
  korrekt und identisch auf beiden Sprachseiten); außerhalb des Auftrags, nicht
  behoben.

## User Setup Required

None — kein externer Dienst nötig. Lief gegen die bereits lokal installierte
Data.p4k (Changelist `12326004`).

## Next Phase Readiness

- **Phase 01.4 ist mit diesem Plan abgeschlossen.** Alle acht Erfolgskriterien der
  ROADMAP sind erfüllt: `vehicles.json` game-sourced (7), Datenblatt in DE/EN
  mindestens gleichwertig oder belegt besser (6), Stichprobenvergleich an vier
  Schiffen belegt (8), alle Tore mindestens auf Messlatten-Niveau.
- **Offen, nicht Teil dieser Phase (dokumentiert, kein Blocker):** `sizeDe` (40
  Abweichungen, Plan 01/03 bewusst unverändert gelassen), P-72 Archimedes
  Emerald/C8R Pisces Rescue (kleine Flugwerte-Rundungsreste, Plan 04 bereits
  benannt), der Rollenfilter aus `datamine-vehicle-roles.mjs` (liegt auf
  `staging`, eigener Strang, in dieser Phase nur als bereits vorhandene,
  unberührte Funktionalität mitgelaufen).
- **`src/data/vehicles-gamefiles.json` bleibt lokale, gitignored Zwischenstufe** —
  erneut erzeugbar per `npm run datamine:loadouts && npm run datamine:vehicles`
  (Reihenfolge wichtig). Ein künftiger Patch-Tag-Lauf schreibt automatisch sowohl
  die Zwischenstufe als auch das ausgelieferte `vehicles.json`.

## Self-Check: PASSED

`src/data/vehicles.json`, `scripts/datamine-vehicles.mjs`,
`scripts/verify-weapon-sizes.mjs`, `scripts/verify-vehicles.mjs`,
`src/content.config.ts`, `src/i18n/vehicleText.ts`, `src/lib/weaponSizes.ts`,
`src/components/ShipDetail.astro`, `src/components/ships/ShipsOverview.astro`,
`src/i18n/ui.ts`, `src/data/ships.json`, `package.json`,
`.github/workflows/build.yml`, `.planning/codebase/INTEGRATIONS.md` auf der Platte
vorhanden und geändert; `scripts/sync-vehicles.mjs`, `scripts/enrich-weapon-sizes.mjs`,
`src/data/vehicles-en.json` bestätigt gelöscht. Commits `b56804f` und `0846487`
verifiziert vorhanden in `git log --oneline`. Alle sieben Tore erneut ausgeführt
und bestanden (build 17351 Seiten, `_verify` 17355/816575 aufgelöst, `audit:site`
0/4/30, `audit:csp` 10/10, `test:e2e` 122/122, `verify:weapons` 204/375 grün +
Gegenprobe rot bestätigt, `verify:vehicles` 0 Feld-Deckung unter der Untergrenze).

---
*Phase: 01.4-fahrzeug-katalog-auf-spieldaten*
*Completed: 2026-08-04*
