---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/cryxml.mjs
  - scripts/datamine-vehicles.mjs
  - scripts/verify-vehicles.mjs
  - scripts/verify-weapon-sizes.mjs
  - scripts/datamine-ship-loadouts.mjs
  - src/data/ship-loadouts.json
  - package.json
  - .gitignore
autonomous: true
requirements: [VEH-01]
user_setup: []

must_haves:
  truths:
    - "Die Extraktor-Vorarbeit liegt versioniert in diesem Zweig und ist committet; ein Aufraeumen des Arbeitsverzeichnisses `.claude/worktrees/` kann sie nicht mehr vernichten (D-01, D-02, VEH-01, Erfolgskriterium 1)."
    - "`npm.cmd run datamine:vehicles` laeuft in DIESEM Zweig gegen den installierten Client (Changelist 12326004) und erzeugt einen Katalog — der Weg Data.p4k -> CryXmlB/DataCore -> Katalog -> Vergleichstabelle ist einmal ganz durchlaufen, nicht nur uebernommen."
    - "`npm.cmd run verify:weapons` ist verdrahtet und laeuft gegen den heutigen Bestand gruen — der Waechter gegen den Namens-Join-Fehler ist scharf, bevor irgendetwas getauscht wird (D-04)."
    - "`src/data/vehicles-gamefiles.json` ist gitignored und bleibt es; die Zwischenstufe wird nicht zum Liefergegenstand (D-03)."
    - "Fuer JEDES Feld, das die Seite heute aus `vehicles.json` liest, steht schriftlich, woher es nach dem Tausch kommt — Spieldaten, Fremddatei, abgeleitet oder mit Verbraucher-Aenderung. Kein Feld bleibt unbeurteilt (D-14, die Lehre aus 01.3)."
    - "Die neu erzeugte `src/data/ship-loadouts.json` ist nicht kleiner als der committete Bestand: 223 Schiffe, und kein Schiff verliert Ports."
  artifacts:
    - scripts/datamine-vehicles.mjs (Katalog-Generator, versioniert)
    - scripts/lib/cryxml.mjs (CryXmlB-Leser, versioniert)
    - scripts/verify-vehicles.mjs (Feld-fuer-Feld-Vergleich, versioniert)
    - scripts/verify-weapon-sizes.mjs (Waffengroessen-Waechter, versioniert, als `verify:weapons` verdrahtet)
    - scratch/01.4-feldmatrix.md (Feld -> Herkunft nach dem Tausch, Urteil je Feld)
    - scratch/01.4-messlatte.txt (Ausgangszahlen und Kennzahlen aller Tore VOR der Phase)
  key_links:
    - "`scripts/datamine-ship-loadouts.mjs` ist die FUENFTE, in D-01 nicht genannte Vorarbeit: im Geschwister-Arbeitsverzeichnis ist es geaendert und liefert zusaetzlich `carrier` (Turm- vs. Pilotwaffe), `cargo` (SCU je Schiff) und `ports`. Die hier liegende Fassung liefert das NICHT — `src/data/ship-loadouts.json` hat heute nur `generatedAt/source/count/ships`. Ohne diese Aenderung liest der Generator `LOAD.cargo` als undefined und `it.carrier` nie: `cargoSCU` waere bei ALLEN 223 Fahrzeugen null und `turretWeapons` bei allen leer."
    - "`scripts/verify-weapon-sizes.mjs` liest `fixedWeaponSizes` und `fixedWeaponMounts` aus `vehicles.json`. Beide Felder stammen heute aus der Wiki-Kette (`enrich-weapon-sizes.mjs`). Der Waechter muss deshalb HEUTE gruen laufen, solange der alte Katalog liegt — sonst ist nach dem Tausch nicht unterscheidbar, ob er den Tausch beanstandet oder schon vorher rot war."
    - "Der Generator iteriert ueber die Schluessel von `src/data/ship-hardpoints.json` (seit Plan 01.3-02: 227), findet aber nur 223 DataCore-Records. Die Differenz ist genau die ATLS-Gruppe aus D-13 — sie faellt hier als `ohne DataCore-Record` heraus und ist damit gemessen statt behauptet."
---

<objective>
Erst die Vorarbeit in Sicherheit bringen, dann den ganzen Weg in diesem Zweig einmal
selbst gehen — und danach schwarz auf weiss festhalten, was der Spieldaten-Katalog heute
NICHT kann.

Ziel der Phase (woertlich aus ROADMAP.md): „Der Schiffskatalog kommt aus der eigenen
`Data.p4k`-Extraktion statt aus der Star-Citizen-Wiki-API. Extern bleiben nur die fuenf
Felder, die in keiner Spieldatei stehen …"

Purpose: Zwei Gruende, warum dieser Plan vor allen anderen steht. Erstens D-02: die vier
(in Wahrheit fuenf) Skripte liegen unversioniert unter `.claude/worktrees/`, und `.claude/`
ist gitignored — ein Aufraeumen des Arbeitsverzeichnisses vernichtet mehrere Tage teuer
erarbeitete Extraktionslogik. Zweitens die Lehre aus Phase 01.3: dort fiel bei
`extract:hardpoints` ein Schiff still aus dem Bestand, weil niemand vorher gezaehlt hatte.
`vehicles.json` ist die meistgelesene Datendatei des Repos; wer sie tauscht, ohne vorher
Feld fuer Feld aufgeschrieben zu haben, was die Seite daraus liest, tauscht blind.

Output: Fuenf versionierte Skripte, drei npm-Eintraege, ein frisch gegen Changelist
`12326004` erzeugter Katalog — und eine Feldmatrix, die fuer jedes Feld der Seite sagt,
woher es nach dem Tausch kommt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-CONTEXT.md
@.planning/phases/01.3-datenschicht-aufraeumen/01.3-02-SUMMARY.md
</context>

<tasks>

<!-- Diese Bezeichner MUESSEN woertlich vorkommen — es sind Datei-, Feld- und
     Skriptnamen, jede Umschreibung waere mehrdeutig. Alle Negativ-Suchen der Abnahme
     sind auf `src/`, `scripts/`, `.github/` oder EINE benannte Datei begrenzt;
     `.planning/` ist von jeder Negativ-Suche ausgenommen, weil CONTEXT.md und dieser
     Plan die Bezeichner selbst tragen. -->
<!-- planner-discipline-allow: vehicles-gamefiles -->
<!-- planner-discipline-allow: sync-vehicles -->
<!-- planner-discipline-allow: vehicle-external -->
<!-- planner-discipline-allow: datamine-ship-loadouts -->

<task type="tracer">
  <name>Task 1: Die Vorarbeit hereinholen und den Weg p4k -> Katalog -> Vergleich in diesem Zweig einmal ganz durchlaufen</name>

  <read_first>
    - .claude/worktrees/buccaneer-waffen-daten-be6ac7/scripts/datamine-vehicles.mjs (vollstaendig, 20 KB — besonders Zeile 100-107: `ourIds` kommt aus `ship-hardpoints.json`; Zeile 273-284: der `it.carrier`-Zweig; Zeile 304: `LOAD.cargo?.[id]`; Zeile 365-373: das Einmischen von `vehicle-external.json`)
    - .claude/worktrees/buccaneer-waffen-daten-be6ac7/scripts/lib/cryxml.mjs (vollstaendig, 6 KB — Zeile 119-142 `applyModification`, der Varianten-Mechanismus)
    - .claude/worktrees/buccaneer-waffen-daten-be6ac7/scripts/verify-vehicles.mjs (vollstaendig, 3,6 KB — die SCALARS-Tabelle Zeile 23-31 ist die Vergleichsdefinition)
    - .claude/worktrees/buccaneer-waffen-daten-be6ac7/scripts/verify-weapon-sizes.mjs (vollstaendig — die drei Pruefungen im Kopfkommentar)
    - scripts/datamine-ship-loadouts.mjs (die HIER liegende Fassung, vollstaendig) und danach `git diff --no-index` gegen die Fassung im Geschwister-Arbeitsverzeichnis
    - scripts/lib/p4k.mjs (Zeile 16-30 — `DEFAULT_P4K`, `SC_P4K`, `openP4k`)
    - package.json (die Bloecke `datamine:*`, `verify:*`, `sync:*` — der Hausstil der Eintragsnamen)
    - .gitignore (Zeile 31-42 — wie die uebrigen `*-gamefiles.json` eingetragen sind)
  </read_first>

  <precondition>
    Die lokale Spielinstallation muss lesbar sein: `F:/Games/Star Citizen/StarCitizen/LIVE/Data.p4k`
    (Vorgabe aus `scripts/lib/p4k.mjs`, ueberschreibbar per `SC_P4K`) und daneben
    `build_manifest.id`. Vor Beginn beides pruefen und die gelesene
    `RequestedP4ChangeNum` protokollieren; sie MUSS `12326004` lauten — denselben Stand
    hat Phase 01.3 in den uebrigen Datendateien verankert. Weicht sie ab, hier anhalten
    und melden. Ausserdem muss `node_modules/` vorhanden sein (`npm.cmd ci`, Node 22);
    ohne Abhaengigkeiten laeuft keines der Tore.
  </precondition>

  <files>scripts/lib/cryxml.mjs, scripts/datamine-vehicles.mjs, scripts/verify-vehicles.mjs, scripts/verify-weapon-sizes.mjs, scripts/datamine-ship-loadouts.mjs, src/data/ship-loadouts.json, package.json, .gitignore</files>

  <action>
Dies ist der duenne Schnitt durch alle Schichten, die diese Phase anfasst: Data.p4k ->
CryXmlB/DataCore -> Katalog -> Vergleichstabelle -> npm-Tor. Er wird EINMAL ganz
durchlaufen, bevor irgendein Feld verhandelt wird.

1. Messlatte zuerst — die Phase endet mit „nicht schlechter als vorher", das setzt ein
   Vorher voraus. `scratch/` anlegen (bereits gitignored) und nach
   `scratch/01.4-messlatte.txt` schreiben: die Abschlusszeilen von `npm.cmd run build`,
   `node scripts/_verify.mjs`, `npm.cmd run audit:site` (Seitenzahl sowie FEHLER/WARNUNGEN
   je Abschnitt), `npm.cmd run audit:csp` und `npm.cmd run test:e2e`. Dazu die Zaehler des
   heutigen Bestands: Fahrzeuge in `src/data/vehicles.json` (227), Schiffe in
   `src/data/ship-loadouts.json` (223), Fahrzeuge in `src/data/ship-hardpoints.json` (227),
   und je Fahrzeug die Zahl der Ports aus `ship-loadouts.json`. Diese Datei ist die
   Vergleichsgrundlage aller vier Plaene dieser Phase.

2. Die vier unversionierten Skripte hereinkopieren (D-01, D-02, VEH-01), unveraendert und
   ohne Umformatierung — sie sind erprobt, und jede Aenderung an dieser Stelle waere eine
   nicht nachvollziehbare Vermischung von Uebernahme und Bearbeitung:
   `scripts/lib/cryxml.mjs`, `scripts/datamine-vehicles.mjs`, `scripts/verify-vehicles.mjs`,
   `scripts/verify-weapon-sizes.mjs`. Quelle ist
   `.claude/worktrees/buccaneer-waffen-daten-be6ac7/`. Nach dem Kopieren je Datei
   sha256 gegen das Original halten und protokollieren — eine stille Abweichung beim
   Kopieren waere der duemmste denkbare Fehler dieser Phase.

3. Die FUENFTE Vorarbeit, die in D-01 fehlt: `scripts/datamine-ship-loadouts.mjs` ist im
   Geschwister-Arbeitsverzeichnis geaendert und liefert drei Dinge zusaetzlich, die der
   Generator zwingend braucht:
   - `carrier: 'turret'` an Items unter einem Turm-Traeger. Ohne dieses Feld laeuft im
     Generator jede Waffe in den Piloten-Topf und `turretWeapons` bleibt bei ALLEN
     Fahrzeugen leer.
   - `cargo`: die Summe der Innenmasse aller verbauten Frachtgitter je Schiff
     (SCU = x·y·z / 1,953125). Der Generator liest `LOAD.cargo?.[id]`; die hier liegende
     `src/data/ship-loadouts.json` hat diesen Schluessel ueberhaupt nicht, also waere
     `cargoSCU` bei allen 223 Fahrzeugen null statt bei 36 auf 0.
   - `ports`: was in welchen Port PASST (Mindest-/Hoechstgroesse und zulaessige Typen).
   Dazu eine kleine, aber folgenreiche Korrektur: `loc()` trimmt kuenftig den Wert aus der
   `global.ini`, weil einzelne Namen dort mit Rand-Leerzeichen stehen („MVSA Cannon ") und
   jeder Join per Namen daran unsichtbar scheitert.
   Diese Aenderung wird NICHT blind kopiert, sondern als Diff gelesen und auf die hier
   liegende Fassung uebertragen — die beiden Basisstaende koennen auseinanderlaufen. Danach
   `npm.cmd run datamine:loadouts` fahren und die Ausgabe pruefen: weiterhin 223 Schiffe,
   die Schluessel `ports` und `cargo` vorhanden, und KEIN Schiff mit weniger Ports als im
   Messlatte-Protokoll. Ein Schiff, das Ports verliert, ist ein Haltepunkt (die Lehre aus
   01.3-02, Deviation 3).

4. Drei npm-Eintraege in `package.json`, im Hausstil der Nachbarn:
   - `datamine:vehicles` -> `node scripts/datamine-vehicles.mjs`
   - `verify:vehicles`   -> `node scripts/verify-vehicles.mjs`
   - `verify:weapons`    -> `node scripts/verify-weapon-sizes.mjs`  (D-04; der Name ist
     durch die Projekt-Erinnerung `display-name-not-a-key` festgelegt und wird nicht
     abgewandelt)
   `datamine:vehicles` gehoert neben die uebrigen `datamine:*`-Eintraege, die beiden
   Pruefer neben `verify:mining`/`verify:items`.

5. `.gitignore`: `src/data/vehicles-gamefiles.json` eintragen (D-03), in denselben Block
   wie die uebrigen `*-gamefiles.json` und mit einer Kommentarzeile, warum: es ist die
   Zwischenstufe des Vergleichs, kein Liefergegenstand — der Liefergegenstand ist am Ende
   `src/data/vehicles.json` selbst. Nach dem Eintrag mit `git check-ignore` belegen, dass
   die Regel greift.

6. Den Lauf fahren: `npm.cmd run datamine:vehicles`. Erwartet werden 223 gebaute Fahrzeuge
   aus 227 Kandidaten; die 4 uebrigen meldet das Skript als „ohne DataCore-Record". Diese
   vier Kennungen woertlich protokollieren — sie sind der gemessene Nachweis fuer D-13 und
   die Aufgabe von Plan 02. Ausserdem die Statistikzeilen protokollieren
   (Implementierungs-XML, Huellen-HP, Flugwerte, Schild, Quantum, Fracht): sie sind die
   Vollstaendigkeitszahlen aus D-05 und muessen sich gegen den installierten Client
   bestaetigen, nicht gegen den Stand vom 02.08.2026.

7. Schrumpfprobe gegen die Vorarbeit (die Lehre aus 01.3): Der Katalog vom 02.08.2026 liegt
   im Geschwister-Arbeitsverzeichnis und wurde gegen eine AELTERE Extraktion erzeugt. Den
   frischen Katalog Feld fuer Feld dagegen halten: je Feld die Zahl der Fahrzeuge mit
   nicht-leerem Wert, vorher und nachher. Jeder Rueckgang ist ein Befund und wird
   namentlich protokolliert, nicht weggerundet. Erwartungswerte aus D-05 als Anhalt:
   223/223 fuer die Kernfelder, `descriptionEn`/`descriptionDe` 219, `shieldHp` 208,
   Flugwerte 183, Quantum 176, `fixedWeapons` 175, `missileRacks` 148, `turretWeapons` 83.

8. `npm.cmd run verify:vehicles` — die Vergleichstabelle. Ihre Zahlen werden vollstaendig
   ins Protokoll uebernommen; sie sind die Ausgangslage fuer die Plaene 02 und 03. Zum
   Abgleich die am 03.08.2026 gemessenen Werte (gegen den Stand vom 02.08.): name 11,
   manufacturer 20, makerCode 13, sizeDe 40, crew 63, cargoSCU 36, hullHp 32, shieldHp 32,
   insClaimMin 10, insExpediteMin 9, cmLaunchers 3. Weicht die frische Messung deutlich
   ab, ist das kein Fehler, sondern der Unterschied zwischen den Client-Staenden — aber er
   gehoert benannt.

9. `npm.cmd run verify:weapons` gegen den HEUTIGEN Bestand (D-04). Das Skript liest
   `src/data/vehicles.json` (noch die Wiki-Fassung) und `src/data/ship-loadouts.json` (frisch
   aus Schritt 3). Es muss jetzt gruen laufen. Laeuft es rot, ist das ein Befund VOR dem
   Tausch und wird gemeldet, nicht uebergangen — nur so ist spaeter unterscheidbar, ob eine
   rote Meldung dem Tausch anzulasten ist. Hinweis-Zeilen („steht nicht im Stock-Loadout")
   sind keine Fehler und beenden den Lauf nicht; ihre Zahl gehoert trotzdem ins Protokoll.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const bm=JSON.parse(fs.readFileSync(process.env.SC_BUILD||'F:/Games/Star Citizen/StarCitizen/LIVE/build_manifest.id','utf8')).Data;if(String(bm.RequestedP4ChangeNum)!=='12326004')throw new Error('Client steht auf '+bm.RequestedP4ChangeNum+', erwartet 12326004');console.log('Client:',bm.Branch,bm.RequestedP4ChangeNum,bm.BuildDateStamp)"</automated>
    <automated>node -e "const fs=require('fs'),cr=require('crypto');const src='.claude/worktrees/buccaneer-waffen-daten-be6ac7/';const f=['scripts/lib/cryxml.mjs','scripts/datamine-vehicles.mjs','scripts/verify-vehicles.mjs','scripts/verify-weapon-sizes.mjs'];const h=p=>cr.createHash('sha256').update(fs.readFileSync(p)).digest('hex');for(const x of f){if(!fs.existsSync(x))throw new Error('nicht uebernommen: '+x);const a=h(x),b=h(src+x);if(a!==b)throw new Error('Kopie weicht vom Original ab: '+x+'\n  hier '+a+'\n  dort '+b);console.log('identisch uebernommen:',x,a.slice(0,12))}"</automated>
    <automated>node -e "const p=require('./package.json').scripts;for(const[k,v] of [['datamine:vehicles','datamine-vehicles.mjs'],['verify:vehicles','verify-vehicles.mjs'],['verify:weapons','verify-weapon-sizes.mjs']]){if(!p[k])throw new Error('npm-Eintrag fehlt: '+k);if(!p[k].includes(v))throw new Error(k+' zeigt nicht auf '+v+', sondern auf '+p[k])}console.log('drei npm-Eintraege verdrahtet')"</automated>
    <automated>node -e "const{execSync}=require('child_process');const out=execSync('git check-ignore -v src/data/vehicles-gamefiles.json || echo NICHT_IGNORIERT',{encoding:'utf8'}).trim();if(/NICHT_IGNORIERT/.test(out))throw new Error('die Zwischenstufe ist nicht gitignored (D-03)');console.log('gitignore greift:',out)"</automated>
    <automated>npm.cmd run datamine:loadouts</automated>
    <automated>node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('src/data/ship-loadouts.json','utf8'));const n=Object.keys(j.ships||{}).length;if(n<223)throw new Error('ship-loadouts geschrumpft: '+n+' statt 223');if(!j.cargo)throw new Error('Schluessel cargo fehlt — der Generator liefert sonst cargoSCU=null fuer alle');if(!j.ports)throw new Error('Schluessel ports fehlt');const c=Object.keys(j.cargo).length;const mitTuermen=Object.values(j.ships).filter(s=>Object.values(s).flat().some(i=>i.carrier==='turret')).length;if(mitTuermen<50)throw new Error('carrier-Kennzeichnung greift nicht: nur '+mitTuermen+' Schiffe mit Turmwaffen');console.log('Loadouts:',n,'Schiffe |',c,'cargo-Eintraege |',mitTuermen,'Schiffe mit Turmwaffen')"</automated>
    <automated>npm.cmd run datamine:vehicles</automated>
    <automated>node -e "const fs=require('fs');const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8'));if(g.vehicles.length<223)throw new Error('Katalog kleiner als die Vorarbeit: '+g.vehicles.length+' statt 223');const hp=Object.keys(JSON.parse(fs.readFileSync('src/data/ship-hardpoints.json','utf8')).ships);const da=new Set(g.vehicles.map(v=>v.id));const fehlt=hp.filter(id=>!da.has(id));console.log('Katalog:',g.vehicles.length,'| erzeugt am',g.generatedAt);console.log('ohne DataCore-Record ('+fehlt.length+'):',fehlt.join(', '));if(fehlt.length!==4)throw new Error('erwartet waren genau 4 fehlende (die ATLS-Gruppe, D-13), gemessen: '+fehlt.length)"</automated>
    <automated>node -e "const fs=require('fs');const alt=JSON.parse(fs.readFileSync('.claude/worktrees/buccaneer-waffen-daten-be6ac7/src/data/vehicles-gamefiles.json','utf8')).vehicles;const neu=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles;const voll=l=>{const m={};for(const v of l)for(const[k,x] of Object.entries(v))if(x!=null&&!(Array.isArray(x)&&!x.length)&&!(typeof x==='object'&&!Array.isArray(x)&&!Object.keys(x).length))m[k]=(m[k]||0)+1;return m};const a=voll(alt),b=voll(neu);const weg=[];for(const k of Object.keys(a))if((b[k]||0)<a[k])weg.push(k+': '+a[k]+' -> '+(b[k]||0));console.log('Feldbelegung alt/neu:');for(const k of Object.keys(a).sort())console.log('  '+k.padEnd(20)+String(a[k]).padStart(4)+' -> '+String(b[k]||0).padStart(4));if(weg.length)throw new Error('Felder geschrumpft gegenueber der Vorarbeit:\n'+weg.join('\n'))"</automated>
    <automated>npm.cmd run verify:vehicles</automated>
    <automated>npm.cmd run verify:weapons</automated>
  </verify>

  <acceptance_criteria>
    - Die gelesene Changelist steht im SUMMARY und lautet `12326004`.
    - Die vier Skripte aus D-01 liegen unter `scripts/` und sind sha256-identisch mit dem Original im Geschwister-Arbeitsverzeichnis; die vier Hashes stehen im SUMMARY (VEH-01, Erfolgskriterium 1).
    - `scripts/datamine-ship-loadouts.mjs` liefert `carrier`, `cargo` und `ports`; die neu erzeugte `src/data/ship-loadouts.json` fuehrt weiterhin 223 Schiffe, hat einen `cargo`-Block und kein Schiff hat gegenueber der Messlatte Ports verloren. Die Zahl der Schiffe mit Turmwaffen steht im SUMMARY.
    - `package.json` traegt `datamine:vehicles`, `verify:vehicles` und `verify:weapons`; alle drei sind einmal ausgefuehrt worden (D-04).
    - `git check-ignore` bestaetigt, dass `src/data/vehicles-gamefiles.json` ignoriert wird (D-03).
    - `npm.cmd run datamine:vehicles` erzeugt mindestens 223 Fahrzeuge; genau 4 Kennungen fallen als „ohne DataCore-Record" heraus und sind namentlich protokolliert (Vorarbeit fuer VEH-07, D-13).
    - Feldbelegung vorher/nachher gegen den Katalog vom 02.08.2026: kein Feld ist geschrumpft; die vollstaendige Tabelle steht im SUMMARY.
    - Die Vergleichstabelle aus `verify:vehicles` steht vollstaendig im SUMMARY — alle 19 Zeilen mit identisch/abweichend/fehlend, nicht nur die Ueberschrift.
    - `npm.cmd run verify:weapons` laeuft gegen den heutigen Wiki-Katalog mit Exit 0; die Zahl der geprueften Schiffe, Waffeneintraege und Hinweiszeilen steht im SUMMARY (D-04).
    - `scratch/01.4-messlatte.txt` existiert und traegt die Kennzahlen aller fuenf Tore plus die Ausgangszaehler.
  </acceptance_criteria>

  <done>
    Die Extraktor-Vorarbeit liegt versioniert und committet in diesem Zweig, der Weg von der
    installierten Data.p4k bis zur Vergleichstabelle ist hier einmal ganz durchlaufen, und
    der Waechter gegen den Waffengroessen-Fehler ist scharf.
  </done>

  <reversibility rating="reversible">
    Es kommen Dateien und npm-Eintraege hinzu; `src/data/ship-loadouts.json` wird neu
    geschrieben, ist aber aus der Git-Historie und durch einen erneuten
    `datamine:loadouts`-Lauf wiederherstellbar. Der ausgelieferte Fahrzeug-Katalog bleibt
    unangetastet — die Seite aendert sich in diesem Plan nicht.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Die Feldmatrix — was die Seite aus dem Katalog liest und woher es nach dem Tausch kommt</name>

  <read_first>
    - src/content.config.ts (Zeile 122-230 — das Schema der `vehicles`-Collection ist die vollstaendige Feldliste; Zeile 96-120 die davon abgeleitete `ships`-Collection)
    - src/i18n/vehicleText.ts (vollstaendig, 166 Zeilen — `vType`/`vSize`/`vStatus`/`vFoci`/`vRole`/`vDesc` und die vier handgepflegten Uebersetzungstabellen)
    - src/pages/schiffe.astro (Zeile 10, 21-22, 34-37, 271-294, 309, 330-331, 360-370 — Filterleiste, Kartenattribute, Fussnote)
    - src/components/ShipDetail.astro (Zeile 15, 83, 92, 377, 986-988, 1226, 1249, 1509, 1587-1589 — Datenstand, Rolle/Groesse/Status, Patch-Rueckgrat, Bildquelle)
    - src/lib/weaponSizes.ts (Zeile 1-45 — warum das Modul existiert und was es zurueckrechnet)
    - src/lib/miningShips.ts (Zeile 1-30 — der einzige Leser von `oreSCU`)
    - discord/bot/src/data.mjs (Zeile 118-135 und 190-210 — welche Felder der Bot uebernimmt)
    - scripts/sync-vehicles.mjs (Zeile 28-47 — das Patch-Rueckgrat und die Varianten-Aliase; Zeile 144-160 die Bildaufbereitung; Zeile 170-215 die Feldabbildung)
    - src/data/vehicles-en.json (nur den Kopf — die `note`-Zeile ist der Beleg fuer D-07)
  </read_first>

  <files>scratch/01.4-feldmatrix.md, scripts/verify-vehicles.mjs</files>

  <action>
Der teuerste Fehler dieser Phase waere, `vehicles.json` zu tauschen und erst danach zu
merken, dass ein Feld verschwunden ist, das eine Seite liest. Genau das ist in Phase 01.3
beinahe passiert (`rsi-hermes` fiel bei `extract:hardpoints` still heraus und wurde nur
gefunden, weil vorher gezaehlt worden war). Diese Aufgabe zaehlt vorher.

1. Die Leser vollstaendig erheben, nicht schaetzen. Ueber `src`, `scripts`, `discord`,
   `tests` und `.github` suchen, welche Dateien `vehicles.json`, die `vehicles`-Collection
   oder die davon abgeleitete `ships.json` lesen. Erwartet werden rund 25 Fundstellen; die
   Liste kommt vollstaendig ins Protokoll, gruppiert nach `src/` (Anzeige), `scripts/`
   (Erzeugung/Pruefung) und `discord/` (Bot). Achtung auf die zweite Ebene:
   `scripts/sync-ships.mjs` erzeugt `src/data/ships.json` AUS `vehicles.json`, und daraus
   lesen wiederum `src/components/dossier/PatchDossier.astro` und der Bot ihre
   Besatzungsangabe — ein Feld kann also ueber zwei Ecken auf einer Seite landen.

2. Die Feldmatrix bauen und nach `scratch/01.4-feldmatrix.md` schreiben. Eine Zeile je
   Feld des heutigen Schemas aus `src/content.config.ts`, mit vier Spalten:
   Feld | wer liest es (Datei:Zeile) | Herkunft nach dem Tausch | Urteil.
   Zulaessige Werte in „Herkunft nach dem Tausch" sind genau vier:
   - `Spieldaten` — der Generator liefert es bereits (D-05)
   - `Fremddatei` — es zieht nach `src/data/vehicle-external.json` (D-06)
   - `abgeleitet` — es entsteht beim Bauen aus anderen Feldern (z. B. Rakentenzahl aus den
     Werfern) oder aus einer anderen Datendatei des Repos
   - `Verbraucher-Aenderung` — es faellt weg, und die lesende Stelle wird umgestellt
   Kein Feld darf ohne Urteil bleiben. Ein Urteil „faellt weg" OHNE benannte
   Verbraucher-Aenderung ist unzulaessig — das waere die stille Verkleinerung, die diese
   Phase ausschliesst.

3. Die Felder, die der Spieldaten-Katalog heute NICHT fuehrt, sind vorab bekannt und
   muessen einzeln beurteilt werden. Sie sind der eigentliche Gegenstand dieser Aufgabe:
   - `statusEn` / `statusDe` — Filter und Zaehler auf der Schiffsliste
     (`schiffe.astro` Zeile 21, 273, 289, 367) sowie `vStatus`. Zu pruefen: ob sich der
     Zustand aus den Spieldaten ableiten laesst (ein Fahrzeug mit Implementierung ist im
     Spiel; die Wiki fuehrt heute 226 flugbereit, 1 im Konzept) oder ob er in die
     Fremddatei gehoert.
   - `fociDe` — `vFoci`/`vRole` und die 65 Zeilen lange Uebersetzungstabelle in
     `vehicleText.ts`, plus `FOCUS_EN` im Bot. Der Spieldaten-Katalog liefert `roleEn`
     und `roleDe` aus CIGs eigener Localization; die Handtabelle wird damit ueberfluessig.
     Das ist ein Gewinn, aber es ist eine Verbraucher-Aenderung und wird als solche notiert.
   - `crewMin` / `crewMax` — Gegenstand von D-11 und Plan 03; hier nur die Leserliste
     festhalten (Schiffskarte, Datenblatt, Bot, `ships.json`).
   - `oreSCU` — 5 Fahrzeuge, einziger Leser `src/lib/miningShips.ts`.
   - `pilotDps`, `turretDps` und `fixedWeapons[].dps` — die Wiki liefert Schadenswerte, der
     Generator nicht. Zu pruefen: ob sie aus dem eigenen Item-Katalog
     (`assets/items-gamefiles.json`, seit Phase 01.3 mit Kennung `12326004`) je Waffe
     berechenbar sind, oder ob die Anzeige sie verliert.
   - `turrets[]` — heute ein strukturiertes Objekt (Bezeichnung, Stationen, Groessen,
     Waffen, Nutzlasttypen, DPS); der Generator liefert die flache Liste `turretWeapons`.
     Der Unterschied ist ein Strukturverlust und braucht eine benannte Entscheidung.
   - `missileCount`, `qtRangeM`, `qtFuel`, `h2Fuel` — je einzeln: ableitbar, aus dem Spiel
     holbar, oder Verbraucher-Aenderung.
   - `patches[]` — das Patch-Rueckgrat. Es ist KEINE Wiki-Angabe: `sync-vehicles.mjs`
     rechnet es aus `src/data/patches/*.json` gegen den normalisierten Schiffsnamen und
     pflegt dafuer eine Aliastabelle. Wenn dieses Skript faellt (D-16), faellt die Rechnung
     mit — die Seite verlaere ihre Archiv-Verknuepfung (`schiffe.astro` Zeile 22, 274,
     292-294; `ShipDetail.astro` Zeile 83). Diese Rechnung MUSS umziehen. Wohin, ist hier
     zu entscheiden und zu begruenden.
   - `image` — die Bild-URL aus der Wiki-Medienbibliothek; sie zieht laut D-06 in die
     Fremddatei. Zu pruefen ist der zweite Leser: `scripts/build-fav-thumbs.mjs`.
   - `fixedWeaponSizes` — heute aus `enrich-weapon-sizes.mjs`, und `src/lib/weaponSizes.ts`
     rechnet daraus muehsam die Zuordnung Waffenname -> Groesse zurueck. Der
     Spieldaten-Katalog traegt die Groesse direkt an jeder Waffe. Das Rueckrechnen wird
     damit ueberfluessig; das ist eine Verbraucher-Aenderung mit Gewinn.
   - `gameVersion` und der Kopf `fetchedAt` — die Seite zeigt beides sichtbar an
     (`schiffe.astro` Zeile 330-331, `ShipDetail.astro` Zeile 1249, 1587). Nach dem Tausch
     muss dort der Extraktionsstand stehen, nicht mehr der Wiki-Stand.
   Zu jedem dieser Punkte gehoert in die Matrix eine Zeile mit Urteil UND Begruendung.
   Wo die Antwort eine Untersuchung braucht (DPS, Status, Quantum-Reichweite), wird sie
   HIER gemacht und beantwortet — nicht auf den Tausch vertagt.

4. Die englischen Beschreibungen (D-07) ausdruecklich behandeln: `src/data/vehicles-en.json`
   traegt laut eigenem Kopfkommentar Texte, die aus dem DEUTSCHEN Wiki-Text
   rueckuebersetzt wurden, und `vDesc` liest sie fuer die englische Fassung. Der
   Spieldaten-Katalog liefert `descriptionEn` und `descriptionDe` als CIGs Originaltexte
   (219 von 223). In der Matrix festhalten: die Datei und die Sonderbehandlung in `vDesc`
   entfallen mit dem Tausch. Dazu eine Stichprobe von drei Fahrzeugen mit beiden Fassungen
   nebeneinander ins Protokoll, damit der Gewinn belegt ist und nicht behauptet.
   Ebenfalls festhalten: die Spieltexte tragen eine Kopfzeile („Manufacturer: … / Focus: …")
   und wortwoertliche `\n`-Folgen. Ob die beim Anzeigen abgeschnitten oder gerendert
   werden, ist eine Frage fuer Plan 04 — hier wird sie nur benannt, damit sie nicht
   untergeht.

5. Den Vollstaendigkeits-Waechter in `scripts/verify-vehicles.mjs` nachruesten. Bisher
   vergleicht das Skript nur Werte; kuenftig prueft es zusaetzlich die DECKUNG: fuer jedes
   Feld des Zielschemas die Zahl der Fahrzeuge mit nicht-leerem Wert, und einen Abbruch mit
   Exit 1, sobald ein Feld unter eine hinterlegte Untergrenze faellt. Die Untergrenzen
   werden aus der heutigen Messung abgeleitet (Schritt 7 von Task 1) und stehen als
   benannte Tabelle im Kopf des Skripts, mit Kommentar, woher die Zahlen stammen. Das ist
   der bleibende Riegel gegen die stille Verkleinerung — dieselbe Bauform wie der
   Schrumpf-Riegel in `build-universal-db.mjs` aus Phase 01.3.
   Gegenprobe, sonst beweist der Riegel nichts: einmal versuchsweise ein Feld im erzeugten
   Katalog leeren, das Skript laufen lassen, Exit-Code und Meldung protokollieren, danach
   den Katalog neu erzeugen.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('scratch/01.4-feldmatrix.md','utf8');const felder=['statusEn','statusDe','fociDe','crewMin','crewMax','oreSCU','pilotDps','turretDps','turrets','missileCount','qtRangeM','qtFuel','h2Fuel','patches','image','fixedWeaponSizes','gameVersion','descriptionDe'];const fehlt=felder.filter(f=>!new RegExp('\\\\b'+f+'\\\\b').test(m));if(fehlt.length)throw new Error('unbeurteilte Felder in der Matrix: '+fehlt.join(', '));const urteile=['Spieldaten','Fremddatei','abgeleitet','Verbraucher-Aenderung'];for(const u of urteile)if(!m.includes(u))throw new Error('Urteilskategorie kommt in der Matrix nicht vor: '+u);console.log('Feldmatrix deckt',felder.length,'kritische Felder ab')"</automated>
    <automated>node -e "const{execSync}=require('child_process');const out=execSync('git grep -l -e \"vehicles.json\" -e \"getCollection..vehicles\" -e \"ships.json\" -- src scripts discord tests || exit 0',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);const fs=require('fs');const m=fs.readFileSync('scratch/01.4-feldmatrix.md','utf8');const fehlt=out.filter(f=>!m.includes(f));if(fehlt.length)throw new Error(fehlt.length+' Leser fehlen in der Matrix:\n'+fehlt.join('\n'));console.log('alle',out.length,'Leser in der Matrix genannt')"</automated>
    <automated>npm.cmd run verify:vehicles</automated>
    <automated>node -e "const fs=require('fs'),cp=require('child_process');const p='src/data/vehicles-gamefiles.json';const sich=fs.readFileSync(p);const j=JSON.parse(sich);for(const v of j.vehicles)v.hullHp=null;fs.writeFileSync(p,JSON.stringify(j,null,2));const r=cp.spawnSync(process.execPath,['scripts/verify-vehicles.mjs'],{encoding:'utf8'});fs.writeFileSync(p,sich);if(r.status===0)throw new Error('Deckungs-Waechter greift nicht: geleertes hullHp lief mit Exit 0 durch');console.log('Gegenprobe ok — Exit',r.status,'| Meldung:',((r.stderr||'')+(r.stdout||'')).split('\n').filter(l=>/hullHp/i.test(l))[0])"</automated>
    <automated>npm.cmd run verify:vehicles</automated>
  </verify>

  <acceptance_criteria>
    - `scratch/01.4-feldmatrix.md` fuehrt JEDES Feld des heutigen `vehicles`-Schemas mit Leser, Herkunft nach dem Tausch und Urteil; kein Feld ohne Urteil.
    - Alle per Suche gefundenen Leser (erwartet ~25 Dateien in `src`, `scripts`, `discord`) sind in der Matrix namentlich genannt — auch die zweite Ebene ueber `src/data/ships.json`.
    - Fuer die 15 Felder, die der Spieldaten-Katalog heute nicht fuehrt, steht je ein Urteil MIT Begruendung; wo eine Untersuchung noetig war (Schadenswerte, Zustand, Quantum-Reichweite), steht ihr Ergebnis dabei, nicht ein Verweis auf spaeter.
    - Das Patch-Rueckgrat hat eine benannte neue Heimat; die Entscheidung steht mit Begruendung im SUMMARY (ohne sie verlaere die Seite die Verknuepfung von 20 Fahrzeugen zum Archiv).
    - D-07 ist mit drei Stichproben belegt: EN-Text der Rueckuebersetzung und EN-Text aus den Spieldateien nebeneinander im SUMMARY.
    - `scripts/verify-vehicles.mjs` bricht mit Exit 1 ab, wenn ein Feld unter seine hinterlegte Untergrenze faellt; die Untergrenzen stehen als kommentierte Tabelle im Skript.
    - Die Gegenprobe ist protokolliert: mit geleertem `hullHp` endet der Pruefer mit Exit 1 und nennt das Feld. Ohne diese Zahl gilt der Riegel als unbewiesen.
  </acceptance_criteria>

  <done>
    Fuer jedes Feld, das die Seite aus dem Fahrzeug-Katalog liest, steht schriftlich, woher
    es nach dem Tausch kommt — und ein Waechter im Pruefskript haelt kuenftig fest, dass
    keines still verschwindet.
  </done>

  <reversibility rating="reversible">
    Eine Messdatei unter `scratch/` (nicht committet) und eine zusaetzliche Pruefung in
    einem Pruefskript. Es aendert sich kein ausgeliefertes Byte.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unversioniertes Arbeitsverzeichnis (`.claude/`, gitignored) → versioniertes Repo | mehrere Tage Extraktionslogik existieren aktuell nur an einem Ort, der jederzeit weggeraeumt werden kann |
| Data.p4k + build_manifest.id (lokale Installation) → erzeugter Katalog | 158 GB Fremdformat; was hier hereinkommt, wird am Ende der Phase auf 227 Datenblaettern ausgeliefert |
| `src/data/ship-loadouts.json` → Fahrzeug-Katalog | der Generator liest Fracht, Turmzuordnung und Ports ausschliesslich aus dieser Datei; ein fehlender Schluessel dort wird zu einem leeren Feld dort |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-01 | Denial of Service | die vier (fuenf) unversionierten Skripte | critical | mitigate | Uebernahme ist Schritt eins der Phase, vor jeder inhaltlichen Arbeit; sha256-Vergleich gegen das Original belegt, dass die Kopie vollstaendig ist (D-02). |
| T-01.4-02 | Denial of Service | `src/data/ship-loadouts.json` (committet, 223 Schiffe) | high | mitigate | Vor dem Neulauf werden Schiffszahl und Ports je Schiff protokolliert; ein Rueckgang haelt den Lauf an. Zusaetzlich wird die Zahl der Schiffe mit Turmwaffen geprueft — sie ist der Nachweis, dass die `carrier`-Kennzeichnung wirklich greift. |
| T-01.4-03 | Tampering | Extraktion gegen eine fremde oder veraltete Data.p4k | medium | mitigate | Die Vorbedingung liest `build_manifest.id` und haelt an, wenn die Changelist nicht `12326004` lautet — derselbe Stand, den Phase 01.3 in allen uebrigen Ausgaben verankert hat. |
| T-01.4-04 | Repudiation | Katalog-Zwischenstufe im Liefergegenstand | low | mitigate | `src/data/vehicles-gamefiles.json` wird gitignored und der Eintrag per `git check-ignore` belegt; die Zwischenstufe kann nicht versehentlich als Datenstand ausgeliefert werden (D-03). |
| T-01.4-05 | Denial of Service | stiller Feldverlust beim spaeteren Tausch | high | mitigate | Die Feldmatrix beurteilt jedes Feld VOR dem Tausch, und der neue Deckungs-Waechter in `verify-vehicles.mjs` bricht ab, sobald ein Feld unter seine gemessene Untergrenze faellt. Beides ist am Bestand erprobt (Gegenprobe mit geleertem Feld). |
| T-01.4-SC | Tampering | npm/pip/cargo installs | low | accept | In diesem Plan wird kein Paket installiert; `npm.cmd ci` stellt ausschliesslich den committeten `package-lock.json` her. Keine neue Lieferkette, daher kein Legitimitaets-Gatter. |
</threat_model>

<verification>
1. Changelist `12326004` gelesen und protokolliert.
2. Vier Skripte sha256-identisch uebernommen; die Hashes im SUMMARY.
3. `datamine-ship-loadouts.mjs` erweitert; `ship-loadouts.json` mit `cargo` und `ports`, 223 Schiffe, keine Port-Verluste.
4. Drei npm-Eintraege verdrahtet und je einmal ausgefuehrt.
5. `vehicles-gamefiles.json` nachweislich gitignored.
6. Katalog erzeugt: >= 223 Fahrzeuge, genau 4 ohne DataCore-Record, namentlich protokolliert.
7. Feldbelegung gegen die Vorarbeit: kein Feld geschrumpft; vollstaendige Tabelle im SUMMARY.
8. Vergleichstabelle aus `verify:vehicles` vollstaendig im SUMMARY.
9. `verify:weapons` gruen gegen den heutigen Bestand.
10. Feldmatrix vollstaendig, alle Leser genannt, jedes Feld beurteilt.
11. Deckungs-Waechter greift; Gegenprobe mit Exit-Code protokolliert.
12. `git diff --stat` zeigt genau die in `files_modified` genannten Dateien und keine weitere.
</verification>

<success_criteria>
- Die vier Extraktor-Skripte liegen versioniert im Repo (Erfolgskriterium 1, VEH-01).
- Der Katalog-Generator laeuft in diesem Zweig gegen den installierten Client und liefert 223 Fahrzeuge; die fehlenden vier sind gemessen benannt.
- Der Waffengroessen-Waechter ist als `verify:weapons` verdrahtet und laeuft gruen (D-04).
- Fuer jedes Feld der Seite ist die Herkunft nach dem Tausch entschieden und begruendet — die Grundlage fuer D-14.
</success_criteria>

## Artifacts this plan produces

Neu geschaffene Bezeichner und Dateien — nachgelagerte Pruefungen duerfen sie NICHT als
Abweichung gegen den Altbestand werten:

| Art | Name | Ort |
|-----|------|-----|
| Skript | Katalog-Generator | scripts/datamine-vehicles.mjs |
| Skript | CryXmlB-Leser | scripts/lib/cryxml.mjs |
| Skript | Feld-fuer-Feld-Vergleich + Deckungs-Waechter | scripts/verify-vehicles.mjs |
| Skript | Waffengroessen-Waechter | scripts/verify-weapon-sizes.mjs |
| npm-Eintrag | `datamine:vehicles`, `verify:vehicles`, `verify:weapons` | package.json |
| Datenfelder | `carrier`, `cargo`, `ports` | src/data/ship-loadouts.json |
| gitignore-Eintrag | `src/data/vehicles-gamefiles.json` | .gitignore |
| Messdatei (nicht committet) | `scratch/01.4-messlatte.txt` | scratch/ |
| Messdatei (nicht committet) | `scratch/01.4-feldmatrix.md` | scratch/ |

<output>
Create `.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-01-SUMMARY.md` when done.
Woertlich protokollieren, nicht „bestanden": die gelesene Changelist, die vier sha256-Hashes,
die Statistikzeilen des Generatorlaufs, die vier Kennungen ohne DataCore-Record, die
vollstaendige Feldbelegungstabelle vorher/nachher, die vollstaendige Vergleichstabelle aus
`verify:vehicles`, das Ergebnis von `verify:weapons` mit Zahlen, die Entscheidung zum
Patch-Rueckgrat mit Begruendung, die drei Beschreibungs-Stichproben und den Exit-Code der
Waechter-Gegenprobe. Die Zahlen sind der Nachweis.
</output>
