---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
plan: 03
type: execute
wave: 3
depends_on: ["01.4-02"]
files_modified:
  - scripts/build-vehicle-external.mjs
  - src/data/vehicle-external.json
  - scripts/datamine-vehicles.mjs
  - package.json
autonomous: true
requirements: [VEH-02, VEH-03, VEH-07]
user_setup: []

must_haves:
  truths:
    - "`src/data/vehicle-external.json` traegt genau die Felder, deren Quellenlosigkeit BELEGT ist — die fuenf aus D-06 plus die Bild-URL, plus `crewMax` (D-17), plus den in Plan 02 belegten Rest. Kein Feld wird eingefroren, das der Suchdurchgang aus dem Spiel geholt hat (D-18, VEH-02, Erfolgskriterium 2)."
    - "Das Datenblatt behaelt die Besatzungs-Spanne: `crewMin` kommt als `crewSize` aus dem Spiel, `crewMax` ist aus dem heutigen Wiki-Stand eingefroren (D-17)."
    - "Der Spieldaten-Katalog fuehrt 227 Fahrzeuge; die vier ATLS-Varianten sind nachweislich drin und verschwinden nicht von der Seite (D-13, VEH-07, Erfolgskriterium 5)."
    - "`npm.cmd run verify:vehicles` meldet fuer `name`, `manufacturer` und `makerCode` 0 Abweichungen (VEH-03, Erfolgskriterium 3)."
    - "Jede der 13 `makerCode`-Abweichungen und der 40 `sizeDe`-Abweichungen hat ein begruendetes Urteil — keine wurde per Sammelregel weggebuegelt (D-09, D-14)."
    - "Der Generator laesst erkennen, welche Eintraege aus den Spieldateien stammen und welche uebernommen sind; ein uebernommener Eintrag ist im Katalog als solcher lesbar."
  artifacts:
    - scripts/build-vehicle-external.mjs (Erzeuger der Fremddatei aus dem heutigen Katalog)
    - src/data/vehicle-external.json (committet — die einzige Fremddatenquelle des Katalogs)
    - scratch/01.4-urteile-kosmetik.md (Urteilstabelle makerCode und sizeDe, je Fahrzeug)
  key_links:
    - "`scripts/datamine-vehicles.mjs` liest `src/data/vehicle-external.json` bereits (Zeile 365-372) und mischt `msrpUSD`, `pledgeUrl` und die drei Abmessungen ein. Die Bild-URL fehlt in dieser Schleife, ebenso `crewMax` und alles, was Plan 02 als einzufrieren ausgewiesen hat — die Schleife wird auf die vollstaendige Feldliste erweitert, sonst steht die Angabe in der Datei und kommt trotzdem nicht im Katalog an."
    - "⚠ ZEITKRITISCH (D-17): das Einfrieren zieht seine Werte aus `src/data/vehicles.json`, solange das noch der Wiki-Schnappschuss vom 18.07.2026 ist. Nach dem Tausch in Plan 05 ist die Quelle unwiederbringlich weg — dann waeren `crewMax` und die Bild-URLs nur noch aus der Git-Historie zu holen. Dieser Plan MUSS vor Plan 05 laufen; die Vorbedingung prueft es."
    - "Welche Felder eingefroren werden, steht NICHT in diesem Plan, sondern im Ergebnis von Plan 02 (`scratch/01.4-feldurteile.md`, Abschnitt „noch einzufrieren"). Dieser Plan setzt jene Liste um. Ein Feld einzufrieren, das Plan 02 aus dem Spiel geholt hat, ist ein Rueckschritt und ein Abnahmefehler (D-18)."
    - "Der Generator iteriert ueber die Schluessel von `src/data/ship-hardpoints.json` (227) und ueberspringt still, was keinen DataCore-Record hat (`stats.noRec`). Genau dort muessen die vier ATLS-Eintraege einhaken — ein `continue` ohne Ersatz ist der Grund, warum der Katalog heute 223 statt 227 fuehrt."
    - "`makerCode` ist NICHT durchweg kosmetisch, anders als D-08 vermutet (jetzt als D-22 festgehalten): gemessen am 03.08.2026 sind alle 13 Abweichungen Marken-Zuordnungen (Blade/Glaive/Stinger/Vanduul Scythe: Spiel `VNCL` vs. Wiki `ESPR`; Fury/Razor je 3 Varianten: Spiel `MISC` vs. Wiki `MRAI`; Khartu-al/Nox/Nox Kue: Spiel `XIAN` vs. Wiki `XNAA`). Eine Praefix-Regel allein entscheidet diese Faelle nicht — sie brauchen ein Urteil."
    - "Auch `manufacturer` ist nicht nur `&` gegen `and` (D-22): von 20 Abweichungen sind 18 die MISC-Schreibweise, aber Defender ist `Banu` gegen `Banu Souli` und Blade ist `Vanduul` gegen `Esperia` — dieselbe Marken-Frage wie bei `makerCode`, und sie muss dieselbe Antwort bekommen."
---

<objective>
Die Fremddaten einfrieren, die vier fehlenden Fahrzeuge zurueckholen und die Namen in
Ordnung bringen — bis die Vergleichstabelle bei Name, Hersteller und Herstellercode auf
null steht.

Ziel der Phase (woertlich aus ROADMAP.md): „Extern bleiben nur die fuenf Felder, die in
keiner Spieldatei stehen: Pledge-Preis, RSI-Link, die drei Abmessungen und die Bild-URL."

Purpose: Nach diesem Plan ist der Spieldaten-Katalog erstmals vollzaehlig (227) und in
seiner Identitaetsschicht deckungsgleich mit dem, was heute auf der Seite steht. Damit
faellt die groesste Klasse von Abweichungen weg, und die Plaene 04 und 05 koennen sich auf
die drei echten Datenfragen und den Tausch konzentrieren. Die Reihenfolge ist zwingend:
die Fremddatei muss existieren, BEVOR der Katalog zum letzten Mal erzeugt wird, denn der
Generator mischt sie ein — und die ATLS-Gruppe muss drin sein, bevor irgendjemand die
Zahl 227 als erreicht meldet.

Der Satz aus der ROADMAP stimmt seit D-18 nicht mehr ganz: es sind nicht fuenf Felder,
sondern fuenf plus die Bild-URL plus `crewMax` (D-17) plus der Rest, den der Suchdurchgang
aus Plan 02 nicht aus dem Spiel holen konnte. Wie viele das sind, weiss dieser Plan nicht
im Voraus — er setzt die Liste um, die Plan 02 hinterlaesst. Genau so ist es gewollt: die
Fremddatei ist so klein wie belegbar, nicht so klein wie versprochen.

Output: Ein Erzeugerskript und die committete Fremddatei mit den belegt quellenlosen
Angaben; vier ATLS-Varianten wieder im Katalog; und eine Vergleichstabelle, die bei den
drei Identitaetsfeldern keine Abweichung mehr zeigt — mit einer Urteilstabelle daneben,
die jede getroffene Entscheidung begruendet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-CONTEXT.md
@.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-01-SUMMARY.md
@.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-02-SUMMARY.md
</context>

<tasks>

<!-- Diese Bezeichner MUESSEN woertlich vorkommen — es sind Datei-, Feld- und
     Fahrzeugkennungen. Alle Negativ-Suchen der Abnahme sind auf `src/`, `scripts/` oder
     EINE benannte Datei begrenzt; `.planning/` ist ausgenommen. -->
<!-- planner-discipline-allow: vehicle-external -->
<!-- planner-discipline-allow: vehicles-gamefiles -->
<!-- planner-discipline-allow: sync-vehicles -->

<task type="auto">
  <name>Task 1: Die Fremddaten einfrieren und die vier fehlenden Fahrzeuge zurueckholen</name>

  <read_first>
    - scripts/datamine-vehicles.mjs (Zeile 21-24 Kopfkommentar zur Fremddatei; Zeile 39-40 die beiden Pfade; Zeile 100-107 `ourIds`/`byId`; Zeile 180-183 der `noRec`-Abbruch; Zeile 365-373 die Lauf-Schleife mit dem Einmischen)
    - src/data/vehicles.json (nur den Kopf und EINEN vollstaendigen Datensatz — nicht die 227 durchlesen; die Felder `msrpUSD`, `pledgeUrl`, `lengthM`, `widthM`, `heightM`, `image`, `crewMax`)
    - scratch/01.4-feldurteile.md (aus Plan 02 — der Abschnitt „noch einzufrieren" IST die Feldliste dieser Aufgabe; ohne ihn ist unklar, was hineingehoert)
    - scripts/sync-vehicles.mjs (Zeile 144-160 — wie die Bild-URL heute aufbereitet wird: MediaWiki-Thumbs, Breitenstufen)
    - scripts/build-fav-thumbs.mjs (der Leser der Bild-URL — er muss die Datei nach dem Umzug weiterhin finden)
    - scratch/01.4-feldmatrix.md (aus Plan 01 — die Leserliste je Feld)
    - .planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-01-SUMMARY.md (die vier Kennungen ohne DataCore-Record)
  </read_first>

  <precondition>
    Aus Plan 01 liegen vor: die fuenf Skripte versioniert, `src/data/ship-loadouts.json`
    mit `cargo`/`ports`/`carrier`, und ein frisch erzeugtes
    `src/data/vehicles-gamefiles.json` mit 223 Fahrzeugen gegen Changelist `12326004`.
    Fehlt eines davon, hier anhalten. Ausserdem muss `src/data/vehicles.json` noch der
    unveraenderte Wiki-Katalog vom 18.07.2026 sein (`fetchedAt` pruefen) — er ist die
    Quelle, aus der die Fremddaten eingefroren werden. Ist er bereits getauscht, ist die
    Quelle weg und der Plan laeuft ins Leere.
  </precondition>

  <files>scripts/build-vehicle-external.mjs, src/data/vehicle-external.json, scripts/datamine-vehicles.mjs, package.json</files>

  <action>
Zwei Dinge, die zusammengehoeren: was aus dem Spiel NICHT zu holen ist, wird einmalig
eingefroren — und was das Spiel gar nicht als Fahrzeug kennt, wird mitsamt seinen Daten
uebernommen. Beides landet in EINER Datei, damit der Rest an Fremddaten an einer Stelle
sichtbar ist und schrumpfen kann.

1. Erzeugerskript `scripts/build-vehicle-external.mjs` (Claude's Discretion aus CONTEXT.md:
   Skript statt Handarbeit, weil nachvollziehbar). Es liest `src/data/vehicles.json` und
   schreibt `src/data/vehicle-external.json` mit dem Kopfmuster des Hauses (`source`,
   `generatedAt`, `count`) plus einer Zeile, die sagt, WARUM es diese Datei gibt: hier steht
   nur, was belegt in keiner Spieldatei zu finden ist, und die Datei soll schrumpfen, nicht
   wachsen.
   Die Feldliste steht NICHT in diesem Plan. Sie kommt aus `scratch/01.4-feldurteile.md`,
   Abschnitt „noch einzufrieren" (Plan 02) — das ist der Sinn von D-18: der Suchdurchgang
   entscheidet, nicht die Planung. Gesetzt sind nur die Faelle, deren Quellenlosigkeit schon
   vorher belegt war:
   - `msrpUSD`, `pledgeUrl`, `lengthM`, `widthM`, `heightM` (D-06)
   - `image` — fotografische Aufnahmen, kein Spielmaterial (Urteil aus Plan 02)
   - `crewMax` — beide Extraktionswege geprueft und verworfen (D-11, D-17)
   Alles Weitere kommt aus jener Liste hinzu; ist sie leer, ist das ein sehr gutes Ergebnis
   und die Datei bleibt bei diesen sieben Feldern. Ein Feld einzufrieren, das Plan 02 aus
   dem Spiel geholt hat, ist ein Rueckschritt — die Abnahme prueft das gegen die
   Urteilstabelle.
   Fahrzeuge, fuer die alle Werte leer sind, kommen gar nicht erst hinein — eine Zeile aus
   lauter `null` ist kein Datum. Die Zahl der ausgelassenen Fahrzeuge nennt das Skript beim
   Lauf.

1b. `crewMax` ausdruecklich (D-17). Der Betreiber hat entschieden: das Datenblatt behaelt
   die Spanne. `crewMin` kommt kuenftig als `crewSize` aus dem Spiel — belegt durch die
   Messung, dass beide auf 223 von 223 Fahrzeugen uebereinstimmen — und `crewMax` wird hier
   einmalig eingefroren. Beim Einfrieren faellt auf, dass die Wiki mindestens einen kaputten
   Wert fuehrt: 600i Touring mit `crewMax` 3 bei `crewMin` 5. Solche Faelle werden NICHT
   stillschweigend uebernommen: wo `crewMax` kleiner ist als das spielseitige `crewSize`,
   wird der Wert ausgelassen und das Fahrzeug zeigt nur die eine Zahl. Die betroffenen
   Fahrzeuge werden gezaehlt und benannt — eine Spanne, die rueckwaerts laeuft, waere auf
   dem Datenblatt sichtbarer Unsinn.
   Das Skript laeuft genau einmal sinnvoll und ist danach ein Beleg, keine Pipeline: es
   ueberschreibt eine bestehende Datei NICHT stillschweigend, sondern verlangt
   `--force`. Grund im Kommentar festhalten: nach dem Tausch (Plan 05) ist
   `src/data/vehicles.json` selbst der Spieldaten-Katalog und traegt diese Felder nur noch
   aus eben dieser Fremddatei — ein zweiter Lauf ohne Riegel wuerde die Datei aus sich
   selbst neu erzeugen und dabei jede spaetere Handpflege verlieren.

2. Die vier ATLS-Varianten (D-13, VEH-07). Zuerst NACHSEHEN, dann uebernehmen — die
   Behauptung „im Spiel keine Fahrzeug-Entitaet" ist zu pruefen, nicht zu glauben:
   `argo-atls`, `argo-atls-geo`, `argo-atls-geo-collector-grad01`,
   `argo-atls-geo-collector-grad03` stehen in `src/data/ship-hardpoints.json` (sie haben
   also ein Modell und ein Hologramm), fallen aber im DataCore durch den Filter auf
   `/spaceships/` und `/groundvehicles/`. Im DataCore nach Records suchen, deren Kennung
   auf den Lader passt, und protokollieren, unter welchem Pfad sie liegen. Findet sich ein
   verwertbarer Record, wird der Filter erweitert und die vier kommen aus dem Spiel — das
   waere der bessere Ausgang und muss zuerst versucht werden.
   Erst wenn das belegbar scheitert: die vier Datensaetze aus dem heutigen
   `src/data/vehicles.json` uebernehmen. Sie gehen in `vehicle-external.json` unter einen
   eigenen, klar benannten zweiten Block — getrennt von den sechs Feldern, damit auf einen
   Blick sichtbar bleibt, dass hier ganze Datensaetze uebernommen sind und nicht nur
   Randangaben. Jeder uebernommene Datensatz traegt ein Feld, das seine Herkunft und das
   Datum der Uebernahme nennt.
   Der Generator baut sie am Ende seiner Schleife mit ein — an derselben Stelle, an der er
   heute `stats.noRec` fuellt. Ein uebernommener Eintrag ist im erzeugten Katalog als
   solcher erkennbar; das ist der Kernwert „keine erfundenen Werte" angewandt auf
   Herkunftsangaben.

3. Den Generator auf die vollstaendige Fremdfeld-Liste erweitern. Er mischt heute fuenf
   Felder ein (Zeile 372); Bild-URL, `crewMax` und alles Weitere aus der Liste fehlen.
   Ohne diese Ergaenzung verlieren 216 Fahrzeuge ihr Bild und 63 ihre Besatzungs-Spanne,
   obwohl die Angaben in der Datei stehen — und das Datenblatt faellt still auf das
   Hersteller-Logo und eine einzelne Zahl zurueck. Die Einmisch-Schleife wird deshalb NICHT
   auf eine feste Feldliste geschrieben, sondern uebernimmt alles, was die Fremddatei je
   Fahrzeug fuehrt; so bleibt sie richtig, wenn die Liste schrumpft. Der Kopfkommentar des
   Generators (Zeile 21-24) nennt die Fremdfelder namentlich und wird mitgezogen, damit er
   nicht luegt.

4. Neuer npm-Eintrag `build:vehicle-external` -> `node scripts/build-vehicle-external.mjs`,
   neben den uebrigen `build:*`-Eintraegen.

5. Lauf und Nachweis: `npm.cmd run build:vehicle-external`, dann
   `npm.cmd run datamine:vehicles`. Der Katalog muss jetzt 227 Fahrzeuge fuehren. Belegen,
   nicht behaupten: die vier ATLS-Kennungen einzeln aus dem erzeugten Katalog ziehen und
   mit Name, Hersteller und Herkunftsangabe protokollieren. Dazu die Deckungspruefung aus
   Plan 01 erneut fahren — die vier Neuzugaenge duerfen keine Untergrenze reissen, aber
   ihre leeren Spieldatenfelder duerfen auch nicht als „alle 227 vollstaendig" durchgehen.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('src/data/vehicles.json','utf8'));if(j.fetchedAt!=='2026-07-18')throw new Error('vehicles.json ist nicht mehr der Wiki-Stand vom 18.07. — Quelle fuer das Einfrieren fehlt. fetchedAt='+j.fetchedAt);console.log('Quelle ok: Wiki-Katalog',j.fetchedAt,'mit',j.vehicles.length,'Fahrzeugen')"</automated>
    <automated>npm.cmd run build:vehicle-external</automated>
    <automated>node -e "const fs=require('fs');const e=JSON.parse(fs.readFileSync('src/data/vehicle-external.json','utf8'));const gesetzt=['msrpUSD','pledgeUrl','lengthM','widthM','heightM','image','crewMax'];const urteile=fs.readFileSync('scratch/01.4-feldurteile.md','utf8');const v=e.vehicles||{};const ids=Object.keys(v);if(!ids.length)throw new Error('Fremddatei ist leer');const benutzt=new Set();for(const id of ids)for(const k of Object.keys(v[id]))benutzt.add(k);const extra=[...benutzt].filter(k=>!gesetzt.includes(k));const unbelegt=extra.filter(k=>!new RegExp('\\\\b'+k+'\\\\b[^\\n]*eingefroren','i').test(urteile)&&!new RegExp('eingefroren[^\\n]*\\\\b'+k+'\\\\b','i').test(urteile));if(unbelegt.length)throw new Error('Felder eingefroren, die Plan 02 NICHT als einzufrieren ausgewiesen hat (D-18): '+unbelegt.join(', '));const mit=k=>ids.filter(i=>v[i][k]!=null).length;console.log('Fremddatei:',ids.length,'Fahrzeuge |',benutzt.size,'Felder:',[...benutzt].join(', '));for(const k of benutzt)console.log('  '+k.padEnd(12),mit(k))"</automated>
    <automated>node -e "const fs=require('fs');const urteile=fs.readFileSync('scratch/01.4-feldurteile.md','utf8');const e=JSON.parse(fs.readFileSync('src/data/vehicle-external.json','utf8')).vehicles;const benutzt=new Set();for(const id of Object.keys(e))for(const k of Object.keys(e[id]))benutzt.add(k);const ausSpiel=[...benutzt].filter(k=>new RegExp('\\\\b'+k+'\\\\b[^\\n]*aus dem Spiel','i').test(urteile));if(ausSpiel.length)throw new Error('RUECKSCHRITT: diese Felder hat Plan 02 aus dem Spiel geholt, werden hier aber trotzdem eingefroren: '+ausSpiel.join(', '));console.log('kein Feld eingefroren, das aus dem Spiel kommt')"</automated>
    <automated>node -e "const fs=require('fs');const alt=JSON.parse(fs.readFileSync('src/data/vehicles.json','utf8')).vehicles;const e=JSON.parse(fs.readFileSync('src/data/vehicle-external.json','utf8')).vehicles;let weg=[];for(const a of alt)for(const k of ['msrpUSD','pledgeUrl','lengthM','widthM','heightM'])if(a[k]!=null&&(!e[a.id]||e[a.id][k]==null))weg.push(a.id+'.'+k);const bild=alt.filter(a=>a.image&&!(e[a.id]&&e[a.id].image)).map(a=>a.id);if(weg.length)throw new Error(weg.length+' Fremdwerte beim Einfrieren verloren: '+weg.slice(0,15).join(', '));if(bild.length)throw new Error(bild.length+' Bild-Angaben verloren: '+bild.slice(0,10).join(', '));console.log('Einfrieren verlustfrei: alle Fremdwerte und',alt.filter(a=>a.image).length,'Bilder uebernommen')"</automated>
    <automated>node -e "const fs=require('fs');const alt=new Map(JSON.parse(fs.readFileSync('src/data/vehicles.json','utf8')).vehicles.map(v=>[v.id,v]));const g=new Map(JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles.map(v=>[v.id,v]));const e=JSON.parse(fs.readFileSync('src/data/vehicle-external.json','utf8')).vehicles;let ok=0,ausgelassen=[];for(const[id,a] of alt){if(a.crewMax==null)continue;const spiel=g.get(id);const min=spiel?spiel.crew:null;if(min!=null&&a.crewMax<min){if(e[id]&&e[id].crewMax!=null)throw new Error(id+': ruecklaeufige Spanne eingefroren (crewMax '+a.crewMax+' < crewSize '+min+') — muss ausgelassen werden');ausgelassen.push(id+' (crewMax '+a.crewMax+' < crewSize '+min+')');continue}if(e[id]&&e[id].crewMax===a.crewMax)ok++}console.log('crewMax eingefroren:',ok,'| als ruecklaeufig ausgelassen:',ausgelassen.length,ausgelassen.length?'-> '+ausgelassen.join(', '):'');if(ok<150)throw new Error('zu wenige crewMax-Werte eingefroren: '+ok+' — die Spanne aus D-17 waere auf den meisten Datenblaettern weg')"</automated>
    <automated>node -e "const{execSync}=require('child_process');const out=execSync('git check-ignore src/data/vehicle-external.json || echo COMMITTIERBAR',{encoding:'utf8'}).trim();if(!/COMMITTIERBAR/.test(out))throw new Error('die Fremddatei ist gitignored — sie ist ein Liefergegenstand und MUSS committet werden');console.log('Fremddatei ist committierbar')"</automated>
    <automated>npm.cmd run datamine:vehicles</automated>
    <automated>node -e "const fs=require('fs');const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8'));if(g.vehicles.length!==227)throw new Error('Katalog fuehrt '+g.vehicles.length+' Fahrzeuge, gefordert sind 227 (VEH-07)');const atls=['argo-atls','argo-atls-geo','argo-atls-geo-collector-grad01','argo-atls-geo-collector-grad03'];const da=new Map(g.vehicles.map(v=>[v.id,v]));const fehlt=atls.filter(id=>!da.has(id));if(fehlt.length)throw new Error('ATLS-Varianten fehlen: '+fehlt.join(', '));for(const id of atls){const v=da.get(id);console.log(id,'|',v.name,'|',v.manufacturer,'| Herkunft:',v.source||v.herkunft||'(keine Herkunftsangabe!)')}const ohne=atls.filter(id=>{const v=da.get(id);return !(v.source||v.herkunft)});if(ohne.length)throw new Error('uebernommene Eintraege ohne Herkunftsangabe: '+ohne.join(', '));console.log('Katalog:',g.vehicles.length,'Fahrzeuge, alle vier ATLS-Varianten drin und als uebernommen gekennzeichnet')"</automated>
    <automated>node -e "const fs=require('fs');const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles;const mit=k=>g.filter(v=>v[k]!=null&&!(Array.isArray(v[k])&&!v[k].length)).length;for(const k of ['msrpUSD','pledgeUrl','lengthM','widthM','heightM','image'])console.log('  '+k.padEnd(12),mit(k)+'/227');if(mit('image')<216)throw new Error('Bild-URL kommt nicht im Katalog an: nur '+mit('image')+' von erwarteten 216 — die Einmisch-Schleife wurde nicht erweitert');if(mit('msrpUSD')<218)throw new Error('Pledge-Preis kommt nicht an: '+mit('msrpUSD'))"</automated>
    <automated>npm.cmd run verify:vehicles</automated>
  </verify>

  <acceptance_criteria>
    - `src/data/vehicle-external.json` existiert, ist committierbar (nicht gitignored) und traegt AUSSCHLIESSLICH `msrpUSD`, `pledgeUrl`, `lengthM`, `widthM`, `heightM` und die Bild-Angabe (VEH-02, Erfolgskriterium 2).
    - Kein Fremdwert ist beim Einfrieren verloren gegangen: jeder nicht-leere Wert aus dem heutigen `src/data/vehicles.json` steht in der Fremddatei. Die Zahlen je Feld stehen im SUMMARY.
    - `scripts/build-vehicle-external.mjs` verweigert das Ueberschreiben einer bestehenden Datei ohne `--force`; der Grund steht als Kommentar im Skript.
    - Die Suche nach ATLS-Records im DataCore ist protokolliert — mit Ergebnis, nicht mit Vermutung. Wurde uebernommen statt extrahiert, steht die Begruendung im SUMMARY.
    - `npm.cmd run datamine:vehicles` liefert 227 Fahrzeuge; die vier ATLS-Kennungen sind einzeln mit Name, Hersteller und Herkunftsangabe protokolliert (VEH-07, Erfolgskriterium 5).
    - Jeder uebernommene Datensatz traegt im Katalog eine maschinenlesbare Herkunftsangabe; ein uebernommener Eintrag ist nicht von einem extrahierten ununterscheidbar.
    - Die Bild-URL kommt im erzeugten Katalog bei mindestens 216 Fahrzeugen an, der Pledge-Preis bei mindestens 218 — die Einmisch-Schleife wurde also wirklich erweitert.
    - `npm.cmd run verify:vehicles` laeuft mit Exit 0 durch (der Deckungs-Waechter aus Plan 01 haelt).
  </acceptance_criteria>

  <done>
    Die Fremddaten sind an einer Stelle eingefroren, der Spieldaten-Katalog ist mit 227
    Fahrzeugen vollzaehlig, und die vier ATLS-Varianten sind mit sichtbarer Herkunftsangabe
    zurueck.
  </done>

  <reversibility rating="reversible">
    Eine neue committete Datendatei, ein neues Skript, eine erweiterte Einmisch-Schleife.
    Der ausgelieferte Katalog bleibt unangetastet; die Seite aendert sich in dieser Aufgabe
    nicht. Zurueckdrehbar, solange `src/data/vehicles.json` noch die Wiki-Fassung ist —
    genau deshalb steht diese Aufgabe vor dem Tausch.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Namen, Hersteller, Herstellercode — bis die Vergleichstabelle auf null steht</name>

  <read_first>
    - scripts/datamine-vehicles.mjs (Zeile 191-211 — `fullName`, `stripMfr`, `clsPrefix`, `makerCode`; Zeile 149-162 `manufacturer()`; Zeile 166-173 die `SIZE_LABEL`-Tabelle)
    - scripts/verify-vehicles.mjs (Zeile 23-33 — die SCALARS-Tabelle und die `near()`-Toleranz)
    - .planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-01-SUMMARY.md (die vollstaendige Vergleichstabelle aus dem frischen Lauf)
    - src/pages/schiffe.astro (Zeile 271 `data-maker` und Zeile 366 der Filtervergleich — der Herstellername ist ein Filterschluessel, keine reine Anzeige)
    - src/i18n/vehicleText.ts (Zeile 14-22 `SIZE_EN` — die Groessenlabels haben eine feste EN-Uebersetzung; ein neues Label ohne Eintrag faellt in der EN-Fassung auf den deutschen Text zurueck)
  </read_first>

  <files>scripts/datamine-vehicles.mjs, scratch/01.4-urteile-kosmetik.md</files>

  <action>
Erfolgskriterium 3 verlangt null Abweichungen bei Name, Hersteller und Herstellercode.
Das ist erreichbar — aber nur ein Teil der Abweichungen ist wirklich Kosmetik. Wer alle
mit einer Regel erschlaegt, faelscht die Haelfte. Deshalb: erst sortieren, dann regeln,
und jede Entscheidung einzeln aufschreiben.

1. Die drei Felder einzeln aus der frischen Vergleichstabelle ziehen
   (`node scripts/verify-vehicles.mjs --field name --all`, ebenso fuer `manufacturer` und
   `makerCode`) und die Abweichungen in drei
   Klassen sortieren. Die Messung vom 03.08.2026 (gegen den Stand vom 02.08.) als Anhalt:

   `name` — 11 Abweichungen, davon:
   - 1× ein abschliessender Zeilenumbruch im Spieltext (`CSV-SM`)
   - 10× ein vorangestelltes Herstellerkuerzel mit Punkten (`C.O. HoverQuad`,
     `C.O. Mustang Alpha` und acht weitere Mustang-Varianten)
   Beides ist echte Kosmetik. Der Generator schneidet in `stripMfr` heute nur das ERSTE
   Wort des Herstellernamens oder sein Kuerzel ab; bei „Consolidated Outland" ist das
   gebraeuchliche Kuerzel aber `C.O.` mit Punkten und trifft keine der beiden Regeln. Die
   Regel wird erweitert, nicht durch eine Namensliste ersetzt: aus den Anfangsbuchstaben
   der Herstellerwoerter ein punktiertes Kuerzel bilden und ebenfalls als Praefix
   zulassen. Zusaetzlich jeden aus der Localization gelesenen Namen trimmen — der
   Zeilenumbruch bei `CSV-SM` ist derselbe Fehlertyp wie die Rand-Leerzeichen, die in
   Plan 01 schon `datamine-ship-loadouts.mjs` betrafen.

   `manufacturer` — 20 Abweichungen, aber NICHT 20-mal dasselbe:
   - 18× die MISC-Schreibweise (Spiel `Musashi Industrial & Starflight Concern`, Wiki
     `… and …`). Kosmetik.
   - 1× `Defender`: Spiel `Banu`, Wiki `Banu Souli`.
   - 1× `Blade`: Spiel `Vanduul`, Wiki `Esperia`.
   Die letzten beiden sind keine Schreibweisen, sondern Marken-Zuordnungen — dieselbe
   Frage wie bei `makerCode` (Schritt 3), und sie bekommen dieselbe Antwort.

   `makerCode` — 13 Abweichungen, KEINE davon kosmetisch:
   - 4× `VNCL` (Spiel) gegen `ESPR` (Wiki): Blade, Glaive, Stinger, Vanduul Scythe
   - 6× `MISC` gegen `MRAI`: Fury, Fury LX, Fury MX, Razor, Razor EX, Razor LX
   - 3× `XIAN` gegen `XNAA`: Khartu-al, Nox, Nox Kue
   D-08 vermutet hier ein gekapptes `Manufacturer.Code` und eine Praefix-Regel als
   Loesung. Die Messung sagt etwas anderes: der Generator nimmt das Klassenpraefix bereits,
   und trotzdem bleiben 13 Faelle stehen, weil Spiel und Wiki verschiedene Marken meinen.

2. Die kosmetischen Faelle regeln (Zeilenumbruch, `C.O.`-Praefix, `&`/`and`). Die
   Normalisierung gehoert in eine benannte, kommentierte Tabelle im Generator — jede Zeile
   mit dem Grund daneben (Claude's Discretion aus CONTEXT.md: Tabelle im Generator statt
   eigener Datei, weil sie zur Extraktion gehoert und ohne sie nicht zu verstehen ist).
   Die `&`-Regel wird NICHT als globales Suchen-und-Ersetzen gebaut: „and" gegen „&" auf
   jedem Herstellernamen zu tauschen, waere eine Regel, die einen kuenftigen Hersteller
   mit „and" im Namen still beschaedigt. Sie gilt fuer den einen benannten Hersteller.

3. Die Marken-Faelle entscheiden (13× `makerCode`, 2× `manufacturer`). Das ist ein Urteil,
   kein Handgriff — je Fall pruefen, was das Spiel selbst sagt: den Record des Herstellers
   aus dem DataCore lesen (Anzeigename und `Code`) und ihn dem Klassenpraefix
   gegenueberstellen. Fuer die drei Gruppen sind das drei zusammenhaengende Entscheidungen:
   - Esperia baut Nachbauten von Vanduul-Schiffen. Die Frage lautet, ob das Datenblatt den
     Erbauer oder den Ursprung nennen soll.
   - Mirai ist eine Untermarke von MISC. Die Frage lautet, ob die Untermarke im
     Herstellercode auftauchen soll.
   - Xian: zwei Kuerzel fuer dieselbe Herkunft.
   Massgeblich fuer die Entscheidung ist die Richtungsvorgabe der Phase („alle Daten aus
   dem Spiel") UND die Vertraeglichkeit mit dem Bestand: der Herstellername ist auf
   `schiffe.astro` ein Filterschluessel (`data-maker`), und `src/data/manufacturer-logos.json`
   aus dem Logo-Datamine haengt an denselben Marken. Beides pruefen, bevor entschieden
   wird — ein Filtereintrag, der ins Leere zeigt, oder ein Schiff ohne Logo waeren stille
   Folgeschaeden.
   Ergebnis ist in jedem Fall: `verify:vehicles` meldet fuer die drei Felder 0
   Abweichungen. Wird zugunsten des Spiels entschieden, wandert der Wiki-Wert in die
   Vergleichsdefinition als bewusst akzeptierte Abweichung — dann aber NICHT still,
   sondern als benannte Ausnahmeliste im Pruefskript mit Begruendung je Eintrag.

4. `sizeDe` mitbeurteilen (40 Abweichungen, D-09). Das Feld steht in keiner
   VEH-Anforderung, aber D-14 verlangt fuer JEDE Abweichungsklasse ein Urteil, und D-09
   nennt es ausdruecklich. Die Verteilung vom 03.08.2026: 12× `Beiboot` gegen `Klein`,
   10× `Mittel` gegen `Klein`, 9× `Groß` gegen `Mittel`, 3× `Klein` gegen `Mittel`,
   3× `Beiboot` gegen `Fahrzeug`, 2× `Kapitalklasse` gegen `Groß`, 1× `Klein` gegen leer,
   1× `Groß` gegen `Klein`. Die Ursache ist die `SIZE_LABEL`-Tabelle im Generator: sie
   bildet die Groessenklasse des Spiels (1-6) auf die sechs gebraeuchlichen Labels ab, und
   diese Abbildung ist empirisch geraten, nicht belegt. Das ist eine ANZEIGE-Entscheidung
   wie `crew` — die Groessenklasse selbst (`sizeClass`) ist unstrittig und kommt aus dem
   Spiel. Zu entscheiden und zu begruenden: bleibt die heutige Abbildung, wird sie an den
   Bestand angepasst, oder zeigt das Datenblatt kuenftig die Spielklasse. Beachten: die
   EN-Fassung uebersetzt die Labels ueber eine feste Tabelle in `vehicleText.ts` — ein
   neues Label ohne Eintrag dort erscheint auf der englischen Seite auf Deutsch.

5. Alle Urteile nach `scratch/01.4-urteile-kosmetik.md`: je Fahrzeug eine Zeile mit Feld,
   Spielwert, Wiki-Wert, gewaehltem Wert und Begruendung. Fuer `makerCode` und
   `manufacturer` einzeln (15 Zeilen), fuer `sizeDe` je Klasse mit Aufzaehlung der
   betroffenen Fahrzeuge. Diese Tabelle ist der Nachweis fuer Erfolgskriterium 4 im Teil
   „keine Abweichung bleibt unbeurteilt" und wird in Plan 04 fortgeschrieben.

6. `npm.cmd run datamine:vehicles` und `npm.cmd run verify:vehicles`. Die drei Felder
   muessen auf 0 stehen. Danach eine Gegenprobe, die den Nachweis vom Zufall trennt: die
   Namen aller 227 Fahrzeuge aus dem neuen Katalog gegen die des Wiki-Katalogs stellen und
   die Menge der Unterschiede ausgeben — sie muss leer sein, und zwar auch dort, wo
   `verify-vehicles.mjs` gar nicht hinsieht (die vier ATLS-Eintraege sind uebernommen und
   sollten trivial passen, aber ein Tippfehler beim Uebernehmen faellt nur so auf).
  </action>

  <verify>
    <automated>npm.cmd run datamine:vehicles</automated>
    <automated>npm.cmd run verify:vehicles</automated>
    <automated>node -e "const fs=require('fs');const w=new Map(JSON.parse(fs.readFileSync('src/data/vehicles.json','utf8')).vehicles.map(v=>[v.id,v]));const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles;const raus={name:[],manufacturer:[],makerCode:[]};for(const x of g){const y=w.get(x.id);if(!y)continue;for(const k of Object.keys(raus))if(x[k]!=null&&y[k]!=null&&x[k]!==y[k])raus[k].push(y.name+': Spiel '+JSON.stringify(x[k])+' vs Wiki '+JSON.stringify(y[k]))}let bad=0;for(const[k,l] of Object.entries(raus)){console.log(k+': '+l.length+' Abweichungen');for(const z of l.slice(0,20))console.log('   '+z);bad+=l.length}if(bad)throw new Error(bad+' Abweichungen in name/manufacturer/makerCode — Erfolgskriterium 3 verlangt 0');console.log('Erfolgskriterium 3 erfuellt: 0 Abweichungen bei Name, Hersteller, Herstellercode')"</automated>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('scratch/01.4-urteile-kosmetik.md','utf8');const faelle=['Blade','Glaive','Stinger','Vanduul Scythe','Fury','Fury LX','Fury MX','Razor','Razor EX','Razor LX','Khartu-al','Nox','Nox Kue','Defender'];const fehlt=faelle.filter(f=>!m.includes(f));if(fehlt.length)throw new Error('unbeurteilte Marken-Faelle: '+fehlt.join(', '));if(!/sizeDe/.test(m))throw new Error('sizeDe fehlt in der Urteilstabelle (D-09)');console.log('Urteilstabelle deckt',faelle.length,'Marken-Faelle und sizeDe ab')"</automated>
    <automated>node -e "const fs=require('fs');const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles;const roh=g.filter(v=>typeof v.name==='string'&&(v.name!==v.name.trim()||/[\r\n]/.test(v.name)));if(roh.length)throw new Error('Namen mit Rand-Leerraum oder Zeilenumbruch: '+roh.map(v=>v.id+'='+JSON.stringify(v.name)).join(', '));const ids=new Set(g.map(v=>v.id));if(ids.size!==g.length)throw new Error('doppelte Kennungen im Katalog');console.log('alle',g.length,'Namen getrimmt und umbruchfrei, Kennungen eindeutig')"</automated>
    <automated>node -e "const fs=require('fs');const g=JSON.parse(fs.readFileSync('src/data/vehicles-gamefiles.json','utf8')).vehicles;const logos=fs.existsSync('src/data/manufacturer-logos.json')?JSON.parse(fs.readFileSync('src/data/manufacturer-logos.json','utf8')):null;if(!logos){console.log('keine Logo-Datei im Baum — Pruefung entfaellt');process.exit(0)}const key=logos.logos||logos.manufacturers||logos;const haben=new Set(Object.keys(key).map(s=>s.toLowerCase()));const ohne=[...new Set(g.map(v=>v.makerCode).filter(Boolean))].filter(c=>!haben.has(String(c).toLowerCase()));console.log('Herstellercodes im Katalog ohne Logo-Eintrag:',ohne.length?ohne.join(', '):'(keine)')"</automated>
  </verify>

  <acceptance_criteria>
    - `npm.cmd run verify:vehicles` meldet fuer `name`, `manufacturer` und `makerCode` je 0 Abweichungen; die Tabelle steht im SUMMARY (VEH-03, Erfolgskriterium 3).
    - Die kosmetischen Regeln stehen als kommentierte Tabelle im Generator, je Zeile mit Grund; die `&`-Regel gilt fuer den EINEN benannten Hersteller, nicht global.
    - Kein Fahrzeugname traegt Rand-Leerraum oder einen Zeilenumbruch.
    - `scratch/01.4-urteile-kosmetik.md` fuehrt alle 13 `makerCode`-Faelle und die beiden `manufacturer`-Marken-Faelle einzeln mit Spielwert, Wiki-Wert, gewaehltem Wert und Begruendung.
    - Die drei Marken-Gruppen (Esperia/Vanduul, Mirai/MISC, Xian) haben je eine Entscheidung MIT Blick auf die Folgen: Filterschluessel `data-maker` auf der Schiffsliste und Logo-Zuordnung sind geprueft; die Zahl der Herstellercodes ohne Logo-Eintrag steht im SUMMARY.
    - Wurde eine Abweichung bewusst akzeptiert statt beseitigt, steht sie als benannte Ausnahme MIT Begruendung im Pruefskript — nicht als stille Toleranz.
    - `sizeDe` hat ein begruendetes Urteil je Abweichungsklasse (8 Klassen, 40 Fahrzeuge); wurde die Abbildung geaendert, ist die EN-Uebersetzungstabelle in `vehicleText.ts` als Folgearbeit fuer Plan 05 vermerkt.
    - Der Katalog fuehrt weiterhin 227 Fahrzeuge und keine Kennung doppelt.
  </acceptance_criteria>

  <done>
    Die Vergleichstabelle steht bei Name, Hersteller und Herstellercode auf null, und jede
    der 15 Marken-Entscheidungen sowie die 40 Groessenlabel-Abweichungen sind einzeln
    begruendet aufgeschrieben.
  </done>

  <reversibility rating="reversible">
    Regeln im Generator und eine Messdatei. Der ausgelieferte Katalog bleibt unangetastet;
    ein Rueckbau kostet einen erneuten Generatorlauf, kein Datenverlust.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wiki-Katalog (Stand 18.07.2026) → committete Fremddatei | die letzte Gelegenheit, Pledge-Preis, RSI-Link, Abmessungen und Bild-URL zu retten; danach ist die Quelle abgeschaltet |
| DataCore-Herstellerrecords → Marken-Anzeige und Filterschluessel | der Herstellername ist nicht nur Text, sondern Filterwert auf der Schiffsliste und Verbindung zur Logo-Datei |
| uebernommene Datensaetze (ATLS) → Katalog | vier Eintraege stammen nicht aus dem Spiel; ohne Kennzeichnung waeren sie von extrahierten nicht zu unterscheiden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-06 | Denial of Service | Fremdwerte (Preis, Link, Masse, Bild) beim Einfrieren | critical | mitigate | Feld fuer Feld gegen den heutigen Katalog geprueft: jeder nicht-leere Wert MUSS in der Fremddatei ankommen, sonst Abbruch. Der Vorbedingungs-Check haelt an, wenn `vehicles.json` nicht mehr der Wiki-Stand ist — nach dem Tausch waere die Quelle unwiederbringlich weg. |
| T-01.4-07 | Denial of Service | vier ATLS-Varianten | high | mitigate | Erst Suche im DataCore, dann Uebernahme; die Katalogzahl 227 ist harte Abnahmebedingung, die vier Kennungen werden einzeln geprueft. |
| T-01.4-08 | Repudiation | uebernommene Datensaetze ohne Herkunftsangabe | medium | mitigate | Jeder uebernommene Eintrag traegt eine maschinenlesbare Herkunfts- und Datumsangabe; die Abnahme bricht ab, wenn sie fehlt. Der Kernwert „keine erfundenen Werte" gilt auch fuer Herkunftsangaben. |
| T-01.4-09 | Tampering | Sammelregel gegen Marken-Abweichungen | high | mitigate | Die 15 Marken-Faelle werden einzeln beurteilt und in einer Tabelle festgehalten; eine globale Ersetzung von „and" gegen „&" ist ausdruecklich untersagt, weil sie kuenftige Hersteller still beschaedigt. |
| T-01.4-10 | Denial of Service | Filterschluessel `data-maker` und Logo-Zuordnung | medium | mitigate | Vor der Marken-Entscheidung werden beide Abhaengigkeiten geprueft; die Zahl der Herstellercodes ohne Logo-Eintrag ist Abnahmezahl. |
| T-01.4-SC | Tampering | npm/pip/cargo installs | low | accept | In diesem Plan wird kein Paket installiert. Keine neue Lieferkette, daher kein Legitimitaets-Gatter. |
</threat_model>

<verification>
1. `src/data/vehicles.json` beim Start noch der Wiki-Stand vom 18.07.2026 — die Einfrier-Quelle ist da.
2. Fremddatei erzeugt, committierbar, ausschliesslich belegt quellenlose Felder.
3. Einfrieren verlustfrei: kein Fremdwert und keine Bild-Angabe verloren; Zahlen je Feld im SUMMARY.
4. ATLS-Suche im DataCore protokolliert; Katalog fuehrt 227 Fahrzeuge, die vier Kennungen einzeln belegt und als uebernommen gekennzeichnet.
5. Bild-URL und Pledge-Preis kommen im erzeugten Katalog an (>= 216 bzw. >= 218).
6. `verify:vehicles`: 0 Abweichungen bei Name, Hersteller, Herstellercode.
7. Urteilstabelle vollstaendig: 13 `makerCode`-Faelle, 2 `manufacturer`-Faelle, 8 `sizeDe`-Klassen.
8. Keine Namen mit Rand-Leerraum oder Zeilenumbruch, keine doppelten Kennungen.
9. Herstellercodes ohne Logo-Eintrag gezaehlt und benannt.
10. `git diff --stat` zeigt genau die in `files_modified` genannten Dateien und keine weitere.
</verification>

<success_criteria>
- `src/data/vehicle-external.json` traegt Pledge-Preis, RSI-Link, Abmessungen und Bild-URL, sonst nichts (Erfolgskriterium 2, VEH-02).
- `node scripts/verify-vehicles.mjs` meldet fuer Name, Hersteller und Herstellercode 0 Abweichungen (Erfolgskriterium 3, VEH-03).
- Der Spieldaten-Katalog fuehrt 227 Fahrzeuge inklusive der vier ATLS-Varianten (Erfolgskriterium 5, VEH-07).
- Jede Marken- und Groessenlabel-Abweichung hat ein begruendetes Urteil (D-14).
</success_criteria>

## Artifacts this plan produces

Neu geschaffene Bezeichner und Dateien — nachgelagerte Pruefungen duerfen sie NICHT als
Abweichung gegen den Altbestand werten:

| Art | Name | Ort |
|-----|------|-----|
| Skript | Erzeuger der Fremddatei (mit `--force`-Riegel) | scripts/build-vehicle-external.mjs |
| Datendatei (committet) | Fremddaten: sechs Felder + uebernommene ATLS-Datensaetze | src/data/vehicle-external.json |
| npm-Eintrag | `build:vehicle-external` | package.json |
| Datenfeld | Herkunftsangabe an uebernommenen Katalogeintraegen | src/data/vehicles-gamefiles.json |
| Regeltabelle | Namens- und Herstellernormalisierung, je Zeile mit Grund | scripts/datamine-vehicles.mjs |
| Messdatei (nicht committet) | `scratch/01.4-urteile-kosmetik.md` | scratch/ |

<output>
Create `.planning/phases/01.4-fahrzeug-katalog-auf-spieldaten/01.4-03-SUMMARY.md` when done.
Woertlich protokollieren, nicht „bestanden": die Feldzahlen der Fremddatei, das Ergebnis
der ATLS-Suche im DataCore (gefunden oder belegt nicht gefunden), die vier ATLS-Eintraege
mit Herkunftsangabe, die Vergleichstabelle mit den drei Feldern auf null, alle 15
Marken-Urteile einzeln mit Begruendung, das `sizeDe`-Urteil je Klasse und die Zahl der
Herstellercodes ohne Logo-Eintrag.
</output>
