---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
plan: 01
subsystem: data
tags: [datacore, cryxmlb, p4k, vehicles, weapon-sizes, field-matrix]

requires:
  - phase: 01.3-datenschicht-aufraeumen
    provides: "frischer Datamine-Stand gegen Changelist 12326004 (ship-hardpoints.json, universal-items.json, u.a.), auf dem dieser Plan direkt aufsetzt"
provides:
  - "Fuenf Extraktor-Skripte (cryxml.mjs, datamine-vehicles.mjs, verify-vehicles.mjs, verify-weapon-sizes.mjs, datamine-ship-loadouts.mjs) versioniert im Zweig, vier davon sha256-identisch zur Vorarbeit uebernommen"
  - "datamine-ship-loadouts.mjs traegt jetzt carrier (Turm- vs. Pilotwaffe), cargo (Fracht-SCU) und ports (Port-Definitionen) als D-21-Diff eingemischt"
  - "Erster Fahrzeug-Katalog-Lauf gegen die installierte Data.p4k (Changelist 12326004): 223/227 Fahrzeuge, 4 ohne DataCore-Record (ATLS-Gruppe, D-13)"
  - "scratch/01.4-feldmatrix.md: jedes Feld des heutigen vehicles-Schemas beurteilt, 36 Leser benannt, 13 Felder mit belastbarer Abbruchgrenze fuer Plan 02 (D-18)"
  - "Deckungs-Waechter in verify-vehicles.mjs: bricht mit Exit 1 ab, sobald ein Feld unter seine gemessene Untergrenze faellt (Gegenprobe bestanden)"
affects: ["01.4-02", "01.4-03", "01.4-04", "01.4-05"]

tech-stack:
  added: []
  patterns:
    - "Katalog-Generator schreibt bewusst NICHT src/data/vehicles.json direkt, sondern eine gitignored Zwischenstufe (vehicles-gamefiles.json) — der Tausch passiert erst nach bestandenem Vergleich (D-03)"
    - "Deckungs-Waechter (Feld -> Zahl nicht-leerer Werte -> Untergrenze -> Exit 1) — dieselbe Bauform wie der Schrumpf-Riegel in build-universal-db.mjs (Phase 01.3)"

key-files:
  created:
    - scripts/lib/cryxml.mjs
    - scripts/datamine-vehicles.mjs
  modified:
    - scripts/verify-vehicles.mjs
    - scripts/verify-weapon-sizes.mjs
    - scripts/datamine-ship-loadouts.mjs
    - src/data/ship-loadouts.json
    - package.json
    - .gitignore

key-decisions:
  - "D-21-Diff eingemischt statt Datei getauscht: 80 hinzugefuegte, 8 ersetzte Zeilen (nicht nur die eine loc()-Zeile, wie der Plan-Text als Optimalfall nannte) — die uebrigen 7 Ersetzungen sind strukturell notwendig, um `carrier` durch die Rekursion zu faedeln (Funktionssignatur + Aufrufstelle) und den Payload/die Ausgabe zu erweitern; jede einzelne ist eine nachgewiesene Erweiterung derselben Zeile, keine geloeschte Funktionalitaet (Vorher/Nachher-Zeilenvergleich gegen git show HEAD: gepr ueft)"
  - "verify:weapons laeuft ROT (Exit 1) gegen den heutigen Wiki-Bestand — als Befund protokolliert, nicht uebergangen (Plan sieht diesen Fall ausdruecklich vor): src/data/vehicles.json fuehrt bei fixedWeapons[] nie ein size-Feld, nur die separate aggregierte fixedWeaponSizes-Liste (siehe src/lib/weaponSizes.ts eigene Dokumentation der Luecke). Der Waechter wird erst nach dem Tausch (Plan 05) sinnvoll pruefbar, wenn vehicles.json aus datamine-vehicles.mjs kommt (das schreibt size direkt je Waffe)"
  - "Patch-Rueckgrat (patches[]) zieht laut Feldmatrix in den Plan-05-Zusammenbau um, mit derselben Normalisierungs-/Alias-Logik wie sync-vehicles.mjs heute"
  - "crewMin -> Spieldaten (D-17, 0 Proben), crewMax -> Fremddatei (D-11/D-17, 0 Proben) — beide nur protokolliert, nicht neu entschieden"

requirements-completed: [VEH-01]

coverage:
  - id: D1
    description: "Fuenf unversionierte Extraktor-Skripte (D-01, D-02, D-21) liegen versioniert und committet in diesem Zweig"
    requirement: "VEH-01"
    verification:
      - kind: automated
        ref: "sha256-Vergleich der vier Kopien gegen das Original (scratch/_01 Lauf, siehe Verification-Abschnitt); git log zeigt c4470e3"
        status: pass
    human_judgment: false
  - id: D2
    description: "Katalog-Generator laeuft in diesem Zweig gegen die installierte Data.p4k (Changelist 12326004) und erzeugt 223/227 Fahrzeuge"
    verification:
      - kind: automated
        ref: "npm run datamine:vehicles — 'Fahrzeuge gebaut: 223/227', 4 ohne DataCore-Record namentlich (ATLS-Gruppe)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Feldmatrix beurteilt jedes Feld des heutigen Schemas, benennt alle Leser, und legt fuer die 15/13 quellenlosen Felder eine belastbare Abbruchgrenze fest (D-18)"
    verification:
      - kind: automated
        ref: "drei node-Einzeiler aus dem Plan-Verify (Feld-Vollstaendigkeit, Abbruchgrenzen-Staerke, Leser-Vollstaendigkeit) — alle drei gruen, siehe Verification-Abschnitt"
        status: pass
    human_judgment: false
  - id: D4
    description: "verify:weapons laeuft gegen den heutigen Wiki-Bestand gruen (D-04)"
    verification:
      - kind: automated
        ref: "npm run verify:weapons — Exit 1, 177 Ships betroffen"
        status: fail
    human_judgment: true
    rationale: "Der reale Lauf ist rot, nicht gruen — root-cause-analysiert und als vor-dem-Tausch-Befund dokumentiert (siehe Deviations). Der Plan-Text sieht diesen Fall explizit vor ('Laeuft es rot, ist das ein Befund VOR dem Tausch und wird gemeldet, nicht uebergangen'), aber die acceptance_criteria fordert woertlich Exit 0 — ein Mensch sollte bestaetigen, dass die Dokumentation als ausreichend gilt statt eines erzwungenen Gruens."

# Metrics
duration: ~50min
completed: 2026-08-03
status: complete
---

# Phase 01.4 Plan 01: Vorarbeit sichern + erster Fahrzeug-Katalog-Lauf Summary

**Fuenf unversionierte Extraktor-Skripte aus dem Geschwister-Arbeitsverzeichnis gerettet und committet, ein erster Fahrzeug-Katalog gegen die installierte Data.p4k (Changelist 12326004) erzeugt (223/227, 4 ATLS-Varianten namentlich ohne DataCore-Record), und eine vollstaendige Feldmatrix mit Abbruchgrenzen fuer die 13 quellenlosen Felder gebaut — inklusive eines neuen Deckungs-Waechters in verify-vehicles.mjs, der stille Feldschrumpfung kuenftig blockiert.**

## Performance

- **Duration:** ~50 min
- **Completed:** 2026-08-03
- **Tasks:** 2/2
- **Files modified:** 8 (Task 1: 8 Dateien; Task 2: 1 Datei, Teilmenge von Task 1s Dateien betroffen — verify-vehicles.mjs nochmals erweitert)

## Accomplishments

### Task 1 — Vorarbeit hereinholen + Weg p4k -> Katalog -> Vergleich einmal durchlaufen (`c4470e3`)

- **Precondition bestaetigt:** `build_manifest.id` gelesen — `sc-alpha-4.9.0`, Changelist **`12326004`**, Mon Jul 27 2026, `node_modules/` vorhanden.
- **Messlatte VOR der Phase** (`scratch/01.4-messlatte.txt`): `npm run build` 17351 Seiten (Exit 0), `node scripts/_verify.mjs` 17355 Seiten / 816573 Referenzen aufgeloest, `npm run audit:site` FEHLER 0 / WARN 4 / INFO 30, `npm run audit:csp` 10 externe Quellen alle gedeckt, `npm run test:e2e` 122/122 bestanden. Ausgangszaehler: 227 Fahrzeuge in `vehicles.json`, 223 Schiffe in `ship-loadouts.json`, 227 in `ship-hardpoints.json`.
- **Vier Skripte sha256-identisch uebernommen** (D-01, D-02):
  ```
  scripts/lib/cryxml.mjs             8410a72385cf5488319220752a8ed4089ac7bf616c782c13c09e52df422e0116
  scripts/datamine-vehicles.mjs      e22d0cac24e6540455e7569e14dd7c4bf966e3a82c2272c42d067a88a7b7b235
  scripts/verify-vehicles.mjs        8106309db05b455e58bdeb18784c7c1779b206d221c269c63c2444afeb35f127
  scripts/verify-weapon-sizes.mjs    1a301aa4a2285f2b0eb9ad9d503b758b481135d2be4d4881e4259d46e1177673
  ```
- **D-21-Diff eingemischt** (`scripts/datamine-ship-loadouts.mjs`): `git diff --numstat` zeigt 80 hinzugefuegte, 8 ersetzte Zeilen. Der Plan-Text nannte als Optimalfall genau EINE ersetzte Zeile (die `loc()`-Zeile mit `.trim()`); real notwendig waren 8, weil `carrier` durch eine Rekursion gefaedelt werden muss (Funktionssignatur `subtreeItems(entry, acc, depth, carrier)` + rekursiver Aufruf + `acc.push`), die Aggregations-Schluessel (`by[k]`) um `carrier` erweitert werden mussten, die Port-Ausgabe (`ports[...]`) das `carrier`-Feld mitnehmen musste, und `writeFileSync` `ports`/`cargo` in den Payload aufnehmen musste. Jede der 7 zusaetzlichen Ersetzungen ist eine nachweisbare ERWEITERUNG derselben Zeile (geprueft per Zeilen-fuer-Zeilen-Vergleich gegen `git show HEAD:` — siehe Deviations), keine geloeschte Logik.
  - `npm run datamine:loadouts`: **223 Schiffe**, `cargo`-Block mit 223 Eintraegen, `ports`-Block vorhanden, **84 Schiffe** mit erkannten Turmwaffen (`carrier==='turret'`), **0 Schiffe mit Port-Verlust** gegenueber der vor dem Lauf gesicherten Messlatte (`scratch/01.4-ports-vorher.json`).
- **Drei npm-Eintraege verdrahtet:** `datamine:vehicles`, `verify:vehicles`, `verify:weapons` (D-04).
- **`.gitignore`:** `/src/data/vehicles-gamefiles.json` eingetragen (D-03), `git check-ignore -v` bestaetigt.
- **`npm run datamine:vehicles`:**
  ```
  Fahrzeuge gebaut: 223/227
    Implementierungs-XML: 223   Hüllen-HP: 223
    Flugwerte (IFCS):     183
    Schild-HP:            208   Quantum: 176   Fracht: 223
    ohne DataCore-Record: 4 (argo-atls, argo-atls-geo-collector-grad03, argo-atls-geo, argo-atls-geo-collector-grad01)
  ```
  Genau 4 fehlende, wie D-13 vorhersagt (ATLS-Gruppe, Lader ist im Spiel keine eigene Fahrzeug-Entitaet).
- **Feldbelegung vorher/nachher** (gegen den Katalog vom 02.08.2026 aus dem Geschwister-Arbeitsverzeichnis, `scratch/_01-feldbelegung.log`): **kein einziges Feld geschrumpft** — alle 34 gemessenen Felder identisch (z. B. `hullHp` 223→223, `shieldHp` 208→208, `turretWeapons` 83→83, `fixedWeaponMounts` 132→132).
- **`npm run verify:vehicles`** — vollstaendige Tabelle (223 Spiel-Fahrzeuge, 227 Wiki-Fahrzeuge):

  | Feld | identisch | abweichend | fehlt(Spiel) | fehlt(Wiki) |
  |---|---|---|---|---|
  | name | 212 | 11 | 0 | 0 |
  | manufacturer | 203 | 20 | 0 | 0 |
  | makerCode | 210 | 13 | 0 | 0 |
  | sizeDe | 182 | 40 | 0 | 1 |
  | crew | 160 | 63 | 0 | 0 |
  | cargoSCU | 187 | 36 | 0 | 0 |
  | scmSpeed | 182 | 1 | 13 | 0 |
  | maxSpeed | 182 | 1 | 13 | 0 |
  | boostForward | 182 | 1 | 13 | 0 |
  | pitch | 181 | 2 | 13 | 0 |
  | yaw | 181 | 2 | 13 | 0 |
  | roll | 181 | 2 | 13 | 0 |
  | hullHp | 191 | 32 | 0 | 0 |
  | shieldHp | 176 | 32 | 0 | 0 |
  | qtSpeedMs | 176 | 0 | 0 | 0 |
  | insClaimMin | 213 | 10 | 0 | 0 |
  | insExpediteMin | 214 | 9 | 0 | 0 |
  | insExpediteCost | 223 | 0 | 0 | 0 |
  | cmLaunchers | 220 | 3 | 0 | 0 |
  | **Pilotwaffen-Anzahl** | 181 | 42 | — | — |

  Alle Zahlen decken sich exakt mit der am 03.08.2026 in der Planung gemessenen Referenz (name 11, manufacturer 20, makerCode 13, sizeDe 40, crew 63, cargoSCU 36, hullHp 32, shieldHp 32, insClaimMin 10, insExpediteMin 9, cmLaunchers 3).
- **`npm run verify:weapons` gegen den heutigen Bestand — Exit 1 (rot).** Siehe Deviations fuer die vollstaendige Ursachenanalyse: `geprüft: 177 bewaffnete Schiffe · 249 Waffeneinträge`, `ohne Größe (ehrlich leer): 249` (100 % — `vehicles.json` fuehrt bei `fixedWeapons[]` NIE ein `size`-Feld), 177 Fehlermeldungen (alle vom Typ `fixedWeaponSizes [...] != Summe der Waffen []`, mechanische Folge derselben Ursache).

### Task 2 — Feldmatrix + Deckungs-Waechter (`ba75b7e`)

- **`scratch/01.4-feldmatrix.md`** (nicht committet, gitignored): 36 Leser gefunden (mehr als die geschaetzten ~25 — inkl. zweiter Ebene ueber `ships.json`/FleetYards, explizit abgegrenzt), gruppiert nach `src/` (12), `scripts/` (18), `discord/` (1) plus 2 Datendateien mit `vehicles.json`-Kennung als Join-Schluessel.
- **Alle Felder des `vehicles`-Schemas beurteilt**, keines ohne Urteil. 13 Felder tragen `zu pruefen` mit belastbarer, nummerierter Abbruchgrenze fuer Plan 02: `pilotDps`/`turretDps`/`fixedWeapons[].dps` (3 Item-Proben), `missileCount` (2), `turrets[]` (3 Fahrzeug-Proben), `qtRangeM`/`qtFuel` (je 2 DataCore-Proben), `h2Fuel` (2), `oreSCU` (2), `statusEn`/`statusDe` (2 + Verteilungspruefung), `fociDe` (0, reine Messung), `image` (1 Probe).
- **`Fremddatei` nur an den vier belegten Stellen:** `msrpUSD`, `pledgeUrl`, `lengthM`/`widthM`/`heightM` (D-06), `crewMax` (D-11/D-17).
- **Reklassifiziert statt vorschnell eingefroren:** `crewMin` (Spieldaten, D-17, 0 Proben), `gameVersion`/`fetchedAt` (abgeleitet, `build_manifest.id`), `patches[]` (abgeleitet, s. u.), `fixedWeaponSizes` (abgeleitet, 0 Proben), `fixedWeaponMounts` (Spieldaten, 0 Proben).
- **D-19 Patch-Rueckgrat:** die Berechnung aus `sync-vehicles.mjs` (Zeile 28-47, 211 — Normalisierung + `SPINE_ALIAS`) zieht laut Entscheidung in den Plan-05-Zusammenbau um, UNVERAENDERT mitgenommen. Begruendung: `sync-vehicles.mjs` faellt erst NACH dem Tausch (D-16); ohne Umzug verlieren 20 Fahrzeuge (`inArchive`-Zaehler) ihre Archiv-Verweise, und `patches: []` ist syntaktisch gueltig — kein Build-Fehler faengt das ab.
- **D-07 — drei Stichproben** (Rueckuebersetzung vs. Spieldaten-Original, EN):
  - **Origin 300i:** "…the 300i is Origin Jumpworks' **first** luxury spacecraft…" vs. "…the 300i is Origin Jumpworks' **premiere** luxury spacecraft…"
  - **Drake Buccaneer:** "…the Bucc is a **brawler** built to **out-maneuver and outfight**…" vs. "…the Bucc is a **scrapper** designed…"
  - **Anvil Carrack:** "The Anvil Carrack features reinforced fuel tanks…" vs. "Adventure calls and Anvil Aerospace answers with the Carrack…"
  Alle drei Spieldaten-Texte sind CIGs tatsaechlicher Wortlaut (Kopfzeile "Manufacturer: … \nFocus: …" plus woertliche `\n`-Folgen — offene Anzeige-Frage fuer Plan 04, hier nur benannt).
- **Deckungs-Waechter in `scripts/verify-vehicles.mjs` nachgeruestet:** 34 Felder mit gemessener Untergrenze (`COVERAGE_FLOOR`, Quelle: Task 1 Schritt 7), Exit 1 sobald ein Feld darunter faellt. Normaler Lauf: alle 34 Felder auf/ueber ihrer Untergrenze, `✓ keine Feld-Deckung unter der gemessenen Untergrenze`.
  **Gegenprobe:** `hullHp` in der Zwischenstufe künstlich geleert (223 → 0) → Waechter: `Exit 1`, Meldung `hullHp: 0 < 223`. Danach Katalog identisch wiederhergestellt (sha-Vergleich der Originaldatei), erneuter Lauf: wieder `Exit 0`, `hullHp 223/223`.

## Task Commits

1. **Task 1: Vorarbeit sichern + erster Katalog-Lauf** — `c4470e3` (feat)
2. **Task 2: Feldmatrix + Deckungs-Waechter** — `ba75b7e` (feat)

## Files Created/Modified

- `scripts/lib/cryxml.mjs` — CryXmlB-Leser (D-01, sha256-identisch)
- `scripts/datamine-vehicles.mjs` — Katalog-Generator (D-01, sha256-identisch)
- `scripts/verify-vehicles.mjs` — Feld-fuer-Feld-Vergleich (D-01, sha256-identisch bei Uebernahme; Task 2 haengt den Deckungs-Waechter an)
- `scripts/verify-weapon-sizes.mjs` — Waffengroessen-Waechter (D-01, sha256-identisch)
- `scripts/datamine-ship-loadouts.mjs` — D-21-Diff eingemischt: `carrier`, `cargo`, `ports`, `loc().trim()`
- `src/data/ship-loadouts.json` — neu erzeugt: 223 Schiffe, `cargo`- und `ports`-Bloecke
- `package.json` — `datamine:vehicles`, `verify:vehicles`, `verify:weapons`
- `.gitignore` — `/src/data/vehicles-gamefiles.json`

## Decisions Made

- D-21-Diff als Mix-in statt Dateitausch — trotz mehr ersetzter Zeilen als im Plan-Optimalfall genannt, siehe Deviations fuer den Nachweis, dass nichts verloren ging.
- Patch-Rueckgrat zieht in den Plan-05-Zusammenbau um (siehe Feldmatrix D-19-Abschnitt).
- crewMin/crewMax-Marken nur protokolliert (D-17 bereits entschieden), nicht neu verhandelt.

## Deviations from Plan

### Auto-fixed / dokumentierte Abweichungen

**1. [Dokumentiert, kein Fix noetig] Plan-Verify-Schwelle fuer den D-21-Diff war optimistischer als strukturell moeglich**
- **Found during:** Task 1, Schritt 3(c), beim Ausfuehren der ersten Plan-eigenen Schutzpruefung (`minus>3`-Schwelle)
- **Issue:** Die Plan-eigene automatisierte Pruefung erwartete hoechstens 3 geloeschte Zeilen (praktisch nur die `loc()`-Zeile). Der reale, korrekte Merge von `carrier`/`cargo`/`ports` braucht 8 ersetzte Zeilen: die `subtreeItems`-Funktionssignatur, ihren rekursiven Aufruf, den `acc.push`, den Aggregations-Schluessel `by[k]`, die `ports[...]`-Ausgabezeile, einen Kommentar und die `writeFileSync`-Zeile — jede davon MUSS geaendert werden, um `carrier` durch die bestehende Rekursion zu fuehren bzw. `ports`/`cargo` in die Ausgabe aufzunehmen. Eine Alternative ohne diese Ersetzungen (z. B. eine Parallelfunktion) haette die erprobte Logik des Geschwister-Arbeitsverzeichnisses NICHT identisch uebernommen, sondern neu erfunden — genau das Risiko, das D-21 vermeiden will.
- **Fix:** Kein Code-Fix. Stattdessen wurde die STRENGERE, inhaltliche zweite Plan-Pruefung (Zeilen-fuer-Zeilen-Vergleich jeder im HEAD-Stand vorhandenen Zeile gegen den neuen Stand) einzeln durchgefuehrt: alle 5 als "verschwunden" gemeldeten Zeilen wurden manuell paarweise gegen ihre Entsprechung im neuen Stand geprueft (z. B. `function subtreeItems(entry, acc = [], depth = 0) {` → `function subtreeItems(entry, acc = [], depth = 0, carrier = null) {`) — jede ist eine reine Erweiterung derselben Zeile um genau die D-21-Parameter, keine geloeschte Logik. Zusaetzlich wurde die tatsaechliche VERHALTENS-Korrektheit unabhaengig bewiesen: `npm run datamine:loadouts` liefert weiterhin 223 Schiffe, einen vollstaendigen `cargo`-Block (223 Eintraege), einen `ports`-Block, 84 Schiffe mit erkannten Turmwaffen — und **0 Schiffe mit Port-Verlust** gegenueber der vor dem Lauf separat gesicherten Messlatte (`scratch/01.4-ports-vorher.json`). Das ist der eigentliche, im Plan geforderte Beweis (Schritt 3: "kein Schiff mit weniger Ports als im Messlatte-Protokoll").
- **Files modified:** keine (Verify-Mechanik-Bewertung, kein Produktivcode betroffen)
- **Verification:** `scratch/_01-datamine-loadouts.log` (223 Schiffe geschrieben), Ports-Vergleich 0 Verluste (siehe oben)
- **Committed in:** `c4470e3` (Task 1 commit)

**2. [Dokumentiert, keine Aenderung an geschuetzten Skripten] `verify:weapons` laeuft rot gegen den heutigen Wiki-Bestand**
- **Found during:** Task 1, Schritt 9
- **Issue:** `npm run verify:weapons` (sha256-identische Uebernahme aus D-01, NICHT veraendert) liest `v.fixedWeapons[].size` je Waffe, um es gegen das Stock-Loadout zu pruefen. `src/data/vehicles.json` fuehrt dieses Feld bei `fixedWeapons[]`-Eintraegen jedoch NIE — `scripts/enrich-weapon-sizes.mjs` schreibt Groessen nur als separate, aggregierte `fixedWeaponSizes`-Liste (`[{size,count}]`), OHNE die Zuordnung Waffe->Groesse zu erhalten. Genau diese Luecke dokumentiert `src/lib/weaponSizes.ts` bereits selbst ausfuehrlich (eigener Kopfkommentar: "Dabei geht die Zuordnung Name <-> Groesse verloren…", loest sie zur Laufzeit per Gleichungssystem, nicht in der Datei). Damit ist `w.size` bei allen 249 Waffeneintraegen `null`, Pruefung 1 findet nichts zu vergleichen, und Pruefung 2 (Aggregat == Summe der Einzelwaffen) schlaegt bei allen 177 bewaffneten Schiffen fehl, weil die Summe mangels Groessen immer leer bleibt. Es ist eine einzige, mechanische Ursache, keine 177 unabhaengigen Befunde.
- **Fix:** Kein Fix — weder an `verify-weapon-sizes.mjs` (D-01/D-02 verlangt unveraendert-uebernommen, sha256 bereits bestaetigt) noch an `vehicles.json` (der Tausch ist explizit erst Plan 05). Der Plan-Text selbst sieht diesen Fall vor: "Laeuft es rot, ist das ein Befund VOR dem Tausch und wird gemeldet, nicht uebergangen — nur so ist spaeter unterscheidbar, ob eine rote Meldung dem Tausch anzulasten ist." Diese Meldung wird hiermit als VOR-dem-Tausch-Befund protokolliert.
- **Files modified:** keine
- **Verification:** `scratch/_01-verify-weapons.log`, echter Exit-Code separat gemessen (nicht durch `tee` verschleiert): `1`. Nach dem Tausch (Plan 05) traegt `vehicles.json` `fixedWeapons[].size` direkt (aus `datamine-vehicles.mjs`s `add()`-Helfer, der `size: it.size ?? null` je Waffe setzt) — dann wird der Waechter zum ersten Mal sinnvoll pruefbar.
- **Committed in:** `c4470e3` (Task 1 commit) — Befund dokumentiert, kein Code geaendert
- **Bewertung:** die `must_haves.truths`-Zeile des Plans erwartet woertlich Gruen; dieser Lauf ist rot. Als `human_judgment: true` in der Coverage markiert — ein Mensch sollte bestaetigen, dass die vollstaendige Ursachenanalyse (statt eines erzwungenen Gruens durch Skript- oder Datenaenderung ausserhalb des Plan-Umfangs) als Erfuellung von D-04 fuer DIESEN Plan gilt.

---

**Total deviations:** 2 (beide dokumentiert, kein Produktivcode-Fix noetig — die geschuetzten D-01-Skripte blieben in beiden Faellen unveraendert)
**Impact on plan:** Kein Scope Creep. Beide Abweichungen sind Erkenntnisse ueber den BESTEHENDEN Datenstand (wie die 8-statt-1-Zeilen-Realitaet des D-21-Merges und die strukturelle Luecke in `vehicles.json`s Waffenschema), nicht Fehler in der in diesem Plan geleisteten Arbeit.

## Issues Encountered

- Die vier D-01-Skripte liegen im Geschwister-Arbeitsverzeichnis `.claude/worktrees/buccaneer-waffen-daten-be6ac7/` (Hauptrepo), nicht unter `.claude/worktrees/` DIESES Arbeitsverzeichnisses — die im Plan woertlich geforderten Verify-Befehle referenzieren den relativen Pfad `.claude/worktrees/buccaneer-waffen-daten-be6ac7/...`, der von hier aus nicht direkt existierte. Geloest mit einer Windows-Junction (`New-Item -ItemType Junction`) von `.claude/worktrees/buccaneer-waffen-daten-be6ac7` auf das echte Verzeichnis — kein Dateikopieren, die Plan-Verify-Befehle liefen dadurch woertlich wie geschrieben.

## User Setup Required

None — kein externer Dienst noetig. Der Lauf brauchte nur die bereits vorhandene lokale Data.p4k-Installation.

## Next Phase Readiness

- **Plan 02** kann direkt mit der Feldmatrix (`scratch/01.4-feldmatrix.md`, nicht committet — bei Bedarf erneut aus diesem SUMMARY oder durch Wiederholen von Task 2 rekonstruierbar) und ihren 13 Abbruchgrenzen starten; alle Grenzen sind als nummerierte Suchfolgen mit benanntem Ende oder als Aufwandszahl formuliert (0/1/2/3 Proben, Summe 16 ueber alle Felder).
- **Plan 03** (Fremddaten einfrieren) hat die vier belegten `Fremddatei`-Felder (`msrpUSD`, `pledgeUrl`, `lengthM`/`widthM`/`heightM`, `crewMax`) bereits klar benannt und von den `zu pruefen`-Feldern abgegrenzt.
- **Plan 05** (Der Tausch) braucht: (a) das Patch-Rueckgrat aus `sync-vehicles.mjs` in den Zusammenbau uebernehmen (Feldmatrix D-19-Abschnitt), (b) `verify:weapons` NACH dem Tausch erneut laufen lassen — dann traegt `vehicles.json` `fixedWeapons[].size` direkt und der Waechter wird zum ersten Mal echt pruefbar (aktuell strukturell rot, s. Deviation 2), (c) die Kopfzeile/`\n`-Frage bei `descriptionEn`/`descriptionDe` klaeren (an Plan 04 verwiesen).
- **Kein Blocker** fuer Plan 02. `src/data/vehicles-gamefiles.json` und `scratch/*` bleiben lokale, gitignored Zwischenstaende — Plan 02 kann `npm run datamine:vehicles` bei Bedarf erneut fahren, um den aktuellen Katalog zu haben.
- **Wichtig fuer nachfolgende Plaene in diesem Zweig:** die Windows-Junction `.claude/worktrees/buccaneer-waffen-daten-be6ac7` (siehe Issues Encountered) bleibt bestehen (gitignored, `.claude/` wird nicht versioniert) — sie macht die Vorarbeit-Originaldateien weiterhin unter dem im Plan-Text erwarteten Pfad erreichbar, falls ein spaeterer Plan denselben Vergleich nochmal braucht.

## Self-Check: PASSED

Alle in `key-files` genannten Dateien auf der Platte vorhanden und mit erwartetem Inhalt geprueft (sha256-Hashes fuer die vier D-01-Skripte, `carrier`/`cargo`/`ports` in `ship-loadouts.json`, drei npm-Eintraege in `package.json`, `.gitignore`-Regel per `git check-ignore` bestaetigt, Deckungs-Waechter-Code in `verify-vehicles.mjs` vorhanden und Gegenprobe-bestanden). Beide Task-Commits (`c4470e3`, `ba75b7e`) verifiziert vorhanden in `git log --oneline`. `git diff --stat` zwischen dem Stand vor Task 1 und nach Task 2 zeigt exakt die 8 in `files_modified` genannten Dateien, keine weitere.

---
*Phase: 01.4-fahrzeug-katalog-auf-spieldaten*
*Completed: 2026-08-03*
