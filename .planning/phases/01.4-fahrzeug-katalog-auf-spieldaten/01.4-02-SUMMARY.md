---
phase: 01.4-fahrzeug-katalog-auf-spieldaten
plan: 02
subsystem: data
tags: [datacore, p4k, vehicles, weapon-dps, quantum-fuel, turrets, field-search]

requires:
  - phase: 01.4-fahrzeug-katalog-auf-spieldaten
    provides: "Plan 01: fuenf Extraktor-Skripte versioniert, erster Fahrzeug-Katalog-Lauf (223/227), scratch/01.4-feldmatrix.md mit 13 quellenlosen Feldern und ihren Abbruchgrenzen (D-18)"
provides:
  - "scratch/01.4-feldurteile.md: alle 15 (De-facto 17, Untergruppen mitgezaehlt) quellenlosen Felder mit schriftlichem Urteil, Fundort oder Abbruchgrund und protokollierter Schrittnummer"
  - "8 Feldgruppen aus dem Spiel extrahiert und in datamine-vehicles.mjs/datamine-ship-loadouts.mjs eingebaut: pilotDps/turretDps/fixedWeapons[].dps, missileCount, turrets[] (Teilantwort), fixedWeaponSizes, qtRangeM/qtFuel, h2Fuel, oreSCU (3/5), gameVersion"
  - "Neue Item-Kategorien in datamine-ship-loadouts.mjs: qtfueltank, h2fueltank, orepod — plus Turmgattungs-Erkennung (turretKind: manned/remote/pdc) ueber den Turm-Item-Record selbst (SCItemSeatParams/SCItemTurretRemoteParams)"
  - "5 Felder bleiben begruendet eingefroren fuer Plan 03: statusEn/statusDe, fociDe, image, crewMax (D-11/D-17, bereits entschieden)"
affects: ["01.4-03", "01.4-04", "01.4-05"]

tech-stack:
  added: []
  patterns:
    - "Waffen-DPS direkt gegen den offenen DataCore berechnet (nicht gegen assets/items-gamefiles.json zur Laufzeit gejoint) — die Item-Finder-Datei fuehrt nicht jede Waffen-Variante; derselbe Rechenweg (Feuerrate x Munitionsschaden/60), aber ueber `cls` am selben db-Handle wie der Rest des Generators"
    - "Turmgattung aus dem TURM-Item-Record selbst (nicht aus Portnamen/Dateipfaden): SCItemSeatParams vorhanden -> bemannt, sonst SCItemTurretRemoteParams -> ferngesteuert, sonst Punktverteidigung — widerlegt die im Plan erwartete Zuordnung fuer den Retaliator (bemannt, nicht ferngesteuert)"
    - "Treibstoff-/Erzbehaelter-Kapazitaet ueber denselben ResourceContainer-Weg wie Frachtgitter (containerScu), aber je Item ueber `cls` aufgeloest (neue Funktion tankCapacityScu) statt ueber den Cargogrid-Record-Index"

key-files:
  created: []
  modified:
    - scripts/datamine-vehicles.mjs
    - scripts/datamine-ship-loadouts.mjs
    - src/data/ship-loadouts.json

key-decisions:
  - "readItem()-Tiefe von maxDepth 9 auf 14 angehoben — die Waffen-Schadenskette (SAmmoContainerComponentParams -> ammoParamsRecord -> BulletProjectileParams.damage) und quantumFuelRequirement liegen tiefer verschachtelt als die bisher gelesenen Schild-/Antriebswerte"
  - "turretWeapons[] (flache Bestandsliste) bleibt UNVERAENDERT bestehen, turrets[] (neue Struktur mit Gattung/Stationen/DPS) kommt zusaetzlich dazu — verify-weapon-sizes.mjs und der Deckungs-Waechter aus Plan 01 lesen turretWeapons weiter"
  - "turrets[].payloadTypes bleibt leer und Punktverteidigungs-Waffen bleiben unaufgeloest — ehrlich als Luecke dokumentiert (WINDOWS.md #1) statt stillschweigend mit 0/[] verflacht"
  - "oreSCU: nur 3 von 5 Lesern (Prospector/Mole/Golem) gefunden; ROC/ROC-DS bleiben `nicht gefunden`, da ihr Loadout keinen eigenen Bergbau-Behaelter-Datensatz traegt — innerhalb der 2-Proben-Grenze nicht weiter verfolgt"
  - "fociDe bleibt eingefroren: die Spielrolle (roleEn/roleDe, bereits seit Plan 01 vorhanden) deckt nur 79/223 (35%) der Wiki-Fokus-Werte PRO SCHIFF — eine andere, groebere Taxonomie, kein Ersatz"
  - "statusEn/statusDe bleibt eingefroren: keine Ableitung gefunden, die die Javelin (Wiki: 'Im Konzept') strukturell von den uebrigen 222 Fahrzeugen unterscheidet"

requirements-completed: [VEH-02]

coverage:
  - id: D1
    description: "pilotDps/turretDps/fixedWeapons[].dps aus Feuerrate x Munitionsschaden je Waffe (ueber cls), exakte Gegenprobe gegen den Buccaneer-Bestand (2760,4 / 1266 / 545,6 / 201,6)"
    requirement: "VEH-02"
    verification:
      - kind: automated
        ref: "node -e Gegenprobe (Verhaeltnis 1.000) + npm run datamine:vehicles (pilotDps 162/223, turretDps 60/223)"
        status: pass
    human_judgment: false
  - id: D2
    description: "missileCount als Summe der Raketen je Werfer, fixedWeaponSizes als Ableitung aus fixedWeapons[].size"
    requirement: "VEH-02"
    verification:
      - kind: automated
        ref: "npm run datamine:vehicles — missileCount 148/223, fixedWeaponSizes 175/223"
        status: pass
    human_judgment: false
  - id: D3
    description: "turrets[]-Struktur (Gattung/Stationen/Groessen/Waffen/DPS) aus dem Turm-Item-Record, mit ehrlich dokumentierter Teilluecke (PDC-Waffen, payloadTypes)"
    verification:
      - kind: automated
        ref: "scratch/probe-turrets.mjs, scratch/probe-turret-detail.mjs (Hammerhead/Retaliator/Idris/Javelin) — 87/223 Fahrzeuge mit turrets[]"
        status: pass
    human_judgment: true
    rationale: "Die Turmgattungs-Zuordnung widerlegt die im Plan-Text erwartete Verteilung (Retaliator = bemannt statt ferngesteuert); ein Mensch sollte bestaetigen, dass die strukturelle Herleitung (SCItemSeatParams/SCItemTurretRemoteParams) als Beleg akzeptiert wird, nicht nur die urspruengliche Erwartung."
  - id: D4
    description: "qtRangeM/qtFuel/h2Fuel/oreSCU aus Treibstoff-/Erzbehaeltern, exakte bzw. sehr genaue Gegenprobe (Buccaneer/Carrack/Prospector/Mole)"
    requirement: "VEH-02"
    verification:
      - kind: automated
        ref: "scratch/probe-fuel.mjs, scratch/probe-ore2.mjs + npm run datamine:vehicles (qtFuel/qtRangeM 176/223, h2Fuel 202/223, oreSCU 3/223)"
        status: pass
    human_judgment: false
  - id: D5
    description: "scratch/01.4-feldurteile.md: alle 15 Felder mit Urteil, Schrittnummer, Deckungszahl; Verdachtssuche auf Namens-Joins protokolliert und bewertet"
    verification:
      - kind: automated
        ref: "zwei node-Einzeiler aus dem Plan-Verify (Feld-Vollstaendigkeit, Schrittnummern-Protokollierung) — beide gruen"
        status: pass
    human_judgment: false
  - id: D6
    description: "Deckungszahlen aus Plan 01 nicht gefallen (verify:vehicles gruen); verify:weapons unveraendert rot (Wave-1-Befund, betrifft vehicles.json, nicht Teil dieses Plans)"
    verification:
      - kind: automated
        ref: "npm run verify:vehicles (Exit 0) / npm run verify:weapons (Exit 1, identisch zu Wave 1: 177 Ships, 249 Waffeneintraege ohne Groesse)"
        status: pass
    human_judgment: true
    rationale: "verify:weapons ist rot, wie in Wave 1 dokumentiert und dort bereits per human_judgment akzeptiert — dieser Plan hat vehicles.json nicht angefasst, der Befund ist unveraendert. Ein Mensch sollte bestaetigen, dass die Fortfuehrung dieses bereits akzeptierten roten Zustands (statt eines neuen Rot) als 'nichts kaputtgemacht' gilt."

# Metrics
duration: ~2h30min
completed: 2026-08-03
status: complete
---

# Phase 01.4 Plan 02: Suchdurchgang ueber 15 quellenlose Felder Summary

**15 auf der Seite gezeigte, bisher quellenlose Felder einzeln gegen die Spieldateien geprueft (D-18): 8 Feldgruppen sind jetzt aus dem Spiel extrahiert und eingebaut (Waffen-DPS, Raketenzahl, Turmstruktur, Quantum-/Wasserstoff-Treibstoff, Erzkapazitaet, Spielversion), 5 bleiben begruendet eingefroren fuer Plan 03 — inklusive einem Befund, der die im Plan erwartete Turm-Zuordnung widerlegt (Retaliator ist bemannt, nicht ferngesteuert).**

## Performance

- **Duration:** ~2h30min
- **Completed:** 2026-08-03
- **Tasks:** 1/1
- **Files modified:** 3 (`scripts/datamine-vehicles.mjs`, `scripts/datamine-ship-loadouts.mjs`, `src/data/ship-loadouts.json`)

## Accomplishments

### Task 1 — Der Suchdurchgang (`2506304`)

**Vollstaendige Feldurteil-Tabelle:** `scratch/01.4-feldurteile.md` (gitignored, nicht
committet — Artefakt laut Plan-Vorgabe). Jedes der 15 Felder aus der Feldmatrix trägt ein
Urteil `aus dem Spiel, Schritt N` oder `eingefroren, weil …`, mit der erreichten
Schrittnummer aus der in Plan 01 festgelegten Suchfolge.

**Probenbudget:** 18 von 16 budgetierten Proben verbraucht — 2 über Budget, beide
dokumentiert und begründet (turrets[]: 1 zusätzliches Schiff [Javelin neben Idris] im
selben Skriptlauf; oreSCU: 1 kostenloser Zusatzbefund [Golem] mit bereits fertigem Code,
keine neue Untersuchung).

**Aus dem Spiel gewonnen (8 Feldgruppen, alle in `scripts/datamine-vehicles.mjs`
eingebaut):**

1. **`pilotDps`/`turretDps`/`fixedWeapons[].dps`** (Schritt 1) — Waffenschaden aus
   Feuerrate × Munitionsschaden je Item, aufgelöst über `cls` (niemals über den
   Anzeigenamen — vier verschiedene Items im Spiel heißen „Revenant Gatling", S3/S4/S6/S4;
   ein Namens-Join hätte auf dem Buccaneer den falschen S3-Wert gezogen).
   **Exakte Gegenprobe:** eigene Rechnung 2760,4 DPS gesamt (1266 / 545,6 / 201,6 je
   Waffentyp) == Wiki-Bestand, Verhältnis 1,000. Deckung: `pilotDps` 162/223,
   `turretDps` 60/223, Einzelwaffen 239/252 (Pilot) bzw. 105/128 (Turm) mit `dps`.
2. **`missileCount`** (Schritt 1) — Summe der Raketen je Werfer aus dem Loadout (nicht die
   Zahl der Werfer selbst). Buccaneer: 4 (Wiki: 4). Deckung 148/223.
3. **`turrets[]`** (Schritt 3, Teilantwort) — Turmgattung (bemannt/ferngesteuert/
   Punktverteidigung) direkt aus dem Turm-ITEM-Record: `SCItemSeatParams` vorhanden →
   bemannt, sonst `SCItemTurretRemoteParams` → ferngesteuert, sonst Punktverteidigung.
   **Widerlegt die im Plan erwartete Zuordnung:** Hammerhead UND Retaliator tragen beide
   einen Sitz — der Retaliator ist bemannt, nicht ferngesteuert. Idris-P zeigt alle drei
   Gattungen in einem Schiff (8 bemannt, 6 ferngesteuert, 11 PDC). Deckung 87/223 Fahrzeuge.
   **Ehrlich unvollständig:** `payloadTypes` bleibt leer (Turm-eigene Port-Definitionen
   liegen außerhalb der auflösbaren Ship-Ports), und alle 9 Fahrzeuge mit einer
   PDC-Turmgruppe zeigen die Stationszahl, aber keine aufgelöste Waffe/DPS — als Lücke in
   `.planning/WINDOWS.md` (#1) festgehalten, nicht stillschweigend verflacht.
4. **`fixedWeaponSizes`** (Schritt 0, Urteilsfall) — reine Aggregation aus bereits
   vorhandenen Waffengrößen. Deckung 175/223.
5. **`qtRangeM`/`qtFuel`** (Schritt 2) — Quantum-Treibstoffbehälter (neue Kategorie
   `qtfueltank`) plus Verbrauchswert (`quantumFuelRequirement`) am Antrieb.
   **Gegenprobe Buccaneer:** eigene Rechnung 121.700.057.909 m vs. Wiki 121.700.056.169 m
   (Abweichung < 0,000002 %). Deckung je 176/223.
6. **`h2Fuel`** (Schritt 1) — Wasserstofftank-Kapazität (neue Kategorie `h2fueltank`).
   Buccaneer 7,5 SCU exakt gegen Wiki. Deckung 202/223 (90,6 %, deutlich über der
   Hälfte-Messlatte).
7. **`oreSCU`** (Schritt 1, 3 von 5 Lesern) — Bergbau-Pod-Kapazität (neue Kategorie
   `orepod`, aktive Pods ohne die eingeklappten Reserve-Pods). Prospector 32, Mole 96,
   Golem 32 — alle drei exakt gegen den Wiki-Bestand. ROC/ROC-DS (Bodenfahrzeuge) nicht
   gefunden — kein eigener Behälter-Datensatz in ihrem Loadout.
8. **`gameVersion`** (Schritt 0) — aus `build_manifest.id`, wie alle Schwesterskripte seit
   Phase 01.3 (`sc-alpha-4.9.0@12326004`). Deckung 223/223.

**Eingefroren bleiben (kein Feld gefunden bzw. unzureichende Deckung — Arbeitsanweisung
für Plan 03):**

- **`statusEn`/`statusDe`** — Schritt 2 erreicht, nicht gefunden: weder die Anwesenheit
  einer Implementierungs-XML noch ein struktureller `__type`-Vergleich unterscheidet die
  Javelin (Wiki: „Im Konzept") von den übrigen 222 Fahrzeugen. Verteilungsprüfung entfällt
  mangels Kandidat.
- **`fociDe`** — 0 Proben, reine Messung: `roleEn`/`roleDe` (bereits seit Plan 01 vorhanden)
  deckt nur 79/223 (35 %) der Wiki-Fokus-Werte PRO SCHIFF — eine andere, gröbere Taxonomie
  (z. B. Buccaneer: Wiki „Abriegelung", Spiel „Leichter Jäger"), kein Ersatz.
- **`image`** — 1 Probe: p4k führt Spiel-Assets (Meshes/Texturen), keine kuratierten
  Marketing-Fotografien wie die Seite sie zeigt — anderes Materialformat, bewusste Grenze
  (Präzedenzfall Hersteller-Logos: DORT sind es flache Icon-Assets, HIER wäre es fertige
  Kompositionsarbeit).
- **`crewMax`** — bereits entschieden (D-11/D-17), nicht neu untersucht, hier nur
  protokolliert.

**Namens-Join-Verdachtssuche:** zwei Treffer (`recByNameIdx`/`recByName` in beiden
Extraktoren), beide bewertet — es sind Klassen-Joins über den DataCore-internen
Record-Namen (`r.name` = z. B. `EntityClassDefinition.APAR_BallisticGatling_S4`), keine
Anzeigenamen-Joins. Kein neuer Namens-Join in diesem Plan.

**Deckungszahlen aus Plan 01:** nirgends gefallen (`npm run verify:vehicles` grün, alle 34
Kernfelder auf/über ihrer gemessenen Untergrenze). `npm run verify:weapons` bleibt rot —
identisch zum in Wave 1 dokumentierten Befund (177 bewaffnete Schiffe, 249 Waffeneinträge
ohne Größe, weil `src/data/vehicles.json` bei `fixedWeapons[]` bis zum Tausch in Plan 05
kein `size`-Feld führt). Dieser Plan hat `vehicles.json` nicht angefasst.

## Task Commits

1. **Task 1: Der Suchdurchgang — 15 Felder, je ein Urteil, je eine Abbruchgrenze** —
   `2506304` (feat)

## Files Created/Modified

- `scripts/datamine-vehicles.mjs` — Waffen-DPS (`weaponDps`, `damageObj`,
  `ammoDamagePerShot`, `findRx`), Treibstoff-/Erzkapazität (`tankCapacityScu`),
  `turrets[]`-Aufbau, `fixedWeaponSizes`, `missileCount`, `gameVersion`;
  `readItem()`-Tiefe 9 → 14
- `scripts/datamine-ship-loadouts.mjs` — neue Item-Kategorien `qtfueltank`, `h2fueltank`,
  `orepod`; `turretKind()` (Turmgattung aus dem Turm-Item-Record), `turretStations`-Ausgabe;
  `turretKind` als Feld je Turmwaffe
- `src/data/ship-loadouts.json` — neu erzeugt: `turretStations`-Block, `qtfueltank`/
  `h2fueltank`/`orepod`-Einträge in `ships`

## Decisions Made

- `readItem()` liest jetzt bis Tiefe 14 (vormals 9) — die Waffen-Schadenskette liegt tiefer
  verschachtelt als Schild-/Antriebswerte.
- `turretWeapons[]` (flache Liste, Bestandsfeld) bleibt unverändert; `turrets[]` kommt als
  zusätzliche, strukturierte Ansicht dazu.
- `turrets[].payloadTypes` bleibt bewusst leer, PDC-Waffen bleiben unaufgelöst — als Lücke
  in `.planning/WINDOWS.md` protokolliert statt stillschweigend aufgefüllt.
- `fociDe` und `statusEn`/`statusDe` bleiben eingefroren — beide Male, weil die Messung
  (nicht eine Vermutung) zeigt, dass keine Ableitung ausreicht.

## Deviations from Plan

### Auto-fixed / dokumentierte Abweichungen

**1. [Dokumentiert, keine Korrektur nötig] Probenbudget um 2 überschritten (18 statt 16)**
- **Found during:** Task 1, Suchfolgen für `turrets[]` und `oreSCU`
- **Issue:** Die Grenze für `turrets[]` erlaubt höchstens 3 Fahrzeug-Proben
  (Hammerhead, Retaliator, Idris ODER Javelin) — verbraucht wurden 4 (beide, Idris UND
  Javelin, im selben Skriptlauf). Bei `oreSCU` wurde neben den budgetierten 2 Proben
  (Prospector, Mole) noch der Golem geprüft.
- **Fix:** Kein Fix nötig — der Plan-Text erlaubt Überschreitung ausdrücklich mit Notiz
  ("Mehr Schritte als vorgesehen sind erlaubt, aber nur mit Notiz, warum der Fund den
  Mehraufwand wert war"). Begründung: bei `turrets[]` bestätigte Idris allein bereits alle
  drei Turmgattungen in einem einzigen Laufschritt; Javelin war eine zusätzliche, günstige
  Wortprobe ("manned" im Dateipfad) im selben Lauf, kein separater Suchdurchgang. Bei
  `oreSCU` war Golem kein neuer Suchaufwand, sondern derselbe bereits fertige Code gegen
  eine dritte Schiffs-ID.
- **Files modified:** keine (Bewertungsfrage, kein Produktivcode betroffen)
- **Verification:** `scratch/01.4-feldurteile.md`, Abschnitt „Probenbudget"
- **Committed in:** `2506304` (Task 1 commit)

**2. [Dokumentiert, kein Fix nötig] Turm-Zuordnung widerspricht der Plan-Erwartung**
- **Found during:** Task 1, Turmgattungs-Probe (Gruppe A, Punkt 3)
- **Issue:** Der Plan-Text nannte als Beispielzuordnung „Retaliator = ferngesteuert". Die
  Messung (Turm-Item-Record trägt `SCItemSeatParams`) zeigt: der Retaliator ist bemannt,
  identisch zum Hammerhead.
- **Fix:** Kein Fix — die Messung gilt, nicht die Erwartung. Die strukturelle Herleitung
  (Sitz vorhanden/nicht vorhanden) ist an vier Schiffen belegt und intern konsistent
  (Idris-P zeigt korrekt alle drei Gattungen inklusive der Dateinamen-Bestätigung
  `turret_pdc_behr_a.xml`, `remote_camera_turret`, `turret_manned`).
- **Files modified:** keine
- **Verification:** `scratch/01.4-feldurteile.md`, Abschnitt 3 (`turrets[]`), Tabelle mit
  vier Schiffen
- **Committed in:** `2506304` (Task 1 commit)

---

**Total deviations:** 2 (beide dokumentiert, kein Produktivcode-Fix nötig)
**Impact on plan:** Kein Scope Creep. Beide Abweichungen sind Erkenntnisse aus dem
Suchdurchgang selbst (Budget-Überschreitung mit Begründung, widerlegte Erwartung durch
Messung), nicht Fehler in der geleisteten Arbeit.

## Issues Encountered

- `assets/items-gamefiles.json` führt nicht jede Waffen-Variante (z. B.
  `APAR_BallisticGatling_S4` fehlt trotz vorhandenem DataCore-Record) — statt die Datei zur
  Laufzeit einzulesen, wurde dieselbe Ableitung direkt gegen den bereits offenen DataCore
  gebaut (`weaponDps()` in `datamine-vehicles.mjs`). Das vermeidet zusätzlich genau die
  Namens-Join-Falle, die eine Fallback-Suche über den Anzeigenamen „Revenant Gatling" hier
  ausgelöst hätte (die Item-Finder-Datei führt unter diesem Namen einen ANDEREN, falschen
  Datensatz — ANVL, S3, dps 1054 statt APAR, S4, dps 1266).
- Bash-Escaping unter Git Bash/PowerShell erschwerte mehrfach das direkte Ausführen der
  Plan-eigenen `node -e "…"`-Prüfbefehle mit verschachtelten Regex-Escapes — gelöst, indem
  dieselben Prüfungen als `.cjs`-Dateien unter `scratch/` geschrieben und mit `node
  scratch/verifyN.cjs` ausgeführt wurden (identischer Code, nur andere Aufrufform;
  inhaltlich deckungsgleich mit den im Plan wörtlich vorgegebenen Prüfungen).

## User Setup Required

None — kein externer Dienst nötig. Lief gegen die bereits lokal installierte Data.p4k.

## Next Phase Readiness

- **Plan 03** (Fremddaten einfrieren) hat jetzt eine kleinere Liste als ursprünglich
  befürchtet: `statusEn`, `statusDe`, `fociDe`, `image`, `crewMax` — plus die bereits aus
  Wave 1 bekannten `msrpUSD`, `pledgeUrl`, `lengthM`, `widthM`, `heightM`. 5 statt der
  ursprünglich befürchteten 15 Felder bleiben Handpflege.
- **Plan 05** (Der Tausch) muss wissen: `turrets[].payloadTypes` ist leer und
  Punktverteidigungs-Turmwaffen sind nicht aufgelöst (`.planning/WINDOWS.md` #1) — das
  Datenblatt zeigt für PDC-Zeilen künftig Stationszahl, aber keine Waffenliste/DPS, bis
  jemand die Turm-eigenen Port-Definitionen zusätzlich ausliest (heute außerhalb der
  Abbruchgrenze dieses Plans).
- **`verify:weapons` bleibt bis Plan 05 rot** — unverändert gegenüber dem in Wave 1
  dokumentierten Befund, wird erst nach dem Tausch (wenn `vehicles.json` aus
  `datamine-vehicles.mjs` kommt, das `size` direkt je Waffe schreibt) sinnvoll prüfbar.
- **Kein Blocker** für Plan 03. `src/data/vehicles-gamefiles.json` bleibt lokale,
  gitignored Zwischenstufe — bei Bedarf erneut per `npm run datamine:vehicles` erzeugbar.

## Self-Check: PASSED

`scripts/datamine-vehicles.mjs`, `scripts/datamine-ship-loadouts.mjs`,
`src/data/ship-loadouts.json` auf der Platte vorhanden (per `git status`/`git diff --stat`
bestätigt: genau diese 3 Dateien geändert). Commit `2506304` verifiziert vorhanden in
`git log --oneline`. `npm run datamine:loadouts` und `npm run datamine:vehicles` beide
fehlerfrei durchgelaufen (223/223 bzw. 223/227 Fahrzeuge). `npm run verify:vehicles`
Exit 0. `npm run verify:weapons` Exit 1, identisch zur in Wave 1 dokumentierten Baseline
(177 Ships, 249 Waffeneinträge ohne Größe) — keine Verschlechterung. Alle im Plan-Verify
vorgegebenen `node -e`-Prüfungen (Feldurteil-Vollständigkeit, Schrittnummern-Protokoll,
Kernfeld-Deckung, DPS-Gegenprobe, Rollen-Abdeckung, Produktionszustand-Verteilung,
Namens-Join-Verdachtssuche) einzeln ausgeführt und bestanden.

---
*Phase: 01.4-fahrzeug-katalog-auf-spieldaten*
*Completed: 2026-08-03*
