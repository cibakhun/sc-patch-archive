---
phase: 01.1-ambiente-effekte-stilllegen
plan: 03
type: execute
wave: 3
depends_on: ["01.1-01", "01.1-02"]
files_modified:
  - scripts/strip-cursorglow.mjs
  - scripts/verify-fx.mjs
  - package.json
  - src/pages/*.astro
  - src/pages/de/*.astro
  - src/pages/account/*.astro
  - src/pages/de/account/*.astro
  - src/pages/topics/*.astro
  - src/pages/de/topics/*.astro
  - src/pages/patches/*.astro
  - src/pages/de/patches/*.astro
  - src/components/MissionDetail.astro
  - src/components/pilot/PilotPage.astro
autonomous: false
requirements: [FX-01, FX-07, FX-08]
user_setup: []

must_haves:
  truths:
    - "Auf keiner gebauten Seite folgt dem Mauszeiger noch ein Schein — weder sichtbar noch als Rechenlast (FX-01, D-01)."
    - "Bei einer Mausbewegung schreibt kein Skript der Site mehr Positionswerte auf das Wurzelelement (FX-01, D-02)."
    - "Der Holo-Viewer-Orbit und der Bildzuschnitt im Konto funktionieren unveraendert weiter (FX-08, D-03)."
    - "Parallaxe, Ken Burns, Scanlines, Vignette und Scroll-Reveal sind unveraendert; die Seite wirkt nicht nackt (FX-07)."
    - "Der gebaute Ausgabestand enthaelt in DE und EN dieselbe Anzahl der neuen Effekt-Bezeichner und null Reste des alten Effekts (SYNC-01)."
    - "Der stille Not-Ausgang per Abfragezeichenkette ist verschwunden; es gibt genau EINEN Mechanismus, die Effekte abzuschalten (D-01 sinngemaess, RESEARCH Pitfall 4)."
  artifacts:
    - scripts/strip-cursorglow.mjs
    - scripts/verify-fx.mjs
  key_links:
    - "Der Beleg ist der GEBAUTE Ausgabestand unter dist/, nicht die Quelle — eine Fundstelle kann in einer .astro-Datei verschwinden und ueber eine Komponente wieder hereinkommen."
    - "Die Negativ-Suchen umfassen Kommentare. Ein erklaerender Kommentar mit dem entfernten Bezeichner laesst die Abnahme reissen — das ist Absicht, nicht ein Fehler der Pruefung."
    - "scripts/ behaelt den Bezeichner legitim (der Codemod heisst danach und tokenize-theme-colors.mjs fuehrt ihn in einer Klassifikationsliste). Die Suchen duerfen deshalb NUR src, assets und dist umfassen."
---

<objective>
Den Schein um den Mauszeiger aus 107 Dateien tilgen — Element, Regel und Zeiger-Horcher —
und beweisen, dass die Phase in beiden Sprachfassungen gleich weit ist.

Purpose: Der sichtbare Teil (das Element) ist der billigste. Der teure ist der Horcher,
der bei jeder Mausbewegung zwei Werte auf `<html>` schreibt und damit eine
Stil-Neuberechnung am Wurzelelement ausloest — genau die Rechenlast, um die es in der
Rueckmeldung geht. Wer nur den Div loescht, liefert eine Seite, die behoben AUSSIEHT.

Output: ein ueberpruefbarer Codemod, 107 gesaeuberte Dateien, das Ende des stillen
Not-Ausgangs, ein bleibendes Pruefskript fuer den gebauten Stand, und eine
Sichtprueferrunde, die sicherstellt, dass die Seite dabei nicht ihre Bildsprache
verloren hat.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-CONTEXT.md
@.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-RESEARCH.md
@.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-PATTERNS.md
@.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-01-SUMMARY.md
@.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-02-SUMMARY.md
</context>

<tasks>

<!-- planner-discipline-allow: cursorglow -->
<!-- planner-discipline-allow: pointermove -->
<!-- planner-discipline-allow: --mx -->
<!-- planner-discipline-allow: --my -->
<!-- planner-discipline-allow: nofx -->

<task type="auto">
  <name>Task 1: Codemod — Element, Regel, Horcher und Not-Ausgang aus 107 Dateien</name>

  <read_first>
    - scripts/tokenize-theme-colors.mjs (Zeilen 1-40 = Kopfkommentar, CLI-Konvention, Hausregel „was sich nicht sicher zuordnen laesst, wird gemeldet statt angefasst"; Zeile 34-35 = Import-Form der Dateisuche)
    - scripts/gate-patch-fx.mjs (das Ergebnis von Plan 02 Task 2 — dieselbe Bauform, dieselbe Anker-Disziplin, hier fuer Loeschungen statt Ersetzungen)
    - src/pages/patches/sc-4-0-0.astro (alle drei Fundstellen in einer Datei: die Regel im inline `<style>`, das Element im Rumpf, der Zeiger-Horcher im inline `<script>`)
    - src/pages/topics/4-0-0-pyro-system.astro (eine Datei mit NUR dem Element — die Regel liegt dort in assets/detail.css, die Plan 01 bereits geraeumt hat)
    - src/pages/item-finder.astro (Zeilen 46-47 — der Not-Ausgang per Abfragezeichenkette, in 10 Dateien byte-gleich)
    - src/components/MissionDetail.astro und src/components/pilot/PilotPage.astro (die beiden Komponenten unter den 107 — kein Sonderfall, aber sie liegen nicht unter src/pages/)
  </read_first>

  <files>scripts/strip-cursorglow.mjs, 107 Dateien unter src/pages/** und src/components/** (siehe files_modified)</files>

  <action>
Neues Skript `scripts/strip-cursorglow.mjs`, gebaut nach `scripts/tokenize-theme-colors.mjs`:
deutscher Kopfkommentar, `--dry` fuer den reinen Bericht, `--only=<pfadteil>` zum
Eingrenzen, Dateisuche per `glob` aus `node:fs/promises` ueber `src/**/*.astro`.

Vier Loeschmuster. Jedes Muster hat eine ERWARTETE Gesamtzahl; weicht die tatsaechliche
Zahl ab, wird NICHTS geschrieben, sondern die Abweichung mit Dateiname und Anzahl
gemeldet, und das Skript endet mit Fehlerstatus. Innerhalb einer Datei gilt: das Muster
muss genau einmal vorkommen, sonst wird DIESE Datei uebersprungen und gemeldet.

  Muster A — das Element. Die Zeile mit dem `<div>`, das die Klasse `cursorglow` und
    `aria-hidden` traegt. Erwartete Gesamtzahl: 107 Dateien.
  Muster B — die Regel im inline `<style>`. Beginnt mit `.cursorglow{` und endet bei der
    naechsten schliessenden geschweiften Klammer. Der Farbwert INNERHALB der Regel ist
    pro Seite verschieden (jede Patch-Seite hat ihre eigene Akzentfarbe) — deshalb wird
    auf die Regelstruktur gematcht, nicht auf eine feste Zeichenkette. Erwartete
    Gesamtzahl: 38 Dateien (die 39. Fundstelle lag in `assets/detail.css` und ist in
    Plan 01 gefallen).
  Muster C — der Zeiger-Horcher. Der `addEventListener`-Aufruf auf `pointermove`, dessen
    Rumpf `setProperty` mit den beiden Positions-Eigenschaften `--mx` und `--my` auf
    `document.documentElement.style` ruft. Erwartete Gesamtzahl: 38 Dateien (die 39. lag
    in `assets/detail.js` und ist in Plan 01 gefallen). Das Skript darf NUR Horcher
    treffen, deren Rumpf diese beiden Eigenschaften nennt — jeder andere `pointermove`
    bleibt unangetastet (D-03/FX-08).
  Muster D — der Not-Ausgang. Das inline `<script>`, das `location.search` auf `nofx`
    prueft und dabei die Sternen-Leinwand entfernt, mitsamt der Zeile. Erwartete
    Gesamtzahl: 10 Dateien. Begruendung fuer die Entfernung (Claude's Discretion, in
    CONTEXT.md ausdruecklich freigestellt): er deckt nur 10 von 107 Seiten ab, ueberdauert
    keinen Seitenwechsel, kennt weder das Archiv noch die Patch-Seiten noch die
    Ember-Leinwand, und die neue gespeicherte Wahl aus Plan 01 leistet dasselbe fuer die
    ganze Site. Zwei Mechanismen nebeneinander sind der sicherste Weg, dass spaeter
    jemand den schwaecheren testet und den staerkeren fuer kaputt haelt (RESEARCH
    Pitfall 4). Die Leinwand selbst bleibt stehen — geloescht wird nur das Skript, das
    sie entfernte (D-07).

Beim Loeschen entstehende Leerzeilen mit aufraeumen, damit der Diff sauber liest.

KEINE GRABSTEIN-KOMMENTARE. An keiner Stelle darf ein Kommentar zurueckbleiben, der einen
der entfernten Bezeichner woertlich nennt — auch nicht als Erklaerung, auch nicht als
„hier stand frueher". Die Abnahme sucht negativ ueber die GANZEN Dateien inklusive
Kommentare; ein gut gemeinter Hinweis laesst sie reissen. Die Begruendung steht in dieser
Planungsakte und im SUMMARY, nicht im Quelltext.

Berichtsformat: pro Datei eine Zeile mit Pfad und den vier Musterzaehlungen, am Ende vier
Summenzeilen und ein Soll/Ist-Vergleich gegen 107 / 38 / 38 / 10.

Ablauf: erst `node scripts/strip-cursorglow.mjs --dry`, den Bericht gegen die vier
Sollzahlen halten, dann derselbe Aufruf ohne `--dry`, dann `git diff --stat` ansehen.

NICHT anfassen (D-03, FX-08): `assets/holo-viewer.js` und `src/scripts/account-dashboard.ts`
liegen ausserhalb des Suchpfads `src/**/*.astro` bzw. treffen Muster C nicht, weil ihr
Horcher-Rumpf die beiden Positions-Eigenschaften nicht nennt. Beide werden in der Abnahme
positiv gegengeprueft.

NICHT anfassen, bewusst: In `scripts/tokenize-theme-colors.mjs` steht der Klassenname in
einer Liste von Selektor-Mustern (Zeile 62). Dieser Eintrag laeuft nach der Tilgung ins
Leere, ist aber harmlos, und `npm run theme` ist als nicht idempotent bekannt — die
Generatorkette wird in dieser Phase nicht angefasst. Das ist eine Entscheidung, kein
Uebersehen, und gehoert ins SUMMARY.
  </action>

  <verify>
    <automated>node scripts/strip-cursorglow.mjs --dry</automated>
    <automated>npm run build</automated>
    <automated>test $(grep -rl cursorglow src assets | wc -l) -eq 0</automated>
    <automated>test $(grep -rl cursorglow dist | wc -l) -eq 0</automated>
    <automated>test $(grep -rl -- '--mx' src assets dist | wc -l) -eq 0</automated>
    <automated>test $(grep -rl -- '--my' src assets dist | wc -l) -eq 0</automated>
    <automated>test $(grep -rl nofx src dist | wc -l) -eq 0</automated>
    <automated>test $(grep -rl pointermove src/pages | wc -l) -eq 0</automated>
    <automated>test $(grep -c pointermove assets/holo-viewer.js) -ge 1</automated>
    <automated>test $(grep -c pointermove src/scripts/account-dashboard.ts) -ge 1</automated>
    <automated>npm run test:e2e</automated>
  </verify>

  <acceptance_criteria>
    - Der `--dry`-Bericht meldet exakt 107 / 38 / 38 / 10 Fundstellen fuer die vier Muster. Jede Abweichung bricht ab; der Bericht wird woertlich ins SUMMARY uebernommen.
    - `grep -rl cursorglow src assets | wc -l` == 0 (Suchpfad bewusst OHNE `scripts/`: dort traegt der Codemod den Namen selbst und `tokenize-theme-colors.mjs` fuehrt ihn legitim in einer Klassifikationsliste).
    - `grep -rl cursorglow dist | wc -l` == 0 — der gebaute Stand ist der eigentliche Beleg, nicht die Quelle.
    - `grep -rl -- '--mx' src assets dist | wc -l` == 0 und dasselbe fuer `--my`.
    - `grep -rl pointermove src/pages | wc -l` == 0.
    - `grep -c pointermove assets/holo-viewer.js` >= 1 UND `grep -c pointermove src/scripts/account-dashboard.ts` >= 1 (FX-08/D-03 — die fremden Nutzer sind unversehrt).
    - `grep -rl nofx src dist | wc -l` == 0.
    - `grep -rl 'id="stars"' src/pages | wc -l` ist unveraendert gegenueber dem Stand vor diesem Task (die Leinwaende bleiben im Bestand, D-07) — Zahl vorher und nachher im SUMMARY nennen.
    - `npm run build` fehlerfrei, `npm run test:e2e` gruen.
    - `git diff --name-only` nennt ausser `scripts/strip-cursorglow.mjs` ausschliesslich `.astro`-Dateien unter `src/pages/` und `src/components/`.
    - FX-07: `git diff --numstat` zeigt fuer keine Datei mehr als 8 geaenderte Zeilen — mehr waere ein Zeichen, dass ein Muster ueber sein Ziel hinaus zugeschlagen und benachbarte Ambiente-Regeln erfasst hat.
  </acceptance_criteria>

  <done>
    Weder Quelle noch gebauter Stand kennen den Effekt noch: kein Element, keine Regel,
    kein Horcher, kein Not-Ausgang — und die beiden fremden Zeiger-Nutzer sind
    nachweislich unberuehrt.
  </done>

  <reversibility rating="reversible">
    Reine Loeschung in versionierten Dateien; ein `git revert` stellt alles her. Der
    Codemod bleibt als Beleg im Baum.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Bleibendes Pruefskript fuer den gebauten Stand und die Sprachparitaet</name>

  <read_first>
    - scripts/_verify.mjs (vollstaendig — die Hauskonvention „gegen den gebauten Stand pruefen", Ausgabeformat und Fehlerstatus)
    - package.json (der `scripts`-Block, insbesondere die vorhandenen `verify*`-Eintraege)
    - .planning/codebase/CONCERNS.md (Class A — 67 handgepflegte EN/DE-Paare, nichts im Build vergleicht sie; das ist der Grund, warum dieses Skript existiert)
    - .planning/phases/01.1-ambiente-effekte-stilllegen/01.1-01-SUMMARY.md und 01.1-02-SUMMARY.md (die tatsaechlich verwendeten Bezeichner und Zahlen)
  </read_first>

  <files>scripts/verify-fx.mjs, package.json</files>

  <action>
Bisher belegt nichts im Build, dass DE und EN gleich weit sind. Nach dieser Phase haengen
40 Schleifen und 107 getilgte Fundstellen an einem Vertrag, den ein einzelner Tippfehler
in einer der beiden Sprachfassungen still zerreissen kann. Deshalb bleibt aus dieser
Phase ein Pruefskript zurueck, kein Einmal-Werkzeug.

Neues Skript `scripts/verify-fx.mjs` nach dem Vorbild von `scripts/_verify.mjs`: liest
den gebauten Stand unter `dist/`, prueft eine Liste von Zusicherungen, gibt je
Zusicherung eine Zeile mit Soll und Ist aus und endet mit Fehlerstatus, sobald eine
reisst. Zusicherungen:

  1. Null Vorkommen des getilgten Klassennamens in `dist/**/*.html`.
  2. Null Vorkommen der beiden getilgten Positions-Eigenschaften in `dist/**/*.html`
     und `dist/assets/*.js`.
  3. Null Vorkommen der Abfragezeichenkette des Not-Ausgangs in `dist/**/*.html`.
  4. Jede gebaute Seite, die die Umschalter-Klasse traegt, traegt sie mindestens zweimal
     (Leiste und Menuefuss) — sonst fehlt eine der beiden Montagestellen.
  5. Der Kopf-Schnipsel, der das Effekt-Attribut setzt, steht in jeder gebauten Seite VOR
     dem ersten Skript-Verweis auf ein Seiten-Asset (Zeichenposition vergleichen). Das ist
     die maschinelle Fassung von D-11.
  6. Sprachparitaet: fuer jede englische Seite unter `dist/` mit einem deutschen
     Gegenstueck unter `dist/de/` muessen die Zaehlungen von Umschalter-Klasse,
     Effekt-Attribut und Ereignisname paarweise UEBEREINSTIMMEN. Abweichungen werden als
     Paar mit beiden Zahlen gemeldet — das ist die Pruefung, die CONCERNS.md Class A
     bisher fehlte.
  7. Genau 38 gebaute Patch-Seiten (19 je Sprache) enthalten den Ereignisnamen.

Einen npm-Eintrag `verify:fx` ergaenzen, der `node scripts/verify-fx.mjs` ruft, direkt
neben den vorhandenen `verify`-Eintraegen. Das Skript setzt einen fertigen `dist/`-Stand
voraus und sagt das in seiner Fehlermeldung, falls `dist/` fehlt.

Das Skript wird NICHT in `npm run build` eingehaengt — der Build ist bereits eine lange
Kette, und eine Pruefung, die den Build blockiert, wird beim ersten Fehlalarm
herausgenommen. Es laeuft bewusst als eigener Aufruf.
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm run verify:fx</automated>
    <automated>test $(grep -c 'verify:fx' package.json) -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `npm run verify:fx` laeuft gegen einen frischen `dist/`-Stand durch und gibt fuer jede der sieben Zusicherungen eine Zeile mit Soll und Ist aus; die vollstaendige Ausgabe kommt ins SUMMARY.
    - Zusicherung 6 vergleicht mindestens 60 Seitenpaare (die 67 handgepflegten Paare aus CONCERNS.md abzueglich der Seiten ohne Gegenstueck) und meldet null Abweichungen.
    - Zusicherung 7 meldet exakt 38.
    - Gegenprobe: wird in EINER deutschen Seite testweise die Umschalter-Montage entfernt, meldet `npm run verify:fx` genau dieses Paar mit beiden Zahlen und endet mit Fehlerstatus. Aenderung danach verwerfen; Ergebnis der Gegenprobe im SUMMARY nennen. (Ohne diese Gegenprobe belegt ein gruenes Skript nur, dass es laeuft, nicht dass es prueft.)
    - `npm run verify:fx` liefert bei fehlendem `dist/` eine verstaendliche Meldung statt eines Stapelabzugs.
  </acceptance_criteria>

  <done>
    Der gebaute Stand ist maschinell darauf geprueft, dass der Effekt restlos weg ist und
    dass DE und EN dieselben Effekt-Bezeichner gleich oft tragen — und die Gegenprobe
    zeigt, dass die Pruefung tatsaechlich anschlaegt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sichtprueferrunde — ist die Seite ruhiger geworden, ohne nackt zu wirken?</name>

  <what-built>
    Ueber alle drei Plaene: der Schein um den Mauszeiger ist ersatzlos aus 107 Dateien
    verschwunden (Element, Regel, Horcher), alle 40 Partikel-Schleifen der Site starten
    nur noch auf ausdrueckliche Wahl, ein Umschalter fuer alle Besucher sitzt in der
    Kopfleiste und im Menuefuss, die Wahl ueberdauert Sitzung und Sprachwechsel,
    Abschalten wirkt sofort ohne Neuladen, und reduzierte Bewegung schlaegt die
    gespeicherte Wahl. Der stille Not-Ausgang per Abfragezeichenkette ist weg.

    Die Rueckmeldung, aus der diese Phase entstand, enthielt ausdrueckliches LOB fuer die
    Bildsprache: „I like the overall look of the entire site and that the design and fonts
    don't change too much from section to section." Eine Seite, die hinterher nackt
    aussieht, hat die Aufgabe verfehlt — auch wenn alle Zahlen stimmen. Diese Runde prueft
    genau das, und nur das; die Zahlen sind bereits maschinell belegt.
  </what-built>

  <how-to-verify>
    Vorbereitung: `npm run build && npm run preview`, dann im Browser localStorage leeren.

    1. Startseite http://localhost:4321/ — sieht die Seite vollstaendig aus? Der Schein
       lag mit `z-index: 8800` ueber allem; sein Wegfall kann Kontraste und
       Farbwahrnehmung veraendern. Wirkt irgendetwas flach oder unfertig?
    2. Werkzeugseite http://localhost:4321/item-finder.html — die Seite, um die es in der
       Rueckmeldung ging. Eine Liste durchscrollen und einen Wert suchen: ist es ruhiger?
    3. Patch-Archiv http://localhost:4321/archiv.html — die Sternen-Reise ist der
       gestalterische Kern dieser Seite. Ohne Sterne: traegt sie noch? Falls sie ohne
       Sterne leer wirkt, ist das ein Befund fuer die Phase, kein Fehler dieser Umsetzung
       — bitte so benennen.
    4. Eine Patch-Seite mit eigener Designwelt, z. B.
       http://localhost:4321/patches/sc-4-2-0.html (Regen und Blitz) und
       http://localhost:4321/patches/sc-4-9-0.html (Staubflirren).
    5. Den Umschalter suchen: findet man ihn ohne Anleitung? Ist beschriftet, wofuer er
       da ist? Einmal ein- und ausschalten und hinsehen, ob der Wechsel sauber aussieht
       (kein Zucken, kein stehengebliebenes Bild).
    6. Das Fenster schmal ziehen (unter 640 px): der Umschalter muss im ausgefahrenen
       Menue erreichbar sein.
    7. Dieselbe Stichprobe auf der deutschen Seite: http://localhost:4321/de/,
       /de/item-finder.html, /de/archiv.html.

    Bitte ausdruecklich Rueckmeldung zu: (a) wirkt irgendeine Seite nackt oder unfertig,
    (b) ist der Umschalter auffindbar und verstaendlich beschriftet, (c) gibt es einen
    sichtbaren Unterschied zwischen der deutschen und der englischen Fassung.
  </how-to-verify>

  <resume-signal>
    „freigegeben" tippen, oder die Befunde beschreiben (Seite, was daran stoert).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codemod → 107 Quelldateien | Ein Skript loescht in 107 handgepflegten, versionierten Seiten; ein zu weit gefasstes Muster nimmt benachbarte Regeln mit, und niemand liest 107 Diffs Zeile fuer Zeile |
| Quelle → gebauter Stand | Eine Fundstelle kann in der Quelle verschwinden und ueber eine Komponente wieder in den Ausgabestand kommen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-08 | Tampering | `scripts/strip-cursorglow.mjs` | medium | mitigate | Vier Muster mit fest erwarteten Gesamtzahlen (107/38/38/10) und Zwang auf genau eine Fundstelle je Datei; jede Abweichung bricht ab statt zu raten. Pflicht-`--dry`-Lauf. Zusaetzlich `git diff --numstat` mit einer Obergrenze von 8 geaenderten Zeilen je Datei, damit ein entgleistes Muster auch dann auffaellt, wenn der Build gruen bleibt. |
| T-01.1-09 | Tampering | Muster C (Zeiger-Horcher) | high | mitigate | Das Muster trifft NUR Horcher, deren Rumpf beide Positions-Eigenschaften nennt. Ein zu grobes `pointermove`-Muster wuerde den Holo-Viewer-Orbit und den Bildzuschnitt im Konto zerstoeren — zwei Funktionen, deren Ausfall erst beim Benutzen auffiele. Die Abnahme prueft beide Dateien POSITIV gegen (sie muessen ihren Horcher behalten), nicht nur negativ. |
| T-01.1-10 | Repudiation | Sprachparitaet DE/EN | medium | mitigate | Nichts im Build vergleicht die 67 handgepflegten Seitenpaare (CONCERNS.md Class A) — eine halb durchgezogene Aenderung faellt heute niemandem auf. `scripts/verify-fx.mjs` Zusicherung 6 vergleicht die Paare im gebauten Stand und wird per Gegenprobe (eine Montage testweise entfernen) auf Wirksamkeit geprueft. |
| T-01.1-11 | Information Disclosure | — | low | accept | Die Phase entfernt Code und fuegt eine Anzeigepraeferenz hinzu. Es werden keine Daten gelesen, gespeichert, uebertragen oder freigelegt. |
| T-01.1-SC | Tampering | npm/pip/cargo installs | low | accept | In diesem Plan wird kein Paket installiert; beide Skripte benutzen ausschliesslich Node-Bordmittel. Keine neue Lieferkette. |
</threat_model>

<verification>
1. `node scripts/strip-cursorglow.mjs --dry` meldet exakt 107 / 38 / 38 / 10.
2. `npm run build` fehlerfrei; `npm run test:e2e` gruen; `npm run verify:fx` gruen inklusive Sprachparitaet.
3. Negativ-Suchen ueber `src`, `assets` und `dist` sind alle null; die beiden fremden Zeiger-Nutzer bestehen ihre Positiv-Gegenprobe.
4. Die Gegenprobe zu `verify-fx.mjs` (eine Montage testweise entfernen) laesst das Skript nachweislich reissen.
5. Sichtprueferrunde freigegeben.
</verification>

<success_criteria>
- FX-01 vollstaendig: Element, Regel und Zeiger-Horcher sind aus Quelle UND gebautem Stand verschwunden.
- FX-08 belegt: Holo-Viewer-Orbit und Konto-Bildzuschnitt behalten ihre eigenen Zeiger-Horcher.
- FX-07 belegt: kein Diff ausserhalb der vier Loeschmuster; Parallaxe, Ken Burns, Scanlines, Vignette und Scroll-Reveal unangetastet.
- DE und EN sind maschinell als gleich weit nachgewiesen, zum ersten Mal in diesem Projekt.
- Ein Mensch hat bestaetigt, dass die Seite ruhiger, aber nicht nackt geworden ist.
</success_criteria>

## Artifacts this phase produces

| Art | Name | Ort |
|-----|------|-----|
| Skript | `scripts/strip-cursorglow.mjs` (Aufrufformen `--dry`, `--only=<pfadteil>`) | neu |
| Skript | `scripts/verify-fx.mjs` (7 Zusicherungen gegen `dist/`) | neu |
| npm-Eintrag | `verify:fx` | package.json |

Entfernte Bezeichner — nachgelagerte Pruefungen duerfen ihr Fehlen NICHT als Regression
werten: die CSS-Klasse `cursorglow`, die CSS-Eigenschaften `--mx` und `--my`, die
Abfragezeichenkette `nofx` samt der 10 zugehoerigen inline-Skripte.

Bewusst NICHT entfernt: der Eintrag des Klassennamens in der Selektor-Klassifikationsliste
von `scripts/tokenize-theme-colors.mjs` (Zeile 62). Er laeuft ins Leere, ist harmlos, und
die Generatorkette (`npm run theme`) gilt als nicht idempotent und bleibt in dieser Phase
unberuehrt.

## Nachgelagerter Hinweis fuer Phase 3

Diese Phase entfernt eine dekorative Deckkraftschicht, die site-weit ueber allem lag
(`z-index: 8800`). Phase 3 zaehlt genau solche Schichten — ihre Bestandsaufnahme muss NACH
dieser Phase erhoben werden, sonst misst sie einen Zustand, den es nicht mehr gibt.
Nach Abschluss in STATE.md unter „Pending Todos" vermerken.

<output>
Create `.planning/phases/01.1-ambiente-effekte-stilllegen/01.1-03-SUMMARY.md` when done.
Den `--dry`-Bericht des Codemods, die vollstaendige Ausgabe von `npm run verify:fx`, das
Ergebnis der Paritaets-Gegenprobe und die Rueckmeldung aus der Sichtprueferrunde woertlich
aufnehmen.
</output>
