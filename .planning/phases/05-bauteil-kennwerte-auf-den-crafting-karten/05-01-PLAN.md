---
phase: 05-bauteil-kennwerte-auf-den-crafting-karten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/crafting.ts
  - src/components/CraftingApp.astro
  - tests/e2e/crafting-specs.test.js
  - scripts/verify-crafting-specs.mjs
  - package.json
autonomous: true
requirements: [CRAFT-01, CRAFT-02, CRAFT-03]

must_haves:
  truths:
    - "Auf den 57 Quantumdrive-Karten der Crafting-Datenbank stehen Groesse, Grade und Ton sichtbar auf der Karte selbst (SC1, SC2)"
    - "Die Karte Frontline zeigt Groesse und Grade, aber keinen Ton-Chip — eine fehlende Angabe bleibt leer statt geraten (SC3)"
    - "Die 15 gleichnamigen Blueprints sind maschinell einzeln geprueft; keiner zeigt die Kennwerte eines anderen Items (SC4)"
    - "Die deutsche und die englische Seite zeigen dieselben Chips, weil beide aus EINEM Koerper gerendert werden (SC5)"
    - "Die Seite bleibt statisch ausgeliefert; die Grundlinie des Seitengewichts ist gemessen und notiert (SC7)"
  artifacts:
    - path: "src/lib/crafting.ts"
      provides: "blueprintSpecs() — die einzige Quelle fuer Groesse/Grade/Ton eines Blueprints"
      contains: "export function blueprintSpecs"
    - path: "src/components/CraftingApp.astro"
      provides: "Chip-Reihe ul.cbp__spec auf der Blueprint-Karte plus CSS"
      contains: "cbp__spec"
    - path: "tests/e2e/crafting-specs.test.js"
      provides: "Nachweis am GEBAUTEN dist/ — Referenzkarten und Chip-Zahlen, DE und EN"
      contains: "dist/topics/crafting.html"
    - path: "scripts/verify-crafting-specs.mjs"
      provides: "Datengatter: Referenzwerte, 15 Dubletten, SizeN-Gegenprobe, Wertebereiche"
      contains: "process.exit(1)"
  key_links:
    - from: "src/components/CraftingApp.astro"
      to: "src/lib/crafting.ts"
      via: "cardsData ruft blueprintSpecs(b) beim Kartenaufbau"
      pattern: "blueprintSpecs"
    - from: "src/lib/crafting.ts"
      to: "src/lib/items.ts"
      via: "itemForBlueprint() + hasGradeSemantics() als Semantik-Tor"
      pattern: "hasGradeSemantics"
---

<objective>
Der erste durchgehende Pfad: ein Bauteiltyp — die 57 Quantumdrives — traegt Groesse,
Grade und Ton sichtbar auf der Karte, von den Spieldaten bis ins gebaute HTML, in DE
und EN, mit laufenden Pruefungen. Erst wenn dieser Pfad steht und gemessen ist, wird
er in Plan 05-02 auf alle 1594 Karten ausgerollt.

Purpose: Die Chip-Kette (Daten → Helfer → Markup → CSS → gebautes HTML) einmal
vollstaendig beweisen, solange nur 57 Karten betroffen sind. Ein Fehler im
Namens-Join oder im Markup-Vertrag faellt hier auf, nicht nach 1594 Karten. Dazu das
Korrektheitsgatter fuer die 15 gleichnamigen Blueprints, das groesste Risiko der
Phase (CRAFT-03).

Output: `blueprintSpecs()` in `src/lib/crafting.ts`, die Chip-Reihe `ul.cbp__spec` in
`CraftingApp.astro`, der dist-Test `tests/e2e/crafting-specs.test.js` und das
Datengatter `scripts/verify-crafting-specs.mjs` (`npm run verify:crafting`).
</objective>

## Nutzergeschichte

**Als** Spieler, der die Crafting-Datenbank durchblaettert,
**moechte ich** Groesse, Grade und Ton schon auf der Karte sehen,
**damit** ich erkenne, womit ich es zu tun habe, ohne erst in die Detailansicht zu klicken.

> Herkunft: woertlich aus der ROADMAP-Zeile `**Goal:**` von Phase 5 abgeleitet
> („Wer die Crafting-Datenbank durchblaettert, erkennt auf der Karte selbst, womit er
> es zu tun hat … statt erst nach einem Klick in der Detailansicht"). Die
> ROADMAP-Zeile steht nicht in der kanonischen „Als/moechte ich/damit"-Form; hier ist
> nichts hinzuerfunden, nur umgestellt. Wer die Form formal will, laesst
> `/gsd mvp-phase 5` darueber laufen.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bauteil-kennwerte-auf-den-crafting-karten/05-CONTEXT.md
@.planning/phases/05-bauteil-kennwerte-auf-den-crafting-karten/05-RESEARCH.md
</context>

## Gemessene Ausgangslage (nicht neu erheben)

Die folgenden Zahlen stammen aus der Erkundung und wurden bei der Planung gegen die
echten Dateien nachgerechnet. Sie sind Vorgabe, nicht Vermutung.

| Was | Wert |
|---|---|
| Blueprints gesamt | 1594 |
| Blueprints mit Quantumdrive-Kategorie | 57 (`Vehiclegear / Quantumdrive / Size1..4`) |
| davon mit Groesse / Grade / Ton | 57 / 57 / **56** — einzige Ausnahme: `Frontline` ohne Ton |
| Blueprints ohne Item-Treffer | 14 |
| gleichnamige Blueprint-Namen | 15 Gruppen (alle Mitglieder einer Gruppe teilen dieselbe Blueprint-Kategorie) |
| Wertebereich Grade | ausschliesslich `A`, `B`, `C`, `D` |
| Wertebereich Groesse | ganze Zahlen 0 bis 7 — `0` ist ein echter Wert (25 Karten, z. B. `Agure`), keine Luecke |

Referenzwerte (Froggys eigene Beispiele), alle aus `assets/universal-items.json`:

| Blueprint | Groesse | Grade | Ton | Item-Kategorie |
|---|---|---|---|---|
| Allegro | 4 | A | Civilian | Vehiclegear / Quantumdrive |
| Atlas | 1 | A | Civilian | Vehiclegear / Quantumdrive |
| Hemera | 2 | A | Civilian | Vehiclegear / Quantumdrive |
| Erebos | 3 | A | Civilian | Vehiclegear / Quantumdrive |
| Drift | 1 | C | Stealth | Vehiclegear / Quantumdrive |
| Lotus | 2 | A | Civilian | Vehiclegear / Powerplant *(erst in 05-02 sichtbar)* |
| Cassandra | 2 | A | Stealth | Vehiclegear / Radar *(erst in 05-02 sichtbar)* |
| Cirrus | 2 | C | Stealth | Vehiclegear / Powerplant *(erst in 05-02 sichtbar)* |

Die 15 gleichnamigen Blueprints und ihr gemeinsamer Item-Treffer:

`antium core jet`, `argus helmet black/silver`, `broadspec`, `cinch scraper module` (3x),
`dustup legs desert`, `foxfire`, `fullforce`, `glacis`, `inquisitor core olive`,
`main powerplant`, `serac`, `stellate`, `truedef-pro arms black/silver`,
`truedef-pro core black/silver`, `truedef-pro legs black/silver`.

## Markup-Vertrag (verbindlich fuer alle drei Plaene)

Der Vertrag ist bewusst zeichensparsam — 1523 Karten tragen ihn zweimal (DE und EN).

- Eine Chip-Reihe pro Karte: `<ul class="cbp__spec">`, direkt nach `.cbp__meta`.
- Groessen-Chip: `<li>` ohne Klasse, Inhalt `S` + Ganzzahl (`S4`, auch `S0`).
- Grade-Chip: `<li>` ohne Klasse, Inhalt genau ein Buchstabe `A`–`D`.
- Ton-Chip: `<li class="tone">`, Inhalt der Ton-Text (`Civilian`, `Ballistic`, …).
- Fehlt eine Angabe, fehlt das `<li>` ersatzlos. Fehlen alle drei, fehlt die ganze `<ul>`.

Groesse und Grade tragen keine eigene Klasse, weil ihr Inhalt sie eindeutig
unterscheidet (`/^S\d+$/` gegen `/^[A-D]$/`) und drei zusaetzliche Klassennamen ueber
1523 Karten rund 40 KB pro Sprachseite kosten wuerden. Damit diese Regel nicht still
brechen kann, sichert das Datengatter aus Aufgabe 2 den Wertebereich beider Felder ab.

<tasks>

<task type="tracer">
  <name>Aufgabe 1: Quantumdrive-Karten tragen Groesse, Grade und Ton — durchgehend bis ins gebaute HTML</name>

  <read_first>
    - `src/lib/crafting.ts` (Zeile 189–201: `itemForBlueprint`, der Namens-Join; Zeile 132–138: `craftRoot`/`craftLeaf`)
    - `src/lib/items.ts` (Zeile 74–103: Typ `ItemGame`; Zeile 358–365: `hasGradeSemantics()`)
    - `src/lib/itemStats.ts` (Zeile 57–70: `specChips()` — das Vorbild, das dieselben drei Felder auf der Item-Seite rendert)
    - `src/components/CraftingApp.astro` (Zeile 167–191: `cardsData`; Zeile 313–350: Karten-Markup samt der beiden Kommentare zur HTML-Diaet; Zeile 562–600: Karten-CSS und Listenansicht)
    - `tests/e2e/layout.test.js` (Zeile 1–25: wie ein Test das gebaute `dist/` liest)
  </read_first>

  <files>src/lib/crafting.ts, src/components/CraftingApp.astro, tests/e2e/crafting-specs.test.js</files>

  <precondition>`npm run build` laeuft in diesem Arbeitsverzeichnis unveraendert durch und erzeugt `dist/topics/crafting.html` sowie `dist/de/topics/crafting.html`. Schlaegt der Build schon vor jeder Aenderung fehl, anhalten und melden — dann ist die Grundlinie nicht messbar.</precondition>

  <reversibility rating="reversible">Der Markup-Vertrag (`ul.cbp__spec`, Unterscheidung ueber den Inhalt) wird spaeter nur von `assets/crafting-app.js` gelesen; eine Aenderung kostet zwei Dateien und keinen Datenlauf.</reversibility>

  <action>
Zuerst die Grundlinie messen, danach erst aendern.

**Grundlinie.** `npm run build` laufen lassen und fuer `dist/topics/crafting.html` und
`dist/de/topics/crafting.html` je die Rohgroesse und die gzip-Groesse in Bytes
notieren (`node -e` mit `fs.statSync` und `zlib.gzipSync`). Beide Zahlenpaare
woertlich in die SUMMARY schreiben — Plan 05-02 rechnet dagegen.

**Helfer.** In `src/lib/crafting.ts` neben `itemForBlueprint()` ergaenzen: das
Interface `BlueprintSpecs` mit den Feldern `size: number | null`, `grade: string | null`,
`tone: string | null` und die Funktion `blueprintSpecs(b: Blueprint): BlueprintSpecs | null`.
Regeln, alle aus den gesperrten Entscheidungen von `05-CONTEXT.md`:
Alle Werte kommen aus den bereits im Bestand liegenden JSON-Dateien; kein
Data.p4k-Lauf, keine Neu-Extraktion, keine Aenderung an `scripts/datamine-*.mjs`
(D-01). Item ueber `itemForBlueprint(b)` holen (D-02: der Namens-Join bleibt, die
guid-Route ist verworfen); `size` und `grade` nur uebernehmen, wenn
`hasGradeSemantics(item)` wahr ist (Recherche, Offener Punkt 4: sonst sind die Werte
konstant und damit eine erfundene Aussage); `size` mit `!= null` pruefen, damit die echte Groesse `0` erhalten
bleibt — genau wie `specChips()` in `itemStats.ts` es tut; `tone` ist `item.game.class`
(D-03: ein Feld `tone` gibt es nicht und wird nicht erfunden); fehlt ein Wert, bleibt
das Feld `null`, es wird nie geraten, nie `0` und nie ein Gedankenstrich eingesetzt
(D-06). Sind alle drei Felder `null`, gibt die Funktion `null` zurueck. Die
Ton-Ableitung fuer Schiffswaffen aus dem Kategorie-Pfad (D-04) kommt bewusst erst in
Plan 05-02 dazu — Ruestung faellt hier schon korrekt ohne Ton heraus, weil ihr
`class` in den Daten leer ist (D-05).

**Karte.** `CraftingApp.astro` ist EIN Koerper fuer DE und EN (D-07) — es gibt keine
zweite Fassung, die nachgezogen werden muesste, und jede Aenderung trifft beide
Sprachseiten im selben Arbeitsschritt. Kein neuer UI-Text nennt die Herkunft der
Daten aus den Spieldateien (D-08). In `CraftingApp.astro` `blueprintSpecs` importieren und in `cardsData` ein
Feld `sp` ergaenzen. Fuer diesen Plan gilt eine ausdrueckliche, in Plan 05-02 wieder
entfernte Ausrollsperre: eine Konstante `SPEC_ROLLOUT` mit dem Kategorie-Praefix der
Quantumdrives; `sp` bleibt fuer alle anderen Blueprints `null`. Die Konstante bekommt
einen einzeiligen Kommentar, der auf Plan 05-02 verweist. Im Karten-Markup nach
`.cbp__meta` die Chip-Reihe nach dem oben festgeschriebenen Markup-Vertrag rendern.
Achtung Astro-Falle: kein Element auf oberster Template-Ebene mit einem nackten
`{ausdruck}` als Kind — hier sitzt die Reihe tief im Kartenbaum, das ist unkritisch,
aber der Ton-Text gehoert trotzdem ueber `set:text` gesetzt, weil er aus den
Spieldaten kommt.

**CSS.** Im vorhandenen `<style is:inline>`-Block bei `.cbp__res` (Zeile 588–600) die
Regeln fuer `.cbp__spec` ergaenzen: dieselbe Chip-Optik wie `.cbp__res li`, der
Ton-Chip in der Zweitfarbe abgesetzt, dazu eine Regel fuer die Listenansicht
(`.cdb-grid.is-list .cbp__spec`). Ausschliesslich vorhandene Farb-Tokens verwenden
(`--veil`, `--line-soft`, `--muted`, `--text`, `--accent-2`) — kein neuer Farbliteral,
damit `npm run theme` keinen Hellmodus-Block neu erzeugen muss und der generierte
`:root[data-theme="light"]`-Block unangetastet bleibt. In `<style is:inline>` niemals
`:global()` verwenden, der Browser wirft die ganze Regel still weg.

**Test.** `tests/e2e/crafting-specs.test.js` neu anlegen, im Stil von
`tests/e2e/layout.test.js` (`node:test`, `node:assert`, liest `dist/` direkt). Er
prueft fuer BEIDE Sprachseiten: die fuenf Referenzkarten Allegro, Atlas, Hemera,
Erebos, Drift tragen die oben tabellierten drei Werte; `Frontline` traegt Groesse und
Grade, aber kein `li` mit der Ton-Klasse; die Seite enthaelt genau 57 Chip-Reihen; ein
Blueprint ausserhalb der Quantumdrives (z. B. `Lotus`) traegt in diesem Plan noch
keine. Der Test kommentiert im Kopf, dass die 57er-Zahl mit Plan 05-02 auf die
Gesamtzahl steigt.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node --test tests/e2e/crafting-specs.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/crafting.ts` exportiert `blueprintSpecs` und das Interface `BlueprintSpecs`; `grep -c "export function blueprintSpecs" src/lib/crafting.ts` ergibt 1
    - `npm run build` beendet sich mit Exit-Code 0
    - `node --test tests/e2e/crafting-specs.test.js` beendet sich mit Exit-Code 0
    - In `dist/topics/crafting.html` UND `dist/de/topics/crafting.html` traegt die Allegro-Karte einen Groessen-Chip mit dem Inhalt `S4`, einen Grade-Chip `A` und einen Ton-Chip `Civilian`
    - In beiden gebauten Seiten traegt die Drift-Karte `S1`, `C`, `Stealth`
    - In beiden gebauten Seiten traegt die Frontline-Karte Groesse und Grade, aber null Elemente mit der Ton-Klasse
    - In beiden gebauten Seiten kommt `cbp__spec` genau 57-mal vor
    - `git diff src/components/CraftingApp.astro` enthaelt kein neues Farbliteral der Form `#` plus sechs Hex-Ziffern und keine Zeile innerhalb eines `:root[data-theme="light"]`-Blocks
    - Grundlinie notiert: Roh- und gzip-Bytes beider Sprachseiten stehen in der SUMMARY
  </acceptance_criteria>

  <done>Wer die englische oder die deutsche Crafting-Datenbank oeffnet, sieht auf jeder Quantumdrive-Karte Groesse, Grade und Ton; die Karte Frontline zeigt zwei statt drei Chips; alle anderen Karten sind unveraendert.</done>
</task>

<task type="auto">
  <name>Aufgabe 2: Datengatter — die 15 gleichnamigen Blueprints und die Wertebereiche maschinell absichern</name>

  <read_first>
    - `src/lib/crafting.ts` (der in Aufgabe 1 ergaenzte `blueprintSpecs()` plus `blueprints`, `itemForBlueprint`)
    - `scripts/verify-item-prices.mjs` (Vorbild fuer ein Gatter-Skript: Befund sammeln, lesbar ausgeben, Exit 1)
    - `package.json` (Zeile 11–20 und 37: die vorhandenen `verify:*`-Eintraege und ihre Namensform)
    - `.planning/phases/05-bauteil-kennwerte-auf-den-crafting-karten/05-RESEARCH.md` (Abschnitt „Messung 1" — die 57/57-Gegenprobe, die hier zum Dauergatter wird)
  </read_first>

  <files>scripts/verify-crafting-specs.mjs, package.json</files>

  <action>
`scripts/verify-crafting-specs.mjs` neu anlegen. Das Skript arbeitet auf den JSON-Daten
(`assets/crafting-db.json`, `assets/universal-items.json`) und ruft dieselbe Logik auf,
die die Karten rendert — es liest die Werte nicht selbst zusammen, sondern importiert
`blueprints` und `blueprintSpecs` aus `src/lib/crafting.ts`, damit Gatter und Anzeige
nicht auseinanderlaufen koennen. Laeuft der Import einer `.ts`-Datei unter blossem
Node nicht, die Regeln stattdessen in einer klar als Spiegel gekennzeichneten Funktion
im Skript nachbilden und diesen Umstand im Kopfkommentar begruenden.

Geprueft wird, jeder Befund mit Blueprint-Name und erwartetem gegen gemessenen Wert:

1. **Referenzwerte** — die acht Beispiele aus der Tabelle oben (Allegro, Atlas,
   Hemera, Erebos, Lotus, Cassandra, Cirrus, Drift) liefern exakt die dort
   verzeichneten drei Werte.
2. **SizeN-Gegenprobe** — fuer jeden Blueprint, dessen Kategorie-String ein
   `SizeN`-Segment traegt (57 Stueck, ausschliesslich Quantumdrives), muss die aus dem
   Item gejointe Groesse gleich der im Kategorie-String kodierten sein. Erwartung:
   57 geprueft, 0 Abweichungen. Das ist die unabhaengige Gegenprobe des Namens-Joins.
3. **Die 15 gleichnamigen Blueprints** — fuer jede der 15 Namensgruppen: alle
   Mitglieder der Gruppe tragen dieselbe Blueprint-Kategorie (sonst zeigte ein
   Kartenpaar die Kennwerte zweier verschiedener Bauteile unter einem Namen), und
   `blueprintSpecs()` liefert fuer alle Mitglieder dasselbe Ergebnis. Die 15 Namen
   stehen als feste Liste im Skript, damit ein neuer, ungeprueft hinzugekommener
   Doppelname auffaellt statt still durchzurutschen: weicht die aus den Daten
   ermittelte Menge der Doppelnamen von der Liste ab, ist das ein Befund.
4. **Wertebereiche** — jeder ausgegebene Grade liegt in `A`, `B`, `C`, `D`; jede
   ausgegebene Groesse ist eine Ganzzahl von 0 bis 7. Diese Pruefung traegt den
   Markup-Vertrag: sie ist der Grund, warum Groessen- und Grade-Chip ohne eigene
   Klasse auskommen duerfen.
5. **Ruestung ohne Ton** — ueber alle 913 Blueprints mit Kategorie-Wurzel `Armour`
   liefert `blueprintSpecs()` null Toene (D-05).
6. **Abdeckung** — die Zahlen je Vehiclegear-Typ werden ausgegeben und gegen die
   Erwartung geprueft: Powerplant 73 von 75 mit Ton, Cooler 72 von 75, Shield 61 von 62,
   Radar 57 von 60, Quantumdrive 56 von 57. Weicht eine Zahl ab, hat sich die
   Datengrundlage bewegt — das ist ein Befund, kein stiller Durchlauf.

Ausgabe: eine Zeile je Pruefblock mit geprueft/abweichend, danach die Befunde im
Klartext. Ohne Befund Exit 0, mit Befund Exit 1. Kein UI-Text, aber auch im
Skriptkopf keine Nennung der Datenherkunft aus den Spieldateien — die Regel gilt
site-weit und `npm run audit:site` erzwingt sie.

In `package.json` bei den vorhandenen `verify:*`-Skripten
`"verify:crafting": "node scripts/verify-crafting-specs.mjs"` ergaenzen.
  </action>

  <verify>
    <automated>npm run verify:crafting</automated>
  </verify>

  <acceptance_criteria>
    - `npm run verify:crafting` beendet sich mit Exit-Code 0
    - Die Ausgabe nennt „57" geprueft und 0 Abweichungen fuer die SizeN-Gegenprobe
    - Die Ausgabe nennt 15 gepruefte Namensgruppen und 0 Abweichungen
    - Die Ausgabe nennt 913 gepruefte Armour-Blueprints mit 0 Toenen
    - Ein absichtlich verfaelschter Erwartungswert im Skript (z. B. Allegro auf Grade B) laesst den Lauf mit Exit-Code 1 enden; die Verfaelschung wird danach zurueckgenommen
    - `grep -c "verify:crafting" package.json` ergibt 1
  </acceptance_criteria>

  <done>`npm run verify:crafting` ist ein Dauergatter: ein falscher Kennwert auf einer Karte — durch Namenskollision oder durch einen kuenftigen Datenlauf — bricht den Lauf mit Exit-Code 1 und nennt den Blueprint beim Namen.</done>
</task>

</tasks>

## Artifacts this phase produces (Artefakte, die dieser Plan erzeugt)

| Art | Name | Ort |
|---|---|---|
| Typ | `BlueprintSpecs` (`size`, `grade`, `tone`) | `src/lib/crafting.ts` |
| Funktion | `blueprintSpecs(b: Blueprint): BlueprintSpecs \| null` | `src/lib/crafting.ts` |
| Datenfeld | `sp` im Kartenmodell `cardsData` | `src/components/CraftingApp.astro` |
| Konstante | `SPEC_ROLLOUT` (nur fuer diesen Plan, in 05-02 entfernt) | `src/components/CraftingApp.astro` |
| CSS-Klasse | `.cbp__spec` (Chip-Reihe) | `src/components/CraftingApp.astro` |
| CSS-Klasse | `.cbp__spec li` (Groessen- und Grade-Chip) | `src/components/CraftingApp.astro` |
| CSS-Klasse | `.cbp__spec li.tone` (Ton-Chip) | `src/components/CraftingApp.astro` |
| CSS-Regel | `.cdb-grid.is-list .cbp__spec` (Listenansicht) | `src/components/CraftingApp.astro` |
| Datei | `tests/e2e/crafting-specs.test.js` | neu |
| Datei | `scripts/verify-crafting-specs.mjs` | neu |
| npm-Skript | `verify:crafting` | `package.json` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spieldaten-JSON → gerendertes HTML | `assets/universal-items.json` und `assets/crafting-db.json` liefern Text, der zur Bauzeit in die Karte geschrieben wird |
| gebautes HTML → Besucher | statisch ueber nginx, keine Nutzereingabe in diesem Plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Ton-Text aus `game.class` im Karten-Markup | low | mitigate | Der Ton-Text wird ueber `set:text` gesetzt, nicht ueber `set:html`; Astro escapet damit den Wert |
| T-05-02 | Information Disclosure | Kommentare und Ausgaben des neuen Gatter-Skripts | medium | mitigate | Keine Nennung der Datenherkunft aus den Spieldateien in UI-Text oder Skriptkopf; `npm run audit:site` prueft das als FEHLER |
| T-05-03 | Repudiation | falscher Kennwert durch Namenskollision | high | mitigate | `scripts/verify-crafting-specs.mjs` prueft die 15 Dubletten und die 57 SizeN-Gegenproben bei jedem Lauf |

Keine Paketinstallation in diesem Plan — kein Legitimitaets-Gatter noetig.
</threat_model>

<verification>
1. `npm run build` — Exit 0
2. `node --test tests/e2e/crafting-specs.test.js` — Exit 0
3. `npm run verify:crafting` — Exit 0
4. Grundlinie des Seitengewichts (roh + gzip, DE und EN) in der SUMMARY notiert
</verification>

<success_criteria>
- Die 57 Quantumdrive-Karten tragen in DE und EN Groesse, Grade und Ton
- `Frontline` traegt zwei Chips statt drei — kein leerer, kein geratener Ton
- Kein Blueprint ausserhalb der Quantumdrives traegt in diesem Plan eine Chip-Reihe
- Die 15 gleichnamigen Blueprints sind maschinell geprueft und unauffaellig
- Die Grundlinie des Seitengewichts ist gemessen und schriftlich festgehalten
</success_criteria>

<output>
Create `.planning/phases/05-bauteil-kennwerte-auf-den-crafting-karten/05-01-SUMMARY.md` when done.
Die gemessenen Grundlinien-Bytes (roh und gzip, `dist/topics/crafting.html` und
`dist/de/topics/crafting.html`) gehoeren woertlich hinein — Plan 05-02 rechnet dagegen.
</output>
