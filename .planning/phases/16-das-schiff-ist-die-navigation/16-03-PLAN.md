---
phase: 16-das-schiff-ist-die-navigation
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - src/components/ShipDetail.astro
  - src/i18n/ui.ts
  - scripts/probes/schiffskonsole-messung.mjs
autonomous: true
requirements: [D-02, D-01, P-3]

must_haves:
  truths:
    - "Ohne JavaScript zeigt jede der 227 Schiffsseiten je Sprache alle Systeme untereinander als schlichte, lesbare Liste — nicht eine leere Flaeche (D-02). Belegt mit ABGESCHALTETEM JavaScript im Browser gegen das gebaute `dist/`, nicht durch Lesen des Codes (`16-CONTEXT.md` Konkrete Vorgabe 2)."
    - "Jedes System steht als eigener, sichtbarer Abschnitt im ausgelieferten HTML — nicht per JavaScript erzeugt, nicht per Versteck-Attribut vorbelegt, nicht in einer Vorlagenhuelle (Detailvertrag Punkt 6.1)."
    - "Die Rail ist im Grundzustand eine reine Ankerliste und funktioniert vollstaendig ohne Skript — exakt das Muster der Kapitel-Sprungleiste aus Phase 14, unveraendert uebernommen."
    - "Kein Bauteiltext steht zweimal im ausgelieferten HTML (D-01/Detailvertrag Punkt 6.2): Bewaffnung und Bauteilliste wandern aus dem Ausstattungs-Kapitel IN die Konsole und bleiben nicht zusaetzlich dort stehen. `verify:shipcard` sieht seit Welle 2 in beide Bloecke und wuerde die Dopplung melden."
    - "Der indexierbare Textbestand je Schiffsseite ist NICHT kleiner geworden (Erfolgskriterium 3) — gemessen mit demselben Verfahren wie der Ausgangswert aus Welle 1, vorher und nachher gegenuebergestellt. Erwartet wird ein leichter Anstieg, weil `prop` und `other` erstmals eine ausgeschriebene Gruppenauflistung bekommen."
    - "Kein Systemeintrag ohne Marker (P-3): die Rail fuehrt ausschliesslich Systeme, fuer die das ausgelieferte `holodata` derselben Seite Marker enthaelt, und die Zahl am Eintrag ist diese Markerzahl."
    - "Die Systemliste ist nicht doppelt gepflegt: die Gruppenzustaende fuer Bewaffnung und Bauteile benutzen dieselben serverseitig vorgerechneten Datenstrukturen, aus denen bisher das Ausstattungs-Kapitel gebaut wurde — keine zweite Aggregation fuer dieselbe Aussage (Detailvertrag Punkt 4a)."
    - "Jede neue sichtbare Zeichenkette hat DE und EN in `src/i18n/ui.ts`; die deutsche und die englische Fassung sind strukturell deckungsgleich (Erfolgskriterium 7)."
    - "Datenherkunft taucht in keinem der neuen sichtbaren Texte auf — `audit:site` erzwingt das als FEHLER."
    - "Der Kapitelbestand aus Phase 14 bleibt gueltig: weiterhin genau eine Sprungleiste und hoechstens vier Kapitel mit den bekannten Kennungen; `ch-gear` traegt danach drei statt vier Unterabschnitte, und der aggregierte Widerstand bekommt darin einen eigenen, benannten Unterabschnitt statt ersatzlos zu verschwinden (Detailvertrag Punkt 9)."
  artifacts:
    - src/components/ShipDetail.astro
  key_links:
    - "`.holo` <-> `.holo__wrap`: `.holo` traegt heute die feste Hoehe und `overflow:hidden` (Z. 462). Wachsen die Systemabschnitte darin, werden sie abgeschnitten. Hoehe und Beschnitt gehoeren deshalb an die Buehne, nicht an den Block — und mit ihnen die beiden absolut positionierten Dekorationen `.holo__grid` und `.holo__pad`, die sonst an den Fuss des GANZEN Blocks rutschen statt unter das Modell."
    - "Systemabschnitte <-> `verify:shipcard`: die Abschnitte liegen in `section.holo`, und seit `15-02` zerlegt der Entdopplungs-Scan genau diesen Block mit. Was hier gedoppelt stehen bleibt, faellt dort auf — das ist Absicht und die einzige Absicherung gegen D-01s Risiko."
    - "`viz.defense` <-> Zusicherung 5 von `verify:shipcard`: die Widerstandswerte stehen in einem gewoehnlichen Wertraster (`dl.sd__grid`/`.sd__item`), NICHT in einer Balkenspur. Wer sie beim Umzug in eine Spur verwandelt, reisst die Zusicherung „Balken nur, wo sie vergleichen"."
    - "`t()` <-> `audit:site`: jede neue Zeichenkette geht durch `src/i18n/ui.ts` in beiden Sprachen. Ein fehlender EN-Schluessel faellt nicht durch einen Uebersetzungsfehler auf, sondern durch eine leere Stelle auf 227 englischen Seiten."
---

<objective>
Die Konsole entsteht zuerst als das, was sie ohne JavaScript ist: eine
schlichte, vollstaendig lesbare Liste im ausgelieferten HTML.

Purpose: D-02 ist die Bedingung, unter der D-01 ueberhaupt sicher ist. Die
Konsole darf die vier Kapitel nur als Seitenaufbau ersetzen, solange jede
Auslesung im Quelltext steht — bei rund 17.000 indexierbaren Seiten und einem
Zulauf, der praktisch vollstaendig aus Suche kommt (30 Tage: Bing 67, DDG 38,
ChatGPT 10, Google 7), nimmt eine im Quelltext leere Schiffsseite den Zulauf
mit. Deshalb entsteht die lesbare Fassung ZUERST und die Bedienung erst
darauf — nicht umgekehrt. Wer die Interaktion zuerst baut, hat den Beweis fuer
D-02 nie in der Hand.

Diese Welle bewegt ueberwiegend TEXT: Bewaffnung und Bauteilliste wandern aus
dem Ausstattungs-Kapitel in die Konsole, und `prop` und `other` bekommen zum
ersten Mal eine ausgeschriebene Gruppenauflistung, die es im Kapitel-Aufbau von
Phase 14 gar nicht gab. Netto ist keine Streichung vorgesehen.

Output: bis zu vier Systemabschnitte je Schiffsseite im ausgelieferten HTML, die
Rail als Ankerliste, ein um zwei Unterabschnitte erleichtertes und um einen
Widerstands-Unterabschnitt ergaenztes Ausstattungs-Kapitel, vier neue
i18n-Schluessel in beiden Sprachen — und der Beweis mit abgeschaltetem
JavaScript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-das-schiff-ist-die-navigation/16-CONTEXT.md
@.planning/phases/16-das-schiff-ist-die-navigation/16-UI-SPEC.md
@.planning/phases/16-das-schiff-ist-die-navigation/16-01-SUMMARY.md
@.planning/phases/16-das-schiff-ist-die-navigation/16-02-SUMMARY.md

@CLAUDE.md
</context>

<planning_notes>
**Bewusst hingenommener Zwischenzustand.** Nach dieser Welle ist die Rail eine
Ankerliste ohne Filterwirkung: die Buehne zeigt weiterhin die bisherige
Startauswahl (Komponenten und Bewaffnung), und ein Klick auf einen Rail-Eintrag
springt zum Textabschnitt, statt die Buehne umzuschalten. Der bisherige
Ebenen-Kasten wird in derselben Welle entfernt, damit nicht zwei Rails
nebeneinander stehen. Das ist ein ehrlicher Zwischenstand und keine Luecke: die
Einfachauswahl und die Umschaltung der Buehne sind Welle 4, und bis dahin ist
die Seite vollstaendig benutzbar und vollstaendig lesbar. Der Zwischenstand ist
im SUMMARY zu benennen.

**Kein Grid in dieser Welle.** Das dreispaltige Raster, die feste Rail-Spalte,
die Auslesung als dritte Spalte und die mobile Stapelung sind Welle 4
(Detailvertrag Punkt 1 und 3). Hier stehen die Abschnitte in Dokumentreihenfolge
unter der Buehne — genau der Grundzustand, den Detailvertrag Punkt 6.1 als
CSS-Grundzustand ohne JavaScript beschreibt.

**Warum die Widerstandswerte bleiben, wo sie sind.** Huelle und Schild sind
aggregierte Ganzschiff-Kennzahlen ohne Einbauort; sie beantworten die Frage „wo
sitzt das" nie und gehoeren deshalb nicht in die Konsole (Detailvertrag
Punkt 9). Sie verlieren aber ihre bisherige Ueberschrift, weil die
Bauteilliste, mit der sie sich einen Unterabschnitt teilten, abwandert — und
bekommen darum eine eigene, benannte Ueberschrift statt ueberschriftslos
anzufallen.

**Reversibilitaet.** Als `costly` gefuehrt: der Umzug von Bewaffnung und
Bauteilliste aus dem Kapitel in die Konsole. Er ist der Kern von D-01, er ist
jederzeit ruecknehmbar — aber die Ruecknahme entwertet die Textbestandsmessung
dieser Welle und die Entdopplungs-Erweiterung aus Welle 2 gleich mit.

**Keine Datenaenderung, kein `npm install`.** `src/data/` bleibt unberuehrt,
`package.json` und `package-lock.json` ebenfalls.
</planning_notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vier Systemabschnitte im ausgelieferten HTML, die Rail als Ankerliste, der Umzug aus dem Kapitel</name>
  <files>src/components/ShipDetail.astro, src/i18n/ui.ts</files>
  <precondition>`node scripts/verify-shipcard.mjs` ist auf dem Stand nach Welle 2 gruen, und `node scripts/verify-shipconsole.mjs` reisst dort mit der protokollierten Meldung. Beides ist der Ausgangspunkt, gegen den diese Welle gemessen wird.</precondition>
  <reversibility rating="costly">Der Umzug von Bewaffnung und Bauteilliste aus `ch-gear` in die Konsole ist der Kern von D-01. Technisch ruecknehmbar, aber jede Textbestandsmessung dieser Phase und die Entdopplungs-Erweiterung aus Welle 2 haengen daran.</reversibility>
  <read_first>
    - `src/components/ShipDetail.astro` Z. 90-175 (`armament`, `armStats`, `slots`, `turretDpsTotal`, `gearDims`/`gearArms`/`gearFlight`/`gearComp`/`hasGear`, die Kapitelliste mit ihren `renders`-Bedingungen), Z. 180-215 und Z. 367-386 (`HoloPort`, `HOLO_GRP`, `HOLO_GRP_ORDER`, `holoData` mit `groups`, `kinds`, `grpLabels`, `icons`), Z. 452-478 (der globale Reset, `.holo` mit fester Hoehe und `overflow:hidden`, `.holo__grid`, `.holo__pad`, `.holo__wrap`, `.holo__stage`), Z. 547-580 (`.holo__layers` und `.holo__layer` samt Gruppenfarben — die Farb- und Icon-Zuordnung, die die Rail uebernimmt), Z. 726-762 (die Medienabfragen fuer `.holo__layers`, `.holo__panel`, `.holo__toggle`), Z. 815-830 (`.sd__jump` und `.sd__jump__in` — das uebernommene Ankerlisten-Muster), Z. 939-1020 (`.sd__sub`, `.sd__ch2col`, `.sd__chsec` und die gescopeten Innenraster-Ueberschreibungen aus Welle 4 von Phase 14), Z. 1184-1195 (`section.holo`), Z. 1306-1326 (der bestehende Ebenen-Kasten samt Icons und Zaehl-Chips), Z. 1492-1510 (Kapitelkopf `ch-gear` und der Beginn von `.sd__ch2col`), Z. 1555-1631 (der Bewaffnungs-Unterabschnitt vollstaendig), Z. 1675-1706 (der Unterabschnitt mit Widerstandswerten UND Bauteilliste — die Trennlinie dieses Umzugs verlaeuft mitten hindurch).
    - `src/lib/holoIcons.ts` VOLLSTAENDIG — `HOLO_GRP_ICON` fuer `core`/`arms`/`prop`/`other`; die Rail fuehrt KEINE neuen Icon-Pfade ein.
    - `src/i18n/ui.ts` Z. 170-215 (der gesamte `holo.`-Block in DE) und die entsprechende Stelle im EN-Block ab Z. 561 — Fundort fuer `holo.grp.*` und `holo.kind.*`, die beide unveraendert wiederverwendet werden.
    - `.planning/phases/16-das-schiff-ist-die-navigation/16-UI-SPEC.md` Detailvertrag Punkt 4 (die zwei Zustaende der Auslesung, besonders 4a), Punkt 5 (Rail), Punkt 6 (ohne JavaScript), Punkt 9 (die Umzugstabelle Zeile fuer Zeile), § Copywriting Contract (die vier neuen Schluessel mit ihren DE- und EN-Texten), § Spacing Scale und § Typography.
    - `.planning/phases/16-das-schiff-ist-die-navigation/16-01-SUMMARY.md` — die gewaehlte P-3-Variante und die Rail-Laengen-Verteilung.
  </read_first>
  <action>
VIER SCHRITTE.

SCHRITT 1 — Der Block muss wachsen duerfen. `.holo` traegt heute die feste
Hoehe und `overflow:hidden`. Waechst er um die Systemabschnitte, werden sie
abgeschnitten. Verschiebe Hoehe und Beschnitt auf die Buehne: `.holo` bekommt
`height:auto` und laesst Inhalt heraus, `.holo__wrap` uebernimmt den bisherigen
Hoehenausdruck und den Beschnitt. Die beiden absolut positionierten
Dekorationen — das perspektivische Bodenraster und der Leuchtfleck darunter —
sind heute an `.holo` verankert und rutschten sonst an den Fuss des ganzen
Blocks statt unter das Modell zu bleiben; sie wandern in denselben Container wie
die Buehne. Der Kommentar an der Fundstelle nennt den Anlass. Zur Erinnerung an
eine bereits zweimal bezahlte Falle: die site-weite Polsterung jedes
`section`-Elements wird von der Reset-Zeile ganz oben in diesem Stilblock
neutralisiert — diese Zeile bleibt unangetastet.

SCHRITT 2 — Die Systemabschnitte. Rendere je vorhandener Gruppe aus
`holoData.groups`, in der Reihenfolge von `HOLO_GRP_ORDER`, einen eigenen
Abschnitt als `section` mit der Klasse `holo__sys` und der Kennung `sys-` plus
Gruppenname, INNERHALB von `section.holo` und NACH der Buehne. Kein
Versteck-Attribut, keine Vorlagenhuelle, kein per Skript erzeugter Knoten. Jeder
Abschnitt traegt:

  — eine Ueberschrift mit dem Systemnamen aus `holo.grp.*` (bestehende
    Schluessel, keine neuen) und dem Gruppen-Icon aus `HOLO_GRP_ICON`;
  — die Zaehlzeile aus dem neuen Schluessel `ship.console.count`, in der die
    Platzhaltermarke durch die Zahl der Ports dieser Gruppe ersetzt wird;
  — darunter GENAU EINE Liste, nach der Bauteilart gruppiert, mit Stueckzahl.

Die Liste entsteht je Gruppe aus der Quelle, die es bereits gibt, und NICHT aus
einer zweiten Aggregation derselben Aussage:

  arms   uebernimmt `armStats` (samt der Gesamt-Turm-Kennzahl) und `armament`
         unveraendert — dieselben Bauformen, nur an neuem Ort.
  core   uebernimmt `slots` unveraendert.
  prop   und `other` bekommen eine NEUE, aber strukturell identische
         Aggregation: die Ports der Gruppe nach ihrer Art buendeln, je Art die
         Anzahl und die Artbezeichnung aus `holo.kind.*` ausschreiben. Dieselbe
         Formel wie `armStats`, nur auf eine bisher nicht ausgeschriebene
         Gruppe angewendet. Fuer `prop` sind das die Triebwerksarten, fuer
         `other` Treibstoff und Taeuschkoerper.

Die Vorbehalte bleiben sichtbar: traegt ein Port die Marke „nicht
physicalisiert" oder „Position geschaetzt", steht der Hinweis am betroffenen
Eintrag und nicht pauschal am ganzen System (Erfolgskriterium 6, UI
Considerations „partial E4"). Kein neuer Vorbehaltstext — die bestehenden
Schluessel tragen ihn bereits.

SCHRITT 3 — Die Rail als Ankerliste. Ersetze den bisherigen Ebenen-Kasten durch
ein `nav` mit der Klasse `holo__rail`, der Kennung `holorail` und dem
`aria-label` aus dem neuen Schluessel `ship.console.rail.aria`. Je vorhandener
Gruppe ein echter Anker auf die zugehoerige Systemkennung, mit Icon, Label aus
`holo.grp.*` und dem Zaehl-Chip (Markerzahl der Gruppe). Das ist exakt das
Muster der Kapitel-Sprungleiste aus Phase 14 und funktioniert vollstaendig ohne
Skript. Die Gruppenfarben kommen unveraendert aus den bestehenden
`data-g`-Regeln; es entsteht keine neue Farbe. Der eingeklappte Zustand des
alten Kastens entfaellt ersatzlos: bei hoechstens vier Eintraegen in einer
eigenen Zeile ist nichts mehr einzuklappen, und der gesparte Platz war nur
noetig, solange die Rail ueber dem Modell SCHWEBTE. Entferne mit dem alten
Kasten auch die Regel, die ihn nur im 3D-Modus einblendet — die neue Rail ist
immer da, weil die Systemabschnitte immer da sind. Der Ausdruck, der den alten
Kasten nur bei mehr als einer Gruppe oder bei vorhandener Dauerbeschriftung
ueberhaupt rendert, wird auf die neue Bedingung gebracht: mindestens eine
Gruppe.

SCHRITT 4 — Der Umzug aus dem Kapitel, mit einer scharfen Trennlinie. Der
Bewaffnungs-Unterabschnitt des Ausstattungs-Kapitels wird VOLLSTAENDIG entfernt;
sein Inhalt steht ab jetzt im System Bewaffnung. Der Unterabschnitt, der heute
Widerstandswerte UND Bauteilliste zusammenfasst, wird geteilt: die Bauteilliste
wandert in das System Komponenten, die Widerstandswerte BLEIBEN im Kapitel und
bekommen dort eine eigene Ueberschrift aus dem neuen Schluessel
`ship.defense.title`. Sie bleiben dabei ein gewoehnliches Wertraster — sie
duerfen NICHT zu einer Balkenspur werden, sonst reisst die bestehende
Zusicherung, dass Balken nur dort stehen, wo sie vergleichen. Die
Sichtbarkeitsbedingungen des Kapitels und seiner Unterabschnitte sind
entsprechend nachzuziehen: das Kapitel rendert kuenftig, wenn Masse, Flugleistung
oder Widerstand etwas zu zeigen haben. Der Kapitel-Zweispalter bleibt
unveraendert anwendbar und traegt danach drei statt vier Zellen.

DIE VIER NEUEN SCHLUESSEL in `src/i18n/ui.ts`, je DE und EN, mit den Texten aus
dem Copywriting Contract: `ship.console.rail.aria`, `ship.console.readout.aria`,
`ship.console.count`, `ship.defense.title`. Der Schluessel fuer die Auslesung
wird in dieser Welle angelegt, aber erst in Welle 4 verwendet — das ist so
gewollt und im Commit zu nennen. Markiere in derselben Datei die durch P-2 und
durch den entfallenen Ebenen-Kasten verwaisten Schluessel als verwaist, ohne sie
zu loeschen. In keinem neuen sichtbaren Text darf die Herkunft der Daten
auftauchen.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/verify-shipcard.mjs</automated>
    <automated>node scripts/verify-shipconsole.mjs --report</automated>
    <automated>npm run audit:site</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('dist/de/schiffe/anvl-carrack.html','utf8');console.log((s.match(/class=\"[^\"]*\bholo__sys\b/g)||[]).length)"` liefert dieselbe Zahl wie die Rail-Laenge der Carrack aus `16-01-SUMMARY.md`, und dieselbe Zahl auch fuer die englische Fassung.
    - Jeder Systemabschnitt traegt eine Kennung aus der Menge `sys-core`, `sys-arms`, `sys-prop`, `sys-other`, keine doppelt; nachgewiesen durch einen Lauf von `node scripts/verify-shipconsole.mjs --report`, dessen Zusicherung 2 keine Befunde meldet.
    - Zusicherung 4 des Berichtslaufs (kein Eintrag ohne Marker) meldet 0 Befunde, und die kleinste gefundene Markerzahl je Rail-Eintrag ist groesser als null.
    - Zusicherung 5 des Berichtslaufs meldet 0 Befunde: kein Systemabschnitt traegt ein Versteck-Attribut, keiner liegt in einer Vorlagenhuelle, alle Rail-Eintraege sind Anker auf eine vorhandene Systemkennung derselben Seite.
    - `node scripts/verify-shipcard.mjs` bleibt gruen — insbesondere Zusicherung 6: kein Zahl-plus-Einheit-Token steht gleichzeitig in der Konsole und im Ausstattungs-Kapitel. Reisst es, ist der Umzug unvollstaendig und der Rest der Zeile im Kapitel zu entfernen, NICHT eine Ausnahme einzutragen.
    - Der Bewaffnungs-Inhalt steht nur noch an einer Stelle: `node -e "const s=require('fs').readFileSync('dist/de/schiffe/anvl-carrack.html','utf8');const i=s.indexOf('id=\"ch-gear\"');const j=i<0?-1:s.indexOf('id=\"ch-context\"');console.log(i<0?'kein ch-gear':(s.slice(i,j>i?j:s.length).match(/arm__list|sd__slots/g)||[]).length)"` liefert 0 oder meldet, dass es kein Ausstattungs-Kapitel gibt. <!-- planner-discipline-allow: arm__list --> <!-- planner-discipline-allow: sd__slots -->
    - Der Widerstands-Unterabschnitt existiert und ist beschriftet: die gebaute Carrack-Seite enthaelt den Text aus `ship.defense.title` genau einmal, in beiden Sprachen.
    - Alle vier neuen Schluessel existieren in BEIDEN Sprachbloecken: `node -e "const s=require('fs').readFileSync('src/i18n/ui.ts','utf8');for(const k of ['ship.console.rail.aria','ship.console.readout.aria','ship.console.count','ship.defense.title'])console.log(k,(s.match(new RegExp(\"'\"+k.replace(/\\./g,'\\\\.')+\"'\",'g'))||[]).length)"` liefert je Schluessel den Wert 2.
    - `npm run audit:site` meldet 0 FEHLER — insbesondere taucht in keinem neuen sichtbaren Text die Herkunft der Daten auf, und es gibt keinen toten Anker.
    - `npm run build` und `npm run gate` sind gruen, ebenso einmal der Vorschau-Bau mit `STAGING=1` — diese Welle beruehrt das Layout.
    - `git status --porcelain` nennt ausschliesslich die zwei Dateien dieses Tasks; `src/data/`, `package.json` und `package-lock.json` bleiben unberuehrt.
  </acceptance_criteria>
  <done>
Jedes System steht als eigener, sichtbarer Abschnitt im ausgelieferten HTML, die
Rail ist eine echte Ankerliste, und Bewaffnung und Bauteilliste stehen an genau
einer Stelle der Seite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Der Beweis mit abgeschaltetem JavaScript — und die Textbestandsmessung danach</name>
  <files>scripts/probes/schiffskonsole-messung.mjs</files>
  <precondition>Task 1 ist gebaut, ein Vorschau-Server serviert das `dist/` DIESES Worktrees, und unter `C:/Program Files/Google/Chrome/Application/chrome.exe` liegt ein installierter Chrome. Kopfloses WebGL braucht die Startschalter `--enable-unsafe-swiftshader`, `--use-gl=angle` und `--use-angle=swiftshader`.</precondition>
  <read_first>
    - `scripts/probes/schiffskonsole-messung.mjs` VOLLSTAENDIG — der eigene Stand nach den Wellen 1: Messgruppen a bis g, die Ableitung der Pruefschiffe aus den Daten, die Meldeform, die Fuellgrad-Sperrklinke.
    - `scripts/verify-shipconsole.mjs` Zusicherung 6 — die Textbestands-Sperrklinke mit ihrem Anlasstext; die Sonde misst dieselbe Groesse und muss dieselbe Zahl liefern.
    - `.planning/phases/16-das-schiff-ist-die-navigation/16-01-SUMMARY.md` § Messgruppe c — Minimum, Median, Maximum des Textbestands vor dem Umbau und der Carrack-Wert.
    - `.planning/phases/16-das-schiff-ist-die-navigation/16-CONTEXT.md` Konkrete Vorgaben 1, 2, 3 und 5.
    - `scripts/browser-smoke.mjs` — die Aufloesung von `--base` und die Browser-Kandidatenliste, falls ein zweiter Kontext mit abgeschaltetem Skript gebraucht wird.
  </read_first>
  <action>
Ergaenze die Sonde um zwei Messgruppen und fahre sie.

MESSGRUPPE j-ohne-javascript (D-02, Erfolgskriterium 2). Oeffne dieselben
Pruefseiten in einem Browserkontext, in dem JavaScript ABGESCHALTET ist — nicht
per Attrappe, nicht durch Lesen des Codes, sondern durch die Kontexteinstellung
des Browsers. Miss je Seite: die Zahl der sichtbaren Systemabschnitte, je
Abschnitt seine gemessene Hoehe und die Bytezahl seines sichtbaren Textes, die
Zahl der Rail-Anker und ob jeder Anker auf eine vorhandene Kennung derselben
Seite zeigt. Soll: die Zahl der sichtbaren Abschnitte gleicht der Rail-Laenge
dieses Schiffs, jede gemessene Hoehe ist groesser als null, und kein Abschnitt
traegt leeren Text. Zusaetzlich messen: der Anker-Sprung fuehrt tatsaechlich zum
Abschnitt (Position nach dem Sprung gegen die Position des Abschnitts), und die
Seite hat keinen waagerechten Ueberlauf. Gegenprobe im selben Lauf: derselbe
Aufruf MIT eingeschaltetem JavaScript liefert dieselbe Zahl sichtbarer
Abschnitte — in dieser Welle wird noch nichts umgehaengt und noch nichts
versteckt, eine Abweichung waere ein Fund.

MESSGRUPPE k-textbestand-danach (Erfolgskriterium 3). Miss den sichtbaren Text
je Schiffsseite mit demselben Verfahren wie Messgruppe c aus Welle 1 und stelle
ihn dem dort protokollierten Ausgangswert gegenueber: Minimum, Median, Maximum
und der Carrack-Wert, je vorher und nachher, mit der Differenz in Bytes und in
Prozent. Soll: kein Wert ist kleiner geworden. Erwartet wird ein leichter
ANSTIEG, weil `prop` und `other` erstmals ausgeschrieben werden.

FAELLT EIN WERT, ist die URSACHE zu suchen und zu beheben — nicht die Marke
abzusenken (Grundsatz 5). Der wahrscheinlichste Anlass waere ein Rest des
Umzugs, der beim Entfernen mehr mitgenommen hat als vorgesehen. Der Fund und
seine Behebung gehoeren woertlich ins SUMMARY.

Fahre danach den vollstaendigen Satz: die Sonde ohne Schalter (alle Messgruppen,
alle Pruefschiffe, beide Sprachen, alle Ansichtsbreiten), den Berichtslauf des
Konsolen-Tors, und `verify:shipcard` als Urteil. Die Fuellgrad-Klinke aus Welle 1
muss weiter halten: die Buehne ist in dieser Welle noch nicht schmaler geworden,
aber sie hat ihren Container gewechselt — genau dort faellt ein Fehler auf.

Protokolliere im SUMMARY: die Tabelle vorher/nachher fuer den Textbestand, die
Zahl der ohne JavaScript sichtbaren Abschnitte je Pruefschiff und Sprache, und
den Fuellgrad je Breite als Gegenprobe zu Welle 1.
  </action>
  <verify>
    <automated>node --check scripts/probes/schiffskonsole-messung.mjs</automated>
    <automated>node scripts/probes/schiffskonsole-messung.mjs --census</automated>
    <automated>node scripts/probes/schiffskonsole-messung.mjs</automated>
    <automated>node scripts/verify-shipcard.mjs</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - Messgruppe `j-ohne-javascript` besteht fuer alle Pruefschiffe in BEIDEN Sprachen: die Zahl der ohne JavaScript sichtbaren Systemabschnitte gleicht der Rail-Laenge des jeweiligen Schiffs, jede gemessene Abschnittshoehe ist groesser als null, und keiner traegt leeren Text.
    - Der Beweis ist gegen das GEBAUTE `dist/` mit ABGESCHALTETEM JavaScript gefuehrt, nicht durch Lesen des Codes — die Sonde druckt in ihrer Kopfzeile, dass der Kontext ohne Skript lief, und das SUMMARY zitiert die Zeile.
    - Jeder Rail-Anker fuehrt ohne JavaScript tatsaechlich zum zugehoerigen Abschnitt; die Sonde meldet die Zahl der geprueften Spruenge und 0 Fehlschlaege.
    - Messgruppe `k-textbestand-danach` meldet fuer Minimum, Median, Maximum und die Carrack keinen kleineren Wert als vor dem Umbau. Die Tabelle vorher/nachher mit Differenz in Bytes und Prozent steht im SUMMARY.
    - Die Fuellgrad-Klinke aus Welle 1 haelt weiterhin: Messgruppe `d-fuellgrad` meldet in allen Laeufen mindestens 70 %.
    - `node scripts/verify-shipcard.mjs` ist gruen, und seine Selbstauskunft nennt weiterhin Regionen aus beiden Bloecken.
    - `npm run gate` gruen, ebenso einmal mit `STAGING=1`; `git status --porcelain` nennt ausschliesslich `scripts/probes/schiffskonsole-messung.mjs`.
  </acceptance_criteria>
  <done>
Dass die Seite ohne JavaScript alle Systeme als lesbare Liste zeigt, ist gegen
das gebaute Artefakt mit abgeschaltetem Skript gemessen — und der Textbestand
ist nachweislich nicht kleiner geworden.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Vollstaendige Aufzaehlung der Bezeichner, die diese PHASE neu anlegt — ueber
alle fuenf Plaene. Der Drift-Abgleich liest diesen Abschnitt, um neu
Entstandenes nicht als Abweichung zu melden. Was hier nicht steht, ist nicht neu.

**Neue Dateien:** `scripts/probes/schiffskonsole-messung.mjs` (Welle 1),
`scripts/verify-shipconsole.mjs` (Welle 2), `scripts/lib/shipconsole-exclusions.mjs`
(Welle 2, NUR falls der Berichtslauf eine benannte Ausnahme erzwingt)

**Neues npm-Skript:** `verify:shipconsole`

**Neue Gate-Registry-Kennung:** `verify:shipconsole` (Schiene A, in Welle 2 mit
`disabled` und benanntem Anlass, in Welle 5 scharf)

**Neue Viewer-API:** `metrics()` am Rueckgabeobjekt von `initHolo`; das
Fensterhandle `window.__holoViewer` erscheint ausschliesslich bei der
Adressmarke `holometrics`

**Neue CSS-Klassen und -Selektoren (Wellen 3 und 4):** `.holo__rail`,
`.holo__rail-ic`, `.holo__rail-lb`, `.holo__rail-ct`, `.holo__readout`,
`.holo__sys`, `.holo__sys-h`, `.holo__sys-ct`, `.holo__sys-list`,
`.holo__sys-row`, `.holo.has-console`, `.holo__rail.is-chips`

**Neue Element-Ids:** `sys-core`, `sys-arms`, `sys-prop`, `sys-other`,
`holorail`, `holoreadout`

**Neue i18n-Schluessel in `src/i18n/ui.ts`, je DE und EN:**
`ship.console.rail.aria`, `ship.console.readout.aria`, `ship.console.count`,
`ship.defense.title`

**Neue Sperrklinken:** Fuellgrad-Klinke (P-1, 70 %, Ausgang rund 25 %) in
`scripts/probes/schiffskonsole-messung.mjs`; Textbestands-Klinke
(Erfolgskriterium 3) in `scripts/verify-shipconsole.mjs`

**Neue Eintraege in der Bildlaufleisten-Ausnahmeliste (sechs Stellen, zwei
Dateien: `assets/mobile-ux.css` § 5c und `SEL_DRAG`/`SEL_FADE` in
`assets/scroll-affordance.js`):** `.holo__readout` und `.holo__rail.is-chips` —
jeweils NUR, falls sie einen eigenen Bildlauf-Kasten bekommen

**Erweitert, nicht neu:** `computeRegions()` in `scripts/verify-shipcard.mjs`
nimmt zusaetzlich zu `div.sd` den Block `section.holo` in seine Regionsbildung
auf; die Ausnahmeliste `scripts/lib/shipcard-exclusions.mjs` bleibt bestehen

**Getilgt, nicht neu:** `.holo__activate` samt Regel und das Element `holoact`;
der Beschriftungs-Umschalter `holoblabels` samt `.holo__layer--lab` und
`.holo__layer-eye`; die Regelgruppe fuer den eingeklappten Ebenen-Kasten; der
Klick-Ladepfad an `btn3d`; der von Video- und Bildverfuegbarkeit abhaengige
Ausdruck fuer `defaultMode` (`ShipDetail.astro` Z. 400)

**Verwaist, nicht geloescht:** die i18n-Schluessel `holo.activate`,
`holo.labels.toggle`, `holo.labels.short`, `holo.layers.title`,
`holo.filter.aria` — als verwaist markiert, Loeschung liegt beim Executor und
ist kein Blocker
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Katalogdaten -> ausgeliefertes HTML | Bauteil-, Waffen- und Turmbezeichnungen aus den Datendateien werden in die Systemabschnitte gerendert. Astro maskiert Text automatisch; kein `set:html`, kein `innerHTML`. |
| i18n-Texte -> ausgeliefertes HTML | Vier neue Zeichenketten, beide Sprachen, aus der eigenen Textdatei. |
| Anker -> Sprungziel | Die Rail-Anker zeigen ausschliesslich auf seiteninterne Kennungen; kein Ziel kommt aus Daten. |
| Vorschau-Server -> Sonde | Die Sonde laedt Seiten ueber HTTP von einem lokal laufenden Server, einmal mit und einmal ohne JavaScript. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-10 | Tampering | Bauteilnamen im neuen Konsolen-Markup | medium | mitigate | Ausschliesslich Astros automatische Textmaskierung; ausdruecklich KEIN `set:html` und kein `innerHTML` in den neuen Abschnitten. Gegenprobe: `grep -n "set:html" src/components/ShipDetail.astro` bringt in den neuen Bloecken keinen Treffer. |
| T-15-11 | Information Disclosure | Datenherkunft im neuen sichtbaren Text | high | mitigate | `audit:site` erzwingt als FEHLER, dass Herkunftshinweise nirgends im sichtbaren Text stehen. Die vier neuen Zeichenketten sind reine Funktionsbezeichnungen. Der Lauf ist Abnahmekriterium dieses Tasks. |
| T-15-12 | Denial of Service | Bis zu 40 zusaetzliche Textzeilen je Seite ueber 454 Seiten | low | accept | Reiner Text ohne Skript und ohne Netzweg; die Textbestandsmessung beziffert den Zuwachs je Seite und macht ihn sichtbar statt ihn zu vermuten. |
| T-15-13 | Repudiation | Der Beweis fuer D-02 laesst sich spaeter nicht nachvollziehen | high | mitigate | Der Nachweis laeuft gegen das GEBAUTE `dist/` mit abgeschaltetem JavaScript; die Sonde druckt Artefakt, Kontextzustand und gemessene Zahlen, und das SUMMARY zitiert sie. Zusaetzlich haelt `verify:shipconsole` Zusicherung 5 die Struktur dauerhaft fest. |
| T-15-SC | Tampering | npm/pip/cargo-Installationen | high | accept | Diese Welle installiert kein Paket. `package.json` und `package-lock.json` stehen NICHT in `files_modified`. Gegenprobe: `git status --porcelain` nennt sie nicht. |

ASVS-Stufe 1, blockierend ab `high`. Die beiden `high`-Befunde sind maschinell
abgedeckt (`audit:site` als FEHLER; der Beweis gegen das gebaute Artefakt);
`T-15-SC` ist gegenstandslos. V5 (Eingabevalidierung) und V14 (Konfiguration)
sind die beruehrten Kategorien.
</threat_model>

<verification>
Vor jedem Push, ohne Ausnahme (`CLAUDE.md` § Liefern):

```
npm run build
npm run gate
```

Beides gruen. Diese Welle beruehrt Layout und Seitenstruktur — deshalb
zusaetzlich einmal als Vorschau bauen:

```
$env:STAGING = '1'; npm run build; npm run gate
```

Auf Windows in PowerShell `npm.cmd` statt `npm`. Der Vorschau-Server im
Worktree serviert dessen EIGENES `dist/`.

Diese Strecken kann dieser Dateisatz reissen und muessen einzeln gruen sein:
`verify:shipcard` (besonders Zusicherung 2, 4, 5 und 6), `audit:site`,
`verify:sync` (Sprachparitaet ueber alle Seitenpaare), `verify:layers`,
`verify:theme`, `verify:metrics`.
</verification>

<success_criteria>
- Bis zu vier Systemabschnitte je Schiffsseite stehen im ausgelieferten HTML,
  sichtbar, ohne Versteck-Attribut und ohne Vorlagenhuelle.
- Die Rail ist eine echte Ankerliste und funktioniert ohne Skript.
- Kein Systemeintrag ohne Marker.
- Bewaffnung und Bauteilliste stehen an genau einer Stelle der Seite;
  `verify:shipcard` bleibt gruen und sieht dabei in beide Bloecke.
- Die Widerstandswerte bleiben im Kapitel und tragen eine eigene Ueberschrift.
- Vier neue i18n-Schluessel, je DE und EN.
- Der Textbestand je Seite ist nicht kleiner geworden; die Tabelle
  vorher/nachher steht im SUMMARY.
- Mit abgeschaltetem JavaScript zeigt die gebaute Seite alle Systeme als lesbare
  Liste — gemessen, nicht behauptet.
- `npm run build` und `npm run gate` gruen, normal UND mit `STAGING=1`;
  `src/data/` unberuehrt.
</success_criteria>

<output>
Create `.planning/phases/16-das-schiff-ist-die-navigation/16-03-SUMMARY.md` when done
</output>
