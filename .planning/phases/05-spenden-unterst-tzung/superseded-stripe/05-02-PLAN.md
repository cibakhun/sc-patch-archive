---
phase: 05-spenden-unterst-tzung
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/consts.ts
  - assets/theme.css
  - src/i18n/ui.ts
  - tests/e2e/support-config.test.js
autonomous: true
requirements: [DON-03, DON-05, DON-09, DON-11]

must_haves:
  truths:
    - "Ohne hinterlegten Stripe-Schluessel meldet die Konfiguration den Demo-Modus"
    - "Der Ko-fi-Weg haengt an einem eigenen Schalter und nicht am Stripe-Schluessel"
    - "Der Zielbetrag ist an einer Stelle aenderbar"
    - "Die Unterstuetzen-Farbe existiert in Dunkel- und Hellmodus als Token"
    - "Die drei Chrome-Strings liegen in DE und EN vor"
  artifacts:
    - path: "src/consts.ts"
      provides: "SUPPORT-Block, SUPPORT_DEMO und KOFI_ENABLED"
      contains: "export const SUPPORT ="
    - path: "assets/theme.css"
      provides: "Block 4 mit --support-gold in beiden Farbmodi"
      contains: "--support-gold"
    - path: "src/i18n/ui.ts"
      provides: "nav.support, nav.deck.sub.support, footer.support in beiden Bloecken"
      contains: "nav.deck.sub.support"
    - path: "tests/e2e/support-config.test.js"
      provides: "Gatter fuer Sentinel-Unabhaengigkeit, Token-Paritaet und i18n-Paritaet"
      min_lines: 50
  key_links:
    - from: "src/consts.ts"
      to: "src/components/SupportForm.astro"
      via: "SUPPORT_DEMO steuert Demo-Banner und Abwesenheit des Fortschrittsbalkens"
      pattern: "SUPPORT_DEMO"
    - from: "assets/theme.css"
      to: "src/components/SupportStrip.astro"
      via: "--support-gold als site-weit gueltige Wiedererkennungsfarbe"
      pattern: "--support-gold"
---

<objective>
Die drei Vertraege, die alle folgenden Plaene lesen, liegen fest: die Konfiguration mit
zwei unabhaengigen Demo-Schaltern, die feste Unterstuetzen-Farbe als Token in beiden
Farbmodi, und die drei wiederkehrenden Chrome-Strings in DE und EN.

Purpose: Plan 05-03 (Seitenkoerper) und Plan 05-05 (site-weite Zugaenge) greifen beide
auf dieselben Konstanten, dasselbe Farb-Token und dieselben i18n-Schluessel zu. Werden
die erst dort nebenbei angelegt, entstehen zwei Wahrheiten — genau das Fehlerbild, das
`CONVENTIONS.md` § Single-source-of-truth benennt. Ausserdem haengt die Ehrlichkeit der
Seite an einem einzigen Detail: Stripe und Ko-fi brauchen ZWEI getrennte Schalter, sonst
verschwindet der Ko-fi-Link, sobald ein Stripe-Schluessel eingetragen wird — oder
schlimmer, ein Ko-fi-Link zeigt ins Leere.

Output: `src/consts.ts` § SUPPORT, `assets/theme.css` Block 4, drei i18n-Schluessel und
ein Testlauf, der die Unabhaengigkeit der Schalter und die Zweisprachigkeit gattert.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spenden-unterst-tzung/05-CONTEXT.md
@.planning/phases/05-spenden-unterst-tzung/05-UI-SPEC.md
@.planning/phases/05-spenden-unterst-tzung/05-STRIPE-ADDENDUM.md
@.planning/phases/05-spenden-unterst-tzung/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-spenden-unterst-tzung/05-01-SUMMARY.md
</context>

<binding_addendum>
**S-07 betrifft diesen Plan unmittelbar.** Der echte veroeffentlichbare Testschluessel
des Betreibers liegt vor und steht in `05-STRIPE-ADDENDUM.md` § S-07. Er wird hier
NOCH NICHT eingetragen: `SUPPORT_DEMO` haengt an genau diesem Feld, und ein
eingetragener Schluessel verlaesst den Demo-Modus, bevor die Edge Functions ihre
Geheimnisse haben — die Seite zeigte dann eine echte Betragswahl mit einem Knopf,
der ins Leere laeuft. Der Platzhalter bleibt bis Welle 7 stehen.
</binding_addendum>

<artifacts_this_phase_produces>
Verbindliche Symboltabelle fuer ALLE Plaene der Phase. Kein Plan erfindet einen Namen neu.

**Datenbank** (Migration `supabase/migrations/20260731000000_donations.sql`)
- Tabelle `public.donations`; Spalten `id`, `created_at`, `stripe_event_id` (UNIQUE),
  `stripe_session_id`, `stripe_subscription_id`, `stripe_customer_id`, `amount_cents`,
  `currency`, `kind`, `display_name`, `show_name`, `message`, `message_status`,
  `user_id`, `customer_email`
- `kind` ∈ `one_time` | `recurring_initial` | `recurring_renewal`
- `message_status` ∈ `none` | `pending` | `approved` | `rejected`
- `public.guard_donations_writes()`, Trigger `trg_guard_donations_writes`
- Policies `donations_admin_select`, `donations_admin_update`
- View `public.donation_wall` — `id`, `created_at`, `display_name`, `show_name`,
  `linked_handle`, `message`
- View `public.donation_progress` — `total_cents`, `supporter_count`
- Rollenquelle: `public.user_roles`

**Edge Functions**: `supabase/functions/create-checkout-session/index.ts`,
`supabase/functions/stripe-webhook/index.ts`; Secrets `STRIPE_SECRET_KEY`,
`STRIPE_WEBHOOK_SIGNING_SECRET` (nie im Repo)

**Konfiguration**: `supabase/config.toml` (`project_id`, zwei `verify_jwt`-Abschnitte);
`src/consts.ts` → `SUPPORT` (`stripePublishableKey`, `kofiName`, `goalCents`),
`SUPPORT_DEMO`, `KOFI_ENABLED`; Sentinel
`REPLACE_WITH_YOUR_STRIPE_PUBLISHABLE_KEY`

**CSS-Tokens** (`assets/theme.css` Block 4): `--support-gold`, `--support-gold-ink`,
`--support-gold-soft`

**CSS-Klassenpraefix** `spt-`: `.spt-card`, `.spt-demo`, `.spt-done`, `.spt-amounts`,
`.spt-pill`, `.spt-freq`, `.spt-cta`, `.spt-status`, `.spt-kofi`, `.spt-bar`,
`.spt-bar__fill`, `.spt-wall`, `.spt-wall__card`, `.spt-wall__badge`, `.spt-wall__more`,
`.spt-strip`, `.spt-mod__card`. Ausserhalb: `.attr-support`, `.snav-deck__row--support`.

**i18n-Schluessel** (beide Bloecke): `nav.support`, `nav.deck.sub.support`, `footer.support`

**Neue Dateien**: `src/components/SupportForm.astro`, `src/components/SupportStrip.astro`,
`src/pages/support.astro`, `src/pages/de/support.astro`,
`src/pages/support-thanks.astro`, `src/pages/de/support-thanks.astro`,
`src/pages/account/support-moderation.astro`,
`src/pages/de/account/support-moderation.astro`, `assets/support-app.js`,
`docs/support-setup.md`, fuenf `tests/e2e/support-*.test.js`

**Routen**: `/support.html`, `/de/support.html`, `/support-thanks.html`,
`/de/support-thanks.html`, `/account/support-moderation.html`,
`/de/account/support-moderation.html`

**`NOINDEX_PATHS`**: `+/support-thanks.html`, `+/account/support-moderation.html`;
`/support.html` NICHT.

**Neue npm-Skripte**: keine.
</artifacts_this_phase_produces>

<resolved_discrepancies>
- **Name des Ko-fi-Schalters:** RESEARCH.md schreibt `KOFI_HIDDEN`, PATTERNS.md schreibt
  `KOFI_ENABLED`. Verbindlich ist **`KOFI_ENABLED`** (positive Logik, wie
  `FEEDBACK_DEMO` gegen den Platzhalter geprueft, aber nicht negiert gelesen).
- **Form des SUPPORT-Blocks:** RESEARCH.md schlaegt drei getrennte Bloecke vor
  (`STRIPE`, `KOFI`, `SUPPORT`), PATTERNS.md einen gemeinsamen. Verbindlich ist der
  **eine gemeinsame `SUPPORT`-Block** aus PATTERNS.md — ein Block, drei Felder.
- **`assets/theme.css` und `npm run theme`:** vom Planer am Quelltext geprueft. Alle drei
  Generatoren (`build-light-palettes.mjs:118`, `tokenize-theme-colors.mjs:258`,
  `build-light-overrides.mjs:139`) durchlaufen ausschliesslich `src/**/*.astro`.
  `assets/theme.css` liegt ausserhalb ihres Zugriffs. Der Hellwert dort ist von Hand zu
  setzen und bleibt es; `npm run theme` danach laufen zu lassen ist erlaubt und aendert
  an dieser Datei nachweislich nichts.
</resolved_discrepancies>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SUPPORT-Block in src/consts.ts mit zwei unabhaengigen Schaltern</name>
  <files>src/consts.ts, tests/e2e/support-config.test.js</files>
  <read_first>
    - `src/consts.ts` Zeilen 22-54 — der `FEEDBACK`/`FEEDBACK_DEMO`-Block als Vorlage fuer Kommentarkopf und Sentinel-Idiom, und `SUPABASE` als Vorlage fuer „oeffentlicher Schluessel im Repo"
    - `.planning/phases/05-spenden-unterst-tzung/05-PATTERNS.md` § „src/consts.ts (new block)" — die exakte Zielform
    - `.planning/phases/05-spenden-unterst-tzung/05-CONTEXT.md` D-05, D-06, D-13
    - `tests/e2e/config.test.js` — die Hausform eines Tests, der Quelldateien liest und Konstanten gegeneinander haelt
  </read_first>
  <behavior>
    - Bei unveraendertem Platzhalter im Schluesselfeld ist `SUPPORT_DEMO` wahr.
    - Bei leerem Ko-fi-Namen ist `KOFI_ENABLED` falsch.
    - Ein eingetragener Stripe-Schluessel schaltet `KOFI_ENABLED` NICHT um — die beiden
      Schalter lesen verschiedene Felder und sind nachweislich unabhaengig.
    - `SUPPORT.goalCents` ist 12000.
  </behavior>
  <action>
    Schreibe den Testlauf `tests/e2e/support-config.test.js` zuerst, gegen die noch
    fehlende Konstante — er scheitert, dann baust du ihn gruen.

    Der Test liest `src/consts.ts` als Text (Hausform aus `config.test.js`, kein Import)
    und behauptet: der `SUPPORT`-Block existiert und traegt die drei Felder
    `stripePublishableKey`, `kofiName`, `goalCents`; `goalCents` ist `12000`;
    `SUPPORT_DEMO` wird aus `stripePublishableKey` abgeleitet und `KOFI_ENABLED` aus
    `kofiName`, und zwar aus verschiedenen Feldern — er prueft ausdruecklich, dass in der
    Ableitungszeile von `KOFI_ENABLED` das Schluesselfeld nicht vorkommt und umgekehrt;
    und dass unter `src/` und `assets/` kein Stripe-Geheimschluessel und kein
    Signaturgeheimnis als Zeichenkette steht (Praefixe siehe RESEARCH.md § Setup Guide
    Schritt 2 und 3). Deutscher Kopfkommentar nach Hausform: was geprueft wird und
    welches Fehlerbild es verhindert — naemlich dass ein eingetragener Stripe-Schluessel
    den Ko-fi-Weg mitschaltet oder ausblendet.

    Ergaenze dann `src/consts.ts` um einen `SUPPORT`-Block nach dem `FEEDBACK`-Muster,
    plaziert direkt nach dem `SUPABASE`-Block. Felder: `stripePublishableKey` mit dem
    Platzhalterwert aus der Artefakttabelle, `kofiName` als leere Zeichenkette,
    `goalCents` mit `12000`. Darunter die beiden abgeleiteten Konstanten `SUPPORT_DEMO`
    (Vergleich des Schluesselfelds gegen den Platzhalter) und `KOFI_ENABLED` (Ko-fi-Name
    ist nicht leer). Beide als eigenstaendige Ableitungen, nicht als von Hand gepflegte
    Wahrheitswerte — ein Wahrheitswert kann vom tatsaechlichen Feld abdriften.

    Der Kommentarkopf traegt die gleiche `SETUP:`-Anleitungsform wie der
    `FEEDBACK`-Block, auf Deutsch, und haelt drei Dinge fest: dass der Publishable Key
    oeffentlich sein darf und ist (wie `SUPABASE.publishableKey`), dass der geheime
    Schluessel und das Signaturgeheimnis ausschliesslich in den Supabase-Function-Secrets
    leben und nie hier (D-06), und dass die beiden Schalter absichtlich getrennt sind,
    weil ein Ko-fi-Link ohne Konto ins Leere zeigt (D-05).
  </action>
  <verify>
    <automated>node --test tests/e2e/support-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/e2e/support-config.test.js` laeuft gruen.
    - `node -e "const s=require('fs').readFileSync('src/consts.ts','utf8');const a=/export const SUPPORT_DEMO =.*/.exec(s)[0];const b=/export const KOFI_ENABLED =.*/.exec(s)[0];if(a.includes('kofiName')||b.includes('stripePublishableKey'))process.exit(1)"`
      endet mit Rueckgabewert 0 — die Ableitungen lesen verschiedene Felder.
    - `grep -c "goalCents: 12000" src/consts.ts` ist `1`.
    - `grep -rEc "sk_(test|live)_[A-Za-z0-9]|whsec_[A-Za-z0-9]" src/ assets/` ergibt in
      Summe `0`.
    - `npx tsc --noEmit -p tsconfig.json` meldet keinen neuen Fehler in `src/consts.ts`.
  </acceptance_criteria>
  <done>
    `src/consts.ts` traegt den SUPPORT-Block; die beiden Schalter sind abgeleitet, nicht
    gepflegt, und ihre Unabhaengigkeit ist gegattert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unterstuetzen-Gold als Token in beiden Farbmodi, drei Chrome-Strings in DE und EN</name>
  <files>assets/theme.css, src/i18n/ui.ts, tests/e2e/support-config.test.js</files>
  <read_first>
    - `.planning/phases/05-spenden-unterst-tzung/05-UI-SPEC.md` § Color → „The fixed Unterstützen gold token (D-09)" — der vollstaendige Block samt Kommentar und die fuenf gemessenen Kontrastwerte
    - `assets/theme.css` Zeilen 1-60 und 155-190 — die bestehende Dreiblock-Struktur und ihre Kommentarform
    - `src/i18n/ui.ts` — die Zeilen um `nav.feedback`, `nav.deck.sub.feedback` und `footer.feedback` in BEIDEN Bloecken
    - `.planning/phases/05-spenden-unterst-tzung/05-PATTERNS.md` § „src/i18n/ui.ts (new chrome strings)"
    - `.planning/phases/05-spenden-unterst-tzung/05-UI-SPEC.md` § Copywriting Contract — die drei Chrome-Zeilen
  </read_first>
  <action>
    Haenge an `assets/theme.css` einen vierten Block nach der Form der bestehenden drei
    an, mit dem Kommentarkopf, den die UI-SPEC dort woertlich vorgibt. Er definiert im
    dunklen `:root` die drei Token `--support-gold` mit `#e8a83c`, `--support-gold-ink`
    mit `#1a1004` und `--support-gold-soft` als `color-mix` aus dem Gold mit 14 Prozent;
    im `:root[data-theme='light']` dieselben drei mit `#7a4e05`, `#ffffff` und einer
    `color-mix` mit 10 Prozent. Der Kommentar haelt fest, warum diese Farbe bewusst
    nicht aus der Seitenpalette kommt (drei Zugaenge, eine Wiedererkennung, D-09) und
    dass der Hellwert hier von Hand steht, weil die Generatoren aus `npm run theme`
    ausschliesslich `src/**/*.astro` durchlaufen und diese Datei nie sehen. Keine
    Layoutregeln — die Datei traegt laut ihrem eigenen Vertrag nur Farbe.

    Ergaenze `src/i18n/ui.ts` um genau drei Schluessel, jeweils in BEIDEN Bloecken an der
    Stelle, die der relativen Position der entsprechenden Feedback-Zeile entspricht:
    `nav.support`, `nav.deck.sub.support` und `footer.support`. Die Werte stehen in der
    Copywriting-Tabelle der UI-SPEC; uebernimm sie woertlich, DE und EN. Ein Schluessel,
    der nur in einem Block steht, loest den sichtbaren EN-Rueckfall aus — das ist genau
    das Fehlerbild, das der Test unten verhindert.

    Erweitere `tests/e2e/support-config.test.js` um zwei Faelle: erstens, dass jeder der
    drei neuen Schluessel in beiden Sprachbloecken von `src/i18n/ui.ts` genau einmal
    vorkommt und die Werte je Sprache verschieden sind, wo die Copywriting-Tabelle das
    vorsieht; zweitens, dass `assets/theme.css` alle drei Gold-Token sowohl im dunklen
    als auch im hellen Abschnitt definiert.

    Fuehre danach `npm.cmd run theme` aus und belege, dass `assets/theme.css` dabei
    unveraendert bleibt — das ist die Probe auf die Aussage im Kommentarkopf.
  </action>
  <verify>
    <automated>node --test tests/e2e/support-config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/e2e/support-config.test.js` laeuft gruen und deckt jetzt auch
      Token und i18n ab.
    - `grep -c 'support-gold' assets/theme.css` ist mindestens `6` (drei Token in zwei
      Modi).
    - `grep -c "nav.deck.sub.support" src/i18n/ui.ts` ist genau `2` — je einmal im
      DE- und im EN-Block. Dasselbe gilt fuer `nav.support` und `footer.support`
      (Zaehlung ueber die vollstaendigen Schluesselzeichenketten inklusive Anfuehrungs-
      zeichen, damit `nav.support` nicht in `nav.deck.sub.support` mitgezaehlt wird).
    - Nach `npm.cmd run theme` ist `git diff --stat -- assets/theme.css` leer.
    - `npm.cmd run build` laeuft durch.
    - In den Werten der drei neuen i18n-Schluessel kommen die Woerter aus der
      Wortwahl-Sperre D-21 nicht vor; der Test prueft das an den extrahierten Werten,
      nicht an der ganzen Datei.
  </acceptance_criteria>
  <done>
    `--support-gold` existiert in beiden Farbmodi und ist gegen den Generator-Irrtum
    dokumentiert und gemessen; die drei Chrome-Strings liegen zweisprachig vor und
    koennen nicht einseitig verkuemmern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → ausgeliefertes Byte | Alles in `src/consts.ts` landet im Browser des Besuchers |
| Konfiguration → Laufzeitverhalten | `SUPPORT_DEMO` entscheidet, ob die Seite Zahlen behauptet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Information Disclosure | `src/consts.ts` | critical | mitigate | Nur der Publishable Key steht hier; Testfall greppt `src/` und `assets/` auf Geheimschluessel- und Signaturgeheimnis-Praefixe und schlaegt bei Fund fehl (Task 1) |
| T-05-10 | Tampering | `SUPPORT_DEMO` | medium | mitigate | Abgeleitete Konstante statt gepflegtem Wahrheitswert — der Schalter kann nicht vom tatsaechlichen Schluesselwert abdriften und keine Zahlen freigeben, die es nicht gibt (Task 1, D-15) |
| T-05-11 | Information Disclosure | `KOFI_ENABLED` | low | mitigate | Eigener Schalter auf eigenem Feld; ein Stripe-Schluessel kann keinen Ko-fi-Link ins Leere freischalten (Task 1, gegattert) |
| T-05-12 | Tampering | `assets/theme.css` | low | accept | Datei ist ein reines Stylesheet ohne Ausfuehrungssemantik; Fehlgriff aeussert sich als Farbfehler, nicht als Sicherheitsluecke |
</threat_model>

<verification>
1. `node --test tests/e2e/support-config.test.js` — gruen.
2. `npm.cmd run build` — durchgelaufen.
3. `npm.cmd run theme` gefolgt von `git diff --stat -- assets/theme.css` — leer.
4. `npm.cmd run test:e2e` — Zahl der fehlschlagenden Dateien unveraendert gegenueber der
   in Plan 05-01 notierten Grundlinie.
</verification>

<success_criteria>
- Der SUPPORT-Block liegt in `src/consts.ts`, beide Schalter sind abgeleitet und
  nachweislich unabhaengig.
- `--support-gold` ist in Dunkel- und Hellmodus gesetzt, mit dokumentierter Begruendung,
  warum der Hellwert hier von Hand steht.
- Die drei Chrome-Strings existieren in beiden Sprachbloecken.
- Kein Geheimnis unter `src/` oder `assets/`.
</success_criteria>

<output>
Create `.planning/phases/05-spenden-unterst-tzung/05-02-SUMMARY.md` when done.
</output>
