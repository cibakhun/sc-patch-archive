---
phase: 12-fundorte-in-der-mining-werkbank-anklickbar
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - assets/mining-workbench.js
  - src/components/MiningWorkbench.astro
  - src/i18n/help.ts
  - tests/e2e/helpers/mining-dom.js
  - tests/e2e/mining-shortlist.test.js
  - scripts/probes/mining-locview-messung.mjs
  - scripts/probes/README.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [D-01, D-03, D-04, D-07]

must_haves:
  truths:
    - "Ein Fundort ist per Adresse aufrufbar: der Parameter `fundort` oeffnet beim Laden die Fundort-Ansicht dieses Ortes, ohne dass eine Seite oder Route entstanden waere (D-04)."
    - "Der Abgleich laeuft case-insensitiv gegen die Schluessel des Fundort-Index — die 45 bekannten Ortsnamen sind global eindeutig (0 Kollisionen ueber alle 521 Paare geprueft)."
    - "Ein unbekannter oder verstuemmelter Adresswert faellt STILL auf die Erz-Ansicht zurueck; es entsteht keine sichtbare Fehlermeldung, weil das ein neues, unbegruendetes Muster waere."
    - "Der rohe Adresswert wird NIE in die Seite geschrieben — er dient ausschliesslich als Vergleichsschluessel, genau wie beim ausgelieferten Parameter fuer das Mineral."
    - "Ein Neuladen OHNE Adressparameter zeigt wieder das Erz; die Ansicht wird nicht im localStorage gemerkt."
    - "Die Adresse gewinnt gegen den gespeicherten Zustand — wer von einem geteilten Verweis kommt, will DIESEN Ort sehen."
    - "Der Hilfetext zur Fundort-Zeile beschreibt beide Bedeutungen der Zeile: die Zeile oeffnet den Fundort, die Nadel rechts heftet ihn zusaetzlich an (D-01) — in BEIDEN Sprachen."
    - "Der Hilfetext zur Fundort-Merkliste sagt, dass ein Klick den Fundort oeffnet und das Kreuz ihn loest (D-03) — in BEIDEN Sprachen."
    - "Die Daempfung der Spurenzeilen erreicht am GERENDERTEN Bildpunkt mindestens 4,5:1 gegen die Zeilenflaeche, in BEIDEN Farbmodi — gemessen, nicht aus dem CSS-Wert geschaetzt. Die Startwerte 62 % und 65 % aus dem UI-SPEC sind ausdruecklich kein Freigabewert."
    - "E7/loading: Kein Ladezustand beim Adressaufruf — die Werkbank laedt nichts nach; der Abgleich laeuft beim Init synchron gegen die Schluessel des Fundort-Index."
    - "E7/error: Ein unbekannter oder verstuemmelter Adresswert faellt still auf die Erz-Ansicht zurueck; der rohe Parameterwert wird nie in die Seite geschrieben — identisch zum ausgelieferten Verhalten des Mineral-Parameters."
    - "E7/unclassified: Der Abgleich gegen die 45 bekannten Ortsnamen und der stille Rueckfall sind zugleich die Sicherheitszusage dieser Phase und werden als eigener, pruefbarer Ladezustand gefuehrt — nicht nebenbei erwaehnt."
    - statement: "E1/overflow: Bis zu 27 Fundortzeilen je Erz und bis zu 128 Merklisteneintraege scrollen in ihren .wb__scroll-Kaesten mit SICHTBARER Bildlaufleiste — die Klasse ist in assets/theme.css UND assets/mobile-ux.css bereits erfasst, und es ist kein Kasten mit einer anderen Klasse entstanden."
      verification: backstop
    - statement: "E2/overflow: Schlimmster gemessener Fall — 17 Erzzeilen plus 3 Gruppenueberschriften, also 20 Zeilen, in der Mittelspalte; bei 1280x720 ist die erste Erzzeile ohne Scrollen sichtbar (Hausregel 'Werkzeug, keine Leinwand') und der Rest scrollt sauber im Kasten."
      verification: backstop
    - statement: "E4/overflow: Die drei Methodengruppen-Ueberschriften kosten in Summe nicht mehr Hoehe, als der Kasten bei 1280x720 traegt; keine Ueberschrift wird abgeschnitten."
      verification: backstop
    - statement: "E5/overflow: Gemischte Spuren- und Vollzeilen brechen im schlimmsten Fall (17 Erze an einem Ort) nicht um und behalten ihre einzeilige Hoehe."
      verification: backstop
    - statement: "E3/overflow: Der Fundort-Kopf ueberschreitet die Hoehe des heutigen Erz-Kopfs NICHT — in px gegen ihn gemessen, nicht geschaetzt; das Hoehenbudget --wb-chrome ist bereits knapp und die 1080p-Zusage bricht bei jeder zusaetzlichen Kopfhoehe."
      verification: backstop
    - statement: "E6/zero-one-many: Eine Kachel, die gleichzeitig markiert, ausgewaehlt und angeheftet ist, laesst alle drei Zustaende einzeln ablesen — in BEIDEN Farbmodi nachgewiesen."
      verification: backstop
    - statement: "E2/long-text: Der laengste Erzname (13 Zeichen) plus Spur-Abzeichen, Balken und Prozentzahl brechen bei 1280 px nicht um."
      verification: backstop
    - statement: "E5/long-text: Das Spur-Abzeichen bleibt bei voller Deckkraft in derselben Zeile wie der gedaempfte Erzname, ohne Umbruch."
      verification: backstop
    - statement: "E3/long-text: Der laengste Ortsname (31 Zeichen) im h2 bei 23,4 px, dazu bis zu fuenf Anflugpunkte in der Unterzeile, haelt der Kopf ohne Hoehenzuwachs."
      verification: backstop
  artifacts:
    - assets/mining-workbench.js
    - src/components/MiningWorkbench.astro
    - src/i18n/help.ts
    - tests/e2e/helpers/mining-dom.js
    - tests/e2e/mining-shortlist.test.js
    - scripts/probes/mining-locview-messung.mjs
    - scripts/probes/README.md
    - .planning/WINDOWS.md
  key_links:
    - "Adressparameter <-> Fundort-Index: der gelesene Wert wird NUR als Vergleichsschluessel gegen die vorhandenen Index-Schluessel benutzt. Ein Treffer setzt den Zustand, ein Fehlschlag tut nichts. Der Rohwert erreicht das DOM auf keinem Weg."
    - "Adressparameter <-> gespeicherter Zustand: der Deep-Link-Zweig laeuft NACH dem Laden aus dem localStorage und VOR dem ersten Zeichnen — dieselbe Reihenfolge, die der Mineral-Parameter heute hat, sonst gewinnt der gespeicherte Zustand."
    - "mining.ctl.locpin <-> D-01: der Text beschreibt heute nur das Anheften. Mit der Zeilen-Klickbarkeit ist er objektiv falsch. verify:help prueft NUR, dass ein Anker existiert und nicht leer ist — nicht, dass der Text stimmt. Das Tor faengt diesen Fehler nicht; Gegenlesen ist Pflicht."
    - "src/i18n/help.ts DE-Block <-> EN-Block: assertHelpParity() erzwingt gleiche Schluessel, nicht gleichen Inhalt. Ein nur auf einer Seite nachgezogener Text bleibt gruen und ist trotzdem falsch."
    - "Messsonde <-> scripts/lib/theme-color.mjs: die Kontrastrechnung kommt aus derselben Bibliothek, die verify-layers.mjs benutzt. Eine zweite Kontrastformel waere ein zweites Fehlerbild fuer dieselbe Zahl."
    - "scripts/probes/ <-> verify:wiring: Sonden liegen ausserhalb der Torkette und werden von keinem npm-Ziel gerufen; die Bijektion in verify-wiring.mjs laeuft ueber scripts/verify-*.mjs und scripts/audit-*.mjs. Eine Sonde in probes/ darf das Tor deshalb nicht beruehren — und das ist zu belegen, nicht anzunehmen."
    - ".planning/WINDOWS.md <-> Sichtrunde: die Datei fuehrt Tabelle UND JSON-Spiegel; beide muessen denselben neuen Eintrag tragen, und open_count/total_count in der Kopfzeile muessen mitwandern."
---

<objective>
Der Abschluss: die Fundort-Ansicht bekommt ihre Adresse, die Werkzeug-Hilfe
sagt wieder die Wahrheit, und die neun offenen Zustandszusicherungen des
UI-SPEC werden belegt — maschinell am gerenderten Bildpunkt UND als benannte
Sichtrunde an den Betreiber.

Purpose: Ohne D-04 ist ein Fundort nicht teilbar. Ohne den Hilfetext-Nachzug
erklaert die Werkzeug-Hilfe eine Zeile, die seit Plan 01 zwei Dinge tut, nur
zur Haelfte — und `verify:help` bleibt dabei gruen, weil es nur die
Anwesenheit eines Ankers prueft, nie seinen Inhalt. Ohne die Messung waeren
die Daempfungswerte fuer die Spurenzeilen geschaetzt statt gemessen; das
UI-SPEC nennt sie ausdruecklich „kein Freigabewert".

Output: Deep-Link-Zweig in `assets/mining-workbench.js`, nachgezogene
Hilfetexte in beiden Sprachen in `src/i18n/help.ts`, die Messsonde
`scripts/probes/mining-locview-messung.mjs` samt Eintrag in der
Sonden-Uebersicht, gegebenenfalls korrigierte Daempfungswerte im Stilblock,
drei neue Testfaelle und ein neuer, offener Eintrag in `.planning/WINDOWS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-CONTEXT.md
@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-RESEARCH.md
@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-PATTERNS.md
@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-UI-SPEC.md
@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-01-SUMMARY.md
@.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-02-SUMMARY.md

@CLAUDE.md
@docs/maschinelle-validierung.md
</context>

<planning_notes>
**Spec-loser Sondenlauf: sichtbar uebersprungen.** Kein `12-SPEC.md`, keine
Requirement-IDs, aus denen sich Praedikate ableiten liessen — der dokumentierte
Sichtbar-Uebersprungen-Pfad, kein Fehlschlag. Es sind **keine**
sondenerzeugten Randpraedikate entstanden. Die Abdeckung kommt aus den 48
Zustandszusicherungen des UI-SPEC: 39 als `must_haves.truths` ueber die drei
Plaene verteilt, 9 als `verification: backstop` in DIESEM Plan, weil hier ihr
Nachweis entsteht.

**Reversibilitaet — D-04 ist als `costly` bewertet, nicht als `one-way`.**
Ein Entscheidungs-Checkpoint ist deshalb ausdruecklich NICHT vorgesehen; die
Bewertung wird gekennzeichnet, nicht gegattert. Der Kern der Bewertung:
`?fundort=<Name>` ist ab Auslieferung eine oeffentliche Zusage. Geteilte
Verweise in Discord und Lesezeichen brechen, wenn spaeter der Parametername
oder die Schreibweise eines Ortsnamens wechselt — und der Name ist der
Schluessel aus den Fundortdaten, haengt also an der Datenextraktion. Der
Executor traegt diese Bewertung in die Zusammenfassung, damit sie in der
Projektgeschichte auffindbar bleibt.

**Beide Nachweiswege sind Pflicht, nicht wahlweise** (Entscheidung des
Betreibers, 15.08.): die neun Zusicherungen werden maschinell gemessen UND als
Sichtrunde uebergeben. Was eine Messung nicht beurteilen kann — ob die
Dreifach-Ueberlagerung auf der Kachel tatsaechlich lesbar WIRKT und ob die
gedaempften Spurenzeilen im Fluss als „vorhanden, aber nicht abbauwuerdig"
gelesen werden — geht als benannter Punkt nach `.planning/WINDOWS.md`. Der
Executor fuehrt die Sichtrunde NICHT selbst durch; Praezedenz: Phasen 1.2, 2,
3, 4, 9 und 10.
</planning_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ein Fundort bekommt eine Adresse (D-04)</name>
  <files>assets/mining-workbench.js, tests/e2e/helpers/mining-dom.js, tests/e2e/mining-shortlist.test.js</files>
  <read_first>
    - `assets/mining-workbench.js` Z. 917-950 vollstaendig: der bestehende Deep-Link-Zweig fuer den Mineral-Parameter (Kommentar Z. 917-919, die sofort ausgefuehrte Funktion Z. 920-927), der erste Zeichenaufruf Z. 929-930 und die Zentrier-Warteschleife Z. 932-949 mit ihren beiden dokumentierten Fallen.
    - Derselbe Datei-Abschnitt in seinem Stand NACH Plan 01/02: der Fundort-Index und die beiden neuen Zustandsfelder.
    - `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-RESEARCH.md` § Pattern 3 (Allow-List, kein Rohtext ins DOM) und § Security Domain — dort steht, dass Ortsnamen global eindeutig sind (0 Kollisionen ueber alle 45 Bodies und alle 521 Paare geprueft), der case-insensitive Einzelvergleich also ohne System als Unterscheidungsmerkmal sicher ist.
    - `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-RESEARCH.md` § Open Questions Nr. 3 — die Empfehlung, dass fuer die Fundort-Ansicht KEIN Zentrieren noetig ist, weil sie den Spalteninhalt ersetzt und bei Bildlaufposition 0 neu gezeichnet wird; das ist hier ausdruecklich zu bestaetigen oder zu widerlegen, nicht zu uebergehen.
    - `tests/e2e/helpers/mining-dom.js` — der Kontext-Eintrag `location` mit dem Feld fuer die Adresszeile und die Option, mit der ein Testfall ihn vorbelegt.
  </read_first>
  <behavior>
    - Ein Aufruf mit dem Parameter und einem bekannten Ortsnamen oeffnet beim Laden die Fundort-Ansicht dieses Ortes.
    - Gross- und Kleinschreibung sowie umschliessende Leerzeichen spielen keine Rolle.
    - Ein unbekannter Wert, ein leerer Wert und ein Wert mit HTML-Sonderzeichen fuehren alle zum selben Ergebnis: Erz-Ansicht, keine Meldung, und der Wert taucht nirgends im DOM auf.
    - Ohne den Parameter zeigt die Werkbank das gespeicherte oder das erste Erz — unveraendertes Verhalten.
    - Traegt die Adresse BEIDE Parameter, gewinnt der Fundort fuer die ANSICHT, der Mineral-Parameter bleibt fuer das gewaehlte Erz wirksam; die Fusszeile zeigt also das genannte Erz, waehrend der genannte Ort offen ist.
  </behavior>
  <reversibility rating="costly">
`?fundort=<Name>` ist ab Auslieferung eine oeffentliche Zusage: geteilte
Verweise brechen, wenn spaeter der Parametername oder die Schreibweise eines
Ortsnamens wechselt, und der Name ist der Schluessel aus der Datenextraktion.
Bewertet, nicht gegattert — `costly` verdient eine Kennzeichnung, keinen
Entscheidungs-Checkpoint.
  </reversibility>
  <action>
Den bestehenden Deep-Link-Zweig (die sofort ausgefuehrte Funktion Z. 920-927)
um einen zweiten, gleich gebauten Abschnitt fuer den Fundort erweitern —
dieselbe Bauform, nicht ein zweites Verfahren: `try`/`catch` um das Lesen der
Adresszeile, frueher Ausstieg bei fehlendem Wert, Vergleich ueber
`trim()` und `toLowerCase()`, und eine Schleife ueber die vorhandenen
Schluessel des Fundort-Index, die bei Uebereinstimmung `S.selLoc` auf den
KANONISCHEN Schluessel setzt (nicht auf den gelesenen Wert) und `S.view` auf
die Fundort-Ansicht. Kein Treffer bedeutet: nichts tun. Es entsteht kein
Fehlertext, kein Konsolenausdruck und keine Umleitung.

Der Parametername ist `fundort`, kleingeschrieben, ohne Umlaut — er steht
neben dem bestehenden `mineral` in derselben Adresse und wird ab Auslieferung
zu einer oeffentlichen Zusage (siehe `<reversibility>`). Ein Kommentar an der
Fundstelle haelt genau das fest, samt der Begruendung, dass der Ortsname der
Schluessel aus den Fundortdaten ist.

Reihenfolge ist Pflicht: der Abschnitt laeuft NACH dem Laden des gespeicherten
Zustands und VOR dem ersten `renderAll()` (Z. 930) — sonst gewinnt der
gespeicherte Zustand, und ein geteilter Verweis fuehrt ins Leere. Beide
Parameter duerfen nebeneinander stehen; der Mineral-Zweig bleibt woertlich
unveraendert und wirkt weiterhin auf das gewaehlte Erz.

Die Zentrier-Warteschleife (Z. 932-949) bleibt an den Mineral-Zweig gebunden
und wird NICHT auf den Fundort ausgeweitet: die Fundort-Ansicht ersetzt den
Inhalt der Mittelspalte und beginnt bei Bildlaufposition 0, es gibt dort keine
bestehende Zeile, zu der gescrollt werden muesste. Bestaetige das beim
Umsetzen an der laufenden Seite und halte das Ergebnis in der Zusammenfassung
fest — die Recherche fuehrt es als offene Frage, nicht als Tatsache. Sollte
sich zeigen, dass die Kachelspalte bei einem Fundort-Aufruf doch eine
markierte Kachel ins Bild holen muss, gilt dieselbe dokumentierte Falle: nicht
ueber die Position gegen das naechste positionierte Elternelement rechnen, und
EIN Bild reicht nicht, weil die Rasterhoehe aus einer sichtfensterabhaengigen
Klemmung kommt.

`tests/e2e/helpers/mining-dom.js`: die Option, mit der ein Testfall die
Adresszeile vorbelegt, ist bereits vorhanden — pruefe das und dokumentiere im
Kopfkommentar, dass sie ab jetzt auch fuer den Fundort-Parameter benutzt wird.
Nur falls sie tatsaechlich fehlt, ergaenze sie im vorhandenen Stil.

`tests/e2e/mining-shortlist.test.js` um drei Faelle erweitern:
T-12-16 Ein Aufruf mit dem Parameter und einem bekannten Ortsnamen in
abweichender Gross-/Kleinschreibung und mit umschliessenden Leerzeichen
oeffnet die Fundort-Ansicht dieses Ortes; `#wb-locname` traegt die kanonische
Schreibweise aus den Daten, nicht die eingegebene.
T-12-17 Ein Aufruf mit einem unbekannten Wert, der HTML-Sonderzeichen und
einen Skript-Anfang enthaelt, laesst die Erz-Ansicht stehen; danach wird das
gesamte gezeichnete Markup der Werkbank daraufhin geprueft, dass der
eingegebene Wert dort in KEINER Form vorkommt — weder als Text noch in einem
Attributwert. Das ist der Sicherheitsnachweis dieser Phase und muss als
solcher im Testnamen stehen.
T-12-18 Ein Aufruf mit BEIDEN Parametern: der genannte Ort ist offen UND das
genannte Erz ist gewaehlt (belegbar am Fusszeilentext), ohne dass sich die
beiden Zweige gegenseitig aufheben.
  </action>
  <verify>
    <automated>node --test tests/e2e/mining-shortlist.test.js</automated>
    <automated>node --test tests/e2e/*.test.js</automated>
    <automated>npm run build</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/e2e/mining-shortlist.test.js` gruen, mindestens 54 Faelle.
    - T-12-16 belegt: abweichende Schreibweise und Leerzeichen treffen; `#wb-locname` zeigt die kanonische Schreibweise aus den Daten.
    - T-12-17 belegt: der eingegebene Wert kommt im gesamten gezeichneten Markup nicht vor — weder als Text noch in einem Attributwert; die Erz-Ansicht steht.
    - T-12-18 belegt: beide Parameter wirken nebeneinander.
    - `grep -c "'fundort'" assets/mining-workbench.js` liefert genau 1 — der Parametername steht an genau einer Stelle.
    - `grep -c 'URLSearchParams' assets/mining-workbench.js` liefert genau 2, und beide Vorkommen stehen innerhalb eines `try`-Blocks (per `grep -n` und Sichtprobe der beiden Fundstellen belegbar).
    - `node -e "const s=require('fs').readFileSync('assets/mining-workbench.js','utf8'); console.log(s.indexOf('fundort') < s.lastIndexOf('renderAll()'))"` gibt `true` aus — der Adresszweig laeuft vor dem ersten Zeichnen.
    - `npm run build` und `npm run gate` gruen.
    - `git status --porcelain` nennt ausschliesslich die drei Dateien dieses Tasks.
  </acceptance_criteria>
  <done>
Ein Fundort laesst sich per Adresse oeffnen und teilen; ein unbekannter Wert
faellt still auf die Erz-Ansicht zurueck und erreicht das DOM auf keinem Weg;
beide Adressparameter vertragen sich.
  </done>
</task>

<task type="auto">
  <name>Task 2: Die Werkzeug-Hilfe sagt wieder die Wahrheit — beide Sprachen</name>
  <files>src/i18n/help.ts</files>
  <read_first>
    - `src/i18n/help.ts` — der deutsche Block um Z. 92-101 mit allen `mining.ctl.*`-Schluesseln und der englische Block um Z. 297-306 mit denselben Schluesseln. Beide vollstaendig lesen: der Wortlaut der Nachbartexte gibt Ton und Laenge vor.
    - `scripts/verify-help.mjs` — die Zusicherung, die `data-help`-Anker je `data-tool-id` zaehlt, und die Paritaetspruefung der Schluessel. Ziel des Lesens: verstehen, WAS das Tor prueft (Anwesenheit und Nicht-Leere eines Ankers, Gleichheit der Schluesselmengen) und was es NICHT prueft (ob der Text stimmt).
    - `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-UI-SPEC.md` § „Copywriting Contract" — die beiden Zeilen mit den Wortlaut-Vorschlaegen fuer beide Schluessel in beiden Sprachen.
    - `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-RESEARCH.md` § Pitfall 1 — warum das Tor diesen Fehler nicht faengt und warum Gegenlesen hier Pflicht ist, nicht Kuer.
  </read_first>
  <action>
Zwei bestehende Schluessel bekommen einen neuen Wert, in beiden Sprachen. Es
entstehen KEINE neuen Schluessel — die Ankerzahl je Werkzeug bleibt dadurch
unveraendert, und das Tor bleibt aus demselben Grund gruen wie vorher.

`mining.ctl.locpin` haengt am Kasten mit den Fundort-Zeilen. Sein heutiger Text
beschreibt die Zeile ausschliesslich als Anheft-Ort. Seit D-01 tut dieselbe
Zeile zwei Dinge: die ganze Zeile oeffnet den Fundort, die Nadel rechts heftet
ihn zusaetzlich an die Merkliste darunter. Der neue Text muss beide Bedeutungen
nennen und dabei klar machen, welcher Bereich welche traegt — der Vorschlag aus
dem UI-SPEC nennt genau diese Aufteilung.

`mining.ctl.shortlist` haengt an der Fundort-Merkliste. Sein heutiger Text
nennt nur, was dort steht. Seit D-03 ist auch dort ein Klick moeglich: die
Zeile oeffnet den Fundort, das Kreuz loest ihn. Der neue Text muss beides
sagen.

Der Wortlaut folgt dem Ton der Nachbartexte im selben Block: eine Aussage pro
Halbsatz, keine Bedienungsanleitung, keine Herkunftsangabe der Daten
(`audit:site` erzwingt letzteres site-weit als FEHLER). Deutsche
Anfuehrungszeichen im deutschen Block, englische im englischen — die
Nachbarzeilen zeigen die Hausform.

Lies beide Faelle nach dem Aendern gegen und pruefe drei Dinge von Hand, weil
kein Tor sie prueft: Erstens sagt jeder Text jetzt tatsaechlich, was das
Bedienelement tut. Zweitens sagen der deutsche und der englische Text
DASSELBE — die Paritaetspruefung erzwingt gleiche Schluessel, nicht gleichen
Inhalt; ein nur einseitig nachgezogener Text bleibt gruen und ist trotzdem
falsch. Drittens beschreibt kein anderer `mining.ctl.*`-Text durch die
Aenderung nun etwas Ueberholtes: geh die uebrigen Schluessel dieses Werkzeugs
einmal durch, besonders den zu den Kacheln und den zum grossen Anheft-Knopf.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/verify-help.mjs --complete</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-help.mjs --complete` meldet 12 von 12 Werkzeugen und alle Zusicherungen erfuellt.
    - `node -e "const t=require('fs').readFileSync('src/i18n/help.ts','utf8'); const m=t.match(/'mining\.ctl\.locpin':/g); console.log(m.length)"` gibt 2 aus — der Schluessel steht weiterhin genau zweimal, je einmal DE und EN; es ist kein dritter entstanden.
    - Dieselbe Zaehlung fuer `mining.ctl.shortlist` gibt ebenfalls 2 aus.
    - Beide deutschen Texte nennen das Oeffnen UND das Anheften bzw. Loesen; beide englischen Texte sagen inhaltlich dasselbe. Nachweis: die vier Werte im Diff nebeneinander gelesen und in der Zusammenfassung zitiert.
    - `npm run audit:site` meldet 0 FEHLER — kein Hinweis auf die Herkunft der Daten ist in einen der neuen Texte geraten.
    - `npm run build` und `npm run gate` gruen.
    - Die Gesamtzahl der `data-help`-Anker in `src/components/MiningWorkbench.astro` ist unveraendert (per `grep -c 'data-help' src/components/MiningWorkbench.astro` gegen den Vergleichsstand).
  </acceptance_criteria>
  <done>
Beide Hilfetexte beschreiben die Zeile, die sie erklaeren, vollstaendig und in
beiden Sprachen deckungsgleich; `verify:help` meldet weiterhin 12 von 12, und
die Ankerzahl hat sich nicht verschoben.
  </done>
</task>

<task type="auto">
  <name>Task 3: Die neun offenen Zusicherungen belegen — gemessen UND als Sichtrunde uebergeben</name>
  <files>scripts/probes/mining-locview-messung.mjs, scripts/probes/README.md, src/components/MiningWorkbench.astro, .planning/WINDOWS.md</files>
  <read_first>
    - `scripts/browser-smoke.mjs` VOLLSTAENDIG — besonders die Suche nach dem installierten Browser (Z. 55-73), der Start ueber den gefundenen Pfad (Z. 351), der Kontextaufbau mit Sichtfenstergroesse und Farbschema (Z. 363-366) und der Hellmodus-Kniff samt Gegenprobe, dass die Umschaltung tatsaechlich gegriffen hat (Z. 155-171).
    - `scripts/lib/theme-color.mjs` — die Ausfuhren `parseColor`, `compositeOver`, `flattenStack`, `luminance` und `contrast`. Diese Bibliothek liefert die Kontrastrechnung; eine zweite Formel fuer dieselbe Zahl ist ausgeschlossen.
    - `scripts/verify-layers.mjs` Z. 1-70 — das Vorbild dafuer, wie in diesem Projekt am gerenderten Bildpunkt gemessen wird (Bilddaten lesen, Ebenen uebereinanderlegen, Kontrast rechnen) und wie ein Messlauf sich selbst auskunftsfaehig macht.
    - `scripts/probes/README.md` — Form und Ton der Sonden-Uebersicht sowie der Satz, dass Sonden ausserhalb der Bau- und Torkette stehen.
    - `docs/maschinelle-validierung.md` § 4 — die sieben Grundsaetze, besonders „Vorgefuehrt rot", „Selbstauskunft" und „Gegen das Artefakt pruefen — und wissen, welches".
    - `.planning/WINDOWS.md` — Kopfzeile mit den Zaehlern, die Tabellenform und der JSON-Spiegel darunter. Die Eintraege 10 und 11 zeigen Umfang und Ton, den eine Sichtrunde dieser Werkbank hat.
    - `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-UI-SPEC.md` § „UI Considerations" — die sechs Zeilen mit dem Nachweis-Vermerk, der Absatz zur 4,5:1-Marke und der Absatz „Wie die 9 Backstops belegt werden".
  </read_first>
  <precondition>
Die Vorschau laeuft gegen das `dist/` DIESES Worktrees (`npm run build`, dann
der Vorschau-Server), `playwright-core` ist im Worktree aufloesbar (es steht
bereits als Entwicklungsabhaengigkeit in `package.json` — `package.json` und
`package-lock.json` duerfen sich dabei NICHT aendern), und ein installierter
Chrome oder Edge ist ueber einen der Pfade aus `scripts/browser-smoke.mjs`
oder ueber die Umgebungsvariable fuer den Browserpfad auffindbar. Ist eine der
drei Bedingungen nicht erfuellt, bricht der Task ab und meldet welche — er
schaetzt nicht ersatzweise aus CSS-Werten.
  </precondition>
  <action>
DREI Ergebnisse: eine Messsonde, gegebenenfalls korrigierte Daempfungswerte,
und ein offener Eintrag in der Fensterliste. Alle drei sind Pflicht.

ERSTENS — `scripts/probes/mining-locview-messung.mjs` anlegen. Eine Sonde, kein
Tor: sie liegt in `scripts/probes/`, wird von keinem npm-Ziel gerufen und
haengt an keiner Strecke. Der Kopfkommentar sagt in der Form der
Nachbardateien, welche Frage sie beantwortet und warum sie nicht im Tor haengt
(sie braucht einen Browser; der Baucontainer hat keinen — dieselbe Begruendung,
die schon fuer den Browser-Rundlauf gilt).

Sie startet den installierten Browser ueber `playwright-core`, mit derselben
Pfadsuche wie der Browser-Rundlauf, und faehrt VIER Laeufe: 1280x720 und
1920x1080, je hell und dunkel. Der Hellmodus wird wie im Browser-Rundlauf
gesetzt und danach GEGENGEPRUEFT — die Seite versucht ihn zurueckzustellen, ein
ungeprueftes Setzen misst sonst zweimal denselben Modus.

Sie oeffnet die Mining-Seite ueber den Adressparameter aus Task 1 an drei
gezielt gewaehlten Orten: dem mit den meisten Erzen, dem mit dem laengsten
Namen, und einem mit den meisten Anflugpunkten. Welche Orte das sind, ermittelt
die Sonde selbst aus `assets/mining-db.json` — sie schreibt keine Namen fest,
sonst veraltet sie beim naechsten Datenlauf still.

Gemessen wird, je Lauf und je Ort:
(a) Kontrast des Zeilentexts einer Spurenzeile gegen die Zeilenflaeche, am
gerenderten Bildpunkt: einen eng zugeschnittenen Bildausschnitt der Zeile
aufnehmen, die Bildpunkte auslesen und den Kontrast mit `contrast()` aus
`scripts/lib/theme-color.mjs` rechnen. Marke: mindestens 4,5:1. Die
`.p`-Schrift liegt bei 13,5 px und damit unter der Schwelle fuer „grossen
Text", die mildere Marke gilt also NICHT. Dasselbe fuer die Unterzeile, die
Prozentzahl und den Text des Spur-Abzeichens gegen dessen eigene Flaeche.
(b) Hoehe des Fundort-Kopfs gegen die Hoehe des Erz-Kopfs, in px. Marke: der
Fundort-Kopf ist NICHT hoeher.
(c) Bei 1280x720: liegt die erste Erzzeile der Fundort-Ansicht vollstaendig im
sichtbaren Bereich, ohne dass die Seite gescrollt wurde.
(d) Zeilenhoehe einer Spurenzeile mit dem laengsten Erznamen und Abzeichen
gegen die einer gewoehnlichen Zeile. Marke: gleich — kein Umbruch.
(e) Scrollt der Kasten der Fundort-Ansicht bei dem Ort mit den meisten Erzen
tatsaechlich innen, und ist seine Bildlaufleiste sichtbar (Differenz aus
aeusserer und innerer Breite groesser als 0)?
(f) Die Dreifach-Ueberlagerung auf einer Kachel: einen Ort oeffnen, ein dort
vorkommendes Erz auswaehlen und dasselbe Erz anheften, dann die Bildpunkte der
Kachelecke, der Kachelflaeche und der Nadelflaeche auslesen. Marke: die drei
unterscheiden sich messbar voneinander, in beiden Farbmodi.

Die Sonde druckt eine Selbstauskunft: wie viele Messpunkte sie tatsaechlich
gefahren hat, je Marke Soll und Ist, und ein Sammelurteil am Ende. Sie faellt
mit einem Rueckgabecode ungleich 0 durch, sobald ein Messwert unter seiner
Marke liegt oder ein Messpunkt uebersprungen wurde — ein stiller Ausfall ist
ein Fehlschlag, kein Nichts.

Trage die Sonde in `scripts/probes/README.md` in die Tabelle ein, mit der
Frage, die sie beantwortet, und dem Ort, an dem ihr Ergebnis liegt (die
Zusammenfassung dieses Plans). Die Einleitung dort nennt bisher nur Sonden
gegen die Spieldaten — erweitere sie um den Satz, dass eine Sonde auch gegen
die gerenderte Seite messen kann, damit die Uebersicht ehrlich bleibt.

VORGEFUEHRT ROT, verpflichtend: setze die Daempfung der Spurenzeilen einmal
absichtlich auf einen offenkundig zu schwachen Wert, lasse die Sonde laufen
und protokolliere ihre Fehlermeldung in der Zusammenfassung; setze danach
zurueck. Eine Messung, die nie durchgefallen ist, ist Dekoration — drei
Zusicherungen dieses Projekts waren nachweislich unfaelschbar gruen, bis
jemand die Gegenprobe gefahren hat.

ZWEITENS — die gemessenen Werte in den Stilblock zurueckschreiben. Die
Startwerte 62 % und 65 % aus dem UI-SPEC sind ausdruecklich kein Freigabewert.
Liegt ein Messwert unter 4,5:1, hebe die Deckkraft des betroffenen Texts an,
bis die Marke in BEIDEN Farbmodi gehalten wird, und schreibe den gemessenen
Wert in einen Kommentar an die Regel — mit Datum, wie es die Nachbarstellen
dieser Datei tun. Die Daempfung bleibt dabei bei ihrem Mechanismus: Textfarbe
und Deckkraft, niemals eine gedimmte Flaeche; die Flaeche der Zeile selbst
bleibt unangetastet, damit die Zeile im Raster erkennbar bleibt und nur ihr
Inhalt zuruecktritt.

DRITTENS — die Sichtrunde an den Betreiber uebergeben. `.planning/WINDOWS.md`
fuehrt eine Tabelle UND einen JSON-Spiegel; beide bekommen denselben neuen
Eintrag mit der naechsten freien Kennung, Phase 12, Art `unrun-verify`, Datei
`.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-03-PLAN.md`,
Status offen. Die Zaehler `open_count` und `total_count` in der Kopfzeile
wandern mit.

Der Eintrag beschreibt, was eine Messung NICHT beurteilen kann, je in beiden
Sprachen, in beiden Farbmodi, bei 1920x1080 UND 1280x720:
(1) Wirkt die Dreifach-Ueberlagerung auf der Kachel — markiert, ausgewaehlt,
angeheftet — tatsaechlich lesbar, oder sieht sie nur messbar verschieden aus?
(2) Werden die gedaempften Spurenzeilen im Fluss als „vorhanden, aber nicht
abbauwuerdig" gelesen, oder als Fehler? Gegenprobe: an einem Ort mit fuenf
Spuren unter neun Erzen zaehlen, ob man sie ohne Nachdenken erkennt.
(3) Traegt die Mitte weiterhin? Fundort-Kopf, Gruppenueberschriften und
Erzliste ueber und untereinander, ohne dass die Werkbank bei 1280x720
aeusseres Scrollen braucht.
(4) Ist der Zurueck-Pfeil als Ausgang erkennbar, ohne mit der Nadel oder dem
grossen Anheft-Knopf der Erz-Ansicht verwechselt zu werden — DIESELBE Frage,
die in Phase 10 ein Preset gekostet hat, als zwei gleich aussehende Knoepfe
gegensaetzlich wirkten.
(5) Ist die ganze Fundort-Zeile als Klickziel spuerbar, ohne dass die Nadel
darin schwerer zu treffen geworden ist — DRITTE Runde an diesem Knopf, die
beiden vorigen scheiterten an Groesse und Sichtbarkeit.
(6) Die Merkliste rechts: oeffnet ein Klick auf die Zeile den Ort, und loest
das Kreuz weiterhin nur — merkt man den Unterschied beim Treffen?
(7) Ein geteilter Verweis mit dem Adressparameter, aus der Zwischenablage in
ein neues Fenster eingesetzt: oeffnet er den richtigen Ort, in beiden
Sprachfassungen?
Der Eintrag nennt ausserdem, welche maschinellen Vorlaeufe gruen sind, mit
ihren Zahlen — so wie die Eintraege 10 und 11 es tun.

Der Executor fuehrt diese Sichtrunde NICHT durch. Sie braucht ein menschliches
Urteil; das ist Hausregel, nicht Bequemlichkeit.
  </action>
  <verify>
    <automated>node scripts/probes/mining-locview-messung.mjs --base http://localhost:4321</automated>
    <automated>npm run build</automated>
    <automated>npm run gate</automated>
    <automated>node --test tests/e2e/*.test.js</automated>
    <human-check>Die Sichtrunde selbst — sieben Punkte, DE und EN, beide Farbmodi, 1920x1080 und 1280x720 — geht als offener Eintrag in .planning/WINDOWS.md an den Betreiber und wird vom Executor ausdruecklich NICHT durchgefuehrt.</human-check>
  </verify>
  <acceptance_criteria>
    - `node scripts/probes/mining-locview-messung.mjs` laeuft durch, meldet Rueckgabecode 0 und druckt die Zahl der tatsaechlich gefahrenen Messpunkte (mindestens 4 Laeufe x 3 Orte x 6 Messgruppen).
    - Jeder gemessene Kontrastwert an Spurenzeilentext, Unterzeile, Prozentzahl und Abzeichentext liegt bei mindestens 4,5:1 — in BEIDEN Farbmodi, am gerenderten Bildpunkt gerechnet, nicht aus dem CSS-Wert.
    - Die gemessene Hoehe des Fundort-Kopfs ist kleiner oder gleich der des Erz-Kopfs; die Differenz steht in der Zusammenfassung in px.
    - Bei 1280x720 liegt die erste Erzzeile der Fundort-Ansicht vollstaendig im sichtbaren Bereich, ohne dass die Seite gescrollt wurde.
    - Die Zeilenhoehe einer Spurenzeile mit dem laengsten Erznamen und Abzeichen ist gleich der einer gewoehnlichen Zeile — kein Umbruch.
    - Der Kasten der Fundort-Ansicht scrollt innen, und die Differenz aus aeusserer und innerer Breite ist groesser als 0 — die Bildlaufleiste ist sichtbar.
    - Die Bildpunkte von Kachelecke, Kachelflaeche und Nadelflaeche unterscheiden sich in beiden Farbmodi messbar voneinander.
    - Die Gegenprobe ist gefahren: mit absichtlich zu schwacher Daempfung faellt die Sonde nachweislich durch, und ihre Meldung steht woertlich in der Zusammenfassung. Danach ist der Wert zurueckgesetzt und die Sonde wieder gruen.
    - `npm run gate` gruen, `verify:wiring` eingeschlossen — die neue Sonde in `scripts/probes/` hat die Torkette nachweislich nicht beruehrt.
    - `npm run verify:theme` gruen — falls Daempfungswerte geaendert wurden, ist keine erzeugte Hellmodus-Entsprechung ausgefallen.
    - `scripts/probes/README.md` fuehrt die neue Sonde in der Tabelle, und die Einleitung nennt die erweiterte Reichweite.
    - `.planning/WINDOWS.md` traegt den neuen Eintrag in der Tabelle UND im JSON-Spiegel, mit derselben Kennung und demselben Text; `open_count` und `total_count` in der Kopfzeile sind je um 1 gestiegen.
    - `node -e "const t=require('fs').readFileSync('.planning/WINDOWS.md','utf8'); const j=JSON.parse(t.slice(t.indexOf('[',t.indexOf('json')))); console.log(j.length, j[j.length-1].phase, j[j.length-1].status)"` gibt 12, 12 und `open` aus.
  </acceptance_criteria>
  <done>
Die neun offenen Zustandszusicherungen sind am gerenderten Bildpunkt gemessen
und die Sonde ist einmal vorgefuehrt rot gewesen; die Daempfungswerte sind
gemessene Werte, keine geschaetzten; und die sieben Fragen, die kein Skript
beantwortet, liegen als offener, benannter Eintrag in der Fensterliste beim
Betreiber.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Vollstaendige Aufzaehlung der Bezeichner, die diese PHASE neu anlegt. Der
Drift-Abgleich liest diesen Abschnitt, um neu Entstandenes nicht als Abweichung
zu melden. Was hier nicht steht, ist nicht neu.

**CSS-Klassen und -Selektoren (neu):**
`.wb__id--loc`, `.wb__locsub`, `.wb__back`, `.wb__back__i`, `.wb__nm`,
`.wb__row2[data-loc]`, `.wb__row2[data-ore]`, `.wb__row2.is-trace`,
`.wb__tag.is-trace`, `.wb__tile.is-here`, `.wb__pin-item[data-loc]`

**Daten-Attribute (neu):** `data-loc`, `data-ore`, `data-back`

**Element-Ids (neu):** `wb-orehead`, `wb-lochead`, `wb-back`, `wb-locname`,
`wb-locsub`, `wb-oreview`, `wb-locview`

**SVG-Symbol-Id (neu):** `wb-i-back`

**Sprachschluessel in `S_DE`/`S_EN` (neu, je Paar):** `backToOre`, `trace`

**Zustandsfelder in `S` (neu):** `S.view`, `S.selLoc`

**Funktionen und Modulkonstanten in `assets/mining-workbench.js` (neu):**
`locIndex` (Modulvariable), `renderLocation()`, die Schwellenkonstante fuer
Spurenerze, die Methoden-Normalisierung, die feste Gruppenreihenfolge, das
Nachschlageobjekt der Kachel-Markierung in `renderList()` und der zweite
Abschnitt des Adresszweigs

**Erweiterte, nicht neue Signaturen:** `row2()` (achter optionaler Parameter),
`pctRight()` (zweiter optionaler Parameter), `pctSub()` (zweiter optionaler
Parameter)

**Adressparameter (neu, oeffentliche Zusage ab Auslieferung):** `?fundort=`

**Testfall-Kennungen (neu):** T-12-01 bis T-12-18

**Neue Datei:** `scripts/probes/mining-locview-messung.mjs` — Messsonde,
ausserhalb der Torkette, von keinem npm-Ziel gerufen

**Neuer Eintrag:** `.planning/WINDOWS.md` Kennung 12, Art `unrun-verify`,
Status offen

**Inhaltlich korrigierte, NICHT neue Schluessel:** `mining.ctl.locpin` und
`mining.ctl.shortlist` in `src/i18n/help.ts`, je DE und EN.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adresszeile → Client-Skript | `?fundort=` ist angreiferkontrollierte Eingabe, die beim Laden gelesen wird. Das ist die EINE echte Angriffsflaeche dieser Phase. |
| Katalogdaten → DOM | Orts- und Erznamen wandern weiterhin in `innerHTML`-gebautes Markup und in Attributwerte. |
| Messsonde → Arbeitsbaum | Die Sonde startet einen Browser und liest Bilddaten; sie darf nichts in den Arbeitsbaum schreiben. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Tampering / Information Disclosure | Adressparameter `fundort`, gelesen in `assets/mining-workbench.js` beim Init | high | mitigate | Abgleich gegen eine ALLOW-LIST: die 45 Schluessel des Fundort-Index, case-insensitiv ueber `trim()` und `toLowerCase()`. Nur bei Uebereinstimmung wird der KANONISCHE Schluessel in den Zustand geschrieben, nie der gelesene Wert. Kein Treffer bedeutet: nichts tun — kein Fehlertext, der den Wert wiederholen koennte. Das Lesen steckt in `try`/`catch`. Nachweis: Testfall T-12-17 prueft, dass ein eingegebener Wert mit Sonderzeichen und Skript-Anfang im gesamten gezeichneten Markup nirgends vorkommt. |
| T-12-10 | Tampering | Jeder Renderpfad, der einen Ortsnamen ausgibt | high | mitigate | Der Kopf wird ueber `textContent` gesetzt, jeder `innerHTML`-Pfad fuehrt Text UND Attributwerte durch `esc()`. In den Plaenen 01 und 02 bereits eingefuehrt und je durch einen Testfall mit den vier HTML-Sonderzeichen belegt; dieser Plan fuegt keinen neuen Ausgabepfad hinzu. |
| T-12-11 | Spoofing | Geteilter Verweis mit gefaelschtem Ortsnamen | low | accept | Die Ansicht liest ausschliesslich Katalogdaten und kennt keinen Nutzerzustand jenseits des lokalen Arbeitsstands. Ein gefaelschter Name trifft entweder einen echten Ort oder gar nichts. Kein Vertrauensgewinn moeglich. |
| T-12-12 | Denial of Service | Messsonde startet einen Browser | low | accept | Die Sonde laeuft ausserhalb der Torkette, von Hand, gegen eine lokale Vorschau. Sie haengt an keiner Strecke und kann kein Auslieferungsimage blockieren; sie schreibt nichts in den Arbeitsbaum. |
| T-12-SC | Tampering | npm/pip/cargo-Installationen | high | accept | Diese Phase installiert kein Paket (`12-RESEARCH.md` § Package Legitimacy Audit: nicht anwendbar). Die Sonde benutzt ausschliesslich `playwright-core` und `sharp`, die beide bereits im Bestand stehen; `package.json` und `package-lock.json` bleiben unveraendert und stehen NICHT in `files_modified`. Gegenprobe: `git status --porcelain` nennt nach diesem Plan ausschliesslich die acht gelisteten Dateien. |

ASVS-Stufe 1, blockierend ab `high`. V5 (Eingabevalidierung) ist die einzige
zutreffende ASVS-Kategorie dieser Phase und wird durch T-12-09 und T-12-10
vollstaendig abgedeckt; beide sind mitigiert. Der Lieferketten-Posten ist
gegenstandslos.
</threat_model>

<verification>
Vor jedem Push (`CLAUDE.md` § Liefern):

```
npm run build
npm run gate
```

Weil dieser Plan Tore, Stilblock und die Sonden-Uebersicht beruehrt,
zusaetzlich einmal als Vorschau in der Form, in der CI baut:

```powershell
$env:STAGING = '1'; npm run build; npm run gate
```

Danach `$env:STAGING` wieder loeschen. Auf Windows in PowerShell `npm.cmd`
statt `npm`; der Vorschau-Server im Worktree serviert dessen EIGENES `dist/`.

Die Messsonde laeuft NICHT im Tor — sie braucht einen Browser, den der
Baucontainer nicht hat. Sie wird von Hand gegen die lokale Vorschau gefahren,
und ihr Ergebnis wandert in die Zusammenfassung.

Beruehrte Strecken, einzeln gruen zu halten: `verify:wiring`, `verify:help`,
`verify:mining`, `verify:theme`, `verify:layers`, `audit:site`, `audit:csp`,
`verify:metrics`, `test:e2e`.

**Auslieferung:** Fertige, gepruefte Arbeit geht auf `staging`, nie auf `main`.
Fertig gemeldet wird erst, wenn die AUSGELIEFERTE Seite den neuen Stand zeigt —
`npm run check:staging` — nicht bei „committet", nicht bei „gemergt", nicht bei
„CI gruen".
</verification>

<success_criteria>
- Ein Aufruf mit dem Fundort-Parameter oeffnet die Fundort-Ansicht dieses
  Ortes; ein unbekannter Wert faellt still auf die Erz-Ansicht zurueck und
  erreicht das DOM auf keinem Weg.
- Beide Adressparameter vertragen sich nebeneinander.
- Beide Hilfetexte beschreiben ihre Zeile vollstaendig, in beiden Sprachen
  deckungsgleich; `verify:help` meldet weiterhin 12 von 12.
- Die Messsonde laeuft gruen, hat ihre Selbstauskunft gedruckt und war einmal
  vorgefuehrt rot.
- Jeder gemessene Kontrastwert der Spurenzeilen liegt bei mindestens 4,5:1, in
  beiden Farbmodi, am gerenderten Bildpunkt.
- Der Fundort-Kopf ist nicht hoeher als der Erz-Kopf.
- Die Sichtrunde liegt als offener, benannter Eintrag in `.planning/WINDOWS.md`
  beim Betreiber — vom Executor ausdruecklich NICHT durchgefuehrt.
- `npm run build && npm run gate` gruen, normal UND mit `STAGING=1`.
- `node --test tests/e2e/*.test.js` gruen; `mining-shortlist.test.js` traegt
  mindestens 54 Faelle.
</success_criteria>

<output>
Create `.planning/phases/12-fundorte-in-der-mining-werkbank-anklickbar/12-03-SUMMARY.md` when done
</output>
