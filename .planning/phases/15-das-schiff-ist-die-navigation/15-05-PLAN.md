---
phase: 15-das-schiff-ist-die-navigation
plan: 05
type: execute
wave: 5
depends_on: ["15-04"]
files_modified:
  - scripts/verify-shipcard.mjs
  - scripts/verify-shipconsole.mjs
  - scripts/lib/gate-registry.mjs
  - scripts/probes/schiffskarte-messung.mjs
  - .planning/WINDOWS.md
autonomous: true
requirements: [D-01, D-02, P-1, P-2, P-3]

must_haves:
  truths:
    - "Die Erweiterung des Entdopplungs-Scans war einmal VORGEFUEHRT ROT: ein Bewaffnungswert stand absichtlich sowohl in der Konsole als auch im alten Kapitel, `verify:shipcard` hat den Fund gemeldet, und die Meldung steht woertlich im SUMMARY (`15-UI-SPEC.md` Punkt 11.5, Grundsatz 1). Vorher ist die gruene Marke nichts wert."
    - "`verify:shipconsole` ist scharf: der Aussetzungsgrund ist aus `scripts/lib/gate-registry.mjs` entfernt, das Tor laeuft im Laufplan der Schiene A, und aus dieser Phase bleibt kein Schuldenposten gedruckt."
    - "Die Textbestands-Sperrklinke ist auf den nach dem Umbau gemessenen Wert festgeschrieben und wandert nur nach oben (Erfolgskriterium 3, Grundsatz 5) — mit Messlauf, Datum, Artefakt und Ist-Wert im Anlasstext."
    - "Die Hoehen-Sperrklinke aus Phase 14 ist NICHT umgangen worden: sie ist gegen den neuen Aufbau nachgemessen, und falls die Konsole einen neuen Hoehentreiber eingefuehrt hat, ist die Klinke praezisiert statt abgesenkt — mit einer Commit-Botschaft, die die Ursache nennt."
    - "`npm run build && npm run gate` sind gruen, ebenso der Vorschau-Bau mit `STAGING=1` — beides einschliesslich `verify:shipcard` und des nun scharfen `verify:shipconsole` (Erfolgskriterium 8)."
    - "Die deutsche und die englische Fassung sind deckungsgleich, in beiden Farbmodi (Erfolgskriterium 7) — am gerenderten Bildpunkt gemessen, nicht aus der Gleichheit des Koerpers gefolgert."
    - "Die fuenf Sichturteile aus dem Gestaltungsvertrag stehen einzeln, offen und mit ihrem gemessenen Wert in `.planning/WINDOWS.md`; Tabelle, JSON-Spiegel und Kopfzeilen-Zaehler sind deckungsgleich."
    - "Sichturteil 5 ist an die tatsaechlich getroffene Buehnen-Entscheidung angepasst und behauptet nicht einen Umzug, der nicht stattgefunden hat."
    - "Jede benannte Ausnahme beider Tore hat in ihrem Lauf mindestens einmal gegriffen — eine Ausnahme, deren Anlass verschwunden ist, deckt sonst spaeter echte Drift zu (Grundsatz 6)."
  artifacts:
    - .planning/WINDOWS.md
  key_links:
    - "Rot-Vorfuehrung <-> gruene Marke: die erweiterte Region wurde in Welle 2 eingebaut, konnte dort aber nicht rot werden, weil es noch keinen Konsolentext zu doppeln gab. Der Schuldenposten wurde dort ausdruecklich benannt und wird HIER eingeloest. Ohne diese Einloesung ist Erfolgskriterium 8 eine Behauptung."
    - "Scharfschaltung <-> Reihenfolge: `verify:shipconsole` wird erst scharf geschaltet, NACHDEM es gruen ist und NACHDEM sein Zombie-Waechter greift. Praezedenz `verify:sync` und `verify:shipcard`: erst beheben, dann scharf."
    - "Hoehenklinke <-> Messgegenstand: die Klinke aus Phase 14 misst die Seitenhoehe des Wurzelelements, der Gestaltungsvertrag spricht vom Kapitelbereich. Diese Doppeldeutigkeit muss aufgeloest werden, sonst misst die Klinke nach dieser Phase etwas anderes als vorher — und eine Klinke mit gewechseltem Massstab misst nichts."
    - "`gsd-tools windows append` <-> `.planning/WINDOWS.md`: der Befehl crasht auf dieser Datei (der Frontmatter-Parser laesst das Wagenruecklaufzeichen der CRLF-Zeilenenden stehen und verwirft seine eigene Zeitstempelzeile). Die Datei wird direkt bearbeitet, Tabelle und Spiegel und Zaehler in einem Schritt."
---

<objective>
Die Tore werden scharf, die Klinken werden festgeschrieben, und die fuenf
Fragen, die kein Skript beantwortet, gehen an den Betreiber.

Purpose: Erfolgskriterium 8 verlangt ausdruecklich, dass `verify:shipcard` an
den neuen Aufbau ANGEPASST wird statt umgangen zu werden. Angepasst ist es seit
Welle 2 — aber eine Anpassung, die nie rot war, ist Dekoration (Grundsatz 1).
Diese Welle loest den in Welle 2 benannten Schuldenposten ein: sie doppelt
absichtlich einen Bewaffnungswert und zeigt, dass die erweiterte Region den
Fund tatsaechlich meldet. Erst danach ist die gruene Marke etwas wert. Ebenso
das Konsolen-Tor: es wurde in Welle 2 rot vorgefuehrt und wird hier scharf
geschaltet, nachdem es gruen ist und sein Zombie-Waechter greift.

Und die Sperrklinken: der Textbestand bekommt seinen nach dem Umbau gemessenen
Wert, und die Hoehenklinke aus Phase 14 wird gegen den neuen Aufbau nachgemessen
— nicht umgangen, nicht abgesenkt, und wenn sie praezisiert werden muss, dann
mit einer Commit-Botschaft, die die Ursache nennt.

Output: die vorgefuehrte Rot-Meldung der erweiterten Region; ein scharfes
`verify:shipconsole` ohne Schuldenposten; zwei festgeschriebene Sperrklinken;
die Schlussmessung in beiden Sprachen und beiden Farbmodi; und fuenf
Sichturteile in `.planning/WINDOWS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-das-schiff-ist-die-navigation/15-CONTEXT.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-01-SUMMARY.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-02-SUMMARY.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-04-SUMMARY.md

@CLAUDE.md
@docs/maschinelle-validierung.md
</context>

<planning_notes>
**Diese Welle aendert die ausgelieferte Seite nicht.** Sie fasst Pruefwerkzeuge,
Registry und Planungsunterlagen an. Die einzige Ausnahme ist die absichtliche,
sofort zurueckgenommene Dopplung fuer die Rot-Vorfuehrung — sie wird NICHT
committet, und das ist ausdruecklich Teil der Aufgabe.

**Warum die Hoehenklinke einen eigenen Absatz braucht.** Phase 14 hat sie als
Seitenhoehe des Wurzelelements gemessen (Carrack, 1280x720, dunkel, hoechstens
4.200 px, Ist-Wert damals 4.179 px). Der Gestaltungsvertrag dieser Phase spricht
in Punkt 12 vom KAPITELBEREICH und stellt fest, dass die Klinke dort weiter gilt
und tendenziell sinkt, weil das Ausstattungs-Kapitel nur noch drei
Unterabschnitte traegt. Beides gleichzeitig kann falsch werden, sobald der
Konsolenblock waechst. Deshalb wird zuerst gemessen und dann entschieden — mit
einer festen Regel, nicht nach Gefuehl.

**Reversibilitaet.** Diese Welle traegt keine `one-way`-Entscheidung; ein
Entscheidungs-Checkpoint entfaellt ausdruecklich. Als `costly` gefuehrt: die
Scharfschaltung des Konsolen-Tors — ab ihr reisst jede kuenftige Aenderung an
der Schiffsseite, die eine seiner acht Zusicherungen verletzt, die
Auslieferung. Das ist der Zweck, aber es ist eine Verschaerfung mit Folgen.

**Kein `npm install`, keine Datenaenderung.**
</planning_notes>

<tasks>

<task type="auto">
  <name>Task 1: Die Rot-Vorfuehrung einloesen und das Konsolen-Tor scharf schalten</name>
  <files>scripts/verify-shipcard.mjs, scripts/verify-shipconsole.mjs, scripts/lib/gate-registry.mjs</files>
  <precondition>Der Stand nach Welle 4 ist gebaut, `node scripts/verify-shipcard.mjs` ist gruen und `node scripts/verify-shipconsole.mjs --report` meldet fuer die Zusicherungen 2 bis 7 keine Befunde.</precondition>
  <reversibility rating="costly">Nach der Scharfschaltung reisst jede kuenftige Aenderung an der Schiffsseite, die eine der acht Zusicherungen des Konsolen-Tors verletzt, die Auslieferung. Das ist der Zweck — aber es ist eine Verschaerfung mit Folgen fuer jede spaetere Arbeit an dieser Seite.</reversibility>
  <read_first>
    - `scripts/verify-shipcard.mjs` — `computeRegions()` in seinem Stand nach Welle 2 (die Erweiterung um den Konsolenblock) und die Selbstauskunft der Entdopplungs-Zusicherung.
    - `scripts/verify-shipconsole.mjs` VOLLSTAENDIG — alle acht Zusicherungen mit ihren Soll-/Ist-Zeilen und der Zombie-Waechter.
    - `scripts/lib/gate-registry.mjs` — die Eintraege `verify:shipcard` und `verify:shipconsole`, besonders das Feld `disabled` und der Kopfkommentar dazu.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-02-SUMMARY.md` — die dort protokollierte Rot-Meldung des Konsolen-Tors und der ausdruecklich benannte Schuldenposten „Rot-Vorfuehrung der erweiterten Region erst in Welle 5 moeglich".
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-04-SUMMARY.md` § Accomplishments, letzter Abschnitt — wie `verify:shipcard` seinerzeit scharf geschaltet wurde (Aussetzungsgrund entfernt, `run-gate --list` als Beleg, kein Schuldenposten mehr gedruckt). Dieselbe Reihenfolge gilt hier.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md` Punkt 11 vollstaendig, besonders 11.5.
    - `src/components/ShipDetail.astro` — die Stelle, an der der Bewaffnungs-Inhalt heute steht, und die Stelle im Ausstattungs-Kapitel, an der er bis Welle 3 stand. Beide werden fuer die Vorfuehrung gebraucht.
  </read_first>
  <action>
ZUERST DIE ROT-VORFUEHRUNG, DANN DIE SCHARFSCHALTUNG. Nicht umgekehrt.

VORFUEHRUNG DER ERWEITERTEN ENTDOPPLUNGS-REGION. Fuege voruebergehend im
Ausstattungs-Kapitel eine Zeile ein, die einen Zahl-plus-Einheit-Wert
wiederholt, der jetzt in der Konsole steht — der Gestaltungsvertrag nennt als
Beispiel einen Bewaffnungs-Kennwert. Baue neu und rufe `verify:shipcard` ohne
Schalter auf. Es MUSS reissen, und der Befund MUSS das Token samt beider
Regionen nennen: eine aus dem Kapitelbereich, eine aus dem Konsolenblock.
Protokolliere die Meldung woertlich im SUMMARY. Nimm die Dopplung danach
zurueck, baue erneut, und belege mit einem zweiten Lauf, dass das Tor wieder
gruen ist. Die Dopplung wird NICHT committet; das SUMMARY nennt ausdruecklich,
dass sie zurueckgenommen wurde, und der zweite gruene Lauf ist der Beleg.

Reisst das Tor bei der absichtlichen Dopplung NICHT, ist die Erweiterung aus
Welle 2 wirkungslos — dann ist der Fehler im Pruefer zu suchen (der wahrscheinlichste
Anlass: der Schnitt trifft den Konsolenblock nicht, weil sein Klassenattribut
inzwischen eine zweite Klasse traegt), bevor irgendetwas scharf geschaltet wird.

DANN DAS KONSOLEN-TOR SCHARF SCHALTEN. Fahre `node scripts/verify-shipconsole.mjs`
ohne Schalter: es muss gruen sein und seine acht Soll-/Ist-Zeilen drucken.
Pruefe die Zusicherung 8: existiert eine Ausnahmeliste, muss JEDE Ausnahme in
diesem Durchgang mindestens einmal gegriffen haben; existiert keine, muss die
Zusicherung das ausdruecklich sagen. Entferne erst danach den Aussetzungsgrund
aus dem Registry-Eintrag. Belege mit `node scripts/run-gate.mjs --list`, dass
das Tor jetzt im Laufplan der Schiene A steht und aus dieser Phase kein
Schuldenposten mehr gedruckt wird.

SCHREIBE DIE TEXTBESTANDS-KLINKE FEST. Setze sie auf den nach dem Umbau
gemessenen Minimalwert ueber alle 454 Seiten, abzueglich der in Welle 2
bezifferten und begruendeten Reserve. Der Anlasstext nennt Messlauf, Datum,
Artefakt, Ist-Wert, den Ausgangswert aus Welle 1 und die Differenz. Die Regel
bleibt „wandert nur nach oben". Liegt der neue Wert UNTER dem Ausgangswert,
wird die Klinke NICHT abgesenkt — dann ist Erfolgskriterium 3 verletzt, die
Ursache zu suchen und zu beheben, und der Vorgang gehoert an den Anfang des
SUMMARY.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/verify-shipcard.mjs</automated>
    <automated>node scripts/verify-shipconsole.mjs</automated>
    <automated>node scripts/run-gate.mjs --list</automated>
    <automated>npm run verify:wiring</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - Die Rot-Meldung der erweiterten Entdopplungs-Region steht woertlich im SUMMARY und nennt das gedoppelte Token samt beider Regionen — eine aus dem Kapitelbereich, eine aus dem Konsolenblock.
    - Nach der Ruecknahme der Dopplung ist `node scripts/verify-shipcard.mjs` wieder gruen (Rueckgabecode 0), und `git status --porcelain` nennt `src/components/ShipDetail.astro` NICHT — die Vorfuehrung wurde vollstaendig zurueckgebaut.
    - `node scripts/verify-shipconsole.mjs` gibt 0 zurueck und druckt fuer alle acht Zusicherungen eine Zeile mit den Woertern `Soll` und `Ist`.
    - Der Zombie-Waechter (Zusicherung 8) ist beantwortet: entweder greift jede benannte Ausnahme mindestens einmal (Trefferzahlen im SUMMARY), oder die Zusicherung meldet ausdruecklich, dass es keine Ausnahmen gibt.
    - `node -e "import('./scripts/lib/gate-registry.mjs').then(m=>{const e=m.CHECKS.find(c=>c.id==='verify:shipconsole');console.log(JSON.stringify({rail:e.rail,hasDisabled:!!e.disabled}))})"` liefert `rail` A und `hasDisabled` false.
    - `node scripts/run-gate.mjs --list` fuehrt `verify:shipconsole` im Laufplan und druckt keinen Schuldenposten aus Phase 15.
    - Die Textbestands-Klinke traegt den nach dem Umbau gemessenen Wert; ihr Anlasstext nennt Messlauf, Datum, Artefakt, Ist-Wert, den Ausgangswert aus Welle 1 und die Differenz, und die Regel lautet weiterhin, dass sie nur nach oben wandert.
    - `npm run verify:wiring` und `npm run gate` gruen, ebenso einmal mit `STAGING=1`.
  </acceptance_criteria>
  <done>
Die erweiterte Entdopplungs-Region war einmal vorgefuehrt rot, das Konsolen-Tor
laeuft scharf in der Torkette, und aus dieser Phase bleibt kein Schuldenposten
gedruckt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Schlussmessung — die Hoehenklinke aus Phase 14, DE gegen EN, beide Farbmodi</name>
  <files>scripts/probes/schiffskarte-messung.mjs</files>
  <precondition>Task 1 ist abgeschlossen, das gebaute `dist/` entspricht dem committeten Stand, und ein Vorschau-Server serviert dieses `dist/`.</precondition>
  <read_first>
    - `scripts/probes/schiffskarte-messung.mjs` VOLLSTAENDIG — die neun Messgruppen, die Ableitung der drei Pruefschiffe aus den Daten, und die Hoehen-Sperrklinke in Hausform samt ihrem Anlasstext aus Phase 14 (Wert 4.200 px, Regel, Ist-Wert 4.179 px DE / 4.117 px EN).
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md` Punkt 12 vollstaendig — die Aussage, dass die Klinke fuer den KAPITELBEREICH gilt und die Konsole ihre eigene, bereits bestehende Hoehenbegrenzung hat.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-04-SUMMARY.md` § Decisions Made, dritter Punkt — warum die Klinke bewusst 21 px ueber dem Ist-Wert stehen blieb.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-04-SUMMARY.md` — die dort gemessenen Buehnenbreiten, Fuellgrade und Sprachparitaets-Zahlen.
  </read_first>
  <action>
ZUERST MESSEN, DANN ENTSCHEIDEN.

Fahre `scripts/probes/schiffskarte-messung.mjs` unveraendert gegen den neuen
Stand: drei Pruefschiffe, zwei Sprachen, zwei Breiten, zwei Farbmodi, alle neun
Messgruppen. Halte die Ergebnisse gegen die in `14-04-SUMMARY.md`
protokollierten Werte.

Der entscheidende Wert ist die Seitenhoehe der Carrack bei 1280x720 im dunklen
Modus gegen die Marke von 4.200 px. Drei Faelle, mit einer festen Regel je Fall:

  Faellt sie oder bleibt sie darunter: nichts weiter zu tun. Der neue Ist-Wert
  wird im Anlasstext ergaenzt, die Marke bleibt stehen (Grundsatz 5: eine
  Klinke ist eine Untergrenze fuer kuenftige Laeufe, kein Momentwert).

  Ueberschreitet sie die Marke, weil der KAPITELBEREICH gewachsen ist: das
  waere ein echter Fehler — das Ausstattungs-Kapitel hat zwei Unterabschnitte
  verloren und darf nicht hoeher geworden sein. Ursache suchen und beheben,
  nicht die Marke anheben.

  Ueberschreitet sie die Marke, weil der KONSOLENBLOCK gewachsen ist: dann
  misst die Klinke seit dieser Phase etwas anderes als vorher, und eine Klinke
  mit gewechseltem Massstab misst nichts. In diesem Fall wird sie PRAEZISIERT,
  nicht abgesenkt: die bestehende Marke von 4.200 px gilt ab dann ausdruecklich
  fuer die gemessene Hoehe des Kapitelbereichs (die Groesse, die der
  Gestaltungsvertrag in Punkt 12 meint), und der Konsolenblock bekommt eine
  eigene, neue Klinke auf seinen jetzt gemessenen Wert. Beide Klinken stehen in
  Hausform mit Wert, Regel und Anlass; der Anlasstext nennt die Ursache, den
  alten und den neuen Massstab und diese Planstelle. Die Commit-Botschaft nennt
  die Ursache ebenfalls — eine Klinke, deren Bezug sich aendert, ohne dass die
  Botschaft es sagt, ist eine stille Absenkung.

Miss ausserdem, weil diese Welle die letzte Gelegenheit dafuer ist:

  — DE gegen EN je Pruefschiff: gleiche Zaehlungen, Hoehenabweichung hoechstens
    5 % (Erfolgskriterium 7, dieselbe Marke wie Phase 14).
  — Beide Farbmodi: die Konsole muss in hell wie dunkel lesbar sein. Der
    Kontrast wird am Bildpunkt gemessen, mit der bestehenden Kontrastbibliothek
    und keiner zweiten Formel — gegen die Marke 4,5:1, an der Zaehlzeile der
    Rail und an der Augenbraue der Auslesung. Bei einem Wert unter der Marke
    ist der Befund zu benennen; ob er behoben oder als Sichtpunkt uebergeben
    wird, entscheidet die Groesse des Eingriffs, aber verschwiegen wird er
    nicht.
  — Die Sprungleiste und die Kapitel aus Phase 14 stehen unveraendert: Zahl der
    Pillen gleich Zahl der Kapitel, keine Balkenspur ausserhalb des
    Leistungsprofils.

Fahre zum Abschluss die vollstaendige Kette in beiden Ausfuehrungen:
gewoehnlich und als Vorschau. Beide muessen gruen sein, einschliesslich der
beiden Schiffs-Tore. Protokolliere im SUMMARY die vollstaendige Messtabelle und
je Zeile den Wert aus Phase 14 daneben.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/probes/schiffskarte-messung.mjs --base http://localhost:4321</automated>
    <automated>node scripts/probes/schiffskonsole-messung.mjs</automated>
    <automated>node scripts/verify-shipcard.mjs</automated>
    <automated>node scripts/verify-shipconsole.mjs</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - Der Lauf der Kapitel-Sonde besteht alle Messpunkte; die Zahl der gemessenen Punkte steht in der Ausgabe und im SUMMARY, zusammen mit den Vergleichswerten aus `14-04-SUMMARY.md`.
    - Die Seitenhoehe der Carrack bei 1280x720 im dunklen Modus steht im SUMMARY, in beiden Sprachen, mit der Angabe, welcher der drei Faelle eingetreten ist und was daraufhin geschah.
    - Wurde die Klinke praezisiert, existieren danach ZWEI Klinken in Hausform (Kapitelbereich und Konsolenblock), jede mit Wert, Regel und Anlass; der Anlasstext nennt die Ursache und den Massstabswechsel, und die Commit-Botschaft nennt ihn ebenfalls. Wurde sie nicht praezisiert, sagt das SUMMARY das ausdruecklich und nennt den Ist-Wert.
    - Die Klinke wurde in keinem Fall abgesenkt: `grep -c "4200\|4\.200" scripts/probes/schiffskarte-messung.mjs` liefert weiterhin mindestens 1, und der Wert 4.200 steht unveraendert im Skript.
    - DE gegen EN: je Pruefschiff gleiche Zaehlungen und hoechstens 5 % Hoehenabweichung; die Tabelle steht im SUMMARY.
    - Beide Farbmodi sind gemessen: der Kontrast an der Zaehlzeile der Rail und an der Augenbraue der Auslesung steht je Farbmodus im SUMMARY, gemessen mit der bestehenden Kontrastbibliothek. Ein Wert unter 4,5:1 ist benannt, nicht verschwiegen.
    - Die Struktur aus Phase 14 steht unveraendert: Zahl der Pillen gleich Zahl der Kapitel, keine Balkenspur ausserhalb des Leistungsprofils.
    - `npm run build` und `npm run gate` gruen, ebenso einmal vollstaendig mit `STAGING=1` — beide einschliesslich `verify:shipcard` und `verify:shipconsole`.
  </acceptance_criteria>
  <done>
Die Hoehenklinke aus Phase 14 ist gegen den neuen Aufbau nachgemessen und nicht
umgangen, DE und EN sind deckungsgleich, beide Farbmodi sind gemessen, und die
Torkette ist in beiden Ausfuehrungen gruen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Die fuenf Fragen, die kein Skript beantwortet</name>
  <files>.planning/WINDOWS.md</files>
  <precondition>Task 2 ist abgeschlossen und die Messwerte liegen vor. Der Vorschau-Server serviert das committete `dist/`; Bildschirmfotos lassen sich mit `playwright-core` gegen den installierten Chrome aufnehmen.</precondition>
  <read_first>
    - `.planning/WINDOWS.md` — Kopfzeilen-Zaehler, Tabellenform, JSON-Spiegelblock, und der in Welle 1 angelegte Eintrag S-0 (er wird NICHT ersetzt, die fuenf neuen Punkte kommen dazu).
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md` § Sichturteile, Punkte 1 bis 5 vollstaendig.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-04-PLAN.md` § planning_notes, erster Absatz — die tatsaechlich getroffene Buehnen-Entscheidung. Sichturteil 5 ist daran anzupassen.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-04-SUMMARY.md` § Issues Encountered — warum `gsd-tools windows append` auf dieser Datei crasht und warum der JSON-Block ueber die Position des Codezauns statt ueber die letzte oeffnende eckige Klammer zu extrahieren ist.
  </read_first>
  <action>
ERST HINSEHEN, DANN SCHREIBEN. Nimm fuer jeden der fuenf Punkte die
Bildschirmfotos auf, die ein Mensch braucht, um ihn zu beurteilen, und schreibe
je Punkt das eigene Urteil in einem Satz ins SUMMARY. Kennzahlen messen die
Untergrenze, nicht die Guete.

Die fuenf Punkte, jeder mit seinem in dieser Phase GEMESSENEN Wert im Text —
ein Sichturteil ohne Messwert zwingt den Betreiber, selbst nachzumessen:

1. Die tragende Frage in ihrer schmalsten Form: Erkennt man bei der gemessenen
   Buehnenbreite auf dem Telefon auf einem drehbaren Mesh tatsaechlich, wo ein
   Marker sitzt — bei der groessten Einzelgruppe des dichtesten Pruefschiffs?
   Oder ist es ein Punktehaufen? Mitgeben: die gemessene Buehnenbreite, ein
   Bild vor UND nach der Auswahl eines einzelnen Markers, und ob das Drehen am
   schmalen Fenster fluessig genug ist, um suchend zu drehen. Dieser Punkt
   ergaenzt S-0 aus Welle 1 um die schmale Breite und die Endfassung; S-0
   bleibt als eigener Eintrag bestehen, weil er den Zustand vor der Konsole
   beurteilt.

2. Traegt die Rail als Einfachauswahl bei 1280x720 als Navigationsmuster, oder
   wirkt der Wechsel von der frueheren Mehrfachauswahl wie ein Rueckschritt fuer
   Nutzer, die mehrere Systeme gleichzeitig vergleichen wollten? Mitgeben: die
   Zahl der Systeme des Pruefschiffs und die Markerzahl je System.

3. Wirkt die Auslesung bei ihrer gemessenen Spaltenbreite noch uebersichtlich,
   oder kippt die Zeilenzahl der dichtesten Gruppe in eine unangenehm lange
   Liste? Mitgeben: die gemessene Spaltenbreite, die Zeilenzahl der dichtesten
   Gruppe und ob ein eigener Bildlauf eingefuehrt wurde.

4. Fuehlt sich das dichteste Pruefschiff in seiner vollsten Einzelgruppe noch
   bedienbar an, oder ist selbst EINE Gruppe dort schon zu voll fuer die
   dreispaltige Konsole? Mitgeben: Schiffskennung und die tatsaechlich
   gerenderte Markerzahl dieser Gruppe — nicht die Rohzahl aus den Daten.

5. Traegt die Buehne mit dem Mesh als Standardansicht noch genug Anziehungskraft
   beim ersten Blick, obwohl das kuratierte Marketing-Video jetzt hinter dem
   Umschalter liegt statt vorne zu stehen? Dieser Punkt ist gegenueber dem
   Gestaltungsvertrag UMFORMULIERT, weil die dort empfohlene Variante (Video
   und Galerie wandern ganz aus der Buehne) NICHT umgesetzt wurde — der
   Umschalter ist geblieben, nur der Standardmodus hat gewechselt. Die
   Abweichung und ihre Begruendung gehoeren in den Eintragstext, damit der
   Betreiber beurteilt, was tatsaechlich gebaut wurde.

DANN FESTSCHREIBEN. `gsd-tools windows append` wird ausdruecklich NICHT
verwendet. `.planning/WINDOWS.md` wird direkt bearbeitet, und zwar in EINEM
Schritt vollstaendig: fuenf Tabellenzeilen, fuenf Eintraege im JSON-Spiegelblock
und die Kopfzeilen-Zaehler (`open_count`, `total_count`, `last_updated`). Laufen
Tabelle und Spiegel auseinander, sind die Eintraege wertlos. Jeder Eintrag
traegt `kind: sight`, `phase: 15`, Status offen.

Pruefe zum Schluss, dass der JSON-Spiegelblock noch als JSON liest, und
verwende dafuer die Position des Codezauns als Blockanfang, NICHT die letzte
oeffnende eckige Klammer — diese naive Extraktion scheitert bereits am
bestehenden Dateiinhalt.

Die Phase wird NICHT als abgeschlossen markiert, solange diese Punkte offen
sind — dieselbe Konvention wie in den Phasen 1.2, 2, 3, 9, 10, 12 und 14 dieses
Projekts. Das SUMMARY sagt das ausdruecklich und nennt die Zahl der offenen
Punkte.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/WINDOWS.md','utf8');const i=s.indexOf('```json');const j=s.indexOf('```',i+7);const arr=JSON.parse(s.slice(i+7,j));const p15=arr.filter(e=>String(e.phase)==='15');console.log('JSON ok, gesamt',arr.length,'Phase 15:',p15.length);process.exit(p15.length===6?0:1)"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/WINDOWS.md','utf8');const i=s.indexOf('```json');const j=s.indexOf('```',i+7);const arr=JSON.parse(s.slice(i+7,j));const open=arr.filter(e=>e.status==='open').length;const m=/open_count:\s*(\d+)/.exec(s);const t=/total_count:\s*(\d+)/.exec(s);console.log('Kopf offen',m[1],'JSON offen',open,'Kopf gesamt',t[1],'JSON gesamt',arr.length);process.exit(Number(m[1])===open&&Number(t[1])===arr.length?0:1)"</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` traegt fuenf NEUE Eintraege mit `kind` gleich `sight` und `phase` gleich 15; zusammen mit S-0 aus Welle 1 sind es sechs Phase-15-Eintraege (die erste Verifikationszeile belegt das mit Rueckgabecode 0).
    - Jeder der fuenf Eintraege nennt den in dieser Phase GEMESSENEN Wert, auf den sich seine Frage bezieht — Buehnenbreite, Systemzahl, Markerzahl je System, Auslesungsbreite und Zeilenzahl der dichtesten Gruppe.
    - Eintrag 5 nennt ausdruecklich, dass die im Gestaltungsvertrag empfohlene Variante NICHT umgesetzt wurde, was stattdessen gebaut wurde und warum.
    - Tabelle und JSON-Spiegelblock sind deckungsgleich, und die Kopfzeilen-Zaehler sind mitgewandert (die zweite Verifikationszeile belegt das mit Rueckgabecode 0).
    - Der JSON-Spiegelblock liest als gueltiges JSON, geprueft ueber die Position des Codezauns statt ueber die letzte oeffnende eckige Klammer.
    - Zu jedem der fuenf Punkte liegen Bildschirmfotos vor, und das SUMMARY nennt je Punkt das eigene Urteil in einem Satz.
    - Das SUMMARY sagt ausdruecklich, dass die Phase wegen der offenen Sichturteile NICHT als abgeschlossen markiert wird, und nennt die Zahl der offenen Punkte.
    - `npm run gate` bleibt gruen; `git status --porcelain` nennt ausschliesslich `.planning/WINDOWS.md`.
  </acceptance_criteria>
  <done>
Die fuenf Fragen, die kein Skript beantwortet, stehen einzeln, offen und mit
ihrem Messwert beim Betreiber — und die Phase gibt ehrlich an, dass sie erst
nach dessen Sichtrunde abgeschlossen ist.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Vollstaendige Aufzaehlung der Bezeichner, die diese PHASE neu anlegt — ueber
alle fuenf Plaene. Der Drift-Abgleich liest diesen Abschnitt, um neu
Entstandenes nicht als Abweichung zu melden. Was hier nicht steht, ist nicht neu.

**Neue Dateien:** `scripts/probes/schiffskonsole-messung.mjs` (Welle 1),
`scripts/verify-shipconsole.mjs` (Welle 2), `scripts/lib/shipconsole-exclusions.mjs`
(Welle 2, NUR falls der Berichtslauf eine benannte Ausnahme erzwingt)

**Neues npm-Skript:** `verify:shipconsole`

**Neue Gate-Registry-Kennung:** `verify:shipconsole` (Schiene A, in Welle 2 mit
`disabled` und benanntem Anlass, in Welle 5 scharf)

**Neue Viewer-API:** `metrics()` am Rueckgabeobjekt von `initHolo`; das
Fensterhandle `window.__holoViewer` erscheint ausschliesslich bei der
Adressmarke `holometrics`

**Neue CSS-Klassen und -Selektoren (Wellen 3 und 4):** `.holo__rail`,
`.holo__rail-ic`, `.holo__rail-lb`, `.holo__rail-ct`, `.holo__readout`,
`.holo__sys`, `.holo__sys-h`, `.holo__sys-ct`, `.holo__sys-list`,
`.holo__sys-row`, `.holo.has-console`, `.holo__rail.is-chips`

**Neue Element-Ids:** `sys-core`, `sys-arms`, `sys-prop`, `sys-other`,
`holorail`, `holoreadout`

**Neue i18n-Schluessel in `src/i18n/ui.ts`, je DE und EN:**
`ship.console.rail.aria`, `ship.console.readout.aria`, `ship.console.count`,
`ship.defense.title`

**Neue Sperrklinken:** Fuellgrad-Klinke (P-1, 70 %, Ausgang rund 25 %) in
`scripts/probes/schiffskonsole-messung.mjs`; Textbestands-Klinke
(Erfolgskriterium 3) in `scripts/verify-shipconsole.mjs`; gegebenenfalls eine
Hoehen-Klinke fuer den Konsolenblock in `scripts/probes/schiffskarte-messung.mjs`,
falls Welle 5 die Praezisierung ausloest

**Neue Eintraege in der Bildlaufleisten-Ausnahmeliste (sechs Stellen, zwei
Dateien: `assets/mobile-ux.css` § 5c und `SEL_DRAG`/`SEL_FADE` in
`assets/scroll-affordance.js`):** `.holo__readout` und `.holo__rail.is-chips` —
jeweils NUR, falls sie einen eigenen Bildlauf-Kasten bekommen

**Erweitert, nicht neu:** `computeRegions()` in `scripts/verify-shipcard.mjs`
nimmt zusaetzlich zu `div.sd` den Block `section.holo` in seine Regionsbildung
auf; die Ausnahmeliste `scripts/lib/shipcard-exclusions.mjs` bleibt bestehen

**Getilgt, nicht neu:** `.holo__activate` samt Regel und das Element `holoact`;
der Beschriftungs-Umschalter `holoblabels` samt `.holo__layer--lab` und
`.holo__layer-eye`; die Regelgruppe fuer den eingeklappten Ebenen-Kasten; der
Klick-Ladepfad an `btn3d` als HAUPTweg (er bleibt als benannter Rueckfallweg
bestehen); der von Video- und Bildverfuegbarkeit abhaengige Ausdruck fuer
`defaultMode` (`ShipDetail.astro` Z. 400)

**Verwaist, nicht geloescht:** die i18n-Schluessel `holo.activate`,
`holo.labels.toggle`, `holo.labels.short`, `holo.layers.title`,
`holo.filter.aria` — als verwaist markiert, Loeschung liegt beim Executor und
ist kein Blocker
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dist/`-Dateien -> Pruefskripte | Beide Tore lesen gebautes HTML; die Rot-Vorfuehrung erzeugt voruebergehend einen abweichenden Bauzustand. |
| Registry -> Torkette | Das Entfernen des Aussetzungsgrunds macht ein Tor auslieferungsentscheidend. |
| Vorschau-Server -> Sonden | Die Schlussmessung laeuft ueber HTTP gegen das lokale `dist/`. |
| Planungsunterlagen -> Betreiber | `.planning/WINDOWS.md` ist die einzige Uebergabestelle fuer Sichturteile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-19 | Tampering | Die absichtliche Dopplung fuer die Rot-Vorfuehrung bleibt versehentlich stehen | high | mitigate | Nach der Vorfuehrung wird zurueckgebaut, neu gebaut und mit einem zweiten gruenen Lauf belegt; Abnahmekriterium ist ausserdem, dass `git status --porcelain` die Komponente NICHT nennt. Das nun scharfe `verify:shipcard` faenge einen Rest ohnehin bei jedem kuenftigen Lauf. |
| T-15-20 | Elevation of Privilege | Ein Tor wird scharf geschaltet, bevor es gruen ist oder bevor sein Zombie-Waechter greift | high | mitigate | Feste Reihenfolge im Task: gruener Lauf und greifender Zombie-Waechter ZUERST, Entfernen des Aussetzungsgrunds danach; `run-gate --list` als Beleg. Dieselbe Reihenfolge wie bei `verify:sync` und `verify:shipcard`. |
| T-15-21 | Repudiation | Eine Sperrklinke wird still abgesenkt oder ihr Bezug still gewechselt | high | mitigate | Feste Regel je Fall im Task; bei Praezisierung nennen Anlasstext UND Commit-Botschaft die Ursache und den Massstabswechsel. Gegenprobe: der Wert 4.200 bleibt nachweislich im Skript. |
| T-15-22 | Information Disclosure | Sichturteile gehen verloren, weil Tabelle und JSON-Spiegel auseinanderlaufen | medium | mitigate | Beide werden in EINEM Schritt geschrieben, und zwei Verifikationszeilen pruefen Gleichstand von Zaehlern und Spiegel mit Rueckgabecode. `gsd-tools windows append` wird ausdruecklich nicht benutzt, weil es auf dieser Datei crasht. |
| T-15-SC | Tampering | npm/pip/cargo-Installationen | high | accept | Diese Welle installiert kein Paket. `package.json` und `package-lock.json` stehen NICHT in `files_modified`. Gegenprobe: `git status --porcelain` nennt sie nicht. |

ASVS-Stufe 1, blockierend ab `high`. Alle vier `high`-Befunde sind mit einer
maschinellen Gegenprobe belegt; `T-15-SC` ist gegenstandslos. V5
(Eingabevalidierung) und V14 (Konfiguration) sind die beruehrten Kategorien.
</threat_model>

<verification>
Vor jedem Push, ohne Ausnahme (`CLAUDE.md` § Liefern):

```
npm run build
npm run gate
```

Beides gruen. Diese Welle schaltet ein Tor scharf und beruehrt Sperrklinken —
deshalb zusaetzlich einmal vollstaendig als Vorschau bauen:

```
$env:STAGING = '1'; npm run build; npm run gate
```

Auf Windows in PowerShell `npm.cmd` statt `npm`. Der Vorschau-Server im
Worktree serviert dessen EIGENES `dist/`.

Diese Strecken kann dieser Dateisatz reissen und muessen einzeln gruen sein:
`verify:wiring`, `verify:shipcard`, `verify:shipconsole`, `verify:metrics`,
und die vollstaendige Kette in beiden Ausfuehrungen.

Die Fertig-Meldung faellt erst, wenn die ausgelieferte Seite den neuen Stand
zeigt — `npm run check:staging`, nicht „committet", nicht „gemergt", nicht
„CI gruen".
</verification>

<success_criteria>
- Die erweiterte Entdopplungs-Region war einmal vorgefuehrt rot; die Meldung
  steht woertlich im SUMMARY, und die Dopplung ist zurueckgenommen.
- `verify:shipconsole` ist gruen, sein Zombie-Waechter greift, und es laeuft
  scharf im Laufplan der Schiene A; aus dieser Phase bleibt kein Schuldenposten
  gedruckt.
- Die Textbestands-Klinke traegt den nach dem Umbau gemessenen Wert und wandert
  nur nach oben.
- Die Hoehenklinke aus Phase 14 ist nachgemessen und nicht umgangen; eine
  etwaige Praezisierung nennt ihre Ursache in Anlasstext und Commit-Botschaft.
- DE und EN sind deckungsgleich, beide Farbmodi sind am Bildpunkt gemessen.
- Fuenf Sichturteile stehen offen in `.planning/WINDOWS.md`, jedes mit seinem
  Messwert; zusammen mit S-0 sind es sechs Phase-15-Eintraege.
- `npm run build` und `npm run gate` gruen, normal UND mit `STAGING=1`.
</success_criteria>

<output>
Create `.planning/phases/15-das-schiff-ist-die-navigation/15-05-SUMMARY.md` when done
</output>
