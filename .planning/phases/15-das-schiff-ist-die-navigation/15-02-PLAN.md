---
phase: 15-das-schiff-ist-die-navigation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - scripts/verify-shipconsole.mjs
  - scripts/lib/gate-registry.mjs
  - package.json
  - scripts/verify-shipcard.mjs
autonomous: true
requirements: [D-02, P-3]

must_haves:
  truths:
    - "Es gibt ein Pruefskript `scripts/verify-shipconsole.mjs`, das gegen das GEBAUTE `dist/` prueft und nicht gegen die Quelle — die Konsole wird am Auslieferungsartefakt belegt, nicht an der Vorlage (Grundsatz 7)."
    - "Das Tor war einmal VORGEFUEHRT ROT: gegen den heutigen Stand von `dist/` reisst es, weil es den ZIELZUSTAND dieser Phase prueft, und seine Meldung steht woertlich im SUMMARY (Grundsatz 1)."
    - "Das Tor druckt je Zusicherung eine Soll-/Ist-Zeile und nennt, WIE VIELE Schiffsseiten es gelesen hat; unter der Klinke von 440 Seiten reisst es (Grundsatz 2)."
    - "Das Tor laeuft zuerst als reiner BERICHT (`--report`, kein Urteil, Rueckgabecode 0) — eine Ausnahmeliste entsteht ERST aus dem, was der Bericht wirklich findet, nicht auf Verdacht."
    - "Der Registry-Eintrag traegt VORERST `disabled` mit dem Anlass und der Nennung des Plans, der ihn scharf schaltet — `npm run gate` bleibt in dieser Welle gruen, und die Schuld wird bei JEDEM Lauf gedruckt (Praezedenz `verify:sync` und `verify:shipcard`: erst beheben, dann scharf)."
    - "Ohne JavaScript zeigt die Seite alle Systeme untereinander als lesbare Liste (D-02) — das Tor prueft das an der Struktur des ausgelieferten HTML: kein Systemabschnitt traegt ein Versteck-Attribut, keiner liegt in einer Vorlagenhuelle, und die Rail besteht aus echten Ankern."
    - "Kein Rail-Eintrag ohne Marker (P-3): die Zahl am Rail-Eintrag ist groesser als null und gleich der Zahl der Marker dieser Gruppe im ausgelieferten `holodata` derselben Seite."
    - "Der indexierbare Textbestand je Schiffsseite hat eine Sperrklinke, die nur nach oben wandert (Erfolgskriterium 3) — mit dem in Welle 1 gemessenen Ausgangswert als Anlass."
    - "Der Entdopplungs-Scan von `verify:shipcard` sieht ab jetzt auch in die Konsole hinein. Ohne diese Erweiterung koennte er die Regel „kein Bauteiltext zweimal" gar nicht pruefen, weil die Konsole ausserhalb von `div.sd` liegt — das waere ein blinder Fleck und keine Entwarnung (`15-UI-SPEC.md` Punkt 11.3)."
    - "Die Erweiterung des Entdopplungs-Scans nimmt AUSDRUECKLICH nur `section.holo` auf, nicht die Kennwerte-Leiste daneben — die wiederholt Laenge, Fracht und Preis absichtlich als erster Fixpunkt (`15-UI-SPEC.md` § Blickfuehrung 1) und wuerde sonst jede Seite als Doppelung melden."
    - "Beide Tore bleiben schienenfaehig fuer Schiene A: kein git, kein Netz, kein Kindprozess, keine `Data.p4k` (Grundsatz 4)."
    - "Diese Welle aendert keine einzige ausgelieferte Zeile: `npm run build` und `npm run gate` bleiben gruen, `git status` nennt ausschliesslich die vier Dateien dieses Plans."
  artifacts:
    - scripts/verify-shipconsole.mjs
  key_links:
    - "`scripts/verify-shipconsole.mjs` <-> `scripts/lib/gate-registry.mjs` <-> `package.json`: alle drei muessen denselben Namen `verify:shipconsole` tragen. Fehlt einer, reisst `verify:wiring` — und das ist Absicht (der Waechter hat schon zweimal seinen eigenen Erbauer gefangen)."
    - "Tor <-> Welle 3/4: dieses Tor beschreibt den Zielzustand, den die Wellen 3 und 4 herstellen. Jede Zusicherung, die hier formuliert wird, ist dort eine Bauvorschrift — wer sie hier zu schwach fasst, baut dort ins Ungefaehre."
    - "Textbestands-Klinke <-> `visibleText()`: Tor und Sonde muessen DIESELBE Textdefinition benutzen, sonst behaupten zwei Zahlen dasselbe und meinen Verschiedenes. Die Zaehlung aus `15-01` hat sie bereits aus `verify-shipcard.mjs` uebernommen."
    - "`section.holo` <-> `div.sd__jump` <-> `div.sd`: drei Bloecke auf derselben Ebene. Der Entdopplungs-Scan nimmt den ersten und den dritten, ausdruecklich nicht die Kennwerte-Leiste `div.holo__bar`, die zwischen ihnen liegt."
---

<objective>
Werkzeug vor Eingriff, zum zweiten Mal in dieser Phase. Diese Welle baut das
Tor, an dem die Konsole spaeter gemessen wird — und fuehrt es einmal absichtlich
rot vor, bevor die erste Zeile Konsole entsteht.

Purpose: Ein Tor, das erst NACH dem Umbau entsteht, ist gegen genau den Zustand
geschrieben, den es beurteilen soll; es kann gar nicht mehr rot werden
(`docs/maschinelle-validierung.md` Grundsatz 1, dieselbe Begruendung wie in
`14-01-PLAN.md`). D-02 ist ausserdem die Bedingung, unter der D-01 ueberhaupt
sicher ist: die Konsole darf die Kapitel nur ersetzen, solange jede Auslesung im
ausgelieferten HTML steht. Diese Zusicherung muss maschinell sein — bei rund
17.000 indexierbaren Seiten und einem Zulauf, der praktisch vollstaendig aus
Suche kommt, ist eine im Quelltext leere Schiffsseite kein Schoenheitsfehler,
sondern der Verlust des Zulaufs.

Dazu kommt eine Reparatur, die keine Kuer ist: `verify:shipcard`s
Entdopplungs-Scan zerlegt heute NUR `div.sd` in Regionen. Die Konsole entsteht
in `section.holo` und liegt damit ausserhalb seines Blickfelds. Ohne
Erweiterung meldete er die Dopplung „21 DPS in der Konsole UND im alten
Ausstattungs-Kapitel" nicht — er saehe sie gar nicht.

Output: `scripts/verify-shipconsole.mjs` mit acht Zusicherungen, sein
Registry-Eintrag (vorerst ausgesetzt) und sein npm-Skript; die vorgefuehrte
Rot-Meldung; und ein `verify:shipcard`, dessen Regionsbildung die Konsole
mitnimmt und das selbst auskunftsfaehig sagt, wie viele Regionen von dort
stammen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-das-schiff-ist-die-navigation/15-CONTEXT.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md
@.planning/phases/15-das-schiff-ist-die-navigation/15-01-SUMMARY.md

@CLAUDE.md
@docs/maschinelle-validierung.md
</context>

<planning_notes>
**Warum ein ZWEITES Tor statt einer Erweiterung von `verify:shipcard`.**
`verify:shipcard` ist seit `14-04` scharf und gruen. Haengte man die
Konsolen-Zusicherungen dort an, waere die Torkette ab dem naechsten Lauf rot —
denn sie pruefen einen Zustand, den es noch nicht gibt — und `npm run gate`
duerfte drei Wellen lang nicht gruen sein. Das verstiesse gegen die Hausregel,
dass vor jedem Push beides gruen ist. Der Ausweg ist der bereits zweimal
gefahrene: ein eigenes Tor mit `disabled` und benanntem Anlass, dessen Schuld
bei jedem Lauf gedruckt wird, scharf geschaltet in Welle 5.

Was NICHT in das neue Tor gehoert, sondern in das alte: der Entdopplungs-Scan.
Er existiert bereits, hat seine Ausnahmeliste und seinen Zombie-Waechter, und
zwei Scans mit zwei Regionenbegriffen waeren zwei Fehlerbilder fuer dieselbe
Frage. Er wird deshalb erweitert, nicht dupliziert — genau wie `15-UI-SPEC.md`
Punkt 11 es vorgibt.

**Warum die Erweiterung des Scans heute gefahrlos ist und trotzdem noetig.**
`section.holo` traegt heute Titel, Rollen-Chips, Buehne, Ebenen-Kasten und die
leere Detailkarte. Zahl-plus-Einheit-Token stehen dort keine, die Erweiterung
ist also gegen den heutigen Stand ein Nulleffekt — das Tor bleibt gruen. Nach
Welle 3 traegt derselbe Block die vollstaendigen Systemlisten, und dann greift
sie. Vorgefuehrt rot werden kann sie erst dort, wo es etwas zu doppeln gibt;
das ist ausdruecklich Aufgabe von Welle 5 (`15-UI-SPEC.md` Punkt 11.5) und hier
als Schuldenposten benannt statt verschwiegen.

**Kein `npm install`.** Am `package.json` wird AUSSCHLIESSLICH eine Zeile im
`scripts`-Block ergaenzt; `package-lock.json` bleibt unberuehrt und steht nicht
in `files_modified`.

**Reversibilitaet.** Diese Welle traegt keine `one-way`-Entscheidung; ein
Entscheidungs-Checkpoint entfaellt ausdruecklich. Als `costly` gefuehrt: die
Formulierung der Zusicherungen — sie sind ab hier die Bauvorschrift fuer die
Wellen 3 und 4, und eine spaetere Lockerung entwertet die Rot-Vorfuehrung.
</planning_notes>

<tasks>

<task type="auto">
  <name>Task 1: Das Konsolen-Tor — erst Bericht, dann Urteil, einmal vorgefuehrt rot</name>
  <files>scripts/verify-shipconsole.mjs, scripts/lib/gate-registry.mjs, package.json</files>
  <precondition>`dist/` existiert und enthaelt `dist/schiffe/anvl-carrack.html` sowie `dist/de/schiffe/anvl-carrack.html`. Fehlt es, zuerst `npm run build` — dieses Skript prueft den GEBAUTEN Stand, nicht die Quelle.</precondition>
  <reversibility rating="costly">Die acht Zusicherungen sind ab hier die Bauvorschrift fuer die Wellen 3 und 4. Wer sie spaeter lockert, um gruen zu werden, entwertet die Rot-Vorfuehrung und mit ihr den ganzen Beleg fuer D-02.</reversibility>
  <read_first>
    - `scripts/verify-shipcard.mjs` VOLLSTAENDIG — die Hausform in ihrer aktuellsten Auspraegung: Abbruch bei fehlendem `dist/`, nummerierte Zusicherungen mit Soll-/Ist-Zeile, `fail()`-Sammelurteil, `--report`-Modus, Tag-Abtastung ohne HTML-Bibliothek, `extractRegion()`, `splitTopLevelChildren()`, `visibleText()`, `findPagePairs()`/`assertMinimumPairs()`, Zombie-Waechter. Diese Datei ist die Vorlage; das neue Tor erfindet nichts davon neu, es importiert oder kopiert mit Quellenangabe.
    - `scripts/lib/shipcard-exclusions.mjs` VOLLSTAENDIG — die Form einer benannten Ausnahmeliste: `id`, Modus, Trefferbedingung, `reason` als vollstaendiger Anlasstext. Der Warnsatz im Kopf gilt hier wortgleich.
    - `scripts/lib/gate-registry.mjs` Kopfkommentar (Feldbeschreibung, besonders `rail`, `env`, `disabled`, `why`) und die Schiene-A-Liste bis einschliesslich des Eintrags `verify:shipcard`.
    - `scripts/verify-metrics.mjs` Kopfkommentar und `scripts/lib/metrics-baseline.mjs` Z. 140-165 — die Sperrklinken-Sprache dieses Hauses (`wert`, `regel`, `toleranzProzent`, `anlass`).
    - `src/components/ShipDetail.astro` Z. 1184-1195 (`section.holo` mit `id` und `data-media-surface`), Z. 1356-1368 (der `holodata`-Skriptblock und die Kennwerte-Leiste `div.holo__bar` DANEBEN), Z. 1382-1392 (`nav.sd__jump` und `div.sd`). Das sind die drei Bloecke auf derselben Ebene, aus denen sich entscheidet, wohin der Entdopplungs-Scan schaut.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md` Detailvertrag Punkt 4, 5, 6 und 11 vollstaendig.
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-01-SUMMARY.md` — gewaehlte P-3-Variante, Rail-Laengen-Verteilung und der gemessene Textbestand (Minimum ueber alle Seiten und der Carrack-Wert).
    - `docs/maschinelle-validierung.md` § 4, besonders die Grundsaetze 1, 2, 3, 4, 5, 6 und 7.
  </read_first>
  <action>
Lege `scripts/verify-shipconsole.mjs` an. Es prueft `dist/schiffe/*.html` und
`dist/de/schiffe/*.html` und sonst nichts. Kein git, kein Netz, kein
Kindprozess, keine `Data.p4k` — schienenfaehig fuer Schiene A. Kopfkommentar in
der Hausform: warum es existiert (D-02 als Bedingung fuer D-01, mit der
Zulaufbegruendung und dem gemessenen Ausgangswert aus Welle 1), gegen WELCHES
Artefakt es prueft, die Aufzaehlung der Zusicherungen, und der Aufrufhinweis.

ACHT ZUSICHERUNGEN, jede mit eigener nummerierter Ueberschrift und einer
Soll-/Ist-Zeile:

1 Bestand und Selbstauskunft. Gelesene Schiffsseiten je Sprache und in Summe;
Sperrklinke bei 440, darunter ist der Lauf selbst kaputt und nicht die Seite.

2 Konsolen-Geruest. Je Seite mindestens ein Element mit der Klasse `holo__sys`;
jedes traegt eine `id` aus der festen Menge `sys-core`, `sys-arms`, `sys-prop`,
`sys-other`, keine doppelt; jedes liegt INNERHALB des Blocks `section.holo`
(Tiefenzaehlung wie `extractRegion()`, nicht Positionsvergleich im Rohtext).
Drucke die Verteilung, wie viele Seiten vier, drei, zwei und ein System tragen —
und halte sie gegen die in Welle 1 protokollierte Verteilung. Weichen die beiden
ab, ist eine der beiden Zaehlungen falsch. Klassen werden IMMER ueber die
zerlegte Klassenliste geprueft, nie ueber die literale Zeichenkette des
Klassenattributs.

3 Bijektion Rail zu System. Jeder Eintrag der Rail zeigt auf genau eine
vorhandene System-`id` derselben Seite, und jedes System hat genau einen
Rail-Eintrag. Drucke die Zahl der geprueften Paare.

4 Kein Eintrag ohne Marker (P-3). Lies den `holodata`-Skriptblock derselben
Seite, zaehle die Ports je Gruppe, und halte die Zahl gegen den Zaehl-Chip des
zugehoerigen Rail-Eintrags: sie muss gleich und groesser als null sein. Eine
linke Spalte, die ein System anbietet und dann eine leere Buehne zeigt, ist
schlimmer als gar kein Eintrag — genau das faengt diese Zusicherung. Drucke die
Zahl der geprueften Gruppen und die kleinste gefundene Markerzahl.

5 Ohne JavaScript sichtbar (D-02). Kein Element mit der Klasse `holo__sys`
traegt im ausgelieferten HTML ein Versteck-Attribut; keines liegt innerhalb
eines Vorlagen-Elements; die Rail-Eintraege sind echte Anker, deren Ziel mit
der Raute beginnt und auf eine System-`id` derselben Seite zeigt. Zusaetzlich:
der sichtbare Text INNERHALB der Systemabschnitte ist je Seite nicht leer —
drucke die kleinste gefundene Bytezahl und die Seite dazu.

6 Textbestand (Erfolgskriterium 3). Miss je Seite den sichtbaren Text nach dem
Verfahren von `visibleText()` und halte ihn gegen eine Sperrklinke in Hausform
(Wert, Regel „wandert nur nach oben", Anlass mit Messlauf, Datum, Artefakt und
Ist-Wert). Die Klinke bekommt in dieser Welle den in Welle 1 gemessenen
Minimalwert ueber alle 454 Seiten, abzueglich einer benannten Reserve gegen
Rundungsschwankungen zwischen zwei Laeufen desselben Standes — die Reserve wird
im Anlasstext beziffert und begruendet, nicht stillschweigend gewaehlt
(Praezedenz: die Hoehenklinke aus `14-04` blieb aus genau diesem Grund 21 px
ueber dem Ist-Wert stehen). Drucke Minimum, Median und Maximum sowie die Seite
am Minimum.

7 Sprachparitaet. Ueber `findPagePairs()` je EN-Seite mit DE-Gegenstueck: die
Zaehlungen von Systemabschnitten, Rail-Eintraegen und Markern je Gruppe muessen
paarweise uebereinstimmen. Ein Koerper fuer beide Sprachen heisst, dass jede
Abweichung hier eine sprachbedingte Verzweigung ist, die es nicht geben darf.
Eigene Untergrenze mit `assertMinimumPairs()`: unter 200 Paaren ist die Paarung
selbst kaputt.

8 Zombie-Waechter. Falls eine Ausnahmeliste entsteht, muss jede Ausnahme in
DIESEM Durchgang mindestens einmal gegriffen haben, und die Zusicherung druckt
je Ausnahme die Trefferzahl. Entsteht keine, druckt sie das ausdruecklich als
„keine Ausnahmen" und bleibt bestehen — eine spaeter hinzugefuegte Ausnahme
faellt sonst unter keinen Waechter.

BERICHTSMODUS. Mit dem Schalter `--report` laeuft dasselbe Verfahren, druckt
alle Befunde, faellt aber KEIN Urteil und gibt 0 zurueck. Begruendung im
Kopfkommentar mit dem Anlass: am 16.08.2026 steckten in diesem Projekt zwei
Fehler im Pruefer selbst — ein neues Tor faehrt zuerst als reiner Bericht.

Eine Ausnahmeliste `scripts/lib/shipconsole-exclusions.mjs` entsteht NUR, wenn
der Berichtslauf in Welle 3 oder 5 sie erzwingt, und dann in der Form von
`scripts/lib/shipcard-exclusions.mjs` mit ausformuliertem Anlass je Eintrag.
Auf Verdacht angelegt reisst sie sofort den Zombie-Waechter.

Trage das Tor in `scripts/lib/gate-registry.mjs` in die Schiene-A-Liste ein,
direkt hinter `verify:shipcard`: `id` und `npm` je `verify:shipconsole`,
`script` der Dateipfad, `rail` A, `checks` in einem Satz. `env` bleibt leer.
Setze `disabled` mit dem Anlass: das Tor beurteilt den Zielzustand dieser Phase
und ist bis zu deren Abschluss zwangslaeufig rot; scharf geschaltet wird es in
`15-05-PLAN.md`. Die Praezedenz („erst beheben, dann scharf") ist im Anlass zu
nennen. Ergaenze in `package.json` im `scripts`-Block die Zeile
`verify:shipconsole`, im Stil der Nachbarn.

FUEHRE DAS TOR ROT VOR. Baue nichts um; rufe das Skript gegen den heutigen Stand
von `dist/` auf, einmal mit `--report` und einmal ohne. Ohne Schalter MUSS es
reissen. Protokolliere im SUMMARY woertlich: die erste reissende Zusicherung mit
ihrer Soll-/Ist-Zeile und die gelesene Seitenzahl. Erwartet wird mindestens:
null Systemabschnitte statt mindestens einem je Seite. Wird das Tor gegen den
heutigen Stand GRUEN, ist es kaputt und nicht die Seite — dann ist der Fehler im
Pruefer zu suchen, bevor irgendetwas anderes passiert.
  </action>
  <verify>
    <automated>node scripts/verify-shipconsole.mjs --report</automated>
    <automated>node scripts/verify-shipconsole.mjs; test $? -ne 0</automated>
    <automated>npm run verify:wiring</automated>
    <automated>node scripts/run-gate.mjs --list</automated>
    <automated>npm run build</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-shipconsole.mjs --report` laeuft durch und gibt 0 zurueck; die Ausgabe nennt eine Seitenzahl von mindestens 440.
    - `node scripts/verify-shipconsole.mjs` gibt gegen den heutigen Stand von `dist/` einen Rueckgabecode ungleich 0 zurueck — die vorgefuehrte Rot-Meldung. Ihre erste reissende Zusicherung steht woertlich im SUMMARY.
    - Die Ausgabe enthaelt fuer JEDE der acht Zusicherungen eine Zeile mit den Woertern `Soll` und `Ist` — Selbstauskunft, Grundsatz 2.
    - Die Textbestands-Klinke steht in Hausform: `grep -c "wandert nur nach oben" scripts/verify-shipconsole.mjs` liefert mindestens 1, und der Anlasstext nennt Messlauf, Datum, Artefakt, Ist-Wert und die bezifferte Reserve.
    - `npm run verify:wiring` bleibt gruen — Beleg fuer die Bijektion Verzeichnis, Dateibestand, `package.json`.
    - `node -e "import('./scripts/lib/gate-registry.mjs').then(m=>{const e=m.CHECKS.find(c=>c.id==='verify:shipconsole');console.log(JSON.stringify({rail:e.rail,hasDisabled:!!e.disabled,hasChecks:!!e.checks}))})"` liefert `rail` A, `hasDisabled` true und `hasChecks` true.
    - `node scripts/run-gate.mjs --list` nennt `verify:shipconsole` als ausgesetzten Schuldenposten und fuehrt es NICHT im Laufplan.
    - `npm run gate` bleibt gruen — das ausgesetzte Tor darf die Kette in dieser Welle nicht reissen.
    - `grep -n "child_process\|execFileSync\|fetch(" scripts/verify-shipconsole.mjs` liefert keinen Treffer — Schienenfaehigkeit fuer Schiene A, Grundsatz 4.
    - `git status --porcelain` nennt ausschliesslich die drei Dateien dieses Tasks; `src/` und `package-lock.json` bleiben unberuehrt.
  </acceptance_criteria>
  <done>
Das Konsolen-Tor existiert, ist verkabelt, war einmal vorgefuehrt rot und seine
Meldung ist protokolliert. `npm run gate` bleibt gruen, weil der Eintrag seinen
Anlass als ausgesetzte Strecke traegt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Der Entdopplungs-Scan bekommt die Konsole ins Blickfeld</name>
  <files>scripts/verify-shipcard.mjs</files>
  <precondition>`dist/` ist auf dem Stand nach Welle 1 gebaut, und `node scripts/verify-shipcard.mjs` ist auf diesem Stand gruen. Ist es das nicht, hat Welle 1 etwas beruehrt, das sie nicht beruehren sollte — dann zuerst das klaeren.</precondition>
  <read_first>
    - `scripts/verify-shipcard.mjs` Z. 136-165 (`extractRegion()` mit ihrer Tiefenzaehlung), Z. 166-235 (`splitTopLevelChildren()`, `regionKeyForChild()`, `splitSubHeadings()`), Z. 240-270 (`visibleText()`, `extractTokens()`), Z. 272-335 (`computeRegions()` und `findingsForPage()`) — das ist der gesamte anzupassende Bereich.
    - `scripts/lib/shipcard-exclusions.mjs` VOLLSTAENDIG — vier bestehende Ausnahmen; keine von ihnen darf durch die Erweiterung ihren Anlass verlieren, sonst reisst der Zombie-Waechter.
    - `src/components/ShipDetail.astro` Z. 1184 (`section.holo` — Klassenattribut, `id`, weitere Attribute), Z. 1355-1380 (das schliessende Tag, der `holodata`-Block und die Kennwerte-Leiste `div.holo__bar` als eigenes, DANEBEN liegendes Element).
    - `.planning/phases/15-das-schiff-ist-die-navigation/15-UI-SPEC.md` Detailvertrag Punkt 10 (letzter Aufzaehlungspunkt) und Punkt 11.3 und 11.5.
    - `.planning/phases/14-schiffs-datenkarte-entstapeln/14-01-PLAN.md` Task 1, Abschnitt zur Zusicherung 6 — der Regionenbegriff in seiner urspruenglichen Formulierung, damit die Erweiterung ihn fortschreibt statt ihn zu ersetzen.
  </read_first>
  <action>
Erweitere `computeRegions()` in `scripts/verify-shipcard.mjs` so, dass die
Regionsbildung ZWEI Bloecke abtastet statt einem: weiterhin `div.sd`, und
zusaetzlich den Block `section.holo`. Beide werden mit derselben
Tiefenzaehlung geschnitten, beide in dieselbe Regionsliste zerlegt, beide
unterliegen denselben benannten Ausnahmen und derselben Unterabschnitts-
Aufteilung. Ein Token, das im einen und im anderen Block steht, ist damit ein
Befund — und genau das ist der Zweck.

Der Kommentar an der Fundstelle nennt den Anlass ausdruecklich: die Konsole
liegt ausserhalb von `div.sd`; ohne diese Erweiterung koennte der Scan die in
`15-UI-SPEC.md` Punkt 6 verbindliche Regel „kein Bauteiltext zweimal im DOM"
gar nicht pruefen — er saehe dort nicht hin, und das waere ein blinder Fleck
und keine Entwarnung.

AUSDRUECKLICH NICHT MITGENOMMEN wird die Kennwerte-Leiste. Sie ist ein eigenes
Element neben `section.holo`, kein Kind davon, und sie wiederholt Laenge,
Besatzung, Fracht, Tempo und Preis absichtlich als ersten verlaesslichen
Fixpunkt der Seite (`15-UI-SPEC.md` § Blickfuehrung 1). Naehme der Scan sie
mit, meldete er auf JEDER der 454 Seiten mehrere Befunde, die alle
Entwurfsentscheidungen sind — Fehlalarme sind teurer als Luecken (Grundsatz 3).
Der Schnitt muss deshalb am Klassenattribut exakt unterscheiden und darf nicht
ueber ein Praefix greifen; schreibe die Gegenprobe dafuer in denselben
Kommentar.

SELBSTAUSKUNFT ERWEITERN (Grundsatz 2). Zusicherung 6 druckt ab jetzt
zusaetzlich, wie viele Regionen aus `div.sd` und wie viele aus `section.holo`
stammen, je Sprache und in Summe. Ohne diese Zahl ist ein leerlaufender Scan
von einem echten nicht zu unterscheiden — und ein Schnitt, der `section.holo`
verfehlt (etwa weil das Klassenattribut spaeter eine zweite Klasse bekommt),
ist genau so ein Leerlauf.

Fahre danach `node scripts/verify-shipcard.mjs` gegen den heutigen Stand. Es
MUSS gruen bleiben: `section.holo` traegt heute Titel, Rollen-Chips, Buehne,
Ebenen-Kasten und die leere Detailkarte, aber keine Zahl-plus-Einheit-Token.
Reisst es doch, ist das ein echter Fund und im SUMMARY zu benennen, statt die
Erweiterung zurueckzudrehen.

DIE ROT-VORFUEHRUNG DIESER ERWEITERUNG IST HIER NOCH NICHT MOEGLICH und wird
ausdruecklich als Schuldenposten in das SUMMARY geschrieben: es gibt heute
keinen Konsolentext, den man doppeln koennte. Sie ist Aufgabe von
`15-05-PLAN.md` (`15-UI-SPEC.md` Punkt 11.5: einen Bewaffnungswert absichtlich
sowohl in der Konsole als auch im alten Ausstattungs-Kapitel stehen lassen und
zeigen, dass die erweiterte Region den Fund meldet). Als Zwischenbeleg, dass
der Schnitt ueberhaupt greift, dient in dieser Welle die neue Selbstauskunft:
sie muss je Seite mindestens eine Region aus `section.holo` melden.
  </action>
  <verify>
    <automated>node --check scripts/verify-shipcard.mjs</automated>
    <automated>node scripts/verify-shipcard.mjs --report</automated>
    <automated>node scripts/verify-shipcard.mjs</automated>
    <automated>npm run verify:wiring</automated>
    <automated>npm run gate</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-shipcard.mjs` bleibt gruen (Rueckgabecode 0) gegen den nach Welle 1 gebauten Stand.
    - Die Ausgabe von Zusicherung 6 nennt getrennt, wie viele Regionen aus `div.sd` und wie viele aus `section.holo` stammen; die zweite Zahl ist groesser als null und im SUMMARY protokolliert.
    - Die Zahl der Regionen aus `section.holo` ist je Seite mindestens 1 — die kleinste ueber alle Seiten gefundene Zahl steht in der Ausgabe.
    - Die Kennwerte-Leiste ist NICHT im Zaehlbereich: das SUMMARY belegt das mit der gemeldeten Zahl der Befunde (unveraendert gegenueber dem Lauf vor der Erweiterung) und nennt die Stelle, an der der Schnitt exakt am Klassenattribut unterscheidet.
    - Der Zombie-Waechter (Zusicherung 8) bleibt gruen: alle vier bestehenden Ausnahmen haben in diesem Durchgang weiterhin gegriffen, und die Trefferzahlen stehen im SUMMARY.
    - Das SUMMARY nennt ausdruecklich als Schuldenposten, dass die Rot-Vorfuehrung dieser Erweiterung erst in `15-05-PLAN.md` moeglich ist, und warum.
    - `npm run verify:wiring` und `npm run gate` bleiben gruen; `git status --porcelain` nennt ausschliesslich `scripts/verify-shipcard.mjs`.
  </acceptance_criteria>
  <done>
Der Entdopplungs-Scan schaut ab jetzt auch dorthin, wo die Konsole entstehen
wird — und sagt selbst, wie viele Regionen er von dort gelesen hat.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Vollstaendige Aufzaehlung der Bezeichner, die diese PHASE neu anlegt — ueber
alle fuenf Plaene. Der Drift-Abgleich liest diesen Abschnitt, um neu
Entstandenes nicht als Abweichung zu melden. Was hier nicht steht, ist nicht neu.

**Neue Dateien:** `scripts/probes/schiffskonsole-messung.mjs` (Welle 1),
`scripts/verify-shipconsole.mjs` (Welle 2), `scripts/lib/shipconsole-exclusions.mjs`
(Welle 2, NUR falls der Berichtslauf eine benannte Ausnahme erzwingt)

**Neues npm-Skript:** `verify:shipconsole`

**Neue Gate-Registry-Kennung:** `verify:shipconsole` (Schiene A, in Welle 2 mit
`disabled` und benanntem Anlass, in Welle 5 scharf)

**Neue Viewer-API:** `metrics()` am Rueckgabeobjekt von `initHolo`; das
Fensterhandle `window.__holoViewer` erscheint ausschliesslich bei der
Adressmarke `holometrics`

**Neue CSS-Klassen und -Selektoren (Wellen 3 und 4):** `.holo__rail`,
`.holo__rail-ic`, `.holo__rail-lb`, `.holo__rail-ct`, `.holo__readout`,
`.holo__sys`, `.holo__sys-h`, `.holo__sys-ct`, `.holo__sys-list`,
`.holo__sys-row`, `.holo.has-console`, `.holo__rail.is-chips`

**Neue Element-Ids:** `sys-core`, `sys-arms`, `sys-prop`, `sys-other`,
`holorail`, `holoreadout`

**Neue i18n-Schluessel in `src/i18n/ui.ts`, je DE und EN:**
`ship.console.rail.aria`, `ship.console.readout.aria`, `ship.console.count`,
`ship.defense.title`

**Neue Sperrklinken:** Fuellgrad-Klinke (P-1, 70 %, Ausgang rund 25 %) in
`scripts/probes/schiffskonsole-messung.mjs`; Textbestands-Klinke
(Erfolgskriterium 3) in `scripts/verify-shipconsole.mjs`

**Neue Eintraege in der Bildlaufleisten-Ausnahmeliste (sechs Stellen, zwei
Dateien: `assets/mobile-ux.css` § 5c und `SEL_DRAG`/`SEL_FADE` in
`assets/scroll-affordance.js`):** `.holo__readout` und `.holo__rail.is-chips` —
jeweils NUR, falls sie einen eigenen Bildlauf-Kasten bekommen

**Erweitert, nicht neu:** `computeRegions()` in `scripts/verify-shipcard.mjs`
nimmt zusaetzlich zu `div.sd` den Block `section.holo` in seine Regionsbildung
auf; die Ausnahmeliste `scripts/lib/shipcard-exclusions.mjs` bleibt bestehen

**Getilgt, nicht neu:** `.holo__activate` samt Regel und das Element `holoact`;
der Beschriftungs-Umschalter `holoblabels` samt `.holo__layer--lab` und
`.holo__layer-eye`; die Regelgruppe fuer den eingeklappten Ebenen-Kasten; der
Klick-Ladepfad an `btn3d`; der von Video- und Bildverfuegbarkeit abhaengige
Ausdruck fuer `defaultMode` (`ShipDetail.astro` Z. 400)

**Verwaist, nicht geloescht:** die i18n-Schluessel `holo.activate`,
`holo.labels.toggle`, `holo.labels.short`, `holo.layers.title`,
`holo.filter.aria` — als verwaist markiert, Loeschung liegt beim Executor und
ist kein Blocker
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dist/`-Dateien -> Pruefskript | Beide Tore lesen gebautes HTML und werten es mit regulaeren Ausdruecken aus. Eingabe ist eigenes Bauprodukt, traegt aber Katalogdaten (Bauteil-, Schiffs-, Shopnamen). |
| eingebettetes JSON -> Pruefskript | Zusicherung 4 liest den `holodata`-Skriptblock als JSON. Der Block wird vom eigenen Build erzeugt. |
| Prozessargumente -> Skripte | `--report` ist vom Aufrufer kontrolliert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Denial of Service | Regulaere Ausdruecke in `scripts/verify-shipconsole.mjs` ueber 454 HTML-Dateien | medium | mitigate | Keine verschachtelten Quantoren, keine Rueckverfolgung ueber ganze Dokumente: Tag-Abtastung wie in `verify-shipcard.mjs` mit einem einzigen linearen Durchlauf je Datei. Laufzeit wird als Ist-Wert gedruckt; ueber 30 s ist ein Befund, kein Normalzustand. |
| T-15-07 | Tampering | JSON-Auswertung des `holodata`-Blocks | low | mitigate | Der Block stammt aus dem eigenen Build. Der Rumpf wird ueber die Tiefenzaehlung geschnitten und mit `JSON.parse` in einem `try` gelesen; ein Parserfehler ist ein FEHLER mit Nennung der Datei, kein stiller Uebersprung — ein stiller Uebersprung waere ein leerlaufender Waechter. |
| T-15-08 | Repudiation | Ein spaeterer Lauf kann nicht mehr sagen, gegen welchen Stand gemessen wurde | medium | mitigate | Beide Tore drucken eine Kopfzeile mit Artefakt und gelesener Einheitenzahl; die Rot-Meldung wandert woertlich ins SUMMARY. |
| T-15-09 | Elevation of Privilege | Ein ausgesetztes Tor geraet in Vergessenheit | medium | mitigate | Der `disabled`-Anlass nennt den Plan, der scharf schaltet (`15-05-PLAN.md`), und wird bei JEDEM Lauf von `run-gate.mjs` als Schuldenposten gedruckt. `15-05` traegt die Scharfschaltung als eigene Zusicherung. |
| T-15-SC | Tampering | npm/pip/cargo-Installationen | high | accept | Diese Welle installiert kein Paket. Am `package.json` wird ausschliesslich eine Zeile im `scripts`-Block ergaenzt; `package-lock.json` bleibt unberuehrt und steht NICHT in `files_modified`. Gegenprobe: `git status --porcelain` nennt es nicht. |

ASVS-Stufe 1, blockierend ab `high`. Der einzige `high`-Befund ist
gegenstandslos (keine Installation). V5 (Eingabevalidierung) ist die einzige
beruehrte Kategorie und betrifft ausschliesslich Werkzeuge auf der
Entwicklungsmaschine und im Baucontainer, keinen ausgelieferten Pfad.
</threat_model>

<verification>
Vor jedem Push, ohne Ausnahme (`CLAUDE.md` § Liefern):

```
npm run build
npm run gate
```

Beides gruen. Dieser Plan aendert nichts am Layout und nichts an der
ausgelieferten Seite — der Vorschau-Bau mit `STAGING=1` ist hier NICHT faellig;
er wird ab Welle 3 wieder gefahren, wo das Layout beruehrt wird.

Auf Windows in PowerShell `npm.cmd` statt `npm`.

Diese Strecken kann dieser Dateisatz reissen und muessen einzeln gruen sein:
`verify:wiring` (der Meta-Waechter — er faengt genau diesen Fehler),
`verify:shipcard`, `verify:metrics`.
</verification>

<success_criteria>
- `scripts/verify-shipconsole.mjs` prueft gegen `dist/`, druckt acht
  Zusicherungen mit Soll und Ist und nennt die gelesene Seitenzahl.
- Das Tor war einmal vorgefuehrt rot; die Meldung steht woertlich im SUMMARY.
- Der Berichtsmodus faellt kein Urteil und gibt 0 zurueck.
- Die Textbestands-Sperrklinke steht in Hausform mit bezifferter, begruendeter
  Reserve.
- Der Registry-Eintrag steht auf Schiene A mit `disabled` und benanntem Anlass;
  `npm run gate` bleibt gruen und druckt die Schuld.
- `verify:wiring` gruen — Verzeichnis, Datei und `package.json` deckungsgleich.
- `verify:shipcard` nimmt `section.holo` in die Regionsbildung auf, bleibt gruen
  und sagt selbst, wie viele Regionen von dort stammen.
- Die Kennwerte-Leiste ist nachweislich NICHT im Zaehlbereich.
- `npm run build` und `npm run gate` gruen; `src/` unberuehrt.
</success_criteria>

<output>
Create `.planning/phases/15-das-schiff-ist-die-navigation/15-02-SUMMARY.md` when done
</output>
