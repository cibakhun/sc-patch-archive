# Vorschau-Build. Gleiche Maschinerie wie deploy-image.yml (der Build braucht
# viel RAM und laeuft deshalb hier statt auf dem Server), aber:
#   - Zweig `staging` statt `main`
#   - Image-Tag `:staging` statt `:latest`
#   - STAGING=1 im Build -> ganze Seite noindex + robots Disallow + sichtbare
#     Marke (siehe src/lib/seo.ts#IS_STAGING)
#   - eigener Coolify-Webhook, damit die Live-App unberuehrt bleibt
#
# EINRICHTUNG (einmalig, ausserhalb dieses Repos):
#   1. Coolify: zweite Anwendung anlegen, Quelle ghcr.io/cibakhun/sc-patch-archive:staging,
#      eigene Domain (z. B. staging.verse-base.com).
#   2. Deren Deploy-Webhook als Repo-Secret COOLIFY_STAGING_WEBHOOK hinterlegen.
#      COOLIFY_API_TOKEN wird mit der Live-Pipeline geteilt.
#   3. Zweig anlegen und pushen: git switch -c staging && git push -u origin staging
#
# ABLAUF danach: PR -> in `staging` mergen -> ansehen -> in `main` mergen.
name: Build & Push Staging Image

on:
  push:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: staging-image-build
  cancel-in-progress: true

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Erst BAUEN, nicht pushen: zwischen Build und Push liegen zwei
      # Pruefungen am fertigen Image. Was nicht besteht, wird nie
      # veroeffentlicht — dieselbe Logik wie beim Tor im Dockerfile, eine
      # Stufe spaeter.
      - name: Build (noch nicht pushen)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          build-args: |
            STAGING=1
            GIT_SHA=${{ github.sha }}
          tags: |
            ghcr.io/cibakhun/sc-patch-archive:staging
            ghcr.io/cibakhun/sc-patch-archive:staging-${{ github.sha }}

      # Gegenprobe im fertigen Image: eine versehentlich indexierbare Kopie der
      # Seite waere Duplicate Content gegen die eigene Domain. Lieber hier hart
      # scheitern als es in der Search Console entdecken.
      - name: Verify the preview is not indexable
        run: |
          CID=$(docker create ghcr.io/cibakhun/sc-patch-archive:staging)
          docker cp "$CID:/usr/share/nginx/html/robots.txt" ./robots.check
          docker cp "$CID:/usr/share/nginx/html/index.html" ./index.check
          docker rm "$CID" >/dev/null
          echo "--- robots.txt ---"; cat ./robots.check
          grep -q '^Disallow: /' ./robots.check || { echo "FEHLER: robots.txt sperrt nicht."; exit 1; }
          grep -q 'name="robots" content="noindex"' ./index.check || { echo "FEHLER: index.html hat kein noindex."; exit 1; }
          echo "OK — Vorschau ist gesperrt."

      # ---- Browser-Rauchtest gegen den LAUFENDEN Container ----------------
      # Warum hier und nicht im Dockerfile-Tor: node:22-alpine hat keinen
      # Browser, und die Content-Security-Policy ist ein nginx-HEADER — sie
      # existiert erst, wenn der Container laeuft. dist/ als Dateibaum kann
      # sie nicht zeigen. Geprueft wird damit genau das Artefakt, das gleich
      # ausgeliefert wird.
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      - name: Browser bereitstellen
        run: |
          for c in google-chrome google-chrome-stable chromium chromium-browser; do
            if command -v "$c" >/dev/null 2>&1; then
              echo "CHROME_PATH=$(command -v $c)" >> "$GITHUB_ENV"
              echo "gefunden: $(command -v $c)"; exit 0
            fi
          done
          echo "kein Browser vorinstalliert — chromium wird nachinstalliert"
          sudo apt-get update -qq && sudo apt-get install -y -qq chromium-browser
          echo "CHROME_PATH=$(command -v chromium-browser)" >> "$GITHUB_ENV"

      # ---- Testpilot-Tor: der Rauchtest braucht einen Weg hindurch --------
      # Das Tor steht auf diesem Container scharf (STAGING=1, Plan 08) --
      # ohne einen Weg hindurch liefe JEDE Leitseite auf /gate.html statt
      # auf sich selbst, und der Rauchtest risse an fast jeder Zusicherung
      # (Plan-09-Anlass). Der Weg ist bewusst KEIN Dauerschluessel
      # (T-14-56/T-14-57/T-14-58, siehe nginx/gate.js check()):
      #   1. je Lauf frisch gewuerfelt (openssl rand -hex 32) und nirgends
      #      gespeichert -- kein Repo-Secret, kein Coolify-Eintrag, keine
      #      Datei. Der Vorschau-Container auf Coolify bekommt ihn NIE.
      #   2. beide Seiten (Container-Umgebungsvariable VB_GATE_BYPASS UND
      #      Rauchtest-Umgebungsvariable SMOKE_GATE_BYPASS) entstehen in
      #      DIESEM Schritt, damit sie nicht auseinanderlaufen koennen.
      #   3. der Wert wird erzeugt und verwendet, aber nie ausgegeben --
      #      keine Zeile in diesem Schritt druckt ihn (T-14-58).
      - name: Rauchtest gegen das fertige Image
        run: |
          GATE_BYPASS=$(openssl rand -hex 32)
          docker run -d --rm -p 8080:80 -e VB_GATE_BYPASS="$GATE_BYPASS" --name smoke ghcr.io/cibakhun/sc-patch-archive:staging
          # auf nginx warten, statt blind zu schlafen
          for i in $(seq 1 30); do
            curl -fsS http://localhost:8080/ >/dev/null 2>&1 && break
            sleep 1
          done
          # `|| code=$?`, damit der Container auch nach einem Fehlschlag
          # gestoppt wird — unter `bash -e` bricht der Schritt sonst sofort ab
          # und laesst ihn stehen. Der Exit-Code bleibt der letzten Pruefung.
          code=0
          SMOKE_GATE_BYPASS="$GATE_BYPASS" node scripts/browser-smoke.mjs --base http://localhost:8080 || code=$?
          # check-gate.mjs (Schiene C, Aufgabe 2) laeuft NACH dem Rauchtest
          # und VOR dem Push, gegen denselben laufenden Container -- dort ist
          # er am Platz, weil dort ein echtes Artefakt steht. Zusicherung 4
          # wuerfelt einen FREMDEN Wert und misst, dass GENAU DER nicht
          # durchkommt (Negativfall, bekommt den echten Wert nicht). Seit
          # WR-01 bekommt er zusaetzlich per --bypass den ECHTEN, fuer DIESEN
          # Lauf gueltigen Wert und misst in Zusicherung 6, dass GENAU DER
          # eine gesperrte Stichprobe oeffnet (Positivfall) -- genau die
          # Pruefung, deren Fehlen CR-02 (fehlende env-Direktive im
          # Dockerfile) unentdeckt liess. Der Wert wird auch hier nicht
          # ausgegeben (T-14-58).
          if [ "$code" -eq 0 ]; then
            # check-gate leitet seine gesperrten Stichproben aus dem gebauten
            # Stand ab (keine zweite, handgepflegte Pfadliste). Hier gibt es
            # kein dist/ -- der Astro-Bau entsteht IM Image, der Runner sieht
            # ihn nie. Am 20.08.2026 riss der Schritt genau daran ab, nachdem
            # browser-smoke alle Zusicherungen erfuellt hatte.
            # Der Webwurzel-Inhalt wird deshalb aus dem laufenden Container
            # geholt. Das ist kein Notbehelf, sondern strenger als ein
            # lokales dist/: die Stichproben stammen dann aus genau dem
            # Artefakt, das geprueft wird (Grundsatz 7).
            mkdir -p image-dist
            docker cp smoke:/usr/share/nginx/html/. image-dist/
            # Gegenprobe, damit ein leeres Verzeichnis nicht wie ein
            # bestandener Lauf aussieht -- check-gate selbst faengt das ueber
            # seine Untergrenzen zwar ab, aber hier ist die Ursache lesbar.
            test -f image-dist/index.html || { echo "FEHLER: image-dist/index.html fehlt -- Webwurzel im Image geaendert?" >&2; exit 1; }
            node scripts/check-gate.mjs --base http://localhost:8080 --dist image-dist --bypass "$GATE_BYPASS" || code=$?
          fi
          docker stop smoke >/dev/null || true
          exit $code

      - name: Push (erst nach bestandenen Pruefungen)
        run: |
          docker push ghcr.io/cibakhun/sc-patch-archive:staging
          docker push ghcr.io/cibakhun/sc-patch-archive:staging-${{ github.sha }}

      # Am 14.08.2026 scheiterte dieser Schritt zweimal mit 401, obwohl derselbe
      # Token, direkt von Hand aufgerufen, sauber "deployment queued" lieferte.
      # Ursache war nicht der Token, sondern sein TRANSPORT: ein an den Secret-Wert
      # geratenes Zeilenende macht den Bearer-Header ungueltig, und `curl -f`
      # verschluckt dabei auch noch den Rumpf, sodass im Log nur "error: 401" stand.
      # Daher jetzt: Rand-Leerraum abschneiden, HTTP-Code und Antwort ausgeben, und
      # die Laenge des Tokens nennen (nicht seinen Wert) — daran ist ein
      # eingeschlepptes Zeilenende sofort zu erkennen.
      - name: Trigger Coolify redeploy (staging)
        env:
          HOOK: ${{ secrets.COOLIFY_STAGING_WEBHOOK }}
          TOKEN: ${{ secrets.COOLIFY_API_TOKEN }}
        run: |
          # Fehlt ein Secret, wird bewusst NICHT rot — ein Fork ohne Coolify soll
          # bauen koennen. Aber als ::warning::, damit es im Lauf sichtbar ist:
          # am 14.08.2026 lief dieser Zweig zweimal unbemerkt durch (das Secret war
          # gesetzt, aber LEER — `gh secret set` ohne Eingabe speichert Leerstring),
          # und der gruene Lauf sah aus wie ein geglueckter Deploy.
          if [ -z "$HOOK" ] || [ -z "$TOKEN" ]; then
            FEHLT=""
            [ -z "$HOOK" ] && FEHLT="COOLIFY_STAGING_WEBHOOK"
            [ -z "$TOKEN" ] && FEHLT="${FEHLT:+$FEHLT und }COOLIFY_API_TOKEN"
            echo "::warning::KEIN Deploy ausgeloest — $FEHLT leer oder nicht gesetzt. Das Image liegt fertig in ghcr; in Coolify manuell Redeploy klicken. Achtung: ein Secret kann gesetzt UND leer sein."
            exit 0
          fi
          CLEAN_TOKEN=$(printf '%s' "$TOKEN" | tr -d '[:space:]')
          echo "Token: ${#TOKEN} Zeichen roh, ${#CLEAN_TOKEN} nach dem Trimmen (Wert wird nicht ausgegeben)."
          CLEAN_HOOK=$(printf '%s' "$HOOK" | tr -d '[:space:]')
          BODY=$(mktemp)
          CODE=$(curl -sS -o "$BODY" -w '%{http_code}' "$CLEAN_HOOK" -H "Authorization: Bearer $CLEAN_TOKEN")
          echo "HTTP $CODE"
          echo "Antwort: $(head -c 400 "$BODY")"
          if [ "$CODE" -ge 200 ] && [ "$CODE" -lt 300 ]; then
            echo "Coolify staging redeploy triggered."
          else
            echo "::error::Coolify lehnt den Deploy-Aufruf ab (HTTP $CODE). Bei 401: Token in Coolify unter Keys & Tokens pruefen (Berechtigung 'deploy') und COOLIFY_API_TOKEN ohne Zeilenende neu setzen."
            exit 1
          fi

      # Nachsehen, ob die Vorschau den frisch gebauten Stand WIRKLICH
      # ausliefert. "Image gepusht" und "Coolify angestossen" sind keine
      # Antwort darauf: am 08.08.2026 lieferte staging knapp vier Stunden den
      # Vortagsstand aus, und an anderem Tag hing Coolify 20 Minuten.
      #
      # ⚠ GEMESSEN am 09.08.2026: Cloudflare weist den GitHub-Runner mit 403
      # ab — dieselbe Sperre gegen Rechenzentren wie bei UEX. Der Schritt
      # kann die Frage von hier aus also NICHT beantworten und bricht bei 403
      # sofort ab (statt sein Fenster auszusitzen); `--weich` haelt ihn dabei
      # gruen, weil "ich komme nicht hin" kein Befund ueber den Deploy ist.
      # Er bleibt trotzdem stehen: er kostet eine Sekunde, protokolliert den
      # Zustand, und wenn die Sperre je faellt, misst er wieder wirklich.
      # Die belastbare Antwort gibt `npm run check:staging` vom
      # Entwicklungsrechner aus — dort ist sie streng.
      - name: Warten, bis die Vorschau den neuen Stand zeigt
        run: |
          node scripts/check-deployed.mjs \
            --base https://staging.verse-base.com \
            --sha ${{ github.sha }} \
            --vorschau \
            --weich \
            --warte 600
